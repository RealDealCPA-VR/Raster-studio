[package]
name = "asset-store"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[dependencies]
anyhow.workspace = true
thiserror.workspace = true
serde.workspace = true
serde_json.workspace = true
blake3.workspace = true
raster.workspace = true
# W9-N: the resource-file parsers read Photoshop's descriptors, Unicode and
# Pascal strings and pattern pixels through the `psd` crate's bounded readers.
psd.workspace = true
layer-model.workspace = true

[dev-dependencies]
tempfile = "3"
