# Thumbnail workflow progress ledger

One row per card in `THUMBNAIL-WORKFLOW-IMPLEMENTATION-PLAN.md`. Statuses:
`not started` · `in progress` · `verified` · `blocked` · `optional deferred`.
The last two are never collapsed into done. Update the row, the "next ready
card" line, and the handoff block every time a card lands.

## Current handoff (update first, every time)

```text
Current task: T070 VERIFIED 2026-09-09 (reviewer PASS round 1 on the
              pickup-verified in-tree implementation: StrokeOp::RefineBoundary
              — mask-only boundary-refine brush, band-bounded local
              smooth+regrade, hard-coverage fixed points, strength option
              wired; gates: tools/app-shell/ui green, clippy 0, fmt clean,
              FULL WORKSPACE GATE PASS 68 binaries 0 failed)
Next task: T071 — deliver the native thumbnail editing milestone (Phase 7 GATE; M4 delivery). Then T072+ (Phase 8, PSD).
Exact checks: phase gate at each phase boundary
Last check: 2026-09-09 — T056 round 3: app-shell 494+10, editor-core 115,
            workspace 68 binaries 0 failed, clippy clean, fmt clean
            (reviewer's own runs).
Remaining work: cards 043–092; 093–098 optional deferred.
Exact next action: T043 per plan card (docs/THUMBNAIL-WORKFLOW-IMPLEMENTATION-
                   PLAN.md — read it first); doer/reviewer per card.
Carried items: T033 frame capture; T032 pointer-frame-handle unit; quick-mask
               transform framing; per-participant Move previews; dock
               Confirm/Cancel buttons; manual device/IME checks; zero-edit
               cancel micro-test; locked-active-transaction drill test;
               un-routed paint tools through sample_to_layer_of; StrokeBuffer
               clip-space conflation; SmartObject done-check variant; T041
               polish (error payload, pivot semantics, Skew-mode aspect pin,
               constructed-corner validation, numeric-field wiring); T042
               remainder (align/distribute actions, smart-guide drawing,
               hidden-layer candidates, user SnapSettings, effects reach,
               shift-snap precedence, self-snap dead zone, per-gesture
               candidate rebuild); T043 remainder (live-preview pose for
               unlinked masks, PSD mask-offset mapping for non-identity
               transforms, multi-chain link IDs, unbounded-mask resample
               cost); T044 remainder (options-bar mode availability, warp
               refusal at mode entry, collapsed-mesh-hull guard); the
               composite-check sturdiness nit (assert the ink color, not
               non-white, in the T045 off-canvas step); refresh_linked_sources
               still canvas-clips (route it through the builder); a live
               transform preview after placing; the place TOCTOU; T037
               follow-ups; T039 polish.
```


## T012 — 2026-09-09

- tools: `SessionGeometry::Transform` + `Tool::live_geometry()` (default None — the
  same call publishes and un-publishes); TransformTool answers its live state/mode/
  grabbed handle.
- tool_input: `ToolPointer::live_geometry()` + `session_doc` (claimed with the gesture,
  retained while the geometry lives — a transform commits on Enter, not on release).
- chrome: `publish_tool_geometry` (production publisher; wrong/absent document clears)
  + `paint_live_tool_geometry` in `Chrome::ui` — `ui::canvas::paint::transform` over
  the canvas region, same camera as the surface, clipped to the docks' leftover.
- shell: publishes after every pointer sample.
- Tests: `a_live_transform_session_publishes_geometry_until_it_ends` (gesture→drag
  moves the published state→Escape clears) and
  `a_published_transform_session_paints_handles_until_the_session_ends` (the painted
  frame contains the quad's closed-line at the dragged corner; ended session paints
  none) — publication through the production publisher from a real gesture.
- Checks: app-shell 420; tools 94; integration green; fmt clean; clippy 0.

## T011 — 2026-09-09

- `crates/app-shell/src/edit_session.rs` (new): PendingEdit (document id, targets
  captured once + validated at begin, label, generation) + EditSessions (one live
  gesture). Editor: `pending_edit()`, `begin_edit(targets, label)`, `commit_edit(cmds)`
  (one labeled Transaction; refused on wrong document / no gesture / refused command —
  doc+history untouched either way), `cancel_edit()` (applies nothing, idempotent).
- 7 tests covering every card-011 check.
- Checks: app-shell 418; fmt clean; clippy 0; integration green.

## T010 — 2026-09-09

- tools: `ToolSetting` enum + `Tool::set_setting` (Choice→set_choice delegation keeps
  existing choice tools working; other kinds default Err) + ToolError::{UnknownOption,
  OptionKindMismatch}; MoveTool answers auto_select/show_transform; `show_transform`
  field added (consumption = cards 012/038).
- chrome: `tool_options(tool)` — every declared option with workspace value (schema
  defaults included).
- Boundary: shell.rs converts ui::OptionValue → tools::ToolSetting; `handle`'s choices
  param is now `&[(String, tools::ToolSetting)]`, applied at pointer-down (pinned),
  failures surface in PointerOutcome::failed.
- Tests: move_tool_adopts_its_declared_options, set_setting_refuses_unknown_keys...,
  tool_options_forwards_every_value..., auto_select_forwarded_through_the_pointer_route
  (behavioral: press over corner ink claims that layer; active canvas layer unmoved).
- Checks: app-shell 411; tools 94; fmt clean; clippy 0 errors; integration green.

## T009 — 2026-09-09

- `crates/app-shell/src/interaction_geometry.rs` (new, pub): screen↔document over
  ui::CanvasCamera/Viewport; document↔layer-local via `document_transform_of`
  (ancestor composition, root-first, once); `GeometryError::SingularTransform` on zero
  determinant; MaskSpace {WithContent, Document} split per `mask.linked` mirroring
  `render_source`; level conjugation identical to `Ctx::level_transform`.
- Convention documented in-module: layer transforms are parent-relative; the full
  document mapping composes ancestors root-first exactly once (the card's
  "never apply both twice" rule).
- Tests (6): zoom round-trip ×3, display-scale round-trip ×3 (ppp 1/1.5/2),
  scaled+translated layer round-trip at levels 0–2, singular refusal, nested
  composition-once, linked/unlinked mask sampling.
- Checks: interaction_geometry 6/6 (two runs); app-shell 409; compositor bounds 8/8;
  fmt clean; clippy -D warnings 0 errors.

## T007 + T008 — 2026-09-09

- T007: `app-shell/src/edit_target.rs` — EditTargetKind/EditTarget/EditTargets;
  read-time validation with Content fallback (mask removed while selected, per-doc
  sticky kind); Editor accessors; `ui::Intent::SetEditTarget` raised by the Properties
  Layer/Mask control (docks.rs); `Pick::EditTarget` → `ChromeOutput::edit_target` →
  `shell.rs::apply_chrome`. 6 new tests (5 module + 1 bridge route).
- T008: `compositor/src/bounds.rs` — pub content/document/styled bounds over the
  compositor's own Ctx answers (content_bounds/document_bounds/style_reach/style_rect
  made pub(crate)); `alpha_bounds` precise per-tile ink with a bounded process-wide
  tile-hash cache; empty/missing → None. 8 new tests.
- Checks: compositor 178+1; app-shell 403+10; ui 9 binaries; tools green;
  integration-tests green; fmt clean; clippy -D warnings clean workspace-wide.

## T007 — 2026-09-09

- `crates/app-shell/src/edit_target.rs` (new): `EditTargetKind {Content, Mask}`,
  `EditTarget {layer, kind}`, `EditTargets` (per-DocumentId sticky kind),
  `EditTarget::resolve`/`resolve_active` — validity computed at read time; a mask
  removed while selected falls back to Content with no repair pass.
- `Editor`: `edit_targets` field (pub(crate)), `edit_target()`, `edit_target_kind(id)`,
  `set_edit_target_kind(kind)` (touch() only, never document dirty).
- UI route: Properties Layer/Mask control emits `ui::Intent::SetEditTarget { mask }`
  (docks.rs, alongside its display echo); `menu_bridge::pick` → `Pick::EditTarget` →
  `ChromeOutput::edit_target` → `shell.rs::apply_chrome` → Editor.
- Tests: 5 in edit_target.rs + 1 bridge route test. All card-007 checks covered.
- Checks: app-shell 403+10; ui 9 binaries; tools 4; integration-tests green
  (11/10/6/5+1; reproducers still 4 ignored); fmt clean; clippy -D warnings clean.

## M0 exit — 2026-09-09

- All six M0 cards verified (rows above). Reproducers E01/E06/E08/E09 demonstrated
  failing for the intended behavior; retained `#[ignore]`d in `thumbnail_reproducers.rs`
  with owning cards named (E01→016+021, E06→046, E08→055, E09→057).
- Phase gate PASS: fmt clean; `cargo check --locked --workspace --all-targets` PASS;
  `cargo clippy --locked --workspace --all-targets -- -D warnings` PASS;
  `cargo test --locked --workspace` PASS (68 binaries ok, 0 failed, 15 ignored).
- Gate repair: `fixture/thumbnail.rs` string-literal `▸` (U+25B8) replaced with `>` —
  `ui::no_typed_ui_glyphs` rejects unrenderable glyphs in library source strings; the
  failure predated this session (T003's uncommitted work; the restart recovery only
  reran the integration package). Gate rerun green after the repair.

## T004 completion — 2026-09-09

- `tests/integration/src/app.rs`: shell-route helpers (`shell_editor`,
  `shell_screen_pt`, `shell_pointer`, `shell_stroke`, `menu_intent`, `apply_intent`,
  `layer_tile_map`, `mask_tile_map`, `set_selection`, `the_opened_layer`).
- `tests/integration/tests/thumbnail_reproducers.rs`: four `#[ignore]`d reproducers
  (E01/E06/E08/E09) with owning cards named in the ignore reasons; run with
  `-- --ignored --nocapture` and each fails for the intended behavior (exact outputs
  in `THUMBNAIL-BASELINE.md` Task 004 section).
- E09 runtime refinement: bare `LayerMask::new` has no coverage tiles; the compositor
  reads absent mask tiles as zero coverage, so all four mask ops composite fully hidden
  (pre-op composite alpha 255, post-op alpha 0). Stronger than the source-traced claim.
- Hygiene: stray `eprintln!("ROUTED …")` removed from `tool_input.rs` (pre-existing
  debug noise on every pointer sample; app-shell suite green after).
- Deps: `ui` regular dep + `text-engine` dev-dep for integration-tests (lock +3 lines).
- Checks: `cargo test --locked -p integration-tests` (end_to_end 11, interchange 10,
  performance 6, thumbnail_workflow 5+1 ignored, thumbnail_reproducers 4 ignored);
  `cargo test --locked -p app-shell` 397+10 green; `cargo fmt --all --check` clean;
  clippy clean on both touched packages.

## Restart recovery — 2026-09-09

- HEAD remains `9ba62c1`. Surviving work is uncommitted: the fixture module,
  workflow tests, fixture assets/native package, dependency changes, and workflow
  documentation. Preserve these files when resuming; do not reset or clean them.
- T001/T002 retain their previously recorded completion; T003's existing tests
  were rerun after the restart from the inner `raster-studio/` workspace:
  `cargo test --locked -p integration-tests --test thumbnail_workflow` exited 0
  with **5 passed, 0 failed, 1 ignored**. The ignored test materializes large assets;
  it was not rerun. This verifies the small fixtures, structure, determinism,
  PNG round-trip, and native save/reopen, not the full editing workflow.
- T004 was recorded in progress before the interruption, but
  `tests/integration/src/app.rs` had no working-tree diff and `thumbnail_workflow.rs`
  contained only the six T003 tests. It was completed on 2026-09-09 in the resumed
  session: see the T004 row and `THUMBNAIL-BASELINE.md`'s Task 004 section.
- Cards 005–092 remain not started; 093–098 remain optional deferred. Full workspace
  gates and application/UI acceptance were not rerun in this recovery review;
  their earlier results are historical evidence only.
- The root `TODO.md` is an older backlog. This ledger and `.pi/goals/ACTIVE.md`
  identify the current work. The separate `CORRECTIONS-TODO.md` C14 hardware
  verification remains open; this recovery does not close it.

## Status table

| ID | Card (short) | Depends | Status | Commit/diff · checks · evidence · remaining issue |
|---|---|---|---|---|
| 001 | Reproduce gaps through the app → `docs/THUMBNAIL-BASELINE.md` | — | **verified** | Baseline report written 2026-09-07 vs `9ba62c1`: all E01–E14 CONFIRMED at source level; fmt/clippy/check green; full workspace suite + doctest repair + `--shot` evidence recorded in the baseline doc. No code changed. |
| 002 | Create this progress ledger | 001 | **verified** | This file. A fresh session can pick the next ready card from it. |
| 003 | Deterministic composition fixtures (`tests/integration/src/fixture/thumbnail.rs`, `tests/project-fixtures/`, `thumbnail_workflow.rs`) | 001 | **verified** | 2026-09-09: `fixture/thumbnail.rs` (background/oversized/logo/portrait generators + mask ramp + `build_scene` + `write_assets` + `fnv1a64`); 5 tests + 1 ignored (full-size materializer) green via `cargo test --locked -p integration-tests --test thumbnail_workflow`; full-size 3628×2041 assets + native package materialized in `tests/project-fixtures/` with hashes recorded in its README; text layers use embedded DejaVu Sans via `compositor::load_font`; deps added: glam+dejavu (lockfile +1 line). Save/reopen composite byte-equality verified. Remaining issue: none. |
| 004 | Shell-route regression harness helpers; E01/E06/E08/E09 reproducers | 003 | **verified** | 2026-09-09: app.rs shell-route helpers (Editor + ToolPointer route + menu resolution); `thumbnail_reproducers.rs` with 4 `#[ignore]`d reproducers naming owning cards; each fails for the intended behavior (exact outputs in THUMBNAIL-BASELINE.md Task 004 section); E09 refined at runtime (bare mask = fully hidden); `ROUTED` eprintln hygiene removed; integration package green (11/10/6/5+1; reproducers 4 ignored in normal mode), app-shell 397+10 green, fmt+clippy clean. Remaining issue: none. |
| 005 | Reuse + dependency inventory | 003 | **verified** | 2026-09-09: 12-subsystem function-level inventory in THUMBNAIL-BASELINE.md "Task 005" — every subsystem has an existing owner (extraction seams only: bounds.rs/edit_target.rs/interaction_geometry.rs/edit_session.rs/text_session.rs/placement.rs/clipboard.rs/mask_edit.rs per plan table); lockfile facts recorded (cosmic-text 0.17.2 declared 0.17 by text-engine only; arboard 3.6.1 transitive via egui-winit, image-data off, undeclared by members; dejavu 2.37; glam 0.29.3); no new core deps planned, no duplicate shaper/compositor. Remaining issue: none. |
| 006 | Freeze acceptance scene + matrix | 003–005 | **verified** | 2026-09-09: THUMBNAIL-BASELINE.md "Task 006" — frozen 7-layer scene (oversized placed bg + Alternatives{Headline A, Logos→Logo, hidden Headline B} + shadow/stroke effect + clipped adjustment + masked portrait + styled Subhead + background), sizes 1280×720/1920×1080/3628×2041, zooms 33.33/100/200 automated, scaling 150/200% marked manual-hardware, all 19 acceptance steps mapped to fixture/test/card, fix order frozen E01→E03/E05→E06→E08/E09→E04. Remaining issue: scene elements "placed background" + "shadow effect" are frozen spec; they materialize with cards 046–048/066–067. |
| 007 | Editor selection/target snapshot (`edit_target.rs`) | 006 | **verified** | 2026-09-09: `app-shell/src/edit_target.rs` (kind + snapshot + per-doc store + read-time validation/fallback); Editor field+accessors; `Intent::SetEditTarget` raised by the Properties Layer/Mask control; `Pick::EditTarget` routed through menu_bridge→ChromeOutput→shell apply; 6 new tests (5 module + 1 bridge). Mask-deleted-while-selected→content ✓; per-doc tab-switch restore ✓; no pixel mutation/no history on select ✓. Checks: app-shell 403+10, ui green, integration-tests green, fmt+clippy -D warnings clean. Remaining issue: none for the card; tools consume the snapshot in 010/055. |
| 008 | Reusable bounds queries (`compositor/src/bounds.rs`) | 007 | **verified** | 2026-09-09: `bounds.rs` pub module — content/document/styled bounds delegating to composite.rs Ctx (methods pub(crate)), `alpha_bounds` tile-hash-cached precise ink; empty→None; 8 tests incl. negative-transform containment + compositor-pinned group answer. compositor 178+1 green; fmt/clippy clean. Remaining issue: none. |
| 009 | Coordinate conversion helpers (`interaction_geometry.rs`) | 008 | **verified** | 2026-09-09: `app-shell/src/interaction_geometry.rs` — screen↔document (ui::CanvasCamera), document↔layer-local (ancestor composition, `document_transform_of`), singular refusal, MaskSpace split (linked=content space, unlinked=document space); convention documented (parent-relative transforms, composed once, level conjugation per Ctx). 6 tests green ×2 runs; app-shell 409 green; fmt/clippy clean. Remaining issue: none. |
| 010 | Typed tool options forwarding | 007 | **verified** | 2026-09-09: `tools::ToolSetting` + `Tool::set_setting` (Choice delegates to set_choice; unknown/mismatch → new ToolError variants, loud); MoveTool answers its spec'd keys; chrome `tool_options` forwards all declared values incl. schema defaults; shell converts ui::OptionValue→ToolSetting at the boundary; `ToolPointer::handle` takes typed settings, applies at pointer-down (pinned), failures surface in `PointerOutcome::failed`. Real-route test: Auto-Select ON → press over corner ink claims that layer, canvas layer unmoved. app-shell 411, tools 94, integration green, fmt+clippy clean. Remaining issue: none. |
| 011 | Pending-edit lifecycle (`edit_session.rs`) | 007,009 | **verified** | 2026-09-09: `edit_session.rs` — PendingEdit (doc id + once-captured targets + label + generation) + EditSessions (one live gesture); Editor begin/commit/cancel. Commit = one labeled Transaction (undo all-at-once); cancel = nothing; wrong-document commit refused with doc+history unchanged; failed commit unchanged; targets captured at begin. 7 tests. app-shell 418 green; fmt/clippy clean. Remaining issue: pointer-route preview wiring = T012/T013. |
| 012 | Publish live tool geometry to CanvasSessions | 008–011 | **verified** | 2026-09-09: `tools::SessionGeometry` + `Tool::live_geometry` (TransformTool impl); `ToolPointer::live_geometry` + `session_doc` retention; chrome `publish_tool_geometry` + `paint_live_tool_geometry` (egui overlay via `ui::canvas::paint::transform` — the route the never-drawn CanvasHost could not provide); shell publishes every frame. Tests: gesture→geometry-follows-drag→Escape-clears; published session paints the quad in a real frame (closed-line at the dragged corner), ended session paints none — publication via the production publisher from a real gesture. app-shell 420; tools 94; integration green; fmt/clippy clean. Remaining issue: none. |
| 013 | Temporary content previews via the one compositor | 011–012 | **verified** | 2026-09-09: compositor `preview_generation` in CompositeOptions (cache identity) + `LayerOverride`/`composite_region_with`; `Ctx::level_transform` override consultation; doc.rs `PreviewOverride` outside the Document + set/clear + composite pass-through; TransformTool layer capture + `quad_affine` pub; shell `settle_preview` after every sample + abandon. Tests: doc-level (moves pixels, no history/dirty, exact-baseline restore, per-doc isolation) + end-to-end route (pixels move before release; cancel exact; commit matches preview ≤3, one entry). app-shell 423; tools 94; compositor 178; integration green; fmt/clippy clean. Remaining issue: none. |
| 014 | Interaction foundation integration verification | 007–013 | **verified** | 2026-09-09: `thumbnail_workflow.rs::the_interaction_foundation_holds_through_the_real_shell_routes` — typed options → Move claims ink under pointer (no-motion click = no history); bounds content+alpha exact; edit target content→mask; pending edit = one labeled entry + whole-gesture undo; camera round-trip. Overlay/preview/cancel/focus-loss covered per-card. **M1 exit gate PASS**: fmt/check/clippy(0)/test --locked --workspace (68 ok, 15 ignored). Remaining issue: none. |
| 015 | Persisted text schema (`layer-model/src/text.rs`) | 005,014 | **verified** | 2026-09-09: `text.rs` — TextLayer {legacy three fields + BaseStyle (no family/size — single authority) + StyleSpans + Paragraph + Frame + Kerning}, serde defaults for legacy compat, linear-straight-RGBA fills, CharStyle-mirroring defaults. 5 tests incl. acceptance-scene round-trip + legacy JSON. layer-model 122 green; clippy clean. Remaining issue: the TextLayer swap in layer.rs + lossless conversion = card 016. |
| 016 | Lossless text model conversion | 015 | **verified** | 2026-09-09: layer.rs re-exports the rich schema; text-engine From impls lossless both ways (base style + spans + paragraph + frame + kerning); all literal callers updated (`..Default::default()` / `TextLayer::legacy`); **E01 reproducer flipped green** (retention rule) and runs in the normal suite; text-engine model test covers styled round-trip. Workspace 68 ok / 0 FAILED; fmt+clippy clean. Remaining issue: document version bump = card 018; validation = card 017. |
| 017 | Text payload/range validation | 016 | **verified** | 2026-09-09: `TextLayer::validate()` + TextError (finite/negative/size-cap/frame/span-bounds/char-boundary/text+span caps) + `normalized_spans()`; editor-core InvalidPayload gate at SetLayerKind apply. 4 layer-model tests + 1 apply/undo test. Workspace 68 ok / 0 FAILED; fmt+clippy clean. Remaining issue: none. |
| 018 | Document version bump + rich-text migration | 016–017 | **verified** | 2026-09-09: version 4 (rich-text semantics); v3→4 migrate step = serde defaults; docs/file-format.md updated; styled native round-trip test (model+composite identical); v1-fixture test stamps to current via the constant. Workspace 68 ok / 0 FAILED; fmt+clippy clean. Remaining issue: none. |
| 019 | Render complete text styles | 016–018 | **verified** | 2026-09-09 (with 020): card 016's lossless conversion already carries style through shape/`render_linear` (glyphs grouped by colour), so the work was proving it + fixing cache identity. `hash_layer(&TextLayer) -> u64` (floats via to_bits, enums by discriminant: base style incl. weight/slant/fill/underline/strikethrough/script/tracking/ligatures/kern-toggle/synthetic, spans, paragraph, frame, kerning) feeds `RunKey.content`. 5 styled tests: weight/tracking/color change the run; alignment/absolute-leading/kerning/box reflow reach layout; a span colours only its own bytes; glyph edges carry partial alpha; any styled edit invalidates. Pitfalls fixed: kern index 0 is a documented no-op (kern before 2nd char); every styled test pins `text_fixture_family()` first (the load_font ONCE mutates the global library mid-suite). compositor 183 green ×3. |
| 020 | Text cache identity/invalidation | 019 | **verified** | 2026-09-09 (with 019): `RunKey {text, family, size_bits, generation, content: hash_layer(layer)}` — fill-only/weight-span-only/frame-only/kerning-only edits each alter warm-cache output; undo restores the first render pixel-identically (`!differs` full-buffer compare; pointer identity dropped as eviction-dependent); same-content re-render pixel-identical (warm hit or eviction re-render). Workspace gate PASS: 68 binaries, 3460 passed, 0 failed; fmt clean; clippy -D warnings 0 errors. Remaining issue: none (thumbnail refresh rides the same cache identity via preview_generation/generation). |
| 021 | Character/Paragraph controls persist | 019–020 | **verified** | 2026-09-09: Character panel gains the direct Fill control (`fill_to_swatch`/`swatch_to_fill` — egui 0.29 stores PREMULTIPLIED Color32, readback via `to_srgba_unmultiplied`; alpha a plain fraction; 8-bit fixed point so an unchanged picker emits nothing). Module doc: range-vs-whole-layer rule (whole layer until canvas text sessions, 025+; nothing to disable today) + gesture contract. End-to-end test: panel setters -> commit -> menu_bridge::pick -> apply_kind_edit (stamped gesture) -> pixels differ -> 3 fields + 2nd same-gesture frame = ONE undo entry -> undo restores exact baseline -> redo -> save/reopen equality + panel readback. ui 960 green; integration-tests green; workspace 68/3464/0; fmt+clippy clean. Remaining issue: none. |
| 022 | Font selection + substitution reporting | 021 | **verified** | 2026-09-09: FontStretch (usWidthClass) through engine+model+compositor+ui; substitution policy single-sourced (`substitute_for` + `attrs_for` — report=render by construction, engine test proves every glyph comes from the reported substitute); searchable family picker + Face combo (DejaVu condensed = same family, usWidthClass 4) + substitution notice; requested name retained through save/reopen (integration test); egui UI fonts unchanged by load_font (measured). **Tile-cache repair**: composite.rs Text arm now hashes the canonical `hash_layer` (weight/fill/stretch-only edits previously served stale tiles — pre-existing bug T021 masked). Reviewer PASS; findings fixed (docs dedup, candidate-pick buffer clear, case-contract pin test). Checks: engine/layer-model/compositor/app-shell/ui/integration suites green (numbers in handoff); clippy -D warnings 0; fmt clean. Deferred: user font-file load route (only-if-needed; seam exists); latent follow-up: load_font × per-doc tile caches. |
| 023 | Point vs paragraph geometry | 019–022 | **verified** | 2026-09-09: Frame controls in the Paragraph panel (Wrap-to-box, Width, Fixed height seeded from laid-out content height, Box height, overset hint); compositor facades text_overset_lines (no-shape early returns) + text_content_height; panel setters refuse out-of-range instead of clamping (deliberately stricter than model validate, documented); explicit breaks survive point↔box; size/box/transform stay distinct (integration test asserts size_px 24 through reflow + reopen Box(90,Some(40))); 5 catalogue keys; reviewer PASS with fixes (early-return guard, range doc). Checks: compositor 186, ui 965+9 bins, integration 10+1, clippy 0, fmt clean. Notes: engine-side overset_line_count suggested; DragValue 2-decimals rounding; Frame/Width/Height literals need keys at wave end. |
| 024 | Styled headline end-to-end verification | 015–023 | **verified** | 2026-09-09: `the_styled_headline_workflow_verifies_end_to_end` — dark backdrop via command route; two headlines created via `Command::create_layer` (the Type tool's route) + styling via the panel route (weight→700, fill→white, subhead→green, tracking→90); four single-field edits each proven to rewrite the composite, with absolute colour pins (fully-white pixel; green-dominant pixel); undo×4 → exact bytes (undo bool asserted), redo×4 → exact bytes; model preservation vs shell-independent full-TextLayer literals; save/reopen → model + byte-identical composite; export PNG encode→decode equality. **M2 exit gate PASS**: fmt clean; check PASS; clippy -D warnings 0; `cargo test --locked --workspace` 68 ok / 0 failed / 3480 passed / 14 ignored. Reviewer PASS (manual item recorded below; export via the app's encode path, not the dialog — headless card; determinism comparative, absolute pins added). Manual item: whether the synthetic-emboldened headline LOOKS right is font/eye-dependent — recorded here, never claimed by the test; verify visually during the human acceptance walk (card 091). |
| 025 | Rich text-session state | 024,011 | **verified** | 2026-09-09: TextSession v2 (original/draft/caret/anchor/composition/created_layer), history-free TextDraft rendering + TextConfirm restore-then-commit (undo inverse = original), Enter/Escape + abandon/tool-switch cancel routing, apply_text_draft (validates, honours locks, same-class guard). Round 1 REJECT (4 criticals) → fixed. Round 2: original 4 PASS; REJECT on new findings → ALL FIXED: N1 second Type click now CONFIRMS the outgoing session (on_pointer_down→confirm; test: both runs keep their text, depth accounting exact); N2 menu-driven tool switches confirm-first (confirm_live_text_session at both menu_bridge::perform sites + RotateCanvas path); N3 confirm under a mid-session lock keeps the draft visible + true depth-delta steps (test); N4 apply_text_draft same-class guard (test). Suites: tools 100 / app-shell 428+10 / integration 11+1 / ui 965+9; clippy 0; fmt clean. **Round 3 PASS** — no new criticals; follow-up for card 026: session×document scoping (tab switch leaves the draft stranded; session should carry DocumentId or reconcile on activate). Remaining issue: none for the card. |
| 026 | Enter existing text layer | 025,008–009 | **verified** (2026-09-09: intent-route + session scoping; reviewer PASS round 2) | — |
| 027 | Caret movement + range selection | 026 | **verified** | 2026-09-09: core landed — TextSession primitives `move_horizontally`/`move_to_paragraph_edge`/`move_by_word`/`select_all` (byte-level but boundary-only: UTF-8 never split, selections always valid; word = alnum+`_`+non-ASCII runs; multi-byte steps whole); `TextEdit::{CaretStep,ParagraphEdge,WordStep,SelectAll}` variants + TypeTool arms (no emission — movement is not history/rendering); shell routes ArrowLeft/Right + Home/End with Shift-extend. Tests: `movement_never_splits_utf8_and_selections_stay_valid` (café+fox walk = one step per character; Shift-selection covers é and the fox whole; collapse), `paragraph_edges_word_steps_and_select_all_behave` (Home/End per-paragraph; word steps across breaks; fox word-steps whole; select-all), `the_movement_edit_variants_reach_the_session` (routing through Tool::text_edit; movement is not history). tools 102; app-shell/integration/ui green; clippy 0; fmt clean. **Reviewer PASS (as scoped)** — contracts verified: boundary-only byte movement, extend/collapse semantics, the word-run rule (alnum+`_`+≥0x80), paragraph-scoped Home/End, movement provably neither history nor rendering; all four hunt items clear (overshoot clamp, space-run termination, ZWJ emoji byte-safety, caret-on-newline stability). Gates at review: tools 102, app-shell 437+10, clippy -D warnings clean, fmt clean. Recorded deferrals (tracked, not dropped): drag selection + Ctrl+A/word-step keyboard routing (card 028's keyboard scope), bidi visual caret geometry (T030's overlay route — the session's byte-level truth is the substrate). Reviewer suggestions for card 028+: clamp_boundary on move_to_paragraph_edge entry (self-healing), debug_assert on the backward word-step's boundary invariant, one-line regression pins for punctuation-run termination and caret-on-newline stability, IME-range note for arrows-during-composition (card 029). Remaining issue: none blocking. |
| 028 | Insert/delete + text clipboard routing | 027 | **verified** (2026-09-09: deletion + style-carrying + OS clipboard; reviewer PASS round 3) | — |
| 029 | IME composition/focus safety | 028 | **verified** (2026-09-09: IME routing + composing-key suppression; reviewer PASS; manual device/IME checks recorded pending) | — |
| 030 | Accurate caret/selection overlays | 027–029,012 | **verified** | 2026-09-09: text-session geometry publisher (`ToolPointer::text_overlay_geometry` shapes the ACTIVE document layer — the draft rides it — caret bar + per-line bidi selection rects via `compositor::text_caret_rect`/`text_selection_rects`, endpoints layer→document once, shell maps document→screen through the click camera; Tool trait `text_caret_anchor` readout) + overlay draw (caret 1.5px emphasis bar, selection 1.0px dim edges, appended to the ants segment set) + card-029 IME placement (`set_ime_allowed` change-cached; `set_ime_cursor_area` anchored on the nonzero-length caret bar at the shaped position). Round-1 REJECT (missing TypeTool override → dead pipeline; 1×1px IME area) fixed and reviewer-verified round 2; live-session test through the real `enter_text_session` route proves document-space zoom-invariance (1.0 vs 2.0) and SelectAll selection edges. Gates: tools 159, app-shell 450, clippy clean, fmt clean. RECORDED follow-ups: caret blinking (the ui-canvas blink helper exists unused; the shell route paints a constant bar — blink scheduling without document dirtying is deferred); magic numbers (`>0.5` document-space bar threshold, 1.5px width, colours) to be named constants beside the ants styling; the caret bar is mapped twice per frame (IME anchor + draw loop); 3 of 4 caret-rect edges are degenerate (harmless, documented); selection lo/hi unclamped (engine is range-safe). Closes card 027's bidi-visual caret-geometry deferral at the geometry layer (engine tests) + overlay level (zoom-invariance test). Remaining issue: none blocking. |
| 031 | Text confirm/cancel/history | 025–030 | **verified** | 2026-09-09: delta landed — Enter inserts a line break (card 023's paragraph model), Ctrl+Enter confirms (the `enter_confirm_chord` guard admits it past the Ctrl early-return; round-1 REJECT for the dead branch), Escape cancels; Ctrl+Enter during a live composition is the IME's (the card-029 guard precedes the arm); session-open status hint set only after all enter guards (the visible affordance; dock Confirm/Cancel buttons recorded as deferred — docks see only the Document, buttons need session-state plumbing); one-cohesive-transaction confirm pinned at tool level (one TextConfirm request) AND shell level (`ctrl_enter_confirms_and_plain_enter_breaks_the_line_in_a_text_session`: newline → IME-consumed Ctrl+Enter → CommitIme → Ctrl+Enter closes the session with committed text + break). **Reviewer PASS (round 3)** — round 1 REJECT (the Ctrl guard made Ctrl+Enter unreachable), round 2 REJECT (two claimed fixes absent from the tree: the pre-guard status hint survived, the ledger row untouched), round 3 verified all fixes by reading: exactly one post-guard set_status; the row code-accurate; the undo contract asserted (history_depth == +1 after the run). Gates: tools 160, app-shell 451, clippy clean, fmt clean. Follow-ups recorded: dock Confirm/Cancel buttons (docks see only the Document — session-state plumbing into the chrome needed; the status hint is the interim affordance); optional per-phase history_depth asserts in the shell test. Remaining issue: none blocking. |
| 032 | Paragraph box create/resize gestures | 023,031 | **verified** | 2026-09-09: first unit landed — drag-creates-paragraph: TypeTool::on_pointer_up measures the drag against the down point (`PARAGRAPH_DRAG_THRESHOLD_PX` 6.0); wider → created layer frame `Frame::Box { width: drag extent (min 8), height: None }`, click → `Frame::Point` (unchanged); the session draft now clones the created payload (fixing a latent default-draft inconsistency that would have dropped the box); stale "no field for" move comment fixed. Test `a_type_drag_creates_paragraph_text_and_a_click_creates_point_text` pins both frames. Suites green (tools 111, app-shell/integration green, clippy 0, fmt clean). **Reviewer PASS as scoped** — all landed contracts verified (threshold semantics strict >, draft mirrors the payload, refuse-don't-clamp, silent no-op, document-space frame edges, one-undoable-edit via card 025's cohesive confirm); the two reachable warnings closed same-day (negative-height refusal — would have poisoned the session draft; width cap 4096 mirroring the panel). Gates: tools 162, app-shell 451, clippy clean, fmt clean. RECORDED deferral (tracked follow-up unit): pointer-driven frame-handle resize (the overlay draws the frame; the pointer hit-test/route for grabbing an edge → ResizeBox, screen→layer width math, cursor disambiguation vs the transform tool) — "unambiguous cursor/controls" is currently satisfied vacuously (transform handles are another tool's); must land with the cursor work before the card's full UX promise holds. Diagnostic note: the sub-minimum-width refusal reuses NotFinite's message ("must be finite, got 5") — a dedicated variant would be truthful; cosmetic. Remaining issue: none blocking. |
| 033 | Repeated headline editing verification | 025–032 | **verified** | 2026-09-09: `the_repeated_headline_editing_sequence_holds_end_to_end` (integration, real routes only: ToolPointer session routes, doc.apply commands, save_to/open_project) — enter, select, PasteText replacement, +line, ResizeBox, Confirm (+1 history entry exactly), TransformLayer translate, reopen/Cancel (no entry, text restored), styled span via SetLayerKind, save/reload round trip (text+frame+span 0..9 survive), font substitution (missing family → pinned default, asserted present + not the missing name), locked layer refuses the session. **Reviewer PASS**; M3 PHASE GATE RUN: full workspace tests 0 failed across 68 binaries, clippy -D warnings clean, fmt clean. RECORDED PENDING (not checked off): the pixel-level "capture an actual editor frame with a caret/range over the headline" — the gpu harness deliberately scopes out the egui overlay; caret-agrees-with-rendering holds by construction (overlay + compositor shape the same run, card 030 pins the geometry). Follow-up micro-test noted: zero-edit cancel (enter → Escape immediately). Remaining issue: none blocking. |
| 034 | Transforms from selected content bounds | 008–014,033 | **verified** | 2026-09-09: ToolContext.active_layer_content_bounds (the shell fills it via compositor::bounds::document_bounds — document space through the layer transform, so a click-placed text layer surrounds its visible text) on BOTH runtime paths (ToolPointer::handle with the effective quick-mask-or-active target computed before the mutable tiles borrow; off_pointer); transform on_pointer_down source chain: explicit pixel selection → layer ink (clipped to the canvas — the stored extent is tile-aligned) → canvas only when no ink; test `a_transform_without_a_selection_surrounds_the_layers_ink_not_the_canvas` (logo rect surrounded; selection wins; canvas fallback). **Reviewer PASS round 2** (round 1 REJECT: dead runtime wiring + content_bounds pre-transform space — both fixed and verified). Gates: tools 163, app-shell 451, clippy clean, fmt clean. RECORDED follow-ups: quick-mask transform frames the canvas (the scratch layer's tiles are empty while coverage lives in its mask — belongs with card 055/045); off-canvas ink clips to zero-size and refuses (Degenerate) rather than framing off-canvas — acceptable, worth a module-doc sentence; invoke-on-menu session bootstrap (start on the menu action, before any click) recorded as the card's remaining unit; pre-existing commit resampling convention is card 035's. Remaining issue: none blocking. |
| 035 | Affine transform commit without resampling | 034 | **verified** | 2026-09-09: whole-layer affine commits as TransformLayer — no resample (raster tile hashes preserved, Text/Shape kinds untouched, one undoable entry); the corner delta conjugates through the parent chain recorded ONCE at session start (`TransformTool.parent`, captured at pointer-down beside the layer id; cancel clears it); scale/rotate/**skew** in the branch (round-1 REJECT: skew omitted — whole-layer skew still rasterized; round-2 REJECT: the parent fix existed only as write-only scaffolding — the commit still read ctx.active_layer_parent_transform); settle_preview now composes P⁻¹·Δ·P·own so the live preview matches the commit for transformed/nested layers — pinned by `the_preview_matches_the_commit_for_an_already_transformed_layer` (preview and committed bytes identical on a pre-moved layer); mask-target/selection-only/warp keep the resample paths (pixel geometry); tests updated: painting.rs move/scale tests assert the transform-update contract, `a_whole_layer_skew_commits_as_a_transform_update`, integration `a_whole_layer_affine_commit_updates_the_transform_without_resampling` (hashes identical, kind stays Raster, +1 history entry, undo restores the baseline). **Reviewer PASS round 3.** Gates: tools 164, app-shell 452, integration 13, clippy clean, fmt clean. Suggestion noted: settle_preview recomputes the parent live vs the commit's session snapshot — only diverges if a PARENT's transform changes mid-session by other means (one comment sentence when touched). Remaining issue: none blocking. |
| 036 | Multi-layer transform + selection sync | 007,035 | **verified** | 2026-09-09: **Reviewer PASS round 2** (round 1 REJECT: the commit-time fallback double-moving a group+child selection + position locks not surfaced). Landed: ToolContext {selected_layers, layer_parents, layer_locks = blocks_transform() (all||position), layer_parent_transforms} filled at both shell sites; ToolRequest::TransformLayers; ToolError::LayerLocked (all-or-nothing, documented); session start normalizes the set (ancestor-shadowed children dropped — the child moves with its ancestor exactly once), lock-checks, and records the REPRESENTATIVE (the active layer when it survived normalization, else the set's first participant) with ITS OWN parent chain; commit re-checks locks and emits one TransformLayers request (>1 participants) or the single TransformLayer (card 035) — never both (the round-2 debug found and fixed the fall-through double-commit); the shell drains TransformLayers into per-participant TransformLayer conjugated through each participant's own chain, wrapped in Command::Transaction (one undo entry), success status only on Ok. Integration tests: `a_multi_layer_transform_moves_the_set_in_one_transaction_and_respects_locks` (two root layers: both move, +1 entry, undo restores both, a locked participant refuses the session at Down) and `selecting_a_group_and_its_child_moves_the_child_once` (the child's TOTAL placement moves exactly the gizmo delta once; the group by the same delta; the press-zone lesson documented — (10,10) is corner 0's rotate band, the exact centre is the Pivot handle, (32,48) is Inside). Debug journey: the gesture-independent 4.4° rotation was the corner rotate band grabbing Rotate(0) at (10,10); a Pivot handle exists at the exact centre. Hygiene landed: the leftover DBG eprintlns removed (transform.rs + tool_input.rs), and the empty-selection self.parent clobber fixed (.or(self.parent) keeps card 035's context fallback). Gates: tools 164, app-shell 452, integration 15, clippy clean, fmt clean. RECORDED follow-ups: per-participant live previews (settle_preview previews only the representative — the others jump on Enter; the fix is per-participant conjugation in the preview, adjacent to card 039); the deferred-request status wording mentions only crop/slices (defense-only today); a parent_transform_of walking ancestors without the leaf would be cleaner than total × own.inverse(). Remaining issue: none blocking. ||position (both fill sites). NEW regression test `selecting_a_group_and_its_child_moves_the_child_once` (child active, group+child selected) currently FAILS: the child's total placement lands at (3.82,-3.54) instead of the gizmo delta (14,10) — the same value across two different gestures, so the committed delta is not the drag's; investigate the conjugation input at commit for the shadowed-child case (self.parent was recorded for the CHILD's chain at Down — but the single-vs-multi path selection may be emitting through the wrong participant/delta pairing). Gates otherwise green (tools, app-shell, integration except the new test, clippy, fmt). REMAINING (debug continues — instrumented): the committed corners are the CANVAS RECT ROTATED 4.4° ABOUT ITS CENTER (delta = [0.997,0.0767,-0.0767,0.997,3.82,-3.54] — exactly rotate_by(4.4°) about pivot (48,48)), IDENTICAL across three different gestures ((32,32)→(24,20), (32,32)→(46,42), (48,48)→(62,58) — the last dead-centre, 67px from every corner, far outside the 6+18px rotate band) — so the committed state does not come from the drag's Inside-translate. Root-cause hypotheses to check next window: (a) the session's `pivot`/`source` at Down is not the canvas (maybe the representative switch ordered self.layer before self.targets was assigned — fixed for the borrow, but verify self.parent is the GROUP's chain and not stale); (b) hit_test's Rotate-band geometry vs the REAL source rect (the source may be smaller than the canvas — the child's document_bounds may return a small tile-aligned rect rather than None); (c) a second event path re-grabbing. A temporary `eprintln!` DBG sits in transform.rs commit (mode/layer/targets/corners/delta) — REMOVE IT before closing. The multi-path itself is verified correct for two root layers (a_multi_layer test passes); only the group+child representative case misbehaves. |
| 037 | Rendered-content hit testing | 008–009,036 | **verified** | 2026-09-09: NEW `crates/app-shell/src/hit_testing.rs` — `visible_content_at` replaces the raw document-coordinate tile sampling: ancestor-composed visibility; adjustment/generator layers and group containers excluded (groups walked through — group-vs-layer selection explicit via ContentHit.is_group); bounds-candidate filter with content bounds composed through document_transform_of (the full ancestor chain — children of transformed groups are candidates; content bounds exclude effect-only shadows by construction); raster/smart-object alpha sampled at the inverse-mapped point gated by effective_opacity × mask coverage routed through LayerMask::coverage (enabled/inverted/density exactly as the composite; absent tile = raw 0.0 through coverage); text/shape pick on shaped/geometric bounds; locks deliberately do not gate picking (policy documented — Move refuses downstream). ToolContext.content_pick is TRI-STATE (outer None = no shell → the raw sampler fallback stays for direct-begin; inner None = the shell authoritatively found nothing — hidden/masked layers cannot resurrect), filled per pointer sample; MoveTool::layer_under honours it. Tests: six module tests (inside/outside ink, invisible ancestor, adjustment exclusion, opacity threshold both sides, mask half-hiding, text shaped-ink pick) + a tools-side tri-state unit test (an authoritative empty pick blocks the raw sampler over stored ink). **Reviewer PASS round 2** (round 1 REJECT with three criticals: the fallback resurrection leak, a query-aborting `.ok()?`, and the bounds filter in the wrong space for nested layers — all fixed and verified; the debug also found and fixed a dropped channel offset in the tile-byte sampler, pinned by the ink test). Gates: app-shell 448+10+1, tools 114, clippy clean, fmt clean. RECORDED follow-ups: vector masks without rasterized tiles would hide the pick (mirror composite active_mask when reachable); gate the pick on Move/auto-select (it runs per sample for every tool today); thread auto_select_threshold through if a setting ever appears; clipped masks (ClipToBelow) out of scope. Remaining issue: none blocking. |
| 038 | Move options wiring | 010,037 | **verified** | 2026-09-09: **Reviewer PASS round 4** (round 1 REJECT: the commit drain kept card 036's divergent inline matrix double-applying own transforms + the missing on-canvas selection sync; round 2 REJECT: the Group-mode sync emitted the raw leaf instead of the climbed pick + a steps under-count; round 3 REJECT narrowly: the lock doc absent at the named filter + refused transactions over-reporting steps — every fix then verified in source). Landed: the `select_groups` Move option (registry + seam; Group mode climbs the picked leaf to its top-level ancestor group, Layer mode keeps the leaf, an authoritative empty pick stays empty); on-canvas selection sync (Move emits SelectLayer for the CLIMBED pick at Down — a canvas click updates the highlighted layer row, replace semantics documented); Show Transform Controls (a display box over the cached ink published via live_geometry with NO edit session — a no-motion click emits nothing; the option-off clears the cache; the box frames only ink it knows — the active layer's); set drags (auto-select off) move the whole Shift-selected set lock-filtered (the lock policy documented at the filter) via TransformLayers performed at pointer-up through the shared perform_transform_layers (returns 1 on a successful Transaction, 0 on refusal — no phantom steps; both drain arms accumulate out.steps; the commit drain's divergent inline copy replaced by the shared performer); the show-transform box frames only the active layer's known ink (no wrong-ink boxes). Tests: move_options_tests (group climbing both modes, show-transform box without an edit, set-drag one TransformLayers request with the right delta) + the chrome options expectation updated + the integration foundation test restored to its intended selection. Gates: tools 168, app-shell 458, integration 15, clippy clean, fmt clean. RECORDED follow-ups: per-participant live previews for multi-selection Move (card 039 adjacency); the empty-canvas SelectLayer(active) collapsing a Shift set is a recorded design call; a locked-active-layer-at-transaction drill test would pin the refusal path explicitly. Remaining issue: none blocking. |
| 039 | Live Move/transform preview | 013,035–038 | **verified** | 2026-09-09: **Reviewer PASS round 2** (round 1 REJECT solely on the Enter-commit path never settling the preview — a stale lens misrendered an immediate keyboard undo; the settle now sits between the commit and the early return, clearing or re-resolving the lens before Enter returns). Landed: screen-point handle hit regions (hit_test_zoomed divides HANDLE_RADIUS/ROTATE_BAND by ctx.view.zoom — constant on screen at any zoom; guarded against 0/NaN; pinned by handle_hit_regions_are_constant_in_screen_pixels at 4× vs 1×); a stable pivot marker (a 5px orange cross at the recorded pivot — mutated only by Pivot/Inside drags — mapped document→screen through the same camera as the handles, same overlay z-order as the ants, guarded by the geometry's document id); the stale-preview sweep (OpenDocument::has_preview + Editor::clear_stale_previews sweeping every NON-ACTIVE document's lens per frame — a tab switch leaves nothing behind; pinned by a_tab_switch_leaves_no_stale_preview_on_the_tab_the_user_left); the committed result while idle holds via settle_preview's no-geometry arms (pinned by the commit→settle equivalence test). Gates: tools 169, app-shell 459, integration 15, clippy clean, fmt clean. RECORDED follow-ups: hoist the pivot-arm constant beside HANDLE_RADIUS/ROTATE_ORDER constants; an explicit !doc.has_preview() assertion after commit→settle; per-participant Move previews (T036 carry). Remaining issue: none blocking. |
| 040 | Edit tools respect transformed coords | 009,035,039 | **verified** | 2026-09-09: **Reviewer PASS round 3** (round 1 REJECT with three criticals — move/up feeding raw document coords, the done-check test asserting the defect, sample_base unmapped; round 2 REJECT narrowly on SmartObject missing at the handle site — the two hand-maintained mapping copies drifted). Landed: ToolContext.sample_to_layer (the ACTIVE paintable layer's document→layer mapping) computed by ONE shared helper `sample_to_layer_of` (paintable = Raster|Generator|SmartObject, matching kind_owning_pixels bit for bit; singular chains refused upstream) used by BOTH ctx build sites — drift structurally impossible; StrokeTool maps EVERY sample (down/move/up, clone anchors, sample_base) through it, so a moved/scaled layer is painted where the pointer displays; brush radius defined in LAYER pixels (documented nonuniform-scale behavior); the selection-space limitation documented at the mapping comment (full re-mapping rides card 044); text/shape/group/adjustment painting fails EXPLICITLY with NotPaintable at commit (no invisible tiles); done-check test `painting_a_moved_layer_marks_the_displayed_pointer_location`: a raster layer moved +32x, a brush stroke at the DISPLAYED location marks layer-local (25,32) black while layer-local (57,32) stays clean (the raw-coordinate bug's signature), the composite shows the ink at the displayed spot, and undo restores the original source hashes. Gates: tools 169, app-shell 460, clippy clean, fmt clean. RECORDED follow-ups: the OTHER paint tools (Eyedropper, Bucket, Wand, RedEye, Patch, MagicEraser, Gradient, Shape-rasterize) still consume raw document-space positions against transformed layers — route them through sample_to_layer_of as follow-up units; the StrokeBuffer clip-space conflation (layer-space dabs clipped against the document rect); a SmartObject variant of the done-check; cross-reference card 044 in the selection-space comment. Remaining issue: none blocking. |
| 041 | Scale/rotate modifiers + numeric fields | 039 | **verified** | 2026-09-09: **Reviewer PASS round 1.** Landed: `drag_with(mode, handle, from, to, preserve_aspect, around_center)` (drag() delegates false/false — existing callers unchanged); scale_corner with the two card-041 modifiers — default corner scaling PRESERVES ASPECT (the two axis factors collapse to the dominant one; Shift flips the constraint to free scaling), Alt scales ALL corners about the recorded quad center (the centroid is invariant; degenerate rel_i guarded); wired at the transform tool's on_pointer_move from event.modifiers (preserve = Corner && !shift; Edge/Inside/Rotate/Pivot drags unaffected); the numeric-fields draft model `TransformState::set_rect(x,y,w,h)` — the same corners field dragging mutates, the pivot re-centered, non-finite and non-positive sizes REFUSED before mutation (no invalid matrix from numbers). Tests: aspect-preserved vs free corner scaling, around-center invariance, numeric set + refusals keeping the draft; the shell's live-transform test updated to drag diagonally (a horizontal-only corner drag legitimately cannot move the corner under aspect preservation). Gates: tools 124, app-shell 450, integration 15, clippy clean, fmt clean. RECORDED follow-ups: the X/Y/W/H/rotation/pivot numeric FIELD wiring rides the options-bar work (the set_rect substrate is landed); the set_rect error payload picks the wrong offending coordinate when y/h is the non-finite field (diagnostic-only); set_rect re-centers the pivot (Photoshop preserves the offset — document or preserve when the fields land); Skew-mode corner drags now also preserve aspect by default (pin or restrict preserve to Scale mode); validate the CONSTRUCTED corners against overflow at the field. Remaining issue: none blocking. |
| 042 | Snapping/alignment to real bounds | 008,036,041 | **verified** | 2026-09-09: **Reviewer PASS round 3** (round 1 REJECT with three criticals — base_bounds never assigned, the gesture route never fed candidates, the commit re-used the raw delta; round 2 REJECT on a u32-saturation collapse in tight_document_bounds for ink mapped to negative doc coords). Landed: tools-side `SnapCandidate{axis,doc}` + `SnapAxis` + the pure `snap_delta(base,delta,candidates,threshold_doc)` (per axis the NEAREST candidate within the threshold for any of the moving rect's min/max/center carries the shift — the moving GEOMETRY snaps, not a cursor dot; empty candidates = identity); shell `snap_candidates_for` — canvas edges/center + every layer NOT in the selection set (nor a descendant of one, parent-walk; candidate LAYERS capped at 64), tight ink bounds via `tight_document_bounds` (raster = `alpha_bounds` corner-mapped through `document_transform_of`, i64 extents, checked_sub; text/shape = exact `document_bounds`); threshold = `SnapSettings::default().threshold() / zoom.max(0.05)`; BOTH ctx routes (handle gesture + off_pointer commit) fill `ctx.snap_candidates`/`snap_threshold_doc`/`active_layer_ink_bounds` pre-access; MoveTool caches the tight base at pointer-down (tile-level fallback), snaps the tentative delta at move, re-snaps the release delta at up (a no-Move drag snaps too; cancel clears the base); NEW ToolContext field `active_layer_ink_bounds`. Tests: `a_move_drag_snaps_the_moving_geometry_to_another_layers_edge` (real ToolPointer::handle route: zoom-1 drag commits tx=22 so A's right edge lands on B's left edge at 62, B stays put, y undrifted; zoom-2 drag commits the CENTER feature onto the canvas center exactly — done-check 'logo centers at multiple zooms'), `tight_bounds_survive_ink_mapped_to_negative_document_coordinates`, snap_tests unit (in/out-of-threshold + per-axis). Gates: tools 173, app-shell 462, integration 15, clippy clean, fmt clean. RECORDED remaining (not blocking): align/distribute panel ACTIONS still unwired (MoveTool::align math exists — menu routes absent); smart-guide INDICATOR drawing on the shell-route snap; hidden layers contribute candidates; hardcoded default SnapSettings (user threshold/toggles ignored); no effects-reach filter (styled_bounds); shift-axis bypasses snap precedence at up; self-snap dead zone when the active layer is deselected; per-gesture candidate rebuild cost. Remaining issue: none blocking. |
| 043 | Linked layers + mask movement | 036,040–042 | **verified** | 2026-09-09: **Reviewer PASS round 2** (round 1 REJECT with two criticals: the counter-transform only preserved the pose when the delta COMMUTED with the layer transform — the fix conjugates through the pre-update L, `Mt ← L⁻¹M⁻¹L·Mt`, exact for any M; and the tile-cache key hashed plain `rect` for non-identity unlinked poses — the fix hashes the pose pre-image). Landed: `with_link_chain(participants, linked)` (dedup, first-seen order, flat acyclic traversal) wired at BOTH move commits — MoveTool expands BEFORE the lock filter (locked chain members follow card 036's stay-put rule) and TransformTool joins before its all-or-nothing lock check; ToolContext.linked_layers filled at both shell ctx sites. Mask movement: `LayerMask.transform: Box<Affine2>` (boxed for the LayerPatch size guard) — pose = layer.transform ∘ mask.transform; LINKED keeps identity (rides exactly as before); UNLINKED accumulates the conjugated inverse per content transform (pose frozen); RELINK preserves the accumulated transform (no jump); undo restores the pair exactly; a degenerate pre-update transform skips the compensation (no NaN poisoning). Compositor: `mask_coverage` is THE pose-aware read (identity pose = the historical 1:1; non-identity resamples through the pose pre-image with feather-grown mask-tile bounds); render_source no longer double-pre-images the mask (applied in document space after the content resample); adjustment_coverage delegates; hash_ids hashes the pose pre-image; hash_layer_props hashes the mask transform. Migration: missing field → identity via LayerMaskRepr default. Tests: chain move/dedup/locked-stay-put (tools ×3), counter-transform + undo + rotated-layer pose (editor-core ×3), migration + serde round-trip (layer-model), raster stay-put + relink pixel-identity (compositor), the painted() fixture now carries a non-identity mask transform through the whole-document save/load equality (project-format). Gates: tools 176, editor-core 115, compositor 190, layer-model 129, project-format 118, app-shell 462, integration 15, clippy clean, fmt clean. RECORDED remaining (not blocking): live-preview drag shows an unlinked mask riding then snapping back on commit (preview/commit pose disagreement — needs the committed transform during previews); PSD import/export mask offsets still use the relative_to_layer convention (identity default correct; a persisted non-identity transform is unmapped); the single-bool link model (multi-chain link IDs = the persisted-id upgrade); unbounded-mask non-identity pose resample cost. Remaining issue: none blocking. |
| 044 | Pixel-selection vs non-affine explicitness | 035,040,043 | **verified** | 2026-09-09: **Reviewer PASS round 1** (two minor warnings fixed in-place: the parametric flag is now computed AFTER the quick-mask reroute at both shell sites; the clamped dest_bounds doc no longer points at phantom overlay callers). Landed: `LayerKind::parametric()` (Text|Shape|SmartObject — the kinds a pixel-patch resample would silently rasterize) + `ToolContext.active_layer_parametric` filled at both shell ctx sites; TransformTool::commit refuses `Distort|Perspective|Warp` on a parametric Layer target with the explicit `ToolError::NonAffineParametric` BEFORE any pixel write — the affine modes stay editable for every kind (card 035's TransformLayer path); the resample no longer canvas-clamps (`dest_bounds_unclipped` + `union_unclipped` — the tile store holds off-canvas content, so stored ink landing outside the picture survives; the clamped variant stays for the pinned tests; MAX_PATCH_TILES still bounds allocation); the collapsed-quad refusal is now explicit and canvas-independent (`quad_signed_area` shoelace guard — the old clamped dest bounds caught it incidentally and the unclamped bounds no longer do). Tests: `a_warp_on_a_parametric_layer_is_refused_not_rasterized` (warp on a TEXT layer → NonAffineParametric, no commands, no tiles under the text layer's pixel key; a SKEW on the same layer commits as a TransformLayer), `a_distort_keeps_the_ink_that_lands_off_the_canvas` (ink pushed past the canvas edge survives off-canvas), the app-shell singular-quad test restored to refusing. Gates: tools 178, layer-model 129, app-shell 462, integration 15, clippy clean, fmt clean. RECORDED remaining (not blocking): the options-bar mode-availability UI (graying Distort/Perspective/Warp for parametric layers — the tools refusal is this milestone's disable); warp-refusal could fire at mode entry; a warp mesh collapsed to a line skips every cell but still clears the source (pre-existing — the shoelace treatment on the mesh hull would close it); full non-affine editable-object parity is NOT claimed. Remaining issue: none blocking. |
| 045 | Transform workflow verification | 034–044 | not started | — |
| 046 | Reusable full-source placement builder | 018,035,045 | **verified** | 2026-09-09: **Reviewer PASS round 2** (round 1 REJECT: the tile-shift math only worked for tile-multiple origins — any other origin silently misplaced every pixel, and the negative-origin test passed without checking pixel alignment). Landed: NEW `crates/app-shell/src/placement.rs` (`place_source` + `Placement`, registered + re-exported) — validates width/height and the rgba8 byte count BEFORE any tile allocation; composes each PLACED tile by sampling the source at `placed_pixel - origin` (per-pixel re-slice straddling source-tile boundaries — ANY origin honored exactly, negative or non-multiple); fully-transparent placed tiles skipped (provably identical to the compositor's absent-tile answer); one Transaction (create layer + PaintTiles); the result retains `source_origin` (may be off-canvas) and `source_size` explicitly so transparent padding cannot hide the intended size; NO canvas-sized copy (the canvas never enters the math; work is source-proportional, each source pixel read exactly once); i32-span and index guards for pathological origins. Tests: the 600×500 asymmetric source on a 64×64 doc retains its far-right/bottom ink in stored tiles (alpha checked, padding transparent); the arbitrary-origin test asserts real pixel values on BOTH sides of the ink (the phase-alignment check round 1 lacked); malformed sources refused before allocation. Gates: 68 binaries 0 failed; app-shell 455+10; clippy clean; fmt clean. RECORDED: persisting source metadata into the asset table rides the placement-consuming cards (047+ decide fit/center first). Remaining issue: none blocking. |
| 047 | Define fit/center placement and color conversion | 046 | **verified** | 2026-09-09: **Reviewer PASS round 1** (two warnings fixed in-place: the per-pixel ICC re-parse → the infallible `to_linear` behind the upfront gate; and the WORKING space's silent identity fallback → gated symmetrically — an unsupported working profile is now an error too). Landed: `place_source_fit(image, name, canvas, working, allow_upscale, tiles)` — aspect-preserving fit, centered, `scale.min(1.0)` unless upsizing is asked; the fit is a LAYER TRANSFORM appended to the placement Transaction (create → paint full-source tiles → TransformLayer; a canvas-filling scale-1 source emits no transform command); the source pixels convert into the working space via `to_linear(source)` → `from_linear(working)` (alpha unchanged) with the original `icc_profile` bytes and the source space retained on the result; an unsupported profile on EITHER side is `ImportError::UnsupportedColorProfile` — no silent sRGB fallback; a zero-area canvas is refused. Tests: the four labeled corners composite at their scaled centered spots (scale 0.25, offset (0,12.5)) with the source's full-resolution tile (1,1) still stored (nothing resampled — scaling back up exposes the detail); a smaller asset is NOT enlarged (scale exactly 1, centered); a Display-P3 source into a Linear-sRGB working space matches the color crate's reference conversion within ±1; garbage ICC bytes → the error. Gates: 68 binaries 0 failed; app-shell 459+10; clippy clean; fmt clean. RECORDED: nothing in production calls place_source_fit yet — the shell's Place-Embedded wiring rides the placement-consuming cards. Remaining issue: none blocking. |
| 048 | Make placement atomic and active-layer aware | 046,047 | **verified** | 2026-09-09: **Reviewer PASS round 1** (stale doc comment fixed in-place; one follow-up recorded). Landed: Editor::place_path rewritten — decode + source-byte read happen BEFORE any mutation (a failed decode/read changes neither the layer stack nor dirty/history); the placement goes through `place_source_fit` (FULL source, fit as a layer transform, color conversion into the doc's working space) replacing the old clipping path; `Placement::with_layer_kind` retypes the placed layer to a SmartObject over the registered asset while keeping the builder's layer id (rewrites the Transaction's CreateLayer in place); insertion ABOVE the active layer (same parent, its index) via an inverse-safe trailing MoveLayer; the placed layer becomes the selection; the asset registration is a field write on the success path only, under a DOCUMENTED STORAGE POLICY (the asset table is append-only for the session — an undone placement leaves its record and embedded blob in place: unreachable, immutable, never a live reference to a removed asset). Tests: place→undo removes the whole object (the asset record survives per policy) → redo restores (net-neutral depth); a failed decode changes no layers/history/dirty; the placement lands directly above the active layer and is selected; an EMBEDDED placement survives native save + reopen with the source file DELETED (composite identical). All pre-existing place tests (smart-object renders, edit-contents round trip, linked re-read) pass unchanged. Gates: 68 binaries 0 failed; app-shell 463+10; clippy clean; fmt clean. RECORDED remaining: refresh_linked_sources still canvas-clips its repaint — semantically inconsistent with the fit-transform placement model for oversized linked sources (route refresh through the builder — follow-up); a live transform preview after placing (interaction work; the fit transform already positions the placement); TOCTOU on decode+read (read bytes once, decode from the buffer). Remaining issue: none blocking. |
| 049 | Make drag-and-drop context-sensitive | 048 | **verified** | 2026-09-09: **Reviewer PASS round 1** (two warnings fixed in-place: the project classification now uses `Editor::is_project_path` — manifest-bearing package DIRECTORIES without the extension also open; and open-branch failures route through `open_path`'s Result into the same accumulator instead of the blocking modal — the "never blocks" claim is now true). Landed: `Shell::on_dropped_files` (crates/app-shell/src/shell.rs) — THE routing seam the WindowEvent::DroppedFile arm calls: an `.rstudio` package (name or manifest), `.psd`/`.psb` OPENS; anything else PLACES into the active composition via place_path (card 048's builder) when a document is open, OPENs when none; per-file steps in arrival order; every failure (open or place) accumulates and is reported in the status AFTER the batch — no later success buries an earlier failure; no blocking dialogs. Tests (shell.rs tests driving the seam directly, like the on_key/on_pointer tests): dropping an image on an open canvas adds a placed SmartObject LAYER (no second tab); dropping a native .rstudio package OPENS it as a second document; dropping with NO document open opens the image; [first.png, garbage, second.png] places both in order (the first file's layer at the bottom of the placed stack) and reports the failure. Gates: 68 binaries 0 failed; app-shell 467+10; clippy clean; fmt clean. RECORDED remaining: no HoveredFile drag-over affordance (a natural follow-up); extension-only .psd detection (content sniffing if users hit it). Remaining issue: none blocking. |
| 050 | Smart-object refresh preserves quality | 048–049 | **verified** | 2026-09-09: **Reviewer PASS round 2** (round 1: PASS on the literal card check + 2 majors/5 minors; all fixed and re-verified). Landed: `refresh_linked_sources` rebuilt as the card demands — the replacement IS the placement builder (`DecodedImage::decode_path` → `working_space_pixels` → `slice_source_tiles`, so refresh cannot drift from placement; an unsupported profile errors, never silently recolors); layers grouped per (document, asset) with the source decoded ONCE per group and ALL sibling layers (duplicate/copy-paste share the asset) refreshed in ONE labeled `Transaction` ("Refresh {layer} +N sibling(s)") with the stamp written only after apply succeeds (reviewer major 1); changed source dimensions handled DELIBERATELY: renormalization = old/new dims ratio conjugated through the layer's own transform (`own·ratio·own⁻¹` pre-multiplied by `TransformLayer` ⇒ `own·ratio`, exact for non-uniform ratios and any existing rotation/skew) so the on-canvas footprint is identical across a resolution change; ghost-tile cleanup clears the coords the new source no longer stores; pre-048 documents without a recorded size refresh without a jump (transform left alone); the recorded size update rides INSIDE the transaction via the new `Command::SetAssetSourceSize { asset, size: Option<(u32,u32)> }` (editor-core: refused with `CommandError::AssetNotFound` when the row is missing, inverse restores the previous Option incl. None, additive wire format, `DirtyTiles::none()`, history `StepKind::Transformed`) so undo reverts appearance AND anchor together (reviewer major 2 — otherwise refresh→undo leaves the record at the new resolution and the next refresh scales by a stale ratio); a missing linked source reports per sibling ("source is missing") and keeps the last good cached appearance; a stamp change with IDENTICAL content (first refresh after reopen — stamps are session-only) re-decodes but pushes NO undo entry. Tests (app-shell 474 lib): 16→32 refresh keeps the ink footprint pixel-exact + ink is the new colour + scale halved (direction-pinned) + new-resolution tiles stored + identity/kind preserved + ONE undo entry restores transform and byte-identical composite; refresh→undo→48 file → footprint still the original (discriminates the stale-size bug); siblings: duplicate shares the asset, both hold new pixels, one history entry, label names the layer, undo restores both; 600×500→32×32 shrink clears ghost tiles (2,1)+(1,0), footprint preserved, surviving tile holds new pixels; missing source: summary says "skipped" + status names "missing" + byte-identical composite; identical content: "No linked source has changed", zero history entries, stamp learned; embedded commit: edit-contents tab is the tile-aligned SOURCE frame (768×512 for 600×500), commit writes at source resolution far from the canvas; editor-core: SetAssetSourceSize round-trip (None→Some, Some→Some, undo restores None, byte-exact) + missing-asset refusal changes nothing. Gates: fmt clean; clippy -D warnings 0; `cargo test --locked --workspace` 68 binaries 0 failed. RECORDED remaining (reviewer-accepted deferrals): linked-mask coverage alignment drifts relative to image content on a resolution-changing refresh (mask DATA preserved; canvas-fixed mask = T043 remainder); a degenerate (zero-area) layer transform makes refresh refuse loudly (safe: NotInvertible → transaction rolls back); mtime-only stamps re-decode once after reopen (now with NO undo entry; content-hash stamps deferred). Remaining issue: none blocking. |
| 051 | OS bitmap clipboard adapter (`clipboard.rs`) | 005,028,046 | **verified** | 2026-09-09: **Reviewer PASS round 1** (2 warnings fixed in-place: wrong doc link pointed at the internal pixel store's setter — now names `set_image_clipboard` and the distinction; the host-bound screenshot check is now a repeatable `#[ignore]`d helper AND was demonstrated for real). Landed: `crates/app-shell/src/clipboard.rs` (new) — `ClipboardImage` (straight RGBA, row-major; `validate`: positive dims, byte-count == w*h*4, both dims ≤ MAX_CLIPBOARD_DIMENSION 16384, bound checked BEFORE the byte multiply so no overflow), `ClipboardError {Busy, Unavailable, InvalidImage}` (every kind recoverable, thiserror), trait `ImageClipboard {get_image -> Result<Option<_>,_>, set_image}`; `OsClipboard` = per-operation `arboard::Clipboard` (opened only for the transfer, closed immediately — arboard's documented Windows behavior, zero-sized handle, nothing held between ops so shutdown needs no explicit release; `ContentNotAvailable` on get = `Ok(None)`, the NORMAL no-image answer; `ClipboardOccupied` → Busy; rest + `#[non_exhaustive]` catch-all → Unavailable); `FakeClipboard` = in-memory slot + `with_failure` injection + `seed`, SAME validation as the real path. Injection: `Editor::image_clipboard: Box<dyn ImageClipboard>` defaulting to OsClipboard in with_state (desktop binary gets the OS clipboard by construction), `set_image_clipboard` for tests, `image_clipboard_mut` for card 052 — deliberately distinct from the pre-existing INTERNAL `clipboard: Option<Clipboard>` store (card 005 Edit▸Copy pixels; its own doc comment names process-crossing as "a separate job" — card 051 IS that job). Dependency: arboard 3.6.1 ALREADY a direct app-shell dep (text route) with `image-data` in default features (verified against the registry manifest) — NO version/feature change, manifest comment added grounding the choice. Tests (7 passing + 2 host-bound ignored): fake round-trips opaque AND transparent RGBA (alpha 0/128 preserved byte-exact), empty = Ok(None) not an error, malformed refused not panicked (byte-count mismatch/zero dim/over-bound/at-bound accepted), set_image validates without half-state, Busy/Unavailable as ordinary errors; `os_clipboard_image_round_trip` (#[ignore]) RUN on this Windows host: set 2×2 → get back BYTE-EXACT; `os_clipboard_read_current` (#[ignore], the repeatable manual-check helper) RUN: read a FOREIGN 7×5 image seeded onto the OS clipboard by a .NET process — the cross-app/screenshot payload path (foreign CF_DIB) works through the adapter. NOTE recorded: running the two host-bound helpers CONCURRENTLY races on the one OS clipboard (set→get interleaving) — run them one at a time (`--test-threads=1`), sequential is green both ways. Gates: fmt clean; clippy -D warnings 0; workspace 68 binaries 0 failed. Remaining issue: none blocking. |
| 052 | Copy/paste freshness rules | 048,051 | **verified** | 2026-09-09: **Reviewer PASS round 2** (round 1: PASS on mechanics + 1 critical/3 warnings; all fixed and re-verified). Landed: Copy/Copy Merged ALSO write the OS image clipboard (same pixels; a refusal degrades to a status note "(the OS clipboard refused: …)", the internal store still filled) and remember an ownership fingerprint (dims + every byte, self-comparison only); plain Paste reads the OS clipboard FIRST and FRESH (never cached): a foreign payload wins and routes through full-source placement (`Editor::paste_external_image` — `place_source_fit`, no upscale, explicit centered semantics documented, pixels read as sRGB, embedded asset bytes are a PNG of the same pixels so Edit ▸ Contents re-decodes, inserted above the active layer, becomes the selection, one undoable step); the editor's OWN copy is recognized by fingerprint and keeps the internal at-origin route; OS clipboard None/Busy/Unavailable falls back to the internal store (the card's "internal Copy → Paste still works when the OS clipboard is unavailable"); Paste Into keeps the internal path (card 053 owns its mask semantics) and is GATED on the internal store only (`ClipboardState::has_internal_pixels`) so the menu cannot enable what perform cannot do; a TEXT payload is never fetched or converted (no URL becomes an image); text-session clipboard chords unchanged (card 028 route wins first). Menu enablement: `ui::ClipboardState` gained additive `external_pixels` — plain Paste enables on internal OR external, Paste Into on internal only; the OS availability probe is CACHED (1 s TTL in `Editor::os_image_probe`, invalidated on window focus changes — the moment a foreign copy can arrive — and set by our own copy; context() is built per frame so an uncached 33 MB screenshot read per frame was the round-1 critical). Keyboard routing (card target): `Action::Copy/Cut/Paste` (catalogue, id slugs, labels, Category::Edit) bound to Ctrl+C/X/V, dispatched through THE SAME `menu_bridge::perform` route the Edit menu drives (one policy), failures become status lines; egui-focused fields keep their clipboard chords (route_key already ignores chords under egui text focus — the round-2 dead-guard was removed); text sessions consume the chords first (card 028). Clipboard hardening: `OsClipboard` serializes ALL OS access behind a process-wide Mutex (parallel tests touching the real Windows clipboard caused STATUS_HEAP_CORRUPTION before it; the shell's direct arboard text calls now share the same lock via `with_os_clipboard_lock`); `check_dims` extracted so `set_image` validates without double-cloning. Hermeticity: `FakeClipboard` injected in editor_tests::bare, menu_bridge::opened, chrome::editor — default tests never touch the real OS clipboard. Tests: 6 new/updated (external-wins with smart-object + foreign pixels + status pin; own-copy internal route; Busy fallback with refusal note; enablement split Paste/Paste Into; Copy Merged OS write + internal paste; keyboard route Action::Paste → foreign smart object) + every_action_does_something preconditions (Copy's observable change = the clipboard, not a revision bump). Host-bound evidence RUN on this Windows host: a FOREIGN 11×7 image seeded by a .NET process (SetImage + OleFlushClipboard so it survives the owner) pasted through the REAL menu route → "Pasted 11×7 from the clipboard", placed SmartObject; arboard set→get round trip byte-exact. Gates: fmt clean; clippy -D warnings 0; workspace 68 binaries 0 failed. RECORDED remaining (reviewer-accepted): the ownership fingerprint assumes the OS round-trips Copy byte-exact (pinned on Windows; some Linux clipboard managers convert pixels — a non-exact return would route our own copy through external placement, cosmetic); TTL staleness ≤1 s for background writers (degrades to a status line, never a crash); FakeClipboard::seed bypasses validation (card 052 tests must seed valid images). Remaining issue: none blocking. |
| 053 | Paste Into with real mask | 009,052 | **verified** | 2026-09-09: **Reviewer PASS round 1** (2 warnings fixed in-place: the lost "selection hides all of it" guard restored — a selection with bounds that never intersects the origin-pinned clip now refuses instead of pasting an invisible layer behind a success message; the fractional test's ink reference corrected to the pixel the clip actually holds, original(9,9) for canvas (5,5); suggestion applied: paste selects the exact created layer id, not root().first(), and the coverage helper's tile loop is bounded by the selection's bounds). Landed: menu_bridge::paste(into=true) rebuilt per the card — the FULL copied image is stored on the new layer (the destructive alpha multiplication is DELETED); confinement is a retained raster mask derived from the selection, attached INSIDE the same Transaction (SetLayerProperties{mask: Patch::Set(LayerMask::new)} linked=true/identity — the layer is born at canvas origin with an identity transform so document-space coverage aligns exactly and rides the layer as it moves) + PaintTiles(PixelTarget::Mask) coverage edits; one undo removes layer+pixels+mask together (verified: mask attach precedes mask paint so resolve_pixel_key never sees a maskless layer; transaction rollback atomic). New focused helper `menu_bridge::pixels::selection_coverage_tiles(&Selection, w, h) -> Vec<(TileCoord, Vec<u8>)>` — card 057's four mask-creation ops reuse it: absent tile = hidden (the compositor's missing-tile convention), fully-inside tiles = all-255 present, boundary tiles carry per-pixel coverage (Rect shape fast path; Selection::Mask via coverage_at so fractional lasso/wand samples map through unchanged), Selection::None yields empty (no selection = all pixels selected = paste unmasked), tile loop bounded by selection bounds. Empty selection refuses ("the selection holds no pixels"); off-canvas selection refuses ("the selection hides all of it"). Paste now selects what it created (both routes; the card-052 external route already did). Tests: paste_into_keeps_the_full_image_under_a_selection_mask (stored pixels = full crop incl. a probe OUTSIDE the selection; composite shows backdrop outside; depth +1; undo removes layer AND mask — backdrop maskless; redo; Mask(Toggle) disables → the crop's ink shows at a pixel inside the crop but outside the selection); paste_into_maps_fractional_coverage_through_the_mask (Selection::Mask with 128 coverage → composite strictly between ink and backdrop — blending not cutoff; the exact byte is not pinned because compositing runs in the working/linear space, recorded). External-payload Paste Into remains card 052's documented out-of-scope (menu gates Paste Into on the internal store). Gates: fmt clean; clippy -D warnings 0; workspace 68 binaries 0 failed. RECORDED: disabling the mask reveals the full COPIED image (the clip was alpha-masked at copy time by card 052's Copy; for lasso selections the recoverable pixels are the clipboard content, not the pre-copy source layer). Remaining issue: none blocking. |
| 054 | Asset reuse/clipboard verification | 046–053 | **verified** | 2026-09-09: **Reviewer PASS round 1** (1 warning fixed in-place: the paste-into layer's persistence after reopen is now pinned — tile map + mask tile map non-empty and the editable mask present; plus a history-depth pin on the garbage refusal and the dead first FakeClipboard removed). Landed: `thumbnail_workflow.rs::the_asset_reuse_and_clipboard_workflows_survive_native_persistence` — composes a 300×200 oversized background (via place_path, the route card 049's drop seam lands in; the Shell-level drop tests live in shell.rs tests and are recorded in row 049), a 40×50 portrait through the REAL Place menu route (ScriptedDialogs place_files + perform PlaceEmbedded), a 16×12 logo pasted from a seeded FakeClipboard through perform Paste (card 052's external placement route), and a 20×20 LINKED asset; verifies 5 smart objects + canvas, records transforms/kinds; Paste Into in-session (Copy from the canvas pixel layer → editable mask + stored tiles, re-pinned AFTER reopen: tile map + mask coverage non-empty); the error path (garbage.png refuses with no layer or history damage); save_to the native package → the original source PNGs RENAMED away + the LINKED file DELETED → a fresh Editor reopens the package → the whole layer stack (names), a byte-identical composite, and per-layer transform/kind all survive; the oversized background's far source tile (1,0) + full-resolution pixels survive persistence (never clipped to the canvas); refresh_linked_sources reports the vanished linked file ("skipped" + status "missing") with a byte-identical cached appearance. Clipboard-dependent manual checks stay SEPARATE in their #[ignore]d host-bound helpers (clipboard.rs round-trip/read-current; menu_bridge foreign-paste) — all previously RUN green on this Windows host with a foreign .NET-seeded payload. **PHASE 5 GATE PASS 2026-09-09**: cargo fmt --all --check PASS; cargo check --locked --workspace --all-targets PASS; cargo clippy --locked --workspace --all-targets -- -D warnings PASS (0 findings); cargo test --locked --workspace PASS — 68 binaries, 0 failed (app-shell lib 488+3 ignored; integration-tests thumbnail_workflow 17+1; ui 965+satellites). Remaining issue: none blocking. |
| 055 | Mask thumbnail targets edit target | 007,014,040,054 | **verified** | 2026-09-09: **Reviewer PASS round 2** (round 1 REJECT with 1 critical + warnings; all fixed and re-verified). Landed: the Layers row carries TWO thumbnail wells — the content well (existing, now clickable) and a mask well (only on layers with a mask, 60% width, mask glyph dimmed when disabled) — clicking either SELECTS THE ROW FIRST (select_only + Intent::SelectLayers, exactly the row's own click; the round-1 critical was the target resolving against the previously active layer) and then emits Intent::SetEditTarget (mask:true/false); the shell's apply_chrome applies select_layers BEFORE the target (same ChromeOutput, order verified). Target border: THE target row's well (row.active + focused kind) draws the thick SelectionStroke border; every other well hairline. Both tool routes resolve through ONE shared resolver `resolve_edit_target(doc, quick_mask, quick_mask_layer, edit_target_is_mask, active_layer, active_mask) -> PinnedEditTarget {layer, mask, paint}` (quick-mask reroute + sticky edit target + maskless fallback in one place; off_pointer unconditional, handle calls it at the Down); the gesture PINS the full triple (PinnedEditTarget on ToolPointer, snapshotted before the tool borrow and written back after) — a mid-gesture thumbnail click or layer chord redirects nothing, and the Up's commit rides the pin; the precomputed bounds/ink/parametric describe the pinned layer; quick mask remains a temporary mode above the sticky target (exiting restores it — the resolver never mutates the sticky kind). New `Editor::edit_target_is_mask()` (validated read-time). **E08 reproducer FLIPPED GREEN** (#[ignore] removed; the intent route replayed exactly as apply_chrome does; recorded in the reproducers table). Tests: ui (mask thumb click → SetEditTarget{true} + property_focus; content click back; the NON-ACTIVE-ROW case selects + aims correctly — the round-1 critical's discriminating test), tool_input (mask-only painting: layer hashes unchanged + mask tiles 0→1; back-to-content; mid-gesture flip cannot redirect; maskless fallback paints content), reproducers E08. Gates: fmt clean; clippy -D warnings 0; workspace 68 binaries 0 failed. RECORDED (reviewer-accepted): the ui property_focus echo diverges from the validated target when a mask is deleted while focused (inherited card 007 display-echo design; painting correctly falls back — the echo fix rides a later card); remove-mask-mid-gesture commit refuses via pixel_key (nothing written; a dedicated error variant is a polish item); layout at large thumb_scale on a narrow panel is cosmetic. Remaining issue: none blocking. |
| 056 | Undoable/composable selection edits | 055 | **verified** | 2026-09-09: **Reviewer PASS round 3** (round 1 PASS-with-warnings; round 2 FAIL on two claimed-but-not-applied comment fixes — both verified applied by grep + read in round 3, per the reviewer's own pass criterion). Landed: the selection-apply loop in tool_input.rs rewritten — SelectionEdits fold SEQUENTIALLY against a local `current` (add on top of replace composes exactly as the direct assignment did), each non-no-op result becomes `Command::SetSelection { selection }` (apply captures the previous selection as an exact, byte-comparing inverse), one gesture's edits coalesce into ONE `Transaction` labeled "Select" (today every selection tool emits at most one edit per gesture, all at Up — the transaction branch is future-proofing, documented); the direct `document.selection` assignment + mark_dirty are gone (dirty rides apply_command's touch); failures are all-or-nothing per gesture. The Select MENU rides the same route: set_selection (All/Deselect/Inverse/Modify/Feather/Grow/Border) + Save/Load/Reselect selection changes all go through `apply_command(Command::SetSelection)`; the stored_selection/bookkeeping clears remain direct field writes with the recoverability invariant documented (saved_selections keeps the entry, so undoing a Load stays recoverable). History row label for a bare SetSelection: "Select" (was the gizmo-era "Transform Selection"; the menu item is separate and untouched). Steps reporting: the depth capture moved ABOVE the selection fold so a selection gesture reports its step (pinned by the marquee test's `steps == 1` assertion). Modifier/sampling pinning verified pre-existing in the tools (select.rs captures op at Down: "releasing shift halfway through a drag must not change whether this gesture adds or replaces"). Tests: marquee UPDATED (1 history step + steps==1 + undo removes the selection + redo restores the rectangle); NEW a_selection_gesture_composes_add_and_subtract_through_history (replace + shift-add + alt-subtract = 3 steps; undo walks each back exactly; redo replays the composed selection; composite byte-identical through everything — not one pixel moves); NEW a_committed_selection_survives_save_and_an_abandoned_gesture_leaves_no_trace (save_to + reopen preserves the committed selection; a down/move/cancel gesture emits nothing); NEW select_all_is_one_undoable_step (menu route); SetSelection serde round-trip (Rect + fractional Mask) added to command_variants_serde_roundtrip. Stale docs updated everywhere the old model was asserted (tool_input module doc, set_selection header, dispatch-site comment, document.rs stored_selection, tools SelectionEdit, quick-mask exception wording; the orphaned MODIFY_RADIUS doc moved onto the constant). Gates: fmt clean; clippy -D warnings 0; workspace 68 binaries 0 failed. RECORDED: the stored_selection clear is not part of the command (asymmetric but recoverable — documented in document.rs); quick-mask enter/exit keeps its direct save/restore (mode machinery per card 055's framing); intersect (shift+alt) is covered at the tools/combine level but not driven shell-level. Remaining issue: none blocking. |
| 057 | Four distinct mask-creation ops | 055–056 | **verified** | 2026-09-09: **Reviewer PASS round 2** (round 1 REJECT with 2 criticals — Hide Selection violated the absent-tile convention outside the selection's bounding tiles, and the document→linked-mask-local conversion was missing; both fixed with a rewritten helper; round 2 PASS-with-warnings, all four findings fixed and verified). Landed: the four menu ops (Reveal All / Hide All / Reveal Selection / Hide Selection) now route from ui/src/menu.rs as `MenuAction::Mask(op)` action intents to the app (the bare-attach emit deleted; the menu keeps gating — layer present, already-masked refuses creation, selection required where applicable) and land in `create_mask` (menu_bridge): one Transaction of SetLayerProperties{mask: Patch::Set(LayerMask::new)} (linked/identity — the compositor samples through the layer transform) + PaintTiles(PixelTarget::Mask) — atomic, undo removes mask+coverage together. New shared helper `pixels::mask_creation_coverage(selection, layer_to_doc, layer_content_tiles, canvas_w, canvas_h, MaskCoverageMode) -> Result<Vec<(TileCoord, Vec<u8>)>, String>`: coverage computed in LAYER-LOCAL space (doc-space selection pre-imaged through `layer_to_doc.inverse()`; per-pixel local→doc via the transform, `Selection::coverage_at`'s 0..1 f32 sampled at the doc point when in-canvas else 0); the tile walk covers the UNION of the layer's content tiles and the canvas rect's pre-image (Reveal All stores 255 over every tile the layer touches; Hide Selection REVEALS outside the selection — including far tiles and off-canvas — per the compositor's missing-tile-means-hidden convention); per-pixel modes: RevealAll c=1, HideSelection c=1−selected, RevealSelection c=selected, HideAll → Ok(empty) (the absent-tile convention IS the state); all-zero tiles omitted; a bounded walk (256 tiles/axis) refuses an exploded pre-image with an Err instead of storing gigabytes; `floor` (not truncation) on pre-image corners so negative fractional tiles are not dropped. Already-masked rule: “the layer already has a mask — delete it first”; missing selection: menu “There is no selection”, app “Select an area first” — both pinned. **E09 reproducer FLIPPED GREEN** (#[ignore] removed; drives `menu_bridge::perform` — the real app route; the menu-intent check asserts the op resolves ENABLED, non-vacuous; recorded in the reproducers table). Tests: the_four_mask_creation_ops_produce_four_distinct_real_coverages (pink/blue two-tone image, four undo-separated ops → four distinct stored coverages + composites; the before-composite is captured pre-undo so Reveal All's assertion is not vacuous); mask_creation_on_a_multi_tile_canvas_respects_the_missing_tile_convention (300×64, selection in tile (0,0) only: Hide Selection hides inside AND the far tile (x=200,y=30) is STORED revealed 255; Reveal All stores 255 over the whole multi-tile extent); reveal_selection_retains_fractional_coverage (Selection::Mask 128 coverage → stored 128 verbatim + composite strictly between); reveal_selection_on_a_moved_layer_lands_on_the_document_space_selection (+12px TransformLayer → Reveal Selection shows the DOCUMENT-space rect, hides the unselected remainder of the moved layer — the end-to-end pin for the local-space conversion); mask_creation_refuses_an_existing_mask_and_a_missing_selection; ui renamed adding_a_mask_routes_to_the_app_and_a_second_one_is_refused. Paste Into keeps the card-053 helper `selection_coverage_tiles_with` unchanged (all paste-into tests green through the shared route). Gates: fmt clean; clippy -D warnings 0; workspace test 0 failed (app-shell lib 502; thumbnail_reproducers 3 passed + 1 ignored). RECORDED (reviewer-verified as narrow edge cases, fixed in this round): negative-fractional pre-image floor fix + bounded walk; the 300×64 test is actually five tiles wide (comment corrected). Remaining issue: none blocking. |
| 058 | Mask paint/erase/invert/fill correctness | 040,055,057 | **verified** | 2026-09-09: **Reviewer PASS round 2** (round 1 FAIL with 2 reproduced criticals — write_mask_coverage's clear path never ran (Invert silently no-op'd on all-255 tiles) and the mask fill lerped toward WHITE instead of toward the paint's value (a black fill changed nothing); plus warnings: double opacity on the solid fill route, fill/invert ignoring the mask pose, stroke dabs clipped against the document-space canvas, cross-document colour-backup loss; all fixed and re-verified round 2 with two residual warnings also fixed in-place: the stale quintuple doc comment and the global-wells/per-doc-target desync on tab activation). Landed: (1) MASK-SPACE PAINTING — `sample_to_mask_of` (menu_bridge: layer transform ∘ mask.transform inverse, the compositor's sampling pose per card 043/composite.rs:996) + `sample_to_paint_target_of` dispatcher wired at BOTH ToolContext build sites; ToolContext gained `paint_space_canvas` (tools/tool.rs) — the canvas rect in the PAINT TARGET's space (identity for content; bbox of the pose-transformed corners for a mask) — and stroke.rs clips dabs against it (a transformed mask's visible strip is no longer unclippable). (2) FILL TARGETS THE MASK — `fill_mask_coverage` (blend = old·(1−amount)+value·amount with value = `mask_coverage_of` luminance, amount = paint alpha — the CoveragePatch::blend shape; non-Normal blend refused with “not meaningful on a coverage mask”; preserve_transparency documented N/A; the selection fold `old·(1−c)+blended·c` retained; opacity arrives baked in the source alpha on the solid route and raw on the pattern route, matching the content fill's two routes — no double application). (3) POSE-AWARE READ/WRITE — `mask_pose`/`canvas_to_mask`/`mask_to_canvas` helpers (pixel-center convention: store pixel m ↔ canvas floor(pose·(m+0.5)−0.5), exact inverses, identity exact); `read_mask_coverage` reads through the pose (nearest store pixel, absent = 0); `write_mask_coverage` resamples canvas-space coverage into store space through the pre-image walk (corner-grow, floor, capped 256 tiles/axis with Err), per-tile diff with the CLEAR rule for all-zero results (the round-1 critical), unchanged tiles skipped, off-canvas store pixels preserved, one labelled Transaction. (4) INVERT — `MaskOp::Invert` (ui enum + ALL list + submenu + resolve gating “The layer has no mask” + action route) → `invert_mask` running in the mask's OWN store space: absent canvas-grid tiles → STORED all-255, existing tiles → 255−v in place, all-zero results CLEARED (both grid and out-of-grid), pose-exact by construction, one undo step, undo exactness via apply_delta's prior-hash inverse. (5) COLOUR PRESERVATION — the wells are now PER-DOCUMENT (`doc_colors: HashMap<DocumentId,(fg,bg)>`; foreground()/background()/set_* read/write the active doc's entry, falling back to the global fields with no document); `set_edit_target_kind` no-ops on a same-kind switch, stashes the doc's CURRENT wells in `content_color_backups` (per-doc map) and swaps to the mask pair (MASK_EDIT_FOREGROUND/BACKGROUND consts), and Content restores; tabbing between documents shows each document's own wells — a fresh content document never inherits a mask pair. (6) Selection constraint pinned: `a_mask_stroke_is_constrained_by_the_active_selection` (outside stroke creates ZERO tiles; inside reveals). Tests: a_stroke_on_a_transformed_mask_lands_through_the_mask_pose (mask.transform=translation(+8), 2px hard brush, probes the STORE bytes: pose-mapped (16,16) revealed, raw doc position (24,16) and pose-mirror (33,16) zero, layer hashes unchanged); fill_targets_the_mask_when_the_edit_target_is_the_mask (Hide All → white fill reveals inside the selection only, layer pixels byte-identical, one undo restores, non-Normal blend refused); the_mask_invert_op_flips_coverage_and_is_undoable; invert_round_trips_over_a_multi_tile_canvas (300×64: Reveal All → Invert → all 0 AND mask_tiles None — stale 255s would fail — Invert again → all 255); mask_fills_blend_toward_the_paint_value_at_the_paint_strength (black hides; 50% black on revealed → ≈128; Gray50 mid; the test documents that 50% WHITE on revealed legitimately changes nothing); mask_fill_maps_through_the_mask_pose (+8px unlinked mask full-canvas fill → store (16,16) AND (33,16) revealed, canvas x=4 reveals — no displaced strip); the_wells_follow_each_documents_own_edit_target (two documents, tabbing, A's red vs B's default, mask pair never stashed as content colours); switching_edit_target_swaps_to_mask_colours_and_restores_the_content_colours (single-doc: stash/recolour/no-op re-switch/restore); ui menu test (Invert enabled with mask / disabled without). Gates: fmt clean; clippy -D warnings 0; workspace 0 failed (app-shell lib 508+3 ignored host-bound; integration 17+3). RECORDED (reviewer-accepted deferrals, round-2 (g)): bucket/gradient/shape tools have PaintTarget::Mask branches but no pose routing (correct for identity poses; the card's named targets are the brush/eraser routes + mask menu operations; the ledger already carries “un-routed paint tools through sample_to_layer_of”); clear_selection/stroke_selection_with still read/write the layer unconditionally (with the well swap, Edit▸Stroke while mask-targeted paints white content — recorded for a follow-up card); content_color_backups is not pruned on close_document (memory-only; mint_id is strictly monotonic so a stale entry can never be mis-restored). Remaining issue: none blocking. |
| 059 | Mask visualization/target controls | 058 | **verified** | 2026-09-09: **Reviewer PASS round 3** (round 1 FAIL — 3 criticals: the popup's op-row strings were never registered (tr() returned “”, blank rows at runtime; KNOWN_KEYS did not cover the keys so the gates could not catch it), the Overlay tint was discarded by the alpha-keyed canvas shader (mix(backdrop, src.rgb, a) — fully-concealed transparent-canvas pixels have alpha 0), and mask.inverted (Properties checkbox + PSD import) was ignored by the views AND the thumbnail; round 2 fixed all three + warnings (wrong-layer popup ops → right-click selects the row; popup state leaks across cull/doc-switch/mask-deletion; density-0 ≠ disabled → affects_composite gate) but FAILED on a new critical: read_mask_coverage sampled through the FORWARD pose (canvas_to_mask's contract now takes the inverse); round 3 fixed the direction with a genuinely discriminating test and closed the remaining warnings — the badge test's dead other-row branch now probes the second row's badge id directly, and apply_mask's mixed-space bake on transformed layers is RECORDED with the derivation rather than shipped as correct). Landed: (1) VIEW STATE — `ui::MaskViewMode {Composite, Grayscale, Overlay}` as panel-owned Workspace state (mask_view + mask_thumbs), the Channels-panel pattern: chrome reads per frame, presenter applies on the last mile, never the document; (2) PRESENTER — `set_mask_view(view, tint)` + mask_view_dirty folded into the dirty-all; `apply_mask_view` inside composite_masked covers EVERY upload path; Grayscale = the mask plane opaque; Overlay = the theme tint blended by k=(255−v)/255 with CARRIED ALPHA (raise-only: a fully-concealed transparent pixel reaches the screen with the tint — the shader would otherwise discard it); both sample the pose-aware coverage through the INVERSE with the mask's INVERTED flag applied (matching LayerMask::coverage), and fall back to the composite when `!mask.affects_composite()` (disabled or density 0 — tinting by ignored coverage would lie); (3) CONTROLS — the mask well's right-click popup: the three view modes (direct panel write, checked row in accent) above Enable/Disable Mask (MaskOp::Toggle) + Toggle Link (MaskOp::ToggleLink) emitted through the SAME menu route; the right-click SELECTS the row first (the ops act on the active layer — the round-1 wrong-layer bug is pinned by a test right-clicking the NON-active row); rows carry widget_info labels + stable ids (mask_view_item/mask_toggle_item/mask_link_item/mask_target_badge); fresh-flag close semantics (mirrors the shared context menu); closes on item click/Escape/outside click; lifecycle before the visibility cull; !has_mask guard; chrome clears the state when the active document changes; anchor clamped to the screen; (4) REAL THUMBNAIL — `OpenDocument::mask_thumbnail` (pose-aware coverage read + the inverted flip, grayscale opaque bytes, box-downscaled) built by the chrome next to layer_thumbs; the well draws it and falls back to the glyph; (5) INDICATOR — card 055's target border + a new accent BADGE on the aimed-at well (hover tooltip, stable id); (6) EXPORT ISOLATION — pinned: the view changes the presented buffer while doc.composite() stays byte-identical; (7) apply_mask now flips the inverted flag too (review round 2). Strings registered (mask.target.badge/disable/enable/toggle.link/view.composite|grayscale|overlay) + KNOWN_KEYS pins them; the duplicated ui.layer_style block deduplicated. Tests: presenter — the_mask_views_show_the_same_geometry_as_the_composite, a_disabled_mask_falls_back_to_the_composite_and_keeps_its_coverage, the_mask_view_never_touches_the_document, an_inverted_mask_presents_the_complement_in_every_view, the_overlay_carries_alpha_over_transparent_concealed_pixels, the_mask_thumbnail_shows_the_coverage_including_the_inversion; menu_bridge — reading_mask_coverage_maps_through_the_pose_inverse (+8px store-side probe discriminating inverse vs forward); ui — the_mask_well_popup_picks_the_view_mode, the_mask_well_shows_the_active_target_badge_and_a_real_thumbnail, the_mask_well_popup_ops_ride_the_menu_route_on_the_right_layer. Gates: fmt clean; clippy -D warnings 0; workspace 0 failed (app-shell lib 515+3 ignored; ui 965+23+6+…; integration 17+3). RECORDED limitations/deferrals (reviewer-accepted): live-transform pose mismatch (the view samples the committed pose while the compositor renders the PreviewOverride — transient, self-heals on commit); mid-range density/feather presented at store strength (geometry matches, values do not); session-only mask_view persistence (the channels/thumb_scale precedent); undo-resurrection re-anchors a popup whose layer+mask return; the carried-alpha rule is approximate for partially-concealed semi-transparent pixels (exact at the extremes); apply_mask's mixed-space bake on transformed layers (store(s)·cov(M⁻¹·s) derivation recorded in menu_bridge — rides T043 remainder/063); no shortcuts added (Q is quick mask; the popup is the surface). Remaining issue: none blocking. |
| 060 | Edge-refinement dialog | 057–059 | **verified** | 2026-09-09: **Reviewer PASS round 3** (round 1 FAIL — 1 critical: the preview double-applied the baseline mask — content was `layer_pixels`, which already carries the enabled mask in alpha, multiplied again by the refined coverage, making expand invisible and the outer feather ramp hidden; warnings: pipeline-test prose contradicting its assertions, an unbounded preview, placement/doc drift, units docs saying “store”; round 2 fixed the critical (content = `pixels::read_layer` RAW store pixels) + bounded preview + placement/docs but FAILED narrowly on process integrity — four claimed comment fixes not applied and a missing host-path test; round 3 fixed every doc line at source and added the discriminating host-path test — the reviewer's own pass condition). Landed: (1) MORPHOLOGY (crates/selection/src/modify.rs) — `RefineParams {feather_px, shift_px, smooth_px, contrast}` + `refine_mask(mask, params)` (fixed pipeline SMOOTH → SHIFT (expand/contract by sign) → FEATHER → CONTRAST, each stage skipped at identity, new mask returned) + `contrast(mask, strength)` (the coverage ramp pushed away from the 128 midpoint, gain 1+3·strength, midpoint pinned, 1.0 binarizes) — exported from the crate root; radii in DOCUMENT pixels (the pose-aware canvas-space coverage + the pose-aware writer). (2) DIALOG (crates/ui/src/dialogs/refine_mask.rs) — `RefineMaskSpec` + `PreviewBackground {Black, White, Checker}` + `RefineMaskDialog::new(raw store pixels, pose-aware baseline, w, h)`; `refined_coverage()` = selection::refine_mask over the baseline CANVAS-ALIGNED by coverage_at sampling (the refined rect may grow); `preview_sized()` composites content×coverage over the backdrop, bounded at PREVIEW_MAX_SIDE=256 (larger canvases nearest-downscale content+baseline AND run the morphology on the downscaled buffer; the CONFIRM is always full-res); sliders in pipeline order; backdrop swatches; the identity spec is refused at the dialog AND DialogAction::is_valid; the subtitle labels it edge refinement, not subject recognition; module-doc RECORDED paragraph (mixed-space raw-store caveat, opacity/blend ignorance, document-pixel radii, downscaled-radii + step-granularity approximations, the checker approximation). (3) WIRING — `MenuAction::RefineMask` (Layer ▸ Refine Mask… beside the Layer Mask submenu; resolve gates need_layer + has_mask with reason, action route; ui gate test both ways); dialog_host arm + `refine_mask_dialog` builder (second defence: refuses maskless) + `DialogAction::RefineMask` routed to `menu_bridge::refine_mask_with` = pose-aware baseline → refine_mask → canvas-align → write_mask_coverage("Refine Mask") — ONE labelled transaction. (4) HOST-PATH TEST — the_refine_dialog_previews_expansion_over_the_raw_content: the dialog opened through the real host; expand(+4) over a baseline hiding the left half; the preview pixel at the newly revealed (22,8) equals the RAW content pixel × the refined coverage — under the round-1 defect it was the backdrop (the reviewer's required discriminator, using the dialog's pub test seams content_pixel_for_test/set_spec_for_test). Tests: selection — the pipeline on a synthetic edge (expand moves the edge RIGHT; the ramp crosses near the shifted edge; contrast pins the midpoint and binarizes at full strength; identity returns the input); ui — the preview geometry per backdrop, the blend = ink·c + backdrop·(1−c) at the ramp pixels, identity blocked + previewing never mutates the spec, a confirmed spec round-trips through the action, the registry (with_spec seeds a non-identity spec); menu_bridge — refining_a_mask_is_one_undoable_step_that_keeps_the_pixels (ONE history entry, layer tiles byte-identical, baked == pipeline output, undo restores the EXACT baseline, redo replays) + the_refine_dialog_opens_only_over_a_mask_and_cancel_writes_nothing (maskless refused; open+drop writes no history; baseline intact). Strings + KNOWN_KEYS registered. Gates: fmt clean; clippy -D warnings 0; workspace 0 failed (app-shell lib 518+3 ignored; ui 969+23+6; selection 116; integration 17+3/10). RECORDED limitations (module doc + ledger): the mixed-space raw-store preview on transformed layers (T043 remainder, the apply_mask family), opacity/blend ignored in the preview, downscaled-radii + step-granularity preview approximations (confirm exact), the checker approximation, no shortcuts (the popup/dialog is the surface). Remaining issue: none blocking. |
| 061 | Boundary-refine brush | 060 | **verified** | 2026-09-09: **Reviewer PASS round 1** (pickup verification: the doer implementation was found complete in-tree after an interrupted session; all nine card criteria checked by the reviewer, tests confirmed non-vacuous, claimed comments verified at source; the reviewer's one suggestion applied — a debug_assert on stroke-rect/patch-box desync in apply_refine_to_mask). Landed: `StrokeOp::RefineBoundary {strength}` — a MASK-ONLY stroke op (works_on_mask includes it; the Content-target path refuses loudly with ToolError::UnsupportedOnMask; automatic matting explicitly deferred in code docs). Local refinement bounded to the painted band: the pipeline runs once over the stroke's bounding rect and ONLY pixels the dabs touch move toward the regraded value (`a = buf·opacity·selection.coverage_at`); smooth = gaussian_blur sigma 0.5+2.5·strength over the stroke rect; the re-grade pushes samples away from the BYTE midpoint 128/255 with gain 1+strength (milder than card 060's contrast because a stroke re-runs per dab pass and must stay repeatable) — plateaus at 0/255/midpoint are exact fixed points, so hard interior/exterior coverage is retained by construction; NOT plain blur relabeled (the monotone-ramp assertions fail for a pure blur). ToolId::RefineBoundary registered with the strength option schema; set_setting clamps 0..1 and ERRORS on unknown keys (the round-1 silent-noop-slider defect regression-tested, including Blur's radius via the same family). Tests: tools refine_tests (monotone stair regrade, displaced-rect/coordinate-mixing correctness, plateau fixed points, unpainted pixels byte-identical, strength option reaches the op), ui forward-set test, app-shell real-route tests (a_touched_blend_mode_presses_clean_through_the_chrome_boundary: ui-supplied blend-mode keys never forward; the_boundary_refine_brush_regrades_the_mask_band_over_the_real_route: one undo step, layer hashes unchanged, the band's coverage regraded, undo restores the exact baseline). Gates: fmt clean; clippy -D warnings 0; tools/app-shell/ui suites green. RECORDED (per the card itself): real hair/skin-edge quality requires a visual comparison on suitable user-owned or licensed photos — synthetic tests alone do not pass that quality gate; recorded as the card's manual item. Remaining issue: none blocking. |
| 062 | Edge color cleanup (separate edit) | 061 | **verified** | 2026-09-09: **Reviewer PASS round 1** (two warnings fixed in-place: is_identity now treats a non-finite strength as identity so the dialog can never confirm what the engine would refuse; the band-detection cost note recorded — O(n·r²) bounded by the box cull, BFS dilation a recorded optimization). Landed: (1) ENGINE — `filters::defringe::defringe(rgba, coverage, w, h, DefringeParams{radius, strength})` (crates/filters/src/defringe.rs, new): band = covered pixels within `radius` (Chebyshev) of a sub-half coverage pixel; reference = average of fully-covered NON-BAND pixels in the window (a fringe pixel is fully covered too — band membership excludes the whole fringe column; references read from a SNAPSHOT so the single pass is order-independent); strength blend on RGB only, alpha untouched; band pixels with no interior reference in reach are left unchanged; work culled to the band's bbox dilated by radius; radius capped MAX_DEFRINGE_RADIUS=64; malformed inputs (length mismatch, over-cap radius, out-of-range/NaN strength) error, never panic; module doc disclaims both feathering and automatic matting, and documents the deliberate gamma-space average (the halo is an encoded-space artifact). (2) DIALOG — `ui::dialogs::defringe` (DefringeSpec radius/strength + PreviewBackground reuse) over the layer's RAW store pixels and the pose-aware coverage; preview = cleaned content composited by the UNCHANGED baseline coverage over three backdrops; downscaled preview on large canvases (card 060's helpers made pub(crate)); identity refused at the dialog; mixed-space caveat recorded (mirrors row 060). (3) WIRING — `MenuAction::RemoveColorFringe` (Layer ▸ Remove Color Fringe… beside Refine Mask…; the same need_layer+has_mask gate; strings + KNOWN_KEYS registered); `DialogAction::Defringe` (+is_valid+label+registry+one_of_each); dialog-host arm + builder (raw-pixels second line of defence); shell arm → `menu_bridge::defringe_with` = raw read → filters::defringe → `pixels::write_layer("Remove Color Fringe")` ONE labelled transaction; maskless refusal; no-change refusal (no empty history entry). The card's reversibility: one undo step restores the EXACT original RGB (pinned); 'disabling' is satisfied by the explicit reversible operation (reviewer-accepted reading; a persistent toggle not required for a discrete edit). Coverage byte-identity after apply pinned. Tests: filters ×5 (fringe pulled to interior ink, interior+transparent bytes exact, strength scaling + identities, no-reference survival, malformed inputs); ui ×4 (preview per backdrop with the fringe visibly reduced, preview keyed to the UNCHANGED baseline coverage, identity/preview-immutability, action round-trip); menu gate test both ways; app-shell ×3 (fringe reduced in one undoable step with coverage untouched + interior exact + undo restores the fringe; maskless and no-op refusals write no history; host path opens over the RAW content and confirms to the Defringe action). Gates: fmt clean; clippy -D warnings 0; filters/ui/app-shell suites green. RECORDED (per the card): finer fringe quality on real photos is a visual gate for the human acceptance walk (card 091); BFS-dilation band seeding and a tracing log on swallowed preview errors recorded as polish. Remaining issue: none blocking. |
| 063 | Mask/source transform alignment | 043,057–062 | **verified** | 2026-09-09: **Reviewer PASS round 1** (pose-inverse derivation verified sound; clear-on-zero proven lossless given absent-reads-as-zero; test assertions confirmed non-vacuous and discriminating). TWO REAL MAPPING DISCREPANCIES FOUND AND FIXED (the card's mandate), both on shared helpers: (1) `paint_space_canvas_of` (tool_input.rs) mapped the canvas corners through the mask pose FORWARD — the visible canvas in mask-local space is pose⁻¹(canvas) — so on any mask with a non-identity document pose (e.g. linked mask after a layer move) the stroke clip covered the wrong half-plane and strokes over existing coverage were SILENT NO-OPs (a commit step, unchanged store bytes; E08 stayed green because it painted absent areas). One-line fix on the shared helper; new unit regression a_stroke_on_a_non_identity_pose_mask_edits_the_pose_mapped_coverage (fails pre-fix). (2) `CoveragePatch::commit` (patch.rs) stored all-zero tiles explicitly instead of clearing them (ColorPatch::commit already cleared) — a dab bbox spanning tile rows −1/0 materialized a phantom off-canvas tile that undo removed and REDO resurrected, breaking byte-exact replay; fixed to absent↔zero symmetry. NEW INTEGRATION TEST the_masked_cutout_alignment_survives_transforms_relink_and_reopen (thumbnail_workflow.rs): linked move (mask rides, edge y=16→32), unlink+move (pose FROZEN — the riding alternative is discriminated at probe (16,40)), relink (byte-identical, no jump), density 0.5 (revealed pixel exactly untouched / concealed pixel strictly between — LayerMask::coverage semantics), feather 4 (edge partial, interior full), eraser stroke on the mask target punches the hole (a BRUSH on a mask RAISES — concealment is the eraser's op — recorded), undo restores coverage, full undo walk with per-step AlignedState uniqueness (tile hashes + mask transform + layer pose + mask params move TOGETHER; the walk also pins that each TransformLayer entry carries the unlinked-mask compensation in its own inverse), redo replay byte-equal, native save/reopen byte-for-byte probes. Zoom note recorded in the test doc: camera zoom is presentation over the same document-space truth (cards 030/039), so identity-zoom alignment IS every-zoom alignment. Gates: fmt clean; clippy -D warnings 0; integration 18+1, tools, app-shell 525, compositor green; FULL WORKSPACE GATE PASS (68 binaries, 0 failed). RECORDED remaining: mask density/feather interaction with layer effects and clip adjustments is not yet exercised (reviewer follow-up; rides cards 066–068). Remaining issue: none blocking. |
| 064 | Manual portrait extraction acceptance walk | 053,055–063 | **verified** | 2026-09-09: **Reviewer PASS round 1** (real-route use, honest bounded undo, source-preservation pin, no ML shortcut, visual gate correctly left unclaimed — all six criteria verified; the eraser-direction assert added per review). Landed: `the_manual_portrait_extraction_walk_holds_end_to_end` (thumbnail_workflow.rs) — rough rect selection → Expand/Contract/Feather through the Select menu's real routes (composed shift/alt gestures are pinned at card 056) → Mask ▸ Reveal Selection (interior ink / outside backdrop probes) → source-preservation pin (portrait tile hashes) → mask-target eraser correction (CONCEAL direction pinned: the corrected pixel shows the backdrop) + undo → mask-target brush reveal → two backdrops placed via place_path and moved to the stack bottom (MoveLayer index MAX = end of the topmost-first root = bottom) → visibility-toggled black/white background inspection (deep interior backdrop-independent; concealed pixels show the active backdrop — the halo read) → bounded undo walk to the setup depth restoring the pre-walk composite byte-for-byte (full undo would strip the portrait itself — bounded honestly) → redo replay probes → native save/reopen probe. Paste Into + mask-target painting together: satisfied jointly by card 054's persistence workflow test and card 063's combined test in the same binary. **PHASE 6 GATE PASS 2026-09-09**: fmt clean; clippy -D warnings 0; `cargo test --locked --workspace` 68 binaries 0 failed. RECORDED (the card's explicit condition): the real-photo visual quality gate (suitable user-owned/licensed photos; hair/skin-edge judgment) remains OUTSTANDING and belongs to card 091's human acceptance walk — synthetic tests alone do not pass it; no ML runtime added. Remaining issue: none blocking. |
| 065 | Layer-stack management practicality | 045,064 | **verified** | 2026-09-09: **Reviewer PASS round 1** (claims verified at source: rename = the exact SetLayerProperties shape LayersModel::rename emits; collapse is panel session state by design — layers.rs documents no LayerPatch field for it; panel-level drag reorder/re-parent/cycle refusal + rename field + multi-select delete all covered by existing ui tests in clicking_the_real_thing.rs and wired_controls.rs). Landed: `layer_stack_management_is_practical_at_scale` (thumbnail_workflow.rs): 44 rows (30 content + 3 groups × 3 children + nested subgroup + canvas) → rename the portrait via the panel command shape → duplicate an alternative via the editor's REAL dispatch (Editor::dispatch(Action::DuplicateLayer)): pixel-equality, " copy" naming, exactly one new layer → move the duplicate into Group 0 → cycle refusal (group into its own descendant: Err, no history, tree unchanged) → step-by-step undo walk restoring order AND content (incl. the duplicate's pixels gone with it — added per review) → redo replay exact → visibility patch round-trip with undo → native save/reopen at 45 rows (renamed + duplicate survive). Gates: fmt clean; clippy -D warnings 0; integration 20+1; FULL WORKSPACE GATE PASS (68 binaries, 0 failed). Remaining issue: none blocking. |
| 066 | Effects integration verified/repaired | 020,063,065 | **verified** | 2026-09-09: **Reviewer PASS round 1** (patch-route fidelity verified byte-for-byte against LayerStyleDialog::confirm; git diff confirms ONLY the test file changed — no effect algorithms touched; scope judged acceptable: the card's named done/check items are shadow distance + stroke width, both covered end-to-end; glow/overlay integration-level save/reopen recorded as a minor follow-up). Landed: `thumbnail_effects_integration_holds_end_to_end` (thumbnail_workflow.rs) — a MASKED portrait whose ink straddles the x=256 tile boundary, styled through the dialog's wholesale-replace patch: drop shadow (falls OPPOSITE the light — angle 0° shifts it left; recorded), styled bounds grow with the distance (compositor::bounds::styled_bounds), distance change visibly rewrites the canvas, undo/redo byte-exact; EFFECT ORDERING AROUND THE MASK: the silhouette is the MASKED one — erasing source ink removes exactly the shadow it cast (shadow(x)=ink(x+40) pinned both directions: kept + lost probes); outside stroke add + width change + undo; movement across tiles then a COLD recompose (save+reopen, fresh caches) byte-identical — no seams, no stale remnants. Gates: fmt clean; clippy -D warnings 0; integration 21+1; FULL WORKSPACE GATE PASS (68 binaries, 0 failed). RECORDED follow-up: outer-glow/color-overlay integration-level save/reopen ride a later card (their per-effect algorithms are unit-covered). Remaining issue: none blocking. |
| 067 | Reusable layer styles | 066 | **verified** | 2026-09-09: **Reviewer PASS round 1** (all seven criteria verified; LayerEffects field audit confirms style-only copying; persistence through the REAL PresetStore::load/persist path; no duplicate storage — the preset store was reused, not a parallel mechanism). Landed: (1) PresetStore gains `styles: Vec<(String,String)>` with #[serde(default)] (old JSON loads unchanged; define_style/styles/latest_style — the same latest-wins rule as patterns); (2) Editor: `copied_style` session field + copy_layer_style / paste_layer_style (ONE undoable wholesale LayerPatch::effects replace — the LayerEffects struct cannot hold position/masks/text/asset identity by construction) / define_style_preset (refuses a default style, stores "Style N") / apply_latest_style_preset (ONE undoable replace); (3) Layer ▸ Layer Style submenu gains Copy/Paste Layer Style + New/Apply Latest Style Preset (need_layer gates; a missing capture/preset is a runtime status error since the menu cannot see the session clipboard); (4) catalog exemption with a comment naming the dedicated test. NEW TEST layer_styles_are_reusable_across_layers_and_restarts (editor_tests.rs): copy A→paste B (effects equal; name/transform/kind/mask untouched; one undo step; undo restores the default style; redo); named preset define→apply to a third layer (one undo step); persist() → a fresh editor over the SAME config root loads the preset from disk and re-applies it. Gates: fmt clean; clippy -D warnings 0; app-shell 526, asset-store 55, ui green; FULL WORKSPACE GATE PASS (68 binaries, 0 failed). Remaining issue: none blocking. |
| 068 | Clipped adjustments + mask targeting | 063,065–066 | **verified** | 2026-09-09: **Reviewer PASS round 1** (no product-code repairs needed — consistent with the card's mandate to repair only reproduced errors; the clippy unused-binding gate violation and two comment nits fixed in-place). Landed: `the_clipped_adjustment_scopes_to_the_portrait_and_survives_reload` (thumbnail_workflow.rs) — a Curves adjustment created above the portrait and clipped via SetLayerProperties(ClipToBelow): portrait darkens while the headline and backdrop are BYTE-UNCHANGED; visibility toggle restores prior pixels exactly and re-showing restores the scope; clip RELEASE reaches the headline (scope check in reverse) with undo restoring; the adjustment's OWN mask edited separately via PaintTiles(Mask) — left quarter keeps the darkening, the rest returns to original ink, headline untouched; two-entry undo (coverage paint + attach) pinned; SetLayerKind swap to Invert while clipped — portrait inverts (±2), headline untouched; save/reopen — kind, ClipToBelow, and composite byte-identical. Scope recorded: Hue/Saturation shares the identical dispatch route and plumbing (two kinds driven live, including a kind swap); group blending/opacity scoping covered by earlier cards and would regress these byte-equality probes. Gates: fmt clean; clippy -D warnings 0; integration 22+1; FULL WORKSPACE GATE PASS (68 binaries, 0 failed). Remaining issue: none blocking. |
| 069 | Asset replacement keeps layout | 050,065–068 | **verified** | 2026-09-09: **Reviewer PASS round 1** (identity/no-delete-create verified; mask/effects/group pinned; undo restores pixels + origin bytes + size as one unit; siblings by AssetId only; serde round-trip entries added per review — both embedded and linked origins). Landed: (1) NEW additive editor-core command `Command::ReplaceAssetSource {asset, origin, source_size}` (AssetNotFound refusal before any write; the inverse restores origin AND size together — one fact about the source; plain additive serde, no version bump, card-050 precedent); (2) `Editor::replace_smart_object_contents` (+ replace_from_dialog over the ScriptedDialogs seam — pick_replace_file added): the ACTIVE layer must be a smart object; POLICIES documented — SHARED-assets replacement (every sibling referencing the AssetId updates in ONE transaction; identity by asset id, never layer names; no per-instance variant ships), origin-kind preserved (embedded stays embedded, linked stays linked), sizing = card-050's fit renormalization own·old/new·own⁻¹ only when a recorded source_size exists (pre-069 records keep their transform); decode via decode_surface_bytes/path → working_space_pixels (loud refusal on unsupported profiles); ONE labelled Transaction: re-sliced PaintTiles per sibling + ghost cleanup + renormalization + ReplaceAssetSource; stamp learning mirrors refresh. (3) Layer ▸ Smart Object ▸ Replace Contents… with the EditSmartObjectContents-style gate; dialog-decline refuses loudly. NEW TESTS (editor_tests.rs, 4): embedded 16→32 replace keeps same LayerId/name/kind/mask/effects/parent/index, footprint preserved (scale halved, direction-pinned), ONE history entry, undo restores old pixels + old origin bytes + old size, redo replays; shared sibling updates together in one transaction (unrelated layers untouched); menu route + raster-layer refusal naming the class + declined-dialog loud refusal; no-recorded-size keeps the transform. Gates: fmt clean; clippy -D warnings 0; editor-core 116, app-shell 530, integration 22+1, ui green; FULL WORKSPACE GATE PASS (68 binaries, 0 failed). RECORDED follow-ups: embedded origin naming uses file_name while place_path uses file_stem (cosmetic inconsistency); linked decode re-reads the file after the bytes capture (TOCTOU window, the card-048 family); ghost-cleanup O(old×new) fine at current counts. Remaining issue: none blocking. |
| 070 | Native reusable composition workflow | 024,065–069 | **verified** | 2026-09-09: **Reviewer PASS round 1** (real routes throughout — duplicate_document, SetLayerKind, replace_smart_object_contents, save_to, open_project, composite; “open as new unsaved document” judged adequately covered: opened files are unsaved until explicit save_to, and the meaningful contract — variants never write back to the template — is directly asserted; the selection-preservation and manifest-immutability suggestions applied). Landed: `native_composition_workflow_supports_independent_variants` (thumbnail_workflow.rs) — a native sample composition with clearly named editable placeholder layers (Background, “Logo — replace me” placed smart object, “Headline — edit me” text layer; all GENERATED assets — no licensing concerns) saved as a native package; VARIANT 1 = Duplicate Document + SetLayerKind text edit → save-as variant-one.rstudio; VARIANT 2 = back to the template tab, duplicate, Replace Contents (card 069's real route) on the logo → save-as variant-two.rstudio; THE TEMPLATE IS UNCHANGED (manifest.json byte-identical); all three reopened fresh — the template's headline text intact, variant 1 changed ONLY the text, variant 2 changed ONLY the logo pixels; each variant exports under its own name (different bytes). No separate template engine — open-as-unsaved + duplicate + save-as. Gates: fmt clean; clippy -D warnings 0; integration 23+1; FULL WORKSPACE GATE PASS (68 binaries, 0 failed). Remaining issue: none blocking. |
| 071 | Native thumbnail milestone (M4 delivery) | 065–070 | not started | — |
| 072 | PSD fidelity contract doc | 006 (after 071 by default) | not started | — |
| 073 | Independent PSD fixtures | 072 | not started | — |
| 074 | Import supported editable text | 018–024,072–073 | not started | — |
| 075 | Import required layer effects | 066,072–073 | not started | — |
| 076 | Masks/adjustments/groups/profile import | 063,068,072–075 | not started | — |
| 077 | Import fidelity report | 074–076 | not started | — |
| 078 | Appearance-preserving PSD export | 072–077 | not started | — |
| 079 | Export supported editable text | 074,078 | not started | — |
| 080 | Export supported effects/masks/adjustments | 075–076,078–079 | not started | — |
| 081 | PSD interchange acceptance matrix | 073–080 | not started | — |
| 082 | Workspace tuning for thumbnails | 071,081 | not started | — |
| 083 | Navigation/guides/shortcuts verification | 042,082 | not started | — |
| 084 | Export dimension/aspect/color verification | 071,082–083 | not started | — |
| 085 | Small-preview + variant export workflow | 070,084 | not started | — |
| 086 | Benchmark real composition + optimize | 020,039,064,071,084 | not started | — |
| 087 | Long ops off interaction thread | 011,013,050,084,086 | not started | — |
| 088 | Native round-trip + portability tests | 018,050,070,087 | not started | — |
| 089 | Undo/redo + crash recovery across workflow | 088 | not started | — |
| 090 | Regression/compatibility/resource checks | 081,083–089 | not started | — |
| 091 | Human acceptance walk | 090 | not started | — |
| 092 | Documentation reconciliation + example | 091 | not started | — |
| 093 | Local subject-selection evaluation | 064,086,092 | **optional deferred** | Plan Phase 11: only if user makes automation required |
| 094 | Bounded local inference service | 093 | **optional deferred** | — |
| 095 | Select Subject / Remove Background UI | 095 deps (093,094) | **optional deferred** | — |
| 096 | Automatic cutout quality + packaging | 095 | **optional deferred** | — |
| 097 | Local asset shelf | 069–070,092 | **optional deferred** | — |
| 098 | Template fields + batch variants | 070,085,092 | **optional deferred** | — |

## Baseline facts (from Task 001, `THUMBNAIL-BASELINE.md`)

- Commit `9ba62c1`; all gaps E01–E14 CONFIRMED (exact code sites in the baseline doc).
- Gates on this host: fmt / check / clippy green; full `cargo test --locked --workspace` recorded in the baseline doc.
- Host: Windows 11, GPU present. Toolchain note: local stable toolchain was missing `rustdoc.exe`
  (corrupt component; doctests failed even on a fresh probe crate). Repaired 2026-09-07 by
  reinstalling stable via rustup → **rustc/rustdoc 1.98.1** (was 1.98.0, patch bump). Recorded here
  because every later gate runs on 1.98.1.

## Phase-gate log

| Gate | When | Result |
|---|---|---|
| M0 exit (001–006) | 2026-09-09 | **PASS** — `cargo fmt --all --check` clean; `cargo check --locked --workspace --all-targets` PASS; `cargo clippy --locked --workspace --all-targets -- -D warnings` PASS (no warnings); `cargo test --locked --workspace` PASS (68 binaries ok, 0 failed, 15 ignored by design). One repair inside the gate: T003's `fixture/thumbnail.rs` had `U+25B8 '▸'` in a string literal (`File ▸ New` expect message), which `ui::no_typed_ui_glyphs` rejects — replaced with `>` and the gate rerun green. This failure was pre-existing relative to T003's uncommitted work (the restart recovery reran only the integration package, not the full gate). |
| M1 exit (007–014) | 2026-09-09 | **PASS** — `cargo fmt --all --check` clean; `cargo check --locked --workspace --all-targets` PASS; `cargo clippy --locked --workspace --all-targets -- -D warnings` PASS (0 errors); `cargo test --locked --workspace` PASS (68 binaries ok, 0 failed, 15 ignored). Phase 1's foundation: edit_target, bounds, interaction_geometry, typed tool options, edit_session, live-geometry publication + painted overlays, compositor previews — all with real-route tests. |
| M2 exit (015–024) | 2026-09-09 | **PASS** — `cargo fmt --all --check` clean; `cargo check --locked --workspace --all-targets` PASS; `cargo clippy --locked --workspace --all-targets -- -D warnings` PASS (0 errors); `cargo test --locked --workspace` PASS (68 binaries ok, 0 failed, 3480 passed, 14 ignored). Phase 2 delivered: persisted rich-text schema + lossless conversion + validation + v4 migration, full-style rendering + canonical cache identity (incl. the tile-cache repair), Character/Paragraph controls writing persistent data, font selection + substitution reporting (searchable picker, condensed faces via FontStretch), point/paragraph geometry with overset reporting, and the styled-headline end-to-end verification. |
| Phase 6 exit (055–064) | 2026-09-09 | **PASS** — fmt clean; clippy -D warnings 0; `cargo test --locked --workspace` PASS (68 binaries ok, 0 failed). Phase 6 delivered: mask thumbnail targeting (055), undoable/composable selections (056), four mask-creation ops (057, E09 flipped), mask paint/fill/invert through the pose (058), mask views/thumbnails/popup (059), Refine Mask dialog (060), boundary-refine brush (061), Remove Color Fringe (062), mask/source transform alignment with two shared-helper mapping fixes (063), and the manual portrait extraction walk (064). |
