# Feature Parity Matrix

Target: feature parity with Photopea for real editing work.

**This file states the truth.** A feature is ✅ only when it is implemented,
tested, and reachable from the UI. Anything else is 🔶 (partial, with the gap
named) or ⬜ (not started). Nothing is marked done on the strength of a type
existing — that is exactly the failure this project was rebuilt to escape.

Status: ✅ done · 🔶 partial (gap named) · ⬜ not started · 🚫 decided not to
ship (reason given) · ❌ deliberately limited (reason given)

**Re-checked at `25b66e0` (2026-09-25)**, after thirteen waves: six fix
waves and the Photopea-parity waves 7-11, 13 and 13X (`8b6c399`, `f9329d0`,
`9a61faa`, `05ec9b1`, `fe978d3`, `06abd74`, `25b66e0`; see the root
`CHANGELOG.md`), plus wave 14's File ▸ Export… row (uncommitted when this
was written). Each wave updated the rows it changed, citing the code or test
they rest on. This pass re-checked the counts (panels, tools, filters,
adjustments, blend modes, open extensions, export formats), the contradictions
a confirming audit of `6e0287d` found, Tier C and the list of what is still
absent against the code at `25b66e0`. The rows' labels (W7-A … W13X-9) name
the wave that brought each feature.

> **Reachability is part of the bar.** A final audit found rows marked done whose
> feature the application had no route to. Those are corrected below and the
> engine/UI split is stated explicitly: 🔶 now covers "the library works and is
> tested, but you cannot get to it from the app". See README "Status".


## Tiers

- **Tier A — Core.** Without all of these the app is not an image editor.
- **Tier B — Pro.** What makes it competitive with Photopea.
- **Tier C — Deferred.** Explicitly out of scope for this release, with reasons.

---

## Tier A — Core

### Document & canvas
| Capability | Status | Notes |
| --- | --- | --- |
| Open PNG / JPEG / WebP / TIFF / GIF / BMP / ICO / TGA / SVG | ✅ | W16-I: File ▸ Open (picker, drop, Open Recent, command line, and File ▸ Revert) reads an SVG as **layers** at its own size (`raster::codec::svg_import::layers`, mapped by `app-shell/src/import_vector_w16.rs` through the Sketch / XD / Figma shape and text mapping): `<g>` → layer group (opacity kept), paths with a solid fill / solid undashed stroke → shape layers, plain text (one run, start-anchored, solid fill, no skew or mirror) → text layers with an estimated 0.8 em ascent, other text → its outlines as shape layers, images → raster layers; groups with a clip path / mask / filter / blend mode and paths with gradient or pattern paint or dashes open as raster layers drawn by `resvg` and are counted in an "SVG import report"; gradients are not live fills and caps / joins open as butt / miter (reported); when the layers cannot be read the SVG opens as one picture and the status line says why (tests `svg_import::layers::tests::*`, `import_vector_w16::tests::file_open_of_an_svg_opens_its_shapes_text_and_group_as_layers`). Before W16-I (and on the flat codec road) an SVG is rasterised at its own size (`width`/`height`, else its `viewBox`, rounded up) by `resvg` 0.45 in `raster::codec::svg_import`, recognised by content or extension; the file and, for a gzip `.svgz`, the inflated text are each capped at 64 MiB (inflated by `raster` through a capped reader, never by `usvg::Tree::from_data`), plus the usual import dimension limits; `<image>` hrefs resolve for `data:` URLs only, and a gzip-compressed embedded SVG is not drawn; no size dialog yet. ICC preserved; File ▸ Open decodes on a worker thread, while drag-and-drop, recent files and the command-line argument open on the UI thread; on every route a 16-bit source opens as RGBA8 tiles in a document that records 16 bits (`OpenDocument::record_source_depth`; test `file_open_on_the_worker_records_a_sixteen_bit_source`) — Image ▸ Mode ▸ 16 Bits widens the tiles |
| Open PDF / AI (import dialog: pages, dpi, artboards or documents), WMF / EMF, Paint.NET PDN (layers), EPS / Sketch / XD / FIG | 🔶 | W13-D. **PDF and PDF-compatible `.ai`**: pages rendered by `hayro` 0.4 (pure Rust, Apache-2.0; 0.5 would switch the whole workspace's `flate2` to the `zlib-rs` backend through feature unification, and 0.6+ needs rustc 1.92, past the MSRV) at one pixel per point, on white paper in `raster::codec::formats::pdf`; File ▸ Open, drag-and-drop, Open Recent and the command line open a multi-page file as one artboard per page (a white plate plus a "Page n" layer, laid out ten to a row on tile boundaries, at most 100 pages, `app-shell/src/editor_open_pages.rs`), a one-page `.ai` as **layers** when its page holds only paths and text (W16-I, `raster::codec::formats::pdf::layers`: a content-stream interpreter over `hayro-syntax`, since `hayro-interpret`'s `Device` trait is not a direct dependency of `raster`; fills / strokes → shape layers, simple-font text → text layers; a page with images, form XObjects, shadings, clips smaller than the page, other colour spaces, Type0 / Type3 fonts or soft masks opens as a picture and the status line says why; a `.pdf` through the W16-K import dialog is still rendered, not layered) (W16-K: a one-page `.pdf` asks through the import dialog below as Photopea does, `editor_open_w13x7.rs`; `menu_bridge::w16k_tests::a_one_page_pdf_asks_through_the_import_dialog_and_an_ai_does_not`), and File ▸ Revert rebuilds the artboards; the flat codec answers page 1. W13X-7: a file of two or more pages first shows Photopea's **import dialog** (`ui::dialogs::pdf_import`, hosted by `DialogHost` as `ActiveDialog::PdfImport`, route in `app-shell/src/editor_open_w13x7.rs`): a thumbnail per page (up to 100, each a click toggle, Select All / None), the resolution in dpi (18-1200; `pdf::render_selected`), and artboards in one document or one document per page; Cancel opens nothing; several multi-page files opened at once (one drop, the command line) queue and ask one after another; File ▸ Revert reads back the chosen pages at the chosen dpi. Not done: encrypted PDFs (refused by name), JPEG 2000 images inside a PDF (hayro 0.4's decoder is an optional OpenJPEG C feature, left off), the blending/isolation/knockout features `hayro` itself lacks, no time bound on a pathological content stream, no dialog for a one-page file (it opens at 72 dpi), and the thumbnails are rendered on the interaction thread when the file is opened. **WMF / EMF**: common GDI records (objects, window/viewport/map modes, EMF world transform, shapes, polygons, Béziers, EMF path brackets, text, StretchDIB(its)) translated to SVG and drawn by `resvg` (`formats::metafile`); arcs/chords/pies, clipping, dash styles, pattern brushes, raster ops and EMF+ records are not drawn. **EPS** (W15-E): the **PostScript artwork**, run by this build's own bounded interpreter (`raster::codec::formats::postscript`: operand and dictionary stacks, procedures and control flow, the graphics state, paths, `fill` / `eofill` / `stroke` / `clip`, `image` / `colorimage` / `imagemask` in both operand forms through the ASCIIHex, ASCII85, RunLength, Flate, DCT and SubFile filters, DeviceGray / RGB / CMYK, ICCBased, Indexed and Separation / DeviceN colour, and `show`-family text in a fallback system font with an estimated 0.55 em advance); W16-I: File ▸ Open maps its marks onto **layers** (`postscript::display_list` → `svg_import::layers`: fills and strokes as shape layers, text as text layers, images as raster layers, marks under a clip flattened into raster layers and reported; test `import_vector_w16::tests::file_open_of_an_eps_with_two_fills_opens_two_shape_layers`) and drawn at one pixel per point through `resvg`, the page cut to `%%HiResBoundingBox` / `%%BoundingBox`; bounded by an operation count (30 M, including each element `bind` walks), a 15 s budget read every 1 024 operations (one operation's own work, such as copying or searching a string as large as the memory budget, is not interrupted), a memory budget, operand-stack, dictionary-stack, nesting, filter-chain (32), colour-space-nesting (4) and path-size caps, with arrays, dictionaries and filter files freed iteratively (the 300 000-deep chains in the tests, of arrays, dictionaries and filters over procedures, free on a 1 MiB stack) and every array and dictionary a run made freed when it ends (no leaked cycles). Checked by eye on real EPS output of matplotlib (against matplotlib's own Agg render of the same figure) and cairo 1.18, Inkscape's exporter (against the shapes, text and image it was told to draw); no Illustrator or CorelDRAW file was available to test. Not drawn, and named in the status line: smooth shading (`shfill`), patterns, `charpath` / `strokepath`, `kshow` / `cshow`, masked images (ImageType 3 / 4), embedded Type 1 / Type 42 / CFF glyph outlines (their text uses the fallback font), unknown operators. When the PostScript cannot be drawn (no bounding box, a budget, a malformed file, nothing painted) the DOS EPS TIFF or WMF preview, else an EPSI hex preview, opens with the reason; with none the file is refused with it. **PDN** opens as **layers** (W13X-7, `raster::codec::formats::vector_docs::pdn`): a bounded MS-NRBF (.NET BinaryFormatter) record reader (nesting 64, 1M values, and the parsed graph's memory held to `MAX_GRAPH_BYTES` = 64 MiB, every kept string, value and object charged before it is stored; a class's name and member names are stored once and shared by its objects, so `ClassWithId` records copy nothing) finds the document, its layer list and each bitmap layer's surface by their members, reads the deferred pixel blocks (gzip or raw chunks, any chunk order; a block longer than its layer's `stride * height` is refused, each block is charged against the import limit on top of all the layers' pixels before its buffer exists, gzip chunks inflate straight into that buffer, and a block is dropped as soon as its layers are built, so a read holds the layers plus at most one block) and opens one raster layer per Paint.NET layer with its name, opacity, visibility and blend mode (Reflect, Glow, Negation and Xor, which have no equivalent here, open as Normal and the status line says so); File ▸ Revert reads the layers back, and refuses (keeping the layers) when the file no longer yields them rather than flatten to the thumbnail. Not done: the older layout whose whole object graph is gzip-wrapped, pixel blocks stored inline or pointing into another block, and any class shape the reader does not recognise open the flattened thumbnail instead, with the reason in the status line; verified against synthetic files built to the record format (`pdn::fixture`), not against files saved by Paint.NET. **Sketch / XD / FIG** open as **layers** (W13X-8, `raster::codec::formats::vector_docs::{design_files,design_fig}` read, `app-shell/src/import_design.rs` maps, on every open route and File ▸ Revert): Sketch `document.json` + `pages/*.json` (first page), XD `artwork/*/graphicContent.agc` (artboard bounds from `resources/graphics/graphicContent.agc`), Figma `canvas.fig` in the ZIP or a bare `fig-kiwi` canvas (the kiwi schema carried in the file decodes the message; chunks raw DEFLATE or Zstandard through `ruzstd` 0.9, MIT); artboards become artboards, groups groups, rectangles / ovals / paths / polygons / Figma primitives shape layers with fill and stroke (placement kept as the layer transform), text a text layer (string, font family, bold / italic, size, colour; fixed-width boxes wrap), bitmaps (Sketch `bitmap`, XD image-pattern fill, Figma `IMAGE` fill) raster layers at their frame size. The properties the readers recognise but cannot keep are named in the "<format> import report": symbol / component instances (not expanded), gradient and pattern fills, extra fills / borders, effects (shadows, blurs), non-union boolean operations, per-run text styles, layer and per-fill blend modes (Sketch `contextSettings.blendMode`, XD `style.blendMode`, Figma `blendMode` on a node or a paint) and masks (Sketch `hasClippingMask`, XD group `mask`, Figma `isMask`), other pages and unknown layer types; a property the readers do not look at is not reported. The Figma decoder holds schema names to 256 bytes and one decoded message to about 512 MiB (value slots, keys, enum names, strings and byte arrays together, charged by their sizes, plus the spare capacity every growing array or field list gains; the allocator's per-allocation overhead and the old buffer alive while a growing list is copied are not charged, so the real peak is somewhat above that), an array's up-front reserve to 4096 slots whatever length it declares, besides its 8M-value and 64-deep caps. Not done: none of the reported properties is kept (blend modes open as Normal, masked layers open unclipped); only the first page opens; rotated bitmaps are placed unrotated; a Figma `VECTOR` without stored `fillGeometry` is drawn as its box; XD point text is placed 0.8 em above its baseline (an approximation); the readers are verified against synthetic files built from the published structure, not against files saved by Sketch, XD or Figma. When the layers cannot be read the embedded preview opens (**Sketch** `previews/preview.png`, **XD** `preview.png`/`thumbnail.png`, a ZIP-packaged **FIG** `thumbnail.png`) and the status line says why. Every preview open sets a status line saying it is the preview. `.svgz` is offered by the Open picker and maps to SVG by extension; `.cube` is offered (default filter and "Color lookup tables"). Tests: `raster::codec::formats::{pdf,metafile,vector_docs}::tests::*`, `raster::codec::formats::vector_docs::design_files::tests::*`, `raster::codec::formats::vector_docs::pdn::tests::*`, `editor::open_any::open_pages::tests::*`, `editor::open_any::import_design::tests::*`, `editor::open_any::w13x7::tests::*` (the dialog driven through `Chrome::ui` frames: a pointer click on page 2's thumbnail, 144 dpi, Enter, the path reopened as the shell applies `open_recent`), `dialogs::pdf_import::tests::*`, `dialogs::tests::the_import_filter_covers_what_the_codec_reads`. |
| New document with presets | ✅ | screen, print and social presets; colour mode and background |
| Pan / zoom / fit / 100% / rotate view / flip view | ✅ | zoom-to-cursor keeps the point under the pointer fixed |
| View ▸ Mode, View ▸ Show ▸ Paths, Layer ▸ New ▸ Artboard, Warp Style ▸ Custom, Fill / Camera Raw chords, the options bars' rows | 🔶 | W16-K (`ui::menu`, `app-shell/src/menu_w16k.rs`, `ui/src/view/toolbar_w16k.rs`). **View ▸ Mode** ▸ Fullscreen / Standard / Menu Bar and Canvas (`MenuAction::SetScreenMode`) sets the screen mode `F` steps through. **View ▸ Show ▸ Paths** (`ViewFlag::Paths`, appended, on by default, one of the Extras) is a row that ticks and unticks; **not done**: no painter reads it yet, so the pen / path overlay still draws with it off (the overlay is published in `chrome.rs` and drawn in `ui::canvas`, outside this wave's files). **Layer ▸ New ▸ Artboard** (`MenuAction::NewArtboard`): the canvas as a white artboard when there is none, else one of the active (else top-most) artboard's size and background 100 px to its right, painted and undone as one step; the Artboard bar's four **+** buttons (`ArtboardNeighbour`) add one on each side (Photopea draws its + on the canvas; here they are bar buttons). **Layer ▸ Text ▸ Warp Style ▸ Custom** sets the W13X-5 mesh warp. **Fill…** is Shift+F5 and **Filter ▸ Camera Raw…** Shift+Ctrl+A, as in Photopea. Options bars: Hand / Zoom **Fit on Screen** and **100%**, Rotate View **Reset** (no Angle field); every selection tool **Refine Edge**, the wand group **Select Subject**; Type **Warp** and **Convert** (to Shape); a **brush-preset picker** on every brush-driven bar (the Brushes panel's presets); Crop's **…** Crop by list (All Layers = Reveal All, Current Layer, Trim, Selection = Crop to Selection; each crops at once, where Photopea sets the crop box first); the Pencil's **Size / Opacity from Pressure** toggles. Each row raises the menu action the menu runs; tests: `menu_bridge::w16k_tests::*`, `view::toolbar::w16k_tests::*` |
| Transparency checkerboard | ✅ | fixed pixel size, drawn inside the image for alpha pixels |
| Rulers, guides, smart guides, grid, snapping | ✅ | guides are saved with the document and changed by an undoable `Command::SetGuides`; View ▸ New Guide / Clear / Lock Guides; rulers honour the Units preference; pixel grid at 800% and above; W10-J: View ▸ Snap To (Guides, Grid, Layers, Slices, Document Bounds; All / None) feeds `tool_input::snap_candidates_for` (`each_snap_to_target_offers_only_its_own_candidates`); View ▸ Extras (Ctrl+H) hides the grid, guides, smart guides, layer edges, slices and the marching ants at once, each keeping its tick (`ViewFlags::shows`; `extras_hides_every_overlay_at_once_and_slices_show_the_committed_set`, `extras_off_hides_the_marching_ants_the_shell_draws`); unticking a Snap To target in the chrome stops a real Move drag catching it (`unticking_each_snap_to_target_in_the_chrome_stops_a_move_drag_catching_it`); View ▸ Show ▸ Slices paints the committed slices; View ▸ New Guide Layout… and New Guides from Shape add their guides as one `SetGuides` step (`new_guide_layout_opens_from_the_view_menu_and_lands_as_one_step`, `new_guides_from_shape_places_six_guides_as_one_step`) |
| Multi-document tabs | ✅ | the document is fitted, centred and drawn inside the canvas area, not under the panels (W5; `the_document_is_fitted_centred_and_confined_to_the_canvas_area`) |

### Layers
| Capability | Status | Notes |
| --- | --- | --- |
| Raster layers with real pixels | ✅ | content-addressed tiles |
| Layer tree, groups, reorder, rename, lock | ✅ | a drag that would nest a group in its own child is refused |
| Visibility / opacity / fill | ✅ | |
| Blend modes (27) | ✅ | includes Hue/Saturation/Color/Luminosity via W3C SetLum/SetSat |
| Layer masks | ✅ | density and feather honoured by the compositor |
| Vector masks | 🔶 | W9-G: `LayerMask::vector` (`layer_model::VectorMask`: SVG path data in layer space with its own enable, density, feather and invert; append-only serde) is rasterised by the compositor (`compositor/src/vector_mask.rs`: anti-aliased non-zero path coverage through the layer's and the mask's pose at each mip level, feathered, density-faded, inverted, multiplied with the pixel mask, and keyed into the tile cache). Layer ▸ Vector Mask ▸ Reveal All / Hide All / Current Path (the Paths panel's selected path, else the pen's Work Path) / Delete Vector Mask / Disable / Enable Vector Mask, one `SetLayerProperties` undo step each. PSD import keeps a non-shape layer's `vmsk`/`vsms` as a live vector mask, with the vector density/feather read from the mask parameter block; the mask record's rendering of the vector is not imported as pixels, and a `real` record becomes the pixel mask. PSD export writes `vmsk` path records and the vector parameter pair; on a layer with both masks the first mask record is the vector's rendering (flagged from-render) and the pixel mask is the `real` record, the convention the import reads. The Layers panel draws a vector-mask thumbnail well (`view/docks.rs` `vector_mask_well`: the path rendered by `compositor::vector_mask::rendering`, crossed out while disabled; clicking it turns Properties to the mask), and draws no pixel well for a vector-only mask. The Properties panel's mask page draws the vector mask's Vector density / Vector feather / Vector invert / Vector enabled rows through `VectorMaskProperties`; for a vector-only mask it draws only those rows (plus Linked), never the dead pixel half's. Layer Mask ▸ Apply on a layer with both masks bakes the pixel mask and keeps the vector mask (one undo step). Tests: `vector_mask_tests::*` (compositor, psd), `mask::tests::a_vector_mask_*` (layer-model), `menu::tests::vector_mask_rows_gate_on_the_layer_its_vector_mask_and_a_path`, `panels::properties::tests::vector_mask_density_and_feather_emit_patches_that_apply`, `menu_bridge::tests::a_vector_mask_from_the_current_path_hides_the_layer_outside_it`, `import::psd_vector_mask::tests::*` (incl. `export_with_both_masks_writes_the_pixel_mask_as_the_real_record`), `menu_bridge::tests::apply_mask_with_both_masks_keeps_the_vector_mask`, `ui/tests/w9g_vector_mask_panels.rs` (headless frames: the thumbnail texture is the triangle, no pixel well on a vector-only mask, dragging Vector density / Vector feather emits the vector patch). Gaps: an existing vector mask's path cannot be edited (Delete, then Current Path); Layer ▸ Layer Mask ▸ Delete Mask on a layer with both masks removes the vector mask too (Layer Mask ▸ Reveal All / Hide All / Reveal Selection / Hide Selection on a vector-only layer adds the pixel mask beside it, keeping the vector mask's pose and Linked state and laying the new coverage in that pose (`menu_bridge::tests::a_pixel_mask_added_beside_an_unlinked_vector_mask_keeps_its_pose`), and Apply Mask stays off while there is no pixel mask); a multi-subpath mask's `.psd` records are flattened to polylines; a vector-only mask is written with no rendered coverage (only an empty record when it carries density or feather), so a reader that ignores `vmsk` sees no mask; W11-C: the pixel mask's own density/feather are exported in the mask record's user-mask parameter pair (`import::psd_resources::tests::mask_density_and_feather_round_trip_through_psd`); only a pixel mask with no stored pixels, which writes no record, still notes them; the thumbnail ignores the vector's invert; not verified in Photoshop |
| Clipping masks | ✅ | Porter-Duff atop against the base layer's alpha |
| Adjustment layers (non-destructive) | ✅ | apply to the backdrop beneath them; themselves clippable. W11-A: `psd::adjustments::{decode, encode}` read and write Photoshop's adjustment-layer payloads (`nvrt` `levl` `curv` `brit` `hue2` `blnc` `blwh` `phfl` `mixr` `post` `thrs` `grdm` `selc` `expA` `vibA` `clrL`, the last with its embedded `.cube`) to and from `layer_model::AdjustmentKind`, bounded (every read through `Cursor`, counts checked against the bytes left, out-of-range values refused by name); PSD import opens them as live adjustment layers and PSD export writes them under their own keys (`app-shell` `import.rs`). Not decoded, and named in the import report with the layer kept empty: malformed payloads, Photo Filter version 3 (XYZ colour), a Color Lookup with no embedded `.cube`, Curves stored as 256-entry maps. Named but not held: per-range Hue/Saturation settings, gradient-map midpoints, curves beyond RGB. Auto / Desaturate / Equalize / Shadows-Highlights / Replace Color / HDR Toning / Match Color have no `.psd` adjustment layer and export empty with a note; so does a setting the layout cannot store (Brightness past ±150/255, a Black & White weight outside -200%..+300%, Posterize 256, curve inputs that collide once quantised to 1/255, a Color Lookup table entry that is not a finite number), named with its reason and never clamped. `curv` version 1 is read as a channel bitmap, version 4 as a curve count; written as version 1. Interoperability is unproven: the layouts follow Adobe's published specification and are verified only by round trips through `psd::adjustments`, not against files written by Photoshop or Photopea. Tests: `psd` `adjustments::tests::*` (including `settings_a_psd_cannot_spell_are_refused_not_clamped`, `whatever_encode_accepts_decodes_again_within_half_a_step`), `app-shell` `import::w11a_adjustment_tests::*` (Levels + Curves + Hue/Sat export and reopen live with the composite equal within 1/255; out-of-range settings reported on export). |
| Fill layers (Solid Color / Gradient / Pattern) | ✅ | W9-B: `LayerKind::Fill` (`layer_model::fill`: `FillSource::Solid` / `Gradient(GradientFill)` / `Pattern(PatternFill)`, append-only serde) is a live source — no tiles; `compositor/src/fill.rs` evaluates it in document space over the whole canvas (the layer transform moves only its mask, which shapes it), hashed into the tile cache key. Layer ▸ New Fill Layer ▸ Solid Color… / Gradient… / Pattern… open `ui::dialogs::FillLayerDialog` (Solid Color is the colour picker; Gradient: ramp swatch opening the nested gradient editor, style, angle, scale, reverse, dither; Pattern: the Define Pattern presets, scale, angle, link) whose OK is one `CreateLayer` undo step; the Properties panel's fill page (`FillProperties`: colour button, gradient angle/scale/reverse/dither, pattern scale/angle/link, each one `SetLayerKind` step) and its "Edit fill" button / Layer ▸ Edit Adjustment… reopen the dialog on the layer (`SetLayerKind`, one undo step); Rasterize ▸ Layer bakes it; `.rstudio` save/reopen keeps it live; PSD import maps `SoCo` / `GdFl` / `PtFl` to live fill layers and export writes them back (pattern pixels in a `Patt` block). Tests: `composite::fill::tests::*`, `fill::tests::*` (layer-model, psd), `dialogs::fill_layer::tests::*`, `panels::properties::w9b_fill_page_tests::*`, `dialog_host::w9b_fill_layer_tests::*`, `import::w8d_pattern_tests::psd_round_trip_keeps_every_fill_layer_live`. Gaps: double-clicking the Layers-panel thumbnail does not open the dialog yet (Properties / Edit Adjustment… do); the dialog-less `new_pattern_fill_layer` road (keyboard/script, not the menu) still bakes pixels; `SoCo` has no alpha, so a translucent colour exports as the layer's fill opacity; a noise gradient in a `.psd` is not mapped |
| Layer styles / effects | ✅ | drop shadow, inner/outer glow, satin, colour and gradient overlays — rendered by the compositor (`effects::render`). W9-H: drop shadows and outer glow blend with their own mode **against the backdrop** (a Multiply shadow multiplies what is under the layer), the layer's mode applies to the layer and its interior effects; contours (Linear, Cone, Gaussian, Ring, Rounded Steps, Custom via the W5 Curves widget) shape shadows, glows and the bevel profile; Blend If (This Layer / Underlying, gray/R/G/B, split handles) is honoured by the compositor and keyed into the tile cache; drop shadow, inner shadow, stroke, colour and gradient overlay repeat (`+`/`-` in the dialog); style presets are picked from a Styles grid (Apply Style Preset…) and `.asl` libraries open through File ▸ Open (see the editors row). Tests: `compositor/src/w9h_style_tests.rs`, `ui` `layer_style::tests` |
| Layers panel: double-clicks, effects list, Alt-click solo, trash drops, panel menu | 🔶 | W16-D (`ui::panels::layers::w16`, drawn by `ui::view::docks` through `docks_layers_w16.rs`; the application half is `app-shell/src/layers_panel_w16.rs`). A double-click on a row away from its name opens Layer Style on Blending Options (a text row enters the text); on a smart object's content thumbnail it opens Edit Contents, on a fill or adjustment layer's thumbnail Edit Adjustment (the fill dialog for a fill layer), on any other thumbnail what a row double-click does; both clicks must land on the same control (egui reports a double-click for any second click in its window). A styled layer lists "Effects" and one row per effect under its row in the Layer Style dialog's order; each effect's eye removes / restores that effect as one `SetLayerProperties` (one undo step; the parameters are kept in `LayersState` while hidden), the "Effects" eye is `effects.enabled`, a double-click opens Layer Style on that effect's page (`DialogHost` now honours `MenuAction::LayerStyle(slot)`'s slot), the fx arrow folds the list. Dragged onto the footer's trash: an effect row deletes that effect, the "Effects" row clears the style, a layer row deletes the layer (and the rest of the selection it belongs to), each one undo step; the trash lights while a row is over it. Alt-click on an eye hides the layer's siblings and its ancestors' siblings (showing it and its groups) as one step; Alt-clicking it again while they are still hidden shows them. The panel menu (the arrow at the right of the filter row, `OptionsItem`): Add "copy" to copied layers (read by `layer_ops::copy_name`, so Duplicate Layer, the chord, Alt-drag copies and Transform Again with Copy follow it), − / + Thumbnail Size over None / Small / Regular / Large (None draws no wells), Thumbnails by Layer (each thumbnail painted from its layer's ink rectangle of the document thumbnail, measured by `tool_input::tight_document_bounds` when the content revision moves) / by Document; stored as `Preferences::layers_panel` through `Preferences::sync_layers_panel`. Tests (all through a real `Chrome` frame, clicks at the drawn rects): `layers_panel_w16::tests::{a_double_click_on_a_layer_row_opens_layer_style, a_double_click_on_a_thumbnail_opens_the_layers_own_editor, the_effects_list_has_an_eye_per_effect_and_opens_layer_style_on_its_page, rows_dragged_onto_the_trash_are_deleted, alt_click_on_an_eye_shows_only_that_layer, the_row_menu_is_photopeas_and_duplicate_layer_follows_the_copy_option, the_panel_menu_sizes_thumbnails_down_to_none_and_crops_them_by_layer}`. Gaps: a hidden effect lives in the panel for the session only (the layer model has no per-effect off), so a document saved with an effect's eye off saves it absent; four thumbnail sizes, not Photopea's 10-200 px steps; the by-layer thumbnail is a crop of the 64-texel document thumbnail, not a render at the layer's bounds; the Duplicate Into… dialog opens at "<name> copy" whatever the copy option says. |
| Merge / flatten / duplicate / rasterize | ✅ | W9-I: Duplicate Layer… has a Destination combo over every open document, and a Layers-panel row dropped on another document's tab copies it there (`Editor::duplicate_layer_into_document`): tiles are filed content-addressed into the target's store, masks, effects, group children and a smart object's asset record travel with it, the copy sits at the same position on top of the target's stack as one undo step in the target (nothing recorded in the source), and the target becomes active. Tests: `chrome::tests::dragging_a_layer_row_onto_another_documents_tab_copies_it_there`, `chrome::tests::duplicate_layer_with_another_destination_copies_into_that_document`, `chrome::tests::picking_another_document_in_the_destination_combo_copies_into_it` (clicks the combo open, clicks the other document's entry, presses Enter), `chrome::tests::a_group_copied_into_another_document_brings_its_children`, `dialogs::duplicate_layer::tests::*` (ui). Gaps: no "New document" destination; a 16-bit layer is refused into an 8-bit document; the mask-tile and smart-object-asset paths of the copy have no test of their own yet |

### Editing
| Capability | Status | Notes |
| --- | --- | --- |
| Undo / redo with a history panel | ✅ | clickable stack with snapshots; a stroke is one step |
| Command journal + crash recovery | ✅ | anchored to a save marker, so replay cannot duplicate work; commands made while a save runs go to a `.journal-hold` side file and are absorbed exactly once afterwards, or on the next open after a crash (W2, W5) |
| Cut / copy / paste / clear | ✅ | incl. Copy Merged, Paste Special (Paste in Place, Paste Into, Paste Outside), Layer Via Cut/Copy. W11-D: Edit ▸ Paste with no document open makes a new document the size of the clipboard's image (the OS clipboard's, else the editor's own copy) with the image pasted into it (`Editor::new_document_for_paste`, the `paste` arm in `menu_bridge`). The menu row is live whenever the clipboard holds an image; the Ctrl+V chord is not refused by `Editor::can` with no document and reads the clipboard on the keypress, so an empty clipboard creates nothing and puts "The clipboard is empty" on the status line. The other pastes still need a document. Tests: `editor::open_any::tests::paste_with_no_document_opens_a_document_of_the_clipboard_size` (fake clipboard, menu resolve + perform), `shell::w11d_tests::{ctrl_v_with_no_document_opens_the_clipboard_image, ctrl_v_with_no_document_and_an_empty_clipboard_creates_nothing}` (Ctrl+V through `Shell::on_key`), `ui` `menu::tests::paste_without_a_document_and_revert_resolve_as_photopea_does`. Gap: File ▸ New does not pre-fill the clipboard image's size |
| Edit ▸ Fade / Preset Manager / Auto-Align Layers / Auto-Blend Layers / Perspective Warp | 🔶 | W10-G. **Fade…** (`app_shell::fade`): the filter, adjustment, fill and stroke routes record the layer's pixels before and after (Liquify, Puppet Warp, the Blur Gallery and Perspective Warp share the filter route, so they fade too); the row is live only while that step is still the document's top undo entry (sealed on its inverse command, so an undo or any later step greys it with `ui::menu::FADE_NOTHING`) and names the step ("Fade Apply Invert…", `MenuAction::label_in` from `MenuContext::fade_step`); OK blends each changed pixel and keeps an unchanged one (outside the step's selection) exactly (`ui::dialogs::FadeSpec::fade`: the step's result laid over the original by the blend mode, mixed back by opacity), undoes the step and applies "Fade <step>" — the history depth is unchanged. **Preset Manager…** (`app_shell::edit_gaps`): one dialog over brushes, swatches, gradients, patterns, styles, custom shapes and tool presets — five from the preset store, swatches and tool presets from the Swatches and Tool Presets panels (published and written back by `edit_gaps::sync_workspace_presets`, called from the per-frame `Editor::sync_panel_presets`, so a swatch edit also reaches the preferences file); rename (unique, non-empty), delete, move up/down, Import / Export the whole library as JSON (an import merges by name, and an entry whose payload the store cannot rebuild refuses the file); OK rebuilds and saves the store and queues the panels' lists for the next frame, Cancel leaves both. **Auto-Align Layers…** (`filters::align`): the bottom selected pixel layer is the reference; Auto = FAST-9 corners on a 4-level pyramid, intensity-centroid orientation, steered BRIEF (256 pairs), cross-checked ratio-tested brute-force Hamming matching, 1500-draw RANSAC similarity with least-squares refit; Reposition = Hann-windowed phase correlation with sub-pixel peak; each other layer gets a `TransformLayer` in one transaction. **Auto-Blend Layers…** (`filters::blend_layers`): Panorama lays the layers bottom first, cutting each overlap along a minimum colour-difference seam (dynamic programming) feathered inside the overlap; Stack keeps the sharpest layer per pixel by box-averaged squared Laplacian, feathered and renormalised; the result is one new layer at the top. **Perspective Warp** (`filters::perspective_warp`): a dialog with a preview canvas — Layout draws quads, Warp drags corners; each quad is the 8x8 DLT homography of its corners, applied by inverse mapping, and a lifted plane leaves transparency. Tests: `edit_gaps::tests::fade_at_fifty_percent_after_invert_gives_mid_values_as_one_step`, `fade_greys_out_once_the_step_is_undone_or_followed`, `auto_align_recovers_a_known_shift_and_rotation_as_a_layer_transform` (±1 px, ±0.5°), `auto_blend_stack_keeps_the_sharp_half_of_each_layer_in_a_new_layer`, `perspective_warp_moves_the_plane_as_one_step_and_an_unmoved_quad_is_refused`, `preset_manager_renames_reorders_deletes_and_round_trips_json`, `preset_manager_edits_the_swatches_and_tool_presets_panels`, `fade_multiply_after_a_selection_limited_invert_keeps_the_unselected_pixels`, `the_fade_row_names_the_step_it_fades` (app-shell, each through the menu row, the dialog host and `perform`); `align::tests::*`, `blend_layers::tests::*` (incl. `focus_stacking_picks_the_sharp_half_of_each_image`), `perspective_warp::tests::a_quad_warped_to_itself_is_the_identity` (filters); `dialogs::{fade,auto_align,preset_manager,perspective_warp}::tests::*` (ui, incl. pointer drags on the warp canvas). Gaps: Auto-Align has no Perspective / Cylindrical projection and does not pick the reference automatically; Auto-Blend writes one new merged layer rather than per-layer masks, and Panorama seams are single straight-axis paths; Perspective Warp runs in a dialog, quads are not joined into a shared mesh, and there are no straighten / auto-level buttons; Fade covers the whole-layer pixel routes only (not paint strokes or mask fills) |
| Edit ▸ Purge (Clipboard / Histories / All) | ✅ | W4-H; Histories and All ask first — the second choice within 10 s confirms (a status-line confirmation, not a modal: the dialog seam has no yes/no question for it yet) |
| Free transform (scale/rotate/skew/distort/perspective/warp) | ✅ | interactive gestures apply real, undoable commands; singular matrices are refused rather than writing NaN; with a partial pixel selection, Free Transform and Move float only the selected pixels as one undo step (W5, `tools::transform::float_selection`; `free_transform_with_a_selection_scales_only_the_selected_pixels`, `move_with_a_selection_moves_only_the_selected_pixels`). W9-L: the options bar reads the live box back (`tools::transform::NumericTransform::read`) at a 3 x 3 reference point and edits it by X / Y, W / H %, Link, Angle and H / V Skew; Interpolation (Nearest / Bilinear / Bicubic) picks the filter of every resampled commit (the lossless whole-layer affine commit is not resampled); Warp presets Arc, Arch, Bulge, Flag, Wave, Fish, Rise, Fisheye, Inflate, Squeeze, Twist with Bend set the warp mesh and put the session on Warp ; the shell forwards those fields to the live session every frame between presses (`ToolPointer::forward_transform_numeric`), so a typed field or a picked preset reshapes the published box with no canvas press and Enter commits what the fields say; the edit counter applies on any change and the bar never reuses a number, so the first edit after the options-bar Reset lands too (`the_first_edit_after_the_options_bar_reset_reshapes_the_live_quad`, `an_edit_counter_that_went_back_down_still_applies`; `tools::transform::w9l_tests`, `ui::view::toolbar::w9l_tests`, `app-shell` `shell::w9l_tests`). W16-F: Ctrl on a handle borrows a mode for that drag (`tools::transform::ctrl_mode`): Ctrl + a side skews it along itself, Ctrl + a corner distorts that corner alone, Ctrl + Alt + Shift + a corner is perspective; a Ctrl-distorted box previews and commits as Distort / Perspective (a resample) and the options bar's mode is left as it was (`transform::w16f_ctrl_tests`, `app-shell` `w16f_route_tests::ctrl_dragging_a_corner_distorts_and_enter_lands_it_as_one_step`, `ctrl_dragging_a_side_skews_it`). The Move tool's Show Transform Controls box has live handles: a press on a corner, a side or (outside the box) a corner's rotate band runs that drag as a Free Transform session over the framed box, committed at the release as one step; inside the box it is still a Move (`edit::w16f_show_transform_tests` — a corner drag scales, a drag just outside a corner rotates — and `a_show_transform_controls_corner_drag_scales_the_layer_in_one_step`). Gap: the warp presets approximate Photoshop's envelopes on the 4 x 4 Bezier mesh (no H / V distortion); a Show Transform Controls handle drag commits at the release instead of staying open for Enter as Photopea's does; Ctrl + Alt / Ctrl + Shift on a side skew as plain Ctrl does (no symmetric or constrained skew) |
| Crop, trim, image size, canvas size, rotate canvas | ✅ | crop (ratio presets, W×H×resolution, straighten, Delete Cropped Pixels, overlay — W4), trim, the Image Size and Canvas Size dialogs and the rotations each apply as one undo step; a crop's rotation and scale ride the layer transforms rather than being baked. W13-I: **Content-Aware** (`tools::edit::CROP_CONTENT_AWARE_KEY`): with it ticked the crop box may run past the canvas, and at Enter the canvas the crop adds (past the old edges, or uncovered by a straighten) is filled on the active raster layer, in its own pixel space, by `filters::content_aware_fill` (`app_shell::tool_input::crop_fill`), appended to the crop's transaction — one undo step (`tool_input::w13i_tests::a_content_aware_crop_past_the_canvas_fills_the_new_columns_in_one_step`, `edit::option_tests::content_aware_lets_the_crop_box_run_past_the_canvas`); with Delete Cropped Pixels also on, a tile the fill shares starts from what the delete left, so cropped-away pixels stay deleted (`a_content_aware_crop_with_delete_cropped_pixels_keeps_the_cropped_pixels_deleted`). a 16- or 32-bit document is refused by `bit_depth != 8` and crops unfilled with the status line saying so, its HDR samples untouched (`a_content_aware_crop_refuses_to_fill_a_16_or_32_bit_document`). Gaps: only the active layer is filled and only in an 8-bit document (the fill writes RGBA8 tiles; no 16-bit or `f32` fill); the synthesis runs on the interaction thread; a new area larger than the fill's context limit crops unfilled and the status line says why |

### Selection
| Capability | Status | Notes |
| --- | --- | --- |
| Marquee (rect / ellipse / row / column) | ✅ | anti-aliased; W9-L: every selection tool's Mode includes Exclude (XOR, `selection::BooleanOp::Exclude`), and the rect / ellipse marquees have Style Normal / Fixed Ratio / Fixed Size with W and H (`tools::select::w9l_tests`) |
| Lasso (free / polygonal / magnetic) | 🔶 | W16-A (`tools::select::LassoTool`, routes in `app-shell/src/select_w16.rs`, proved through the shell in `select_w16_shell_tests`): the polygonal and magnetic lassos hold the outline open between presses and close on Enter, a double-click or a press on the first point; Backspace / Delete remove the last point (before the keymap's Clear); Escape drops it; each close is one undo step. The magnetic lasso is click, then move: hovers lay anchors every 12 px, each pulled onto the strongest edge within Width (Contrast weights it, 0 turns it off), a click adds one, a drag released on its start still closes; the path between anchors is snapped at close (`lasso_magnetic`). Alt pressed while dragging the freehand lasso draws a straight segment; released with Alt held the outline stays open for straight clicks, and letting Alt go (on the next pointer move) or Enter closes it. Not done: the magnetic **Frequency** is answered by the tool (`frequency`, 0-100) but the options bar does not offer it (the registry spec is outside this wave's files); a double-click is recognised as a second press within 4 screen px with no travel between, with no timer; the live overlay shows the anchors, not the snapped path between them. |
| Drag a selection's outline with a selection tool | ✅ | W16-A: with any marquee, lasso, Magic Wand, Quick Selection or Object Selection, a press inside the selection in New mode with no modifier moves the outline once the pointer travels 3 screen px (a shorter press stays the tool's click): the ants follow live, the pixels do not move, the release lands one `SetSelection` step, Shift constrains to 45 degrees, Escape puts it back; a press outside, or with Shift / Alt, selects as before (`app-shell/src/select_w16.rs`). |
| Magic wand / quick select / colour range | ✅ | tolerance, contiguous flag, anti-aliasing |
| Select Subject | 🔶 | W10-K: Select ▸ Subject is back, as a classical pipeline with no neural model (`selection::subject`): saliency on a working grid of at most 384 px (frequency-tuned contrast x a border-connectivity geodesic prior x a centre prior), Otsu seeds (the most salient connected region is foreground, low-saliency border-connected pixels background), GrabCut (5-component RGB Gaussian mixtures + a Dinic max-flow/min-cut on the 8-connected pixel grid, `selection::grabcut`), then a full-resolution re-cut in a band around the boundary; it runs on the job worker with progress on the status line, the frame loop's idle wake (`Editor::jobs_pending` / `poll_jobs`, driven by the shell's `pump_jobs`) counts it so an idle window lands it with no pointer move, and it lands as one `SetSelection` undo step; a flat image selects nothing and says so (`select_subject_is_a_live_select_menu_row_that_selects_the_disc`, `the_job_runs_off_the_ui_thread_and_lands_on_a_later_frame`, `select_subject_is_a_job_pump_jobs_waits_on_and_lands`, `a_textured_disc_is_selected_with_iou_over_0_9`, `the_boundary_is_recut_at_full_resolution_not_upsampled` (8 px working cells: the refined mask splits cells along the rim and is within the rim length of the truth), and `the_graph_cut_decides_the_band_the_seeds_leave_open`: the seeds alone score IoU < 0.9 and the cut must put 99% of the undecided pixels on the right side). Gap: colour-driven, not semantic — it selects the ONE region that differs most from the frame's border, so a subject sharing the background's palette, touching the border over most of its outline, or two separate subjects are not handled as Photopea's model does; the edge is hard (0/255). W11-G: the Object Selection tool exists (`ToolId::ObjectSelection`, third in the wand slot on W): the drag records a rectangle (`tools::select::ObjectSelectionTool`), `tool_input` hands it to `selection::select_object` (GrabCut initialised from the rectangle) on the editor's job spawner, and the object's mask is combined by the gesture's mode (New / Add / Subtract / Intersect / Exclude, Shift adds, Alt subtracts) as one `SetSelection` step (`object_selection_selects_the_object_inside_the_dragged_rectangle`: IoU >= 0.85 against a textured disc through the real pointer route; `the_job_runs_on_a_worker_and_lands_on_a_later_frame`); `Editor::jobs_pending` counts the running job and `Editor::poll_jobs` lands it, so the shell's idle loop keeps waking until it lands with no pointer move (`the_editor_job_pump_counts_and_lands_the_job`). Gap: there is no hover-to-highlight Object Finder |
| Modify: feather, expand, contract, smooth, border | ✅ | true morphology on fractional coverage |
| Invert, grow, similar, transform selection | ✅ | |
| Select Pixels (Ctrl+click a layer / mask thumbnail) | ✅ | W9-A: Ctrl+click a layer thumbnail = a new selection from the layer's transparency (the compositor's alpha over a staged copy, so text, shapes and transformed layers select the ink they draw), Ctrl+Shift adds, Ctrl+Alt subtracts, Ctrl+Shift+Alt intersects; a mask thumbnail selects the mask's coverage through the mask's pose; the layer-row menu's Select Pixels acts on the active layer; one `SetSelection` undo step (`a_ctrl_click_on_a_thumbnail_selects_exactly_the_layers_ink_and_shift_adds`). W13-M, Photopea's other mask-thumbnail gestures (`view/docks.rs` `mask_well`): Shift+click disables the mask (one undo step through Layer Mask's Toggle) and draws a red cross (the chrome's `close` icon in the danger colour) over the thumbnail, and enables it again; Alt+click shows the mask alone on the canvas (Alt+Shift+click: the rubylith overlay) and again puts the composite back; Photopea's `mskView` keys are canvas keys on the shell's key route (`shell::mask_view_key`, answered in `Shell::on_key` before the keymap, only while the active layer has a mask): backslash toggles the rubylith overlay, backquote the mask alone, Escape puts the image back, with the Layers panel closed or the layer inside a folded group (`shell::w13m_tests::*`: real chrome frames clicking the drawn thumbnail and asserting the document's mask flag, the painted red strokes and `Chrome::mask_view`; the key tests press through `Shell::on_key`). |
| Quick mask, save/load selection | ✅ | quick mask composes (`Q` / Select ▸ Edit in Quick Mask Mode: edits land in a scratch mask, leaving converts the painted coverage into the selection); selection itself (outline, marching ants, save/load) is reachable |

### Tools
| Capability | Status | Notes |
| --- | --- | --- |
| Brush that actually paints | ✅ | one undoable command per stroke; overlapping dabs do not darken |
| Brush dynamics, sampled tips, Define Brush from pixels, `.abr` import | 🔶 | W9-E. `tools::brush::BrushDynamics` (append-only on `BrushSettings`, `#[serde(default)]`): Shape Dynamics (size jitter with a minimum diameter, angle jitter, roundness jitter with a minimum roundness), Scattering (amount in diameters, both axes, count 1-16, count jitter), Transfer (opacity and flow jitter, per dab) and Color Dynamics (foreground/background, hue, saturation and brightness jitter), all drawn from a SplitMix64 generator seeded by the brush's seed and the stroke's first point, so a stroke replays to the same dabs (`brush::tests::size_jitter_varies_the_dab_radius_and_a_seeded_stroke_replays_identically`, `scatter_throws_dabs_off_the_path_and_count_multiplies_them`). `BrushSettings::tip` is `Round` or `Sampled(TipId)` (the BLAKE3 hash of the tip's pixels); a sampled tip is a grayscale image stamped scaled so its longer side is the diameter, rotated by the angle and squashed by the roundness, sampled bilinearly (`a_sampled_tip_stamps_its_own_shape`). **Edit > Define Brush Preset** makes the tip from the active layer's pixels inside the selection (dark and opaque = paint, cropped to the ink, box-shrunk past 5000 px), stores the pixels with the presets (the JSON names each tip by hash; the pixels are a one-byte-a-pixel binary file per tip in `presets.tips/`, written once and hash-checked when read back, so a tampered or truncated file drops that tip; a `presets.json` from before, with pixels inline, still loads and moves them out on the next save; `presets::tests::tip_pixels_live_in_a_binary_file_not_in_the_json`; registered again at startup), lists it in the Brushes panel and makes it the active painting tool's brush at the sample's size; one click then stamps the sample's shape (`editor_brush_tests::define_brush_preset_samples_the_selected_pixels_and_a_stroke_stamps_that_shape`). **File > Open of a `.abr`** (v6+ sampled brushes, raw or PackBits, 8/16-bit; `asset_store::abr`, bounds-checked with a 64 MB file cap, 1000-brush cap, 5000 px side cap and a 256 Mi-pixel cap on the decoded total, taken before each tip is decoded so a PackBits bomb is refused, not expanded; malformed input is an error; `abr::tests::a_packbits_bomb_is_refused_by_the_decoded_pixel_budget`) adds every brush to the Brushes panel as "<file> N" (`file_open_of_an_abr_adds_its_brushes_to_the_brushes_panel`, `a_malformed_abr_is_an_error_not_a_panic_and_adds_nothing`, `abr::tests::malformed_files_are_errors_never_panics`); a Brushes-panel click carries the tip and dynamics onto the brush. The Brush editor dialog has Tip, Shape Dynamics, Scattering, Color Dynamics and Transfer sections. Gaps: colour dynamics vary once per stroke, not per tip (the stroke compositor paints one colour per stroke); no Texture, Dual Brush, Wet Edges, Build-up or Noise; no pen tilt/pressure controls for the jitters; the `.abr` `desc` section is skipped, so brush names, spacing and saved dynamics are not read, and v1/v2 (computed) brushes are refused; the Brushes panel's tiles draw a sampled brush as a round tip; "Save current brush" in the panel keeps only the options-bar keys (not the tip or dynamics) |
| Eraser, pencil, paint bucket, gradient | ✅ | W9-L: the gradient and the paint bucket are offered the paint blend Mode (`tools::composites_strokes`) and composite through it, opacity included (`gradient::w9l_tests`, `bucket::w9l_tests`); a mask target reads only the opacity. W11-I: the Gradient options bar's **Transparency** toggle (on by default); off, the ramp's opacity stops are ignored and it draws opaque (`GradientRamp::opaque`, `gradient_transparency_off_ignores_the_ramps_opacity_stops`). W13-I: Style **Shape Burst** (`GradientShape::ShapeBurst`, appended): the ramp parameter is the Euclidean distance in from the edge of the target's own alpha (alpha over one half; the render rect's border is an edge; a layer with no opaque pixel bursts from the border), over the drag's length (`gradient::shape_burst::BurstField`, an exact distance transform); (`gradient::w13i_tests::shape_burst_follows_the_layer_alpha_not_the_drag_axis`); on a mask it measures the mask's coverage (`gradient::w13i_tests::shape_burst_on_a_mask_follows_the_mask_coverage`). Gap: the distance transform allocates an f64 grid, an f32 grid and a bool mask over the whole render rect on the interaction thread (about 13 bytes a pixel) |
| Move tool Align / Distribute | ✅ | W9-L: six Align and two Distribute buttons in the Move options bar raise Layer ▸ Align / Distribute (`the_move_bar_align_and_distribute_buttons_raise_the_layer_menu_actions`). W11-I: with two or more layers selected and no pixel selection, Align aligns them to each other (their combined bounds, Photopea's learn/layer-manipulation "so they all have centers at the same point, or to get their upper edge to the same height"); one layer aligns to the canvas; a pixel selection still wins (`layer_ops::w11i_align_tests`). W13-I: a **Quick Export** button after Distribute raises `MenuAction::QuickExportLayer` (also File ▸ Export ▸ Quick Export Layer as PNG…), which writes the active layer composited alone (unrelated layers hidden; its groups and, for a group, its children kept) at canvas size over transparent as one PNG where the user picks (`app_shell::tool_input::quick_export`; `the_move_bar_align_and_distribute_buttons_raise_the_layer_menu_actions`, `tool_input::w13i_tests::quick_export_writes_the_active_layer_alone_as_a_png`). Gap: it always asks where (no remembered folder), and it encodes on the interaction thread |
| Move tool Alt+drag duplicate | 🔶 | W13-A (Photopea learn/layer-manipulation, learn/moving-selected-data). A Move drag pressed with Alt held moves a COPY, as ONE undo step: with no pixel selection the pointer route hands the tool's layer move to `tool_input::move_duplicate::copy_and_move`, which runs Duplicate Layer's commands (`layer_ops::duplicate_commands`) for every moved layer — a multi-layer or linked set included — seats each copy directly above its source, applies the move to the copies and selects them (History row "Duplicate and Move"); with a pixel selection `MoveTool::copy_selected_pixels` lays a copy of the selected pixels down at the whole-pixel drop over what was there, leaves the originals and moves the ants ("Duplicate Selection"). `shell::temporary_tool_for` lends nothing while the Move tool is in hand (Alt is never the Eyedropper there) and Ctrl+Alt lends the Move tool on the tools Ctrl already lends it on (not on the ones `ctrl_keeps_the_tool` names: Hand, Zoom, Rotate View, the pens and anchor tools, Path / Direct Selection, the four type tools, Free Transform, Crop, Perspective Crop, Slice, Slice Select, Artboard), so Ctrl+Alt+drag copies there. Tests (through `ToolPointer::handle` with Alt on the samples): `tool_input::move_duplicate::tests::*` (Alt+drag gives N+1 layers, the copy offset and the original unmoved, one Undo restores the stack; a multi-layer set; a selection copy keeps the original pixels; plain drags still move without copying; the lent-tool rules; a Ctrl+Alt+drag from the Brush through the lent Move tool), `tools::edit::tests::move_options_tests::an_alt_drag_with_a_selection_copies_the_pixels_and_keeps_the_originals`. W13X-6: a group is copied WITH its subtree — `layer_ops::duplicate_commands` copies every child (names, pixels, masks, labels, order; nested groups too) into the new group, so Alt+drag on a group and Duplicate Layer on a group (the menu arm and the Ctrl+Alt+J action, which now runs the same copy) are one step each (`move_duplicate::tests::alt_drag_on_a_group_copies_the_group_with_its_children_as_one_step`, `duplicate_layer_on_a_group_copies_its_children_on_both_routes`). W13X-1: Alt+arrow copies too, through the same `copy_and_move` (the arrow-key nudge row below). Status partial: the pixel copy samples nearest-neighbour, exact for an untransformed layer only |
| Arrow-key nudge (Move tool, selection tools, open transform, path tools, Slice Select) | ✅ | W13X-1 (Photopea learn/layer-manipulation). `Action::Nudge(NudgeDirection)` is bound to the four arrows with every Shift / Alt combination (`Keymap::defaults`, rebindable in the shortcut editor); the shell's `on_key` reads the step and the copy off the chord and drives `ToolPointer::nudge` (`tool_input::move_duplicate::nudge`). **Move tool**: a fresh `MoveTool` (Auto-Select off, so the active layer) is pressed and released 1 px apart (10 px with Shift) over the off-pointer context with the snap candidates emptied, so an arrow moves exactly what a drag would — the active layer and the panel's layer selection and link groups, locked layers filtered, or, with a pixel selection, the selected pixels with the ants ("Move Selection"); **Alt+arrow** moves a COPY through W13-A's `move_duplicate::copy_and_move` (N+1 layers, the original unmoved, one step) or floats a copy of the selected pixels. **Selection tools** (the four marquees, the three lassos, Magic Wand, Quick and Object Selection): the arrows move the outline only, a rectangle exactly and a mask by whole pixels (`SetSelection`, one step). Every press is its own history step (not coalesced); a held arrow's repeats keep nudging (`shell::route_key` admits arrow repeats), and with Alt only the press copies. The arrows never nudge while a text field holds the keyboard (`route_key` ignores keys while egui has text focus) or while a Type run or a pointer gesture is live (`nudge` refuses). Other tools: the arrows do nothing. `Editor::dispatch(Action::Nudge(..))` alone is the plain 1 px nudge. Tests, all through `Shell::on_modifiers` + `Shell::on_key`: `shell::nudge_tests::*` (1 px per press and one step each, Shift 10 px, Alt N+1 layers with the copy moved and two Undos restoring the stack, held repeats, the pixel-selection float, a marquee's and a lasso mask's outline moving with no pixel or layer change, a panel field with focus and an open Type run not nudging); `editor::tests::every_action_does_something` runs the four `Nudge` actions. W16-F (Photopea learn/free-transform, learn/vg-manipulation, learn/slices): with an **open Free Transform** the arrows move the whole box 1 px (10 with Shift; `transform::keys::NUDGE_X` / `NUDGE_Y`), landing at Enter with the rest of the session; **Path Select** moves its selected path components and **Direct Selection** its selected knots (`nudge_x` / `nudge_y` then `Tool::commit`), and **Slice Select** the picked slice (`slices_export::remember_edited`), each press one step (`app-shell` `w16f_route_tests::the_arrows_move_an_open_transform_box_and_enter_lands_it`, `path_select_moves_nudges_reorders_and_deletes_one_component`, `direct_selection_arrows_nudge_the_shift_selected_knots`, `slice_select_arrows_nudge_the_picked_slice_one_step_each`, driven through `ToolPointer::nudge`, the call `on_key` makes). Gaps: Ctrl+arrow (Photoshop's Move-tool nudge from other tools) is not bound; a nudge with Show Transform Controls on re-frames the box on the next pointer sample, not at once; Slice Select nudges the one picked slice (it picks one slice, not a Shift set) |
| Clone stamp, healing, spot healing, patch, red-eye | ✅ | W9-D: Clone Stamp, Healing Brush and Spot Healing Brush (and Blur / Sharpen / Smudge) have the Sample option (Current Layer / Current & Below / All Layers, `tools::tool::SampleLayers`); a stroke that is not Current reads its source and neighbourhood from the composite the shell lends at the press (`ToolContext::composite_sampler`, `app_shell::tool_input` `DocumentCompositeSampler`: a clone of the committed document, composited lazily per tile the stroke reads, Current & Below hiding the layers above the active one) and writes onto the active layer, which may be empty (`tool_routes.rs`: `clone_stamp_sampling_all_layers_copies_the_photo_into_an_empty_layer`, `clone_stamp_sampling_the_current_layer_copies_nothing_from_an_empty_layer`, `clone_stamp_sampling_current_and_below_skips_the_layers_above`, `spot_healing_sampling_all_layers_heals_the_blemish_onto_an_empty_layer`, `every_sampling_retouch_tool_writes_the_composite_onto_an_empty_layer`). Gaps: no "ignore adjustment layers" toggle; on a moved/scaled layer the composite is read nearest-neighbour; a Content-Aware spot heal that samples the composite synthesises on the interaction thread (not deferred to the worker) |
| Content-aware fill, Spot Healing Content-Aware type, Content-Aware Scale | 🔶 | W7-I, no model: Edit ▸ Fill ▸ Contents: Content-Aware synthesises the selection from the rest of the layer by multi-scale PatchMatch (`filters::patchmatch`, 7-px patches, propagation + random search, weighted EM voting, fixed seed; bounded to the selection's bounding box plus a context margin) on a job worker (`app_shell::menu_bridge::content_aware_job`; the result lands as one undo step on the next frame, and is discarded if the document, layer, selection or pixels changed while it ran); the Spot Healing Brush's Type option (Proximity Match / Content-Aware) runs the same synthesis over the brushed area after release on the same worker (W8-D: the tool hands the snapshot over through `Tool::take_deferred_commit`, the status line counts seconds, Esc drops it, it lands as one undo step and is discarded if the pixels under the stroke changed or its document is no longer the active one; round 2: a heal released while another content-aware job runs waits in a queue in release order and, when its turn comes, re-snapshots the pixels (`DeferredStroke::refresh`), so click-click heals all land, each as its own undo step, byte-equal to the same strokes released synchronously one after another; a Fill or Scale is still refused while any job runs or waits; `menu_bridge::content_aware_job::spot_heal_tests::*`, `stroke::live_tests::a_deferred_content_aware_spot_heal_lands_the_synchronous_bytes`); Edit ▸ Content-Aware Scale seam-carves the active layer (gradient-magnitude energy) on the same worker to 80/90/110/125% of the canvas width or height, centred. Tests from the dialog to the pixels: `shell::w7i_tests::*`. Gaps: no percentage progress (a running timer only), and Esc's route is also the focus-loss route, so losing focus drops a running heal; the fill refuses a context window over 2 M pixels; W10-J: Edit ▸ Content-Aware Scale ▸ With Handles (Alt+Shift+Ctrl+C) is Free Transform's Content-Aware mode — the handles (or the bar's W / H) size an upright box, the options bar's Amount (shown only in this mode) blends seam carving with a plain scale, Enter commits one `PaintTiles` step (`tools::transform::content_aware_resample`; `content_aware_mode_carves_the_ground_and_keeps_the_square_as_one_step`; from the menu row and the chord through the shell: `content_aware_scale_with_handles_from_the_menu_commits_a_seam_carved_step`, `alt_shift_ctrl_c_starts_content_aware_scale_with_handles`), the fixed steps stay as presets; the box previews as a plain scale, the commit runs on the interaction thread (not the job worker), and there is no protect-skin or protection channel; the Content-Aware spot heal's live preview shows the Proximity Match answer and only the release synthesises |
| Dodge / burn / sponge, blur / sharpen / smudge | ✅ | W11-I: Dodge / Burn **Protect Tones** (on by default; keeps each pixel's HSL hue and saturation and changes only lightness, `tools::stroke::protect_tones`) and Sponge **Vibrance** (on by default; a Saturate dab is scaled by how unsaturated the pixel already is, `tools::stroke::sponge_factor`; Desaturate is unchanged) are registry Bool options the options bar draws (`tests/w11i_tool_options.rs`) |
| Eyedropper, move, hand, zoom, rotate view | ✅ | W13-I: the Eyedropper's **Sample** choice (Current Layer / Current & Below / All Layers, default All Layers; replaces the Sample All Layers box; a direct set of the old `sample_all_layers` key maps onto the choice, on = All Layers, off = Current Layer — `edit::option_tests::the_legacy_sample_all_layers_box_overrides_a_lent_composite` — but the options bar stores only registry keys, so a tool preset saved with the old box drops it and gets the default) reads the composite the shell lends at the press (`DocumentCompositeSampler`; lent on the registry default too, so the untouched Eyedropper reads the composite) (`tool_input::w13i_tests::the_eyedropper_sample_choice_reads_current_and_below_or_all_layers`); while it is dragged on the canvas a **sampling ring** at the pointer shows the colour being picked (upper half) over the foreground the drag started from (lower half) (`ui::view::eyedropper_ring`, painted at the end of the egui pass; `an_eyedropper_drag_on_the_canvas_draws_new_over_old_and_release_clears_it`). Gaps: the ring is drawn only while the options bar is (not with the panels hidden by Tab) |
| Held-key tool gestures (Alt-sample, Shift-line, Ctrl-Move, Ctrl/Alt+Space zoom, Alt+wheel) | 🔶 | W11-F. The shell lends a tool while a modifier is held (`shell::temporary_tool_for`, `Editor::set_temporary_tool`, answered first by `Editor::effective_tool`; synced on every `ModifiersChanged`, on Space press/release and at each press): **Alt** on Brush / Pencil / Colour Replacement / Mixer Brush / Paint Bucket / Gradient is the Eyedropper, so an Alt-click sets the foreground from what the Eyedropper samples (its own options; Sample All Layers is on by default, i.e. the composite) with no history step; **Ctrl** is the Move tool on every tool except Move, Hand, Zoom, Rotate View, the pen and anchor tools, Path / Direct Selection, the type tools, Free Transform, Crop, Perspective Crop, Slice, Slice Select and Artboard, and releasing Ctrl gives the selected tool back; **Ctrl+Space** / **Alt+Space** (when the keymap binds neither) hold the space bar's slot and lend the Zoom tool, a click zooming in / out at the point; **Alt+wheel** inverts Scroll Wheel Zooms, as in Photopea (W13-M, `wheel_gesture`: with it off Alt+wheel zooms about the pointer, with it on Alt+wheel pans); **Ctrl+wheel** pans sideways when it does not zoom (Photopea's Hand swaps the axes under Ctrl) and **Shift+wheel** pans sideways (`shell::w13m_tests::the_wheel_follows_photopeas_modifiers_on_the_shell_route`). **Shift-click** on any stroke tool (`tools::stroke::StrokeTool`) starts the stroke where the last released stroke of that tool ended (kept in document space) and extends it to the click, one undo step. Tests: `shell::w11f_tests::*` (through the shell's `on_modifiers` / `on_key` / `on_pointer` / `on_wheel` routes: Alt-click picks the red under it and paints nothing, a Shift-click line, a Ctrl-drag moves the layer and the Brush paints again after release, Ctrl+Space zooms in about the click and Alt+Space out, Alt+wheel zooms with wheel-pan configured), `shell::tests::the_wheel_gesture_follows_the_preference_and_the_modifiers`. The canvas cursor follows the lent tool (`CanvasExtras::tool_cursor` reads `Editor::effective_tool`). Gaps: the line anchor lives in the stroke tool instance, so it is lost when another tool (a Ctrl-Move, an Alt-sample) acted in between, and it is not reset on a document switch; Shift+Alt is not special-cased; shape tools leave Alt unused (no draw-from-centre) |
| Ruler (Straighten Layer), Color Sampler, History Brush | ✅ | W4-G |
| Paint symmetry; Eraser Mode; Colour Replacement Mode / Sampling / Limits / Anti-alias; Background Eraser Sampling / Limits / Protect Foreground Colour; Sharpen Protect Detail | 🔶 | W13-H. Options-bar controls declared in `tools::registry` and answered by `StrokeTool::set_setting` (`tools::stroke_options::StrokeTool::w13h_setting`). **Symmetry** (Brush, Pencil, Eraser; `tools::symmetry`): Off / Vertical / Horizontal / Dual Axis / Diagonal / Radial / Mandala with a Segments field (2-32, default 6); every dab is mirrored about the canvas centre (taken into the paint target's space at the press) in both the release and the live preview (`StrokeTool::stamped`). **Eraser Mode**: Brush (the brush as set), Pencil (hard, aliased: each pixel erased fully or not), Block (a pixel-snapped 16x16 square at full opacity and flow, whatever the brush). **Colour Replacement**: Mode Hue / Saturation / Colour / Luminosity (Hue, Saturation and Luminosity through the layer blend of that name; Colour keeps the earlier luminance-preserving recolour), Sampling Continuous / Once / Background Swatch, Limits Discontiguous / Contiguous / Find Edges, Anti-alias. **Background Eraser**: Sampling, Limits and Protect Foreground Colour (a pixel within the tolerance of the foreground is never erased). **Sharpen**: Protect Detail (on by default) lets each sharpened pixel leave its 3x3 neighbourhood's range by at most a quarter of that range, so the halo a plain unsharp mask leaves is cut to a fraction (a hard edge is still crisped). Contiguous floods 4-connected from the dab centres through matching pixels under the brush; Find Edges also stops the flood at a luminance step above 0.1 between neighbours. Defaults are Photopea's (Colour, Continuous, Contiguous, Anti-alias on, Protect Detail on), the Background Eraser's Sampling included since W13X-6: Continuous, so a stroke erases each colour it crosses (tests/integration `tool_routes::background_eraser_by_default_erases_every_colour_it_crosses`; Once, chosen in the options bar, keeps them: `background_eraser_on_once_clears_only_the_colour_first_touched`). Tests: `tools::stroke_options::tests` (the pixels each choice produces), `tools::symmetry::tests`, and the app-shell routes `w13h_the_symmetry_picked_in_the_drawn_options_bar_mirrors_a_real_brush_stroke` (drop-down drawn headless, clicked, forwarded, painted) and `w13h_every_new_option_reaches_its_tool_through_the_chrome_and_the_press`. **Not done:** the symmetry axis is fixed at the canvas centre (no draggable axis or on-canvas overlay); a sampled (image) tip is mirrored by turning its angle, not flipped; the Block is 16 document pixels, not 16 screen pixels; the Segments field is shown for every symmetry, not only Radial / Mandala; Protect Detail is an overshoot limit, not Photoshop's own algorithm; the Background Eraser has no colour decontamination |
| Pencil Auto Erase, Pattern Stamp, Colour Replacement, Background / Magic Eraser, Pattern Fill, Slice, Refine Boundary | ✅ | every palette tool is driven through the real pointer route by `tests/integration/tests/tool_routes.rs`; `tools::ToolId::ALL` has 69 entries, 68 of them on the palette plus Free Transform (entry 51); W11-G added Object Selection (entry 11, after Quick Selection) and the Note tool (W10-B) is the last entry |
| Content-Aware Move, Slice Select, Spiral shape, Edit ▸ Define Custom Shape, independent link groups | 🔶 | W10-A. **Content-Aware Move** (Healing slot, `tools::content_aware_move`): a drag outside the selection lassoes one; a drag from inside it lifts the selected pixels, fills the hole with the PatchMatch content-aware fill (`filters::content_aware_fill`, seeded) and lays the patch in at the drop with its edge feathered over Adaptation px; Mode Move / Extend (Extend keeps the source); the paint and the moved selection are one `Transaction`, one undo step. **Slice Select** (Crop slot, `tools::slice_select`): built over the document's committed slice set at every press; a body drag moves a slice (kept on the canvas), an edge or corner drag resizes it, Alt+click deletes it, and Delete / Backspace (Edit ▸ Clear's key) deletes the slice the last press picked instead of clearing pixels (`slices_export::delete_picked_slice`); the edited set replaces the committed one at pointer-up (what File ▸ Export ▸ Slices writes) as one History step (`Command::SetSlices`, W11-E; Undo and history jumps resync the slice store, `slices_export::resync_active_slices`). Between presses the shell rebuilds the tool from the document's store after every menu pick and pointer sample (`ToolPointer::sync_slice_select`, via `begin_pending_session`), so after Delete or Slice Options the overlay shows the store in the same frame; the overlay (`SessionGeometry::SliceSelect`) labels each slice by its name (`slice_select::slice_label`: `03` for `slice_03`, else the name — also View ▸ Show ▸ Slices) and the chrome draws the picked slice with the thick selected-handle outline. Each slice keeps its options (`tools::slice_select::SliceOptions`: name, URL, alt; `slices_export::SliceStore`) through moves, resizes and deletes — names are no longer renumbered — and Export Slices names each file by its slice (`<document>_NN` for the Slice tool's `slice_NN`; a given name is sanitised to a file stem — `ui::dialogs::slice_options::slice_file_stem`: `/ \ : * ? " < > |` and control characters become `_`, then every character other than ASCII letters, digits, `- _ ( )` and space becomes `_` and the stem is cut at 64 characters, so `hero?` exports as `hero_.png`). **File ▸ Export ▸ Slice Options…** (`ui::dialogs::slice_options`, hosted by the dialog host, applied by `slices_export::perform_slice_options`) edits the picked slice's name (non-empty, and refused when its sanitised file stem equals another slice's ignoring case — `hero?` beside `hero!`, `poster_03` beside the Slice tool's `slice_03` in `poster`, `Hero` beside `hero` — in both the dialog and `SliceStore::set_options`), URL and Alt Tag; should a set still hold two names with one stem (a document retitled after its slices were named), Export Slices writes the later one as `<stem>_2`, `<stem>_3`, … (`slices_export::write_named_slices`), so no slice overwrites another; without a picked slice the row says to pick one with Slice Select. When any slice has a URL or alt text, Export Slices also writes `<document>.html` (`slices_export::write_slice_page`): each image placed where its slice sits, with its alt text, wrapped in a link to its URL, all escaped. **Spiral** (shape slot, `vector::shapes::spiral`): a filled Archimedean band fitted to the drag box's ellipse, options Turns / Inner Radius / Direction. **Edit ▸ Define Custom Shape** (`menu_bridge::custom_shape`): the active shape layer's outline (through its transform), else the Paths panel's current path, normalised to the unit box, stored as knots in the shape presets beside `.csh` imports (written to the presets file at once), and listed in the Custom Shape tool's Shape list. **Link Layers** (`menu_bridge::link_groups`, `layer_model::Layer::link_group`, append-only serde): each click makes a new group (`LayerTree::next_link_group`); a selection that is exactly one group unlinks it; the Layers panel's link button goes through the same ids (`link_groups::panel_link`, the bridge's arm for its `linked` patch: a link gets a fresh group per click, an unlink clears the group); the `linked` flag decides *whether* a layer is linked and the group only *which* (`Layer::link_key`), so a layer the panel unlinked never moves with its old group; the pointer route hands the tools only the groups of the selection (`LayerTree::link_partners`), so moving one member moves its own group; an older document's `linked` flag reads as one legacy group (`Layer::link_key`). Tests: `tests/integration/tests/tool_routes.rs` `content_aware_move_*`, `slice_select_*` (incl. `slice_select_delete_key_removes_the_picked_slice_and_names_survive_to_the_export`: the overlay and real chrome frames before and after Delete, and Slice Options typed into the real dialog through to the exported HTML), `slices_export::tests::the_slice_options_row_opens_its_dialog_over_the_picked_slice` (the chrome's menu-click route), `slices_export::tests::slice_options_refuses_a_name_that_exports_to_another_slices_file`, `slices_export::tests::slices_whose_names_sanitise_to_one_file_are_all_written`, `spiral_*`, `define_custom_shape_*`, `link_layers_*`, `the_layers_panel_link_button_makes_a_group_per_click_and_unlinks_what_it_shows` (real chrome frames clicking the panel's rows and button); unit tests in each module. Gaps: Slice Options opens only from its File ▸ Export ▸ Slice Options… row (no double-click on a slice, no options-bar button); Content-Aware Move has no Structure/Color sliders and its fill runs on the UI thread; a custom shape renamed or deleted in Edit ▸ Preset Manager (W10-G) is changed in the saved presets, but the Custom Shape tool's in-session Shape list (`tools::registry::register_custom_shapes`, append-only) keeps the old entry until the next start |
| Perspective Crop, Vertical Type, Horizontal / Vertical Type Mask, Mixer Brush, Artboard, Curvature Pen, Freeform Pen | 🔶 | W7-F: each is a registry tool in its Photopea slot (Crop; Type; Brush; Move; Pen) with a drawn icon and an options schema, driven through the real pointer route by `tool_routes.rs`, one undo step each except Vertical Type, which is two history entries like the Type tool (the click's empty layer, then the confirmed run) (`perspective_crop_rectifies_a_dragged_and_adjusted_quad_as_one_step`, `vertical_type_lays_the_typed_run_down_a_column`, `the_type_masks_turn_the_typed_glyphs_into_one_undoable_selection`, `mixer_brush_carries_the_red_it_picked_up_into_the_blue`, `artboard_drag_makes_one_artboard_group_with_a_white_plate`, `curvature_pen_clicks_then_enter_make_one_smooth_shape_layer`, `freeform_pen_drag_is_fitted_into_one_short_curved_shape_layer`). **Perspective Crop** drags a quad, drags its corners by the four handle squares drawn on it (with a 3 x 3 perspective grid, W8-C: `a_perspective_crop_quad_is_painted_with_corner_handles_and_a_grid`), and Enter resamples **every pixel layer** (hidden ones too, as Photoshop crops all layers) through a homography into the upright rect (size from the quad's edges or the W / H options) and crops the canvas to it, one transaction (`perspective_crop_rectifies_every_layer_not_only_the_active_one`); text, shape and smart-object layers and layer masks are cropped, not warped. **Vertical Type** stores `Paragraph::vertical` (append-only serde) and `text_engine` lays it out by re-positioning the horizontally shaped glyphs upright, one em cell per cluster, one column per paragraph, columns right to left (cosmic-text has no vertical mode): no rotated Latin runs, no vertical punctuation forms, no decorations, a box does not wrap it; W8-C: the caret is a bar across the column that walks down it, a click hit-tests down the column, selections are one rect per column, and the Vertical Type tool enters an existing run on a click (`vertical_type_caret_and_click_follow_the_column`); `compositor::text::hash_layer` hashes the flag, so the run cache and tile key never serve a vertical run the horizontal pixels of the same string and style. **Type Masks** type into a temporary layer — W8-C: with the quick-mask red over everything outside the glyphs while typing (`a_type_mask_session_shows_the_quick_mask_red_outside_its_glyphs`) — and their confirm turns its glyph coverage into the selection, combined by the options bar's Mode (New / Add / Subtract / Intersect, `the_type_mask_mode_adds_to_and_subtracts_from_the_selection`), and takes the layer away (one history entry). **Mixer Brush** carries a reservoir (Wet, Load, Mix, Flow, Load / Clean after each stroke) simulated dab by dab; W8-C: every Move previews the stroke so far through the W4-B preview lens and the release commits exactly the previewed pixels (`mixer_brush_previews_the_wet_stroke_while_it_is_dragged`). **Artboard** drags a group whose raster plate (`RasterLayer::artboard`, append-only) names the rect and background and is filled with it; W8-C: the compositor clips an artboard's contents to its rect (in the group's own space, so the clip moves with the artboard), and File ▸ Export ▸ Artboards to Files writes one image per artboard, each composited alone over its rect with the last Export As settings (`artboards_clip_their_children_and_export_one_file_each`). W10-J: the Properties panel shows the active artboard's position, size and background and lists every artboard, a row selecting its group (`the_properties_panel_draws_the_artboard_page_and_a_row_selects_its_board`); File ▸ New's Artboard box makes the new canvas one artboard ("Artboard 1", the chosen background as its plate, an empty "Layer 1" inside, nothing to undo) (`a_new_document_with_the_artboard_box_is_one_canvas_sized_artboard`, `clicking_the_artboard_box_makes_the_confirmed_spec_an_artboard`). **Curvature Pen** clicks points a Catmull-Rom curve passes through; **Freeform Pen** fits a freehand drag (Ramer-Douglas-Peucker at Curve Fit) into smooth anchors; both publish through the Pen's mode, paint and combine. |
| Live stroke preview, brush-size ring, per-tool cursors, right-click canvas menu | ✅ | W4-B/C; the right button opens the menu and never reaches a tool (`a_right_click_on_the_canvas_opens_its_menu_and_a_row_performs`) |
| Layer-row right-click menu; Layer ▸ Hide / Show Layers; Layer ▸ Matting | 🔶 | W16-D (supersedes W10-I's list, which was not Photopea's: it carried Enable Layer Mask, Link Layers and Merge Visible, which Photopea's row menu does not, and lacked Duplicate Into, Rasterize Layer Style and Convert to Shape). The row menu (`ui::context_menu::layer_items`; a right-click selects the row first) has Photopea's rows in its order with its separators (`MenuItem::separator_after`): Blending Options…, Select Pixels / Duplicate Layer (a copy at once: `LayersRequest::DuplicateLayer` → `Action::DuplicateLayer`, no dialog), Duplicate Into… (the Duplicate Layer dialog with its Destination document), Delete / Convert to Smart Object, on a smart object New Smart Object via Copy, Open (Edit Contents), Reset Transform, Replace Contents, Export Contents, Convert to Layers, then Rasterize, Rasterize Layer Style, Convert to Shape / on a text layer Convert to Point Text and Convert to Paragraph Text / Create or Release Clipping Mask (whichever applies), Copy / Paste / Clear Layer Style, Merge Down (Merge Layers over a multi-selection), Flatten Image / the colour labels. Tests: `layers_panel_w16::tests::the_row_menu_is_photopeas_and_duplicate_layer_follows_the_copy_option` (real right-click, the labels and separators, Duplicate Layer and Duplicate Into… clicked through `Chrome`), `wired_controls::the_layer_row_context_menu_offers_the_layer_operations`. Gaps: Photopea's Smart Object, Layer Style and Color submenus are flat rows; its Stack Mode and Turn into JPG rows are not in the row menu; the text row shows both conversions (Photopea shows the one that applies — the menu context does not say which kind of text the layer is). Earlier (W10-I): each resolves through the menu bar's own gate, so a greyed row carries its reason. Layer ▸ Hide Layers / Show Layers set every selected layer's eye (the active one when none is selected) as one undo step, greyed with the reason when the one layer already is so; Ctrl+, stays the application's show / hide toggle of the active layer, so the row paints no chord. Layer ▸ Matting ▸ Remove Black / Remove White Matte un-mix each partly transparent pixel from its matte in the encoded values (`t = (s − m·(1 − a)) / a`), inside the selection when there is one, one undo step; a layer with no partly transparent pixel refuses with the reason. Tests: `wired_controls::the_layer_row_context_menu_offers_the_layer_operations` (a real right-click on a layer row, the fifteen labels, rows 1, 3 and 14 clicked), `menu_bridge::layer_extras::tests::{hide_layers_hides_every_selected_layer_in_one_step_and_ctrl_comma_stays_the_toggle, remove_black_and_white_matte_take_the_matte_back_out}` (clicked through `Chrome::menu_click`). Gaps: (W11-E closed the colour-label gap, see the row below) the Matting submenu does not list Defringe (Layer ▸ Remove Color Fringe… is the mask-edge cleanup and stays its own row) |
| Merge Layers (Ctrl+E), Transform Again / Again with Copy, Arrange ▸ Reverse, Select Linked Layers, Smart Object Convert to Linked / Embed Linked, layer colour labels, New Layer Based Slice | 🔶 | W11-E. **Merge Layers**: Ctrl+E (Merge Down's chord and row) over two or more selected layers composites the selected layers — a selected group with every layer inside it — into one layer in the topmost one's slot, named after it, and selects it — one undo step; when the selection sits inside an unselected group the result stays in that group, which is composited neutral for the merge (`layer_ops::w11e::neutralize_containing_groups`) so its opacity / blend / mask / effects are neither lost nor applied twice (Merge Down gets the same); the row reads "Merge Layers" on the menu bar and in the layer-row menu (`MenuAction::label_in`, `menu_bridge::MergeScope::Selected`). **Edit ▸ Transform ▸ Again** (Shift+Ctrl+T) / **Again with Copy** (Shift+Alt+Ctrl+T): the whole-layer Free Transform commit records its document-space affine (`tools::transform::take_committed_affine`, kept by `Editor::last_transform`); Again conjugates it onto the active layer (one `TransformLayer` transaction), Again with Copy runs Duplicate Layer's commands plus that transform as one transaction; greyed with `NO_TRANSFORM_TO_REPEAT` until a transform is committed. **Layer ▸ Arrange ▸ Reverse**: the selected siblings swap end for end, unselected layers keep their slots, one undo step. **Layer ▸ Select Linked Layers**: adds every layer of the selection's link groups (`LayerTree::link_partners`); greyed when there is none to add; a layer-selection change, like Select ▸ All Layers, is not a history step. **Layer ▸ Smart Object ▸ Convert to Linked…** writes the embedded bytes to the picked file and **Embed Linked** reads the linked file in: each is one transaction (`ReplaceAssetSource` + `SetLayerKind` for every object sharing the source), pixels unchanged; greyed over the other kind with the reason. **Colour labels** (`layer_model::ColorLabel`, kept in `DocumentExtras::layer_colors`, append-only serde): No Color / Red / Orange / Yellow / Green / Blue / Violet / Gray as flat rows at the end of the layer-row menu and under Layer ▸ Color Label, one `SetDocumentExtras` undo step for every selected layer; the row paints a chip in its left margin; Duplicate Layer carries the label. **Layer ▸ New Layer Based Slice**: a slice over the active layer's ink (clipped to the canvas), named after the layer (numbered on a clash), picked, Slice Select raised. Tests: `layer_ops::w11e::tests::*` (eleven, each through the menu bar's `Chrome::menu_click` or a real `Chrome` frame: the right-click on the layer row, the click on Red, the red chip in the next frame's shapes; Transform Again after a real Free Transform drag + commit through `ToolPointer`), `color_label::tests::*` (layer-model), `wired_controls::the_layer_row_context_menu_offers_the_layer_operations`. W13-B: colour labels cross a PSD both ways — `lclr` 0..=7 opens as the label and a labelled layer's record carries its index (`import::tests::every_colour_label_round_trips_through_a_psd_as_its_lclr_index`). Gaps: Transform Again repeats only the whole-layer affine (Scale / Rotate / Skew), not a Distort / Perspective / Warp or a floated selection;  the record of the last transform is per session, not saved |
| Layer Style ▸ Scale Effects / Create Layers, New ▸ Artboard from Layers, Layer Mask ▸ From Transparency, Smart Object ▸ Reset Transform / Stack Mode, Animation ▸ Make Frames / Unmake Frames / Merge | 🔶 | W13-G (`ui::menu::LayerExtraOp`, gated by `ui::menu::LayerExtraFacts`, performed by `menu_bridge::layer_ops_w13`; each one `Transaction`, one undo step; a row is greyed with the reason its click would give for no style, a switched-off style (Create Layers), any layer with Lock All (Merge), an empty layer (From Transparency), a source with no recorded size (Reset Transform) and an embedded PSD with fewer than two visible top-level layers (Stack Mode; W13X-3: `ui::menu::psd_header_layer_count` walks the layer records, so `lsct` / `lsdk` group dividers, a group's children and hidden layers are not counted and a source holding one group is greyed, and it follows a 16- / 32-bit file's `Lr16` / `Lr32` block, where the classic layer-info block is empty)). **Create Layers** splits the active layer's style the way the compositor draws it: each drop shadow and the outer glow becomes a raster layer directly under the layer (the pass's blend mode, the layer's opacity); each interior effect (pattern / gradient / colour overlays, satin, inner glow, inner shadows, bevel highlights and bevel shadows as two layers) becomes a raster layer clipped to the layer, above its existing clipped layers, in the effect's mode; each stroke a layer above the clipping group. The inks come from the compositor, not re-derived: exterior passes and strokes rendered alone at zero fill, an interior effect rendered in Normal atop an opaque black and an opaque white copy of the layer's shape and solved for its colour and coverage. The layer keeps its pixels, opacity, fill, blend mode and Blend If. **Scale Effects…** (W13X-3: one row opening `ui::dialogs::scale_effects::ScaleEffectsDialog`, the dialog host's `ActiveDialog::ScaleEffects`: a percent field 1–1000% and a Preview switch showing the document composited at that percent, rendered by `layer_ops_w13::scale_effects_preview`; OK is blocked at 100%; the confirmed percent is the `LayerExtra(ScaleEffects(n))` pick): shadow / satin distance and size, glow size, bevel size and soften, stroke width, pattern scale and offset, gradient-overlay offset. **Artboard from Layers**: the selected top-level layers move, in order, into a new transparent artboard ("Artboard N") at their combined ink bounds, seated where the topmost was (the picture is unchanged). **From Transparency**: the layer's alpha becomes a new pixel mask and every covered pixel opaque in its own colour; the mask becomes the paint target. **Reset Transform**: the object's source at 100%, upright, about its current centre (rounded to whole pixels). **Stack Mode ▸** Entropy / Kurtosis / Maximum / Mean / Median / Minimum / Range / Skewness / Standard Deviation / Summation / Variance, per colour channel across the visible top-level layers of the object's layered PSD source (alpha: the union of their coverage), baked into a new layer "<name> (<Mode>)" posed as the object, above it, with the object kept hidden. **Make Frames / Unmake Frames** rename the selected top-level layers to and from `_a_<name>,100`; **Merge** replaces every top-level layer with one flattened raster frame per `_a_` frame (same name, visibility and play order), so an animation export is unchanged. Tests (`menu_bridge::layer_ops_w13::tests::*`, each through the menu bar's `Chrome::menu_click` route): `create_layers_splits_the_style_and_recomposites_the_styled_original` (every effect kind at once, within 2/255, one undo step back to the style), `create_layers_holds_under_partial_opacity_and_fill`, `scale_effects_*` (including `scale_effects_asks_for_any_percent_previews_it_and_lands_37_percent_as_one_step`: menu row, dialog, 37%, Preview, Escape and Enter through the host's own `ui`), `the_scale_effects_row_asks_for_its_percent`, `ui::dialogs::scale_effects::tests::*` (a headless frame draws the field, the switch and the preview texture), `artboard_from_layers_*`, `from_transparency_*`, `reset_transform_*`, `stack_mode_*` (median and maximum of a three-layer PSD; a 16- and a 32-bit layered source live and a one-layer one greyed; `stack_mode_counts_only_visible_top_level_layers_so_a_single_group_is_greyed`: a lone group and one-visible-among-hidden greyed for every mode, a group beside a layer live), `the_stack_statistics_are_the_textbook_ones`, `make_unmake_and_merge_frames_through_the_menu`, `each_row_sits_in_photopeas_submenu`, `merge_is_greyed_while_any_layer_is_locked`, `create_layers_is_greyed_while_the_style_is_switched_off`, `from_transparency_is_greyed_on_an_empty_layer`, `smart_object_rows_are_greyed_when_the_source_cannot_serve_them`. Gaps: the Scale Effects preview is an image inside the dialog, not a live canvas preview; Stack Mode is baked, not a live property of the object, and needs a layered PSD source (it stays enabled, then refuses, for a linked source, whose file is not read to grey the row, an embedded one whose layer records cannot be walked, or one that fails to decode); Create Layers refuses a group or a clipped layer, and is inexact for a stroke in a mode other than Normal, a non-Normal interior effect under a fill below 100%, an overlapping stroke under an opacity below 100%, and effects that read the shape beyond the canvas; Create Layers, From Transparency, Stack Mode and Merge work on 8-bit documents only; Artboard from Layers takes top-level layers only and asks no name |
| Tablet pressure | 🔶 | winit `Touch` events (pen force included) drive the mouse's pointer route with the force as the stroke's pressure (`pen_input.rs` via `Shell::set_pen_pressure`); a finger with no force paints at full pressure; the OS's emulated mouse for the same contact is dropped; losing window focus mid-contact drops the contact and resets the pressure to full (winit on Windows never reports a cancelled contact). Size from Pressure, Flow from Pressure and Opacity from Pressure (each dab's alpha times the pressure; W8-A) toggles exist. winit maps a pen's zero pressure to no force, so a contact id that has reported a force or hovered is a pen and its forceless samples are zero pressure (a pen touching down at zero lays a zero-weight first dab), while an id never seen either way (a finger) paints at full pressure (W8-A). Keyed on the contact id, not a device type: Windows reuses contact ids from one pool shared by pens and fingers, so a finger given an id a pen used earlier paints at zero pressure until the id ages out of the 16 remembered pen ids; contacts still down when focus is lost have their moves dropped, rather than read as a hovering pen, until their lift is reported (`Ended`/`Cancelled`) or they touch down again (`Started`), so a pen whose lift was lost does not move the pointer or the brush ring while hovering until its next touch-down. Otherwise a pen hovering in range (winit `Touch` `Moved` with nothing down) moves the shell's pointer as a hover and the brush ring follows it; the ring stays where the pen left range until the mouse moves (W8-A). No tilt/rotation/eraser end; Brushes-panel presets do not carry Opacity from Pressure. Verified with synthetic events only, not yet with a physical pen |

### Adjustments
| Capability | Status | Notes |
| --- | --- | --- |
| Brightness/Contrast, Levels, Curves, Exposure | ✅ | Curves is a Fritsch-Carlson monotone spline, edited as a draggable curve over the histogram with a channel choice (W5, `ui::dialogs::adjustment_dialog::curve_widget`); Levels shows its histogram; Ctrl+L / Ctrl+M open them |
| Vibrance, Hue/Saturation, Colour Balance | ✅ | |
| B&W, Photo Filter, Channel Mixer, Invert | ✅ | Invert opens no dialog: Image ▸ Adjustments ▸ Invert or Ctrl+I applies it at once (W5-E, `AdjustmentId::has_dialog`, ui/src/menu.rs; test `ctrl_i_inverts_the_layer_at_once_without_a_dialog`) |
| Posterize, Threshold, Gradient Map, Selective Colour | ✅ | |
| Auto tone / contrast / colour | ✅ | |
| Desaturate, Equalize | ✅ | W4-E: Image ▸ Adjustments, no dialog, one undo step; Desaturate is Shift+Ctrl+U and keeps linear luminance; Equalize reads the histogram of the selected pixels. Destructive-only, as in Photopea |
| Shadows/Highlights, Replace Color | ✅ | W4-E: dialogs with live preview; Shadows/Highlights reads a blurred luminance over its radius; Replace Color has a selection preview and samples from a click. Destructive-only, as in Photopea |
| HDR Toning | ✅ | W7-G: Image ▸ Adjustments ▸ HDR Toning, dialog with live preview, one undo step, destructive-only as in Photopea. Local adaptation on the active layer: log2 luminance split by two self-guided filters into base, local contrast (Edge Glow radius, gain `1 + strength`) and fine detail (gain `1 + detail`); gamma compresses the base about middle grey, exposure shifts it in stops; vibrance/saturation after (`adjustments::hdr`). Not Photoshop's: no 32-bit merge, no shadow/highlight sliders or toning curve, and no Method choice (Local Adaptation only). Tests `zero_strength_and_detail_is_the_identity`, `strength_increases_local_contrast`, `hdr_toning_confirmed_from_its_dialog_raises_local_contrast_as_one_step` |
| Match Color | ✅ | W7-G: Image ▸ Adjustments ▸ Match Color, dialog with live preview and a Source list of every open document's merged image and every other pixel layer; Luminance, Color Intensity, Fade, Neutralize; one undo step, destructive-only. Reinhard mean/deviation transfer per CIELAB channel (`adjustments::match_color`); the target is measured at full size from the selected pixels when applied, the source from a copy downsampled to 1024 px. Tests `matching_moves_the_target_means_to_the_source`, `full_fade_is_the_identity`, `match_color_confirmed_from_its_dialog_moves_the_layer_means_to_the_source_layer` |
| Color Lookup (3D LUT) | ✅ | W4-E: a `.cube` file (file picker) or one of five built-in looks (Invert, Warm, Cool, Sepia, High Contrast). Image ▸ Adjustments bakes it as one undo step; Layer ▸ New Adjustment Layer ▸ Color Lookup creates the layer at the identity table, and Layer ▸ Edit Adjustment… or the Properties panel's "Open editor…" reopens the same dialog on that layer, whose OK rewrites the layer's table as one undo step |

### File
| Capability | Status | Notes |
| --- | --- | --- |
| Save / open the native `.rstudio` project | ✅ | integrity-sealed, version-gated, crash-safe swap; tiles deflate-compressed and reused from the package being replaced; saved on a worker thread with the journal held; File ▸ Open accepts a package through its `manifest.json` (W5) |
| Pixel data persisted | ✅ | reopening composites to byte-identical output |
| Animated GIF / APNG / WebP open and export as `_a_` frame layers | ✅ | W9-J. `raster::animation::decode_animation_*` returns every frame composited to a full canvas by `image`'s animation decoders (GIF disposal and offsets, APNG / WebP blending), bounded by `ImportLimits`, `MAX_ANIMATION_FRAMES` (1000) and `MAX_ANIMATION_BYTES` (1 GiB); `app_shell::import::document_from_animation` makes one raster layer per frame named `_a_Frame <n>,<delay ms>` (frame 1 at the bottom, only it visible). Both open routes use it: File ▸ Open's import job (`jobs::ImportOutcome::Animated`) and the synchronous `Editor::open_path` (recent files, drag-and-drop, command line). A file whose animation cannot be read opens as its first frame, as before. Export As rows carry `ExportEntry::animated` (on by default; the host passes the count of top-level `_a_` layers to `ExportAsDialog::set_animation_frames`, and the Animated checkbox, captioned with that count, shows on GIF / PNG / WebP rows only when it is above zero): `Editor::request_export` sends such rows of a document with top-level `_a_` layers to a worker that composites one frame per `_a_` layer (that frame shown, the other frames hidden, every other layer as it is) and writes a looping GIF, APNG (`acTL`/`fcTL`/`fdAT`) or animated WebP (`VP8X`/`ANIM`/`ANMF`, lossless frames) at the row's scale. Tests: `raster::animation::tests::*` (a hand-written 3-frame GIF with offsets and restore-to-background disposal), `editor::animation_tests::*` (File ▸ Open of a 3-frame GIF gives 3 `_a_` layers with the right pixels and delays; GIF / APNG / WebP export re-reads as 3 frames; a hidden non-frame layer shows in no exported frame; a still PNG is unaffected), `editor::animation_tests::the_export_as_dialog_offers_animated_only_for_a_document_with_frame_layers` (File ▸ Export As ▸ GIF through `DialogHost` and a drawn egui frame: no checkbox over a still PNG, checkbox and "(3)" over a 3-frame GIF), `ui::dialogs::export_as::tests::the_animated_option_is_drawn_for_formats_that_animate_and_reaches_the_job`. W10-I, the frame timeline (Window ▸ Animation, `ui::panels::animation`, `PanelId::Animation` appended last): the frames are the top-level `_a_` layers bottom first, one thumbnail cell each with a delay field under it (an edit renames the layer's `,<ms>`, clamped to 10..60000 ms; a drag of the field is held as view state and lands once, on the release, as one undo step; a typed value is held by the text edit and lands once on Enter / focus loss, not per keystroke); a click on a frame selects its layer and shows it alone among the frames (one command); Play / Stop steps the preview well through the frames at their own delays, looping; the onion-skin switch paints the previous frame at reduced alpha under the current one; Add frame makes an empty `_a_Frame <n>` on top with the last frame's delay, Duplicate frame copies the current frame's layer (same tile hashes) right after it in play order, Delete frame deletes it — each an ordinary undoable command; playback and onion skin are egui view state, never history. Tests: `panels::animation::tests::*` (the whole `Workspace::ui` with only the Animation panel open: cell and delay-field rects, click-to-show, add / duplicate / delete applied to the document, a delay-field drag applied frame by frame through a `History` renaming the layer as ONE undo step, a typed `250` + Enter landing as one rename and ONE undo step with no intermediate rename, the panel's words drawn from the `ui.animation.*` catalogue rows, playback timing 50 / 150 / 320 / 650 ms → frames 1, 2, 3, 1, Stop holding, onion skin painting two textures, the previous fainter). Gaps: File ▸ Export… (single file) writes a still; the timeline has no tweening, per-frame layer visibility beyond the `_a_` layers themselves, or frame reordering by drag (reorder the layers in the Layers panel); frames are 8-bit and written without an ICC profile. |
| Export PNG / JPEG / WebP / TIFF / GIF / BMP / TGA / ICO / SVG with presets | ✅ | W9-N: TGA (32-bit with alpha, the `image` crate encoder) is in Export As and the export picker; correct un-premultiply and linear→sRGB on the way out; ICO carries 16/32/48/256 px entries; SVG from File ▸ Export… and from an Export As row at 100% is the W10-F vector SVG (row below); an Export As SVG row at another scale wraps the scaled raster composite as an embedded PNG. W14: **File ▸ Export…** is a File-menu row above Export As (`ui::menu::MenuAction::ExportByName`, routed by `app_shell::menu_bridge` to `Action::Export`, whose keymap chord is Ctrl+Alt+Shift+S; greyed with no document), so Help ▸ Search Commands lists it too: one flat file in the format the typed extension names (`doc::export_format_for`: PNG, JPEG, lossless WebP, TIFF, GIF, BMP, TGA, PPM / PGM / PBM, uncompressed DDS, AVIF, EXR, JPEG XL; `.svg` through `doc::write_vector_svg`; `.psd` / `.psb` through the layered writer); ICO, BC3 DDS, lossy WebP and MP4 are Export As rows only. The row paints no chord (painting one needs `keymap::menu_twin` to name it). Test: `menu_bridge::tests::file_export_is_a_menu_row_command_search_finds_and_writes_by_extension` (the File-menu row, the Search Commands entry, the click route writing a JPEG from a `.jpg` name) |
| Open PPM / PGM / PBM, DDS, JPEG XL | ✅ | W10-F (`raster::codec::formats`, recognised by content first, then by extension; offered by File ▸ Open's filters). Netpbm `P1`-`P6`, ASCII and binary, `maxval` 1-65 535 (above 255 opens 16-bit; PBM `1` is black) (`formats::pnm`); DDS: the top mip of the first surface, uncompressed RGB(A) / luminance / alpha by channel masks at 8-32 bpp, BC1 / BC2 / BC3 (`DXT1`-`DXT5`, `DXT2`/`DXT4` un-premultiplied), and the `DX10` header's BC1-BC3 and 8-bit RGBA/BGRA; BC4-BC7 and float formats are refused by name (`formats::dds`); JPEG XL through `jxl-oxide` 0.12 (pure Rust, MIT OR Apache-2.0; first keyframe, orientation applied, 16-bit above 8 bits per sample, a colour space it cannot convert without an ICC engine keeps its profile; CMYK refused by name; decode fixture written by libjxl, `a_lossless_rgba_fixture_decodes_exactly`). Every reader checks the declared size against `ImportLimits` before allocating, and truncation / bit-flip sweeps error without panicking. App route: `ppm_pgm_pbm_dds_xcf_and_jxl_open_as_documents` (`Editor::open_path`) |
| Open PSB (large document format) | ✅ | W10-F: `psd::read` reads version 2 as well as 1 (`PsdHeader::read_any`; `Cursor::with_large` / `Cursor::length`): 64-bit layer-and-mask, layer-info and channel lengths, 64-bit lengths on the long-key tagged blocks (`LMsk Lr16 Lr32 Layr Mt16 Mt32 Mtrn Alph FMsk lnk2 FEid FXid PxSD`), 32-bit RLE row counts, and canvases to `ReadOptions::max_psb_dimension` (300 000) under the same decode budget. It opens through the PSD road (both start `8BPS`), so layers, groups, masks and effects arrive as they do from a `.psd` (`a_psb_reads_to_the_same_document_as_its_psd_twin`, `a_psb_opens_as_a_layered_document_and_an_avif_is_refused_by_name`). W11-H: written too: `psd::write_psb` / `write_psb_with` write version 2 (64-bit layer-and-mask, layer-info and channel lengths, 64-bit lengths on the long-key blocks, 32-bit RLE row counts, canvases to 300 000 px), and `psd::write` writes a canvas past 30 000 px as a `.psb` because nothing else can hold it. File ▸ Export… / Save as PSD to a `.psb` (offered in both pickers) routes through `doc::write_atomically`, which rewrites the layered bytes as version 2; a `.psd` name given `.psb` bytes is refused with a message naming `.psb`. Tests: `psd` `psb_write_tests::*` (a `.psb` reads back to the same layers, blocks and composite as its `.psd` twin at 8/16/32 bits; a byte walk of the widened fields; a 30 001 px canvas), `app-shell` `doc::formats_w11h_tests::file_export_to_psb_writes_a_large_document_file`, `psb_bytes_are_refused_under_a_psd_name_and_written_under_a_psb_one`. The app's layered-document builder (`import::psd_from_document`) takes a canvas up to `psd::write::MAX_PSB_DIMENSION` (300 000 px), so a document past 30 000 px is saved from the app: File ▸ Save as PSD on it leads its picker with "Photoshop large document" and suggests a `.psb` name (`dialogs::arm_psd_save_for`), writes version 2, and the `.psb` reopens at full size with its pixels; the same canvas under a `.psd` name is refused, naming `.psb` (`save_as_psd_past_30000_px_offers_and_writes_a_psb`) |
| Open EXR / HDR, ICNS, IFF, KRA | 🔶 | W11-H (`raster::codec::formats::{float, icns, iff, kra}`, recognised by content first, then by extension; offered by File ▸ Open's filters). OpenEXR (`image`'s `exr` feature over the pure-Rust `exr` 1.74, header read and every part's data and display window checked against `ImportLimits` before a block is decoded) and Radiance RGBE `.hdr` (`image`'s `hdr`): linear float, EXR alpha un-premultiplied, opened as a **32 Bits/Channel** document: `app-shell` `doc::float_open::promote_float_source` (called from `OpenDocument::open_image_decoded`, which both File ▸ Open's import job and `open_path` end in) replaces the layer's pixels with `formats::float::decode_linear`'s unclipped samples as W10-H `f32` tiles, sRGB-encoded with the curve extended past 1.0, marks the document 32-bit, and leaves nothing to undo; a 4.0 highlight is kept (`a_float_file_opens_at_32_bits_and_its_highlights_reach_exr`). The codec facade's 16-bit `DecodedSurface` of a float file (used by Place and the 8-bit decode) still clips above 1.0. ICNS: the largest PNG (`ic07`-`ic14`, `icp4`-`icp6`, any PNG entry), ARGB (`ic04`/`ic05`) or 24-bit RLE (`is32`/`il32`/`ih32`/`it32` with their 8-bit masks) entry; JPEG 2000 and palette entries are not read (a JPEG 2000-only icon is refused by name). IFF: `FORM ILBM` and `FORM PBM `, 1-8 planes through `CMAP` with EHB and HAM6/HAM8, 24 and 32 planes, masking 1 and 2, uncompressed or ByteRun1; other FORM types and compressions are refused by name. KRA: the ZIP's `mergedimage.png` (this crate's own bounded ZIP reader: stored / deflate, inflate capped at the allocation ceiling, ZIP64 and encryption refused), so a `.kra` opens flattened. W16-L: `raster::codec::formats::kra::layers::read` now reads the layer tree from `maindoc.xml` (names, opacity, visibility, Krita composite-op ids, groups) and each 8- or 16-bit RGBA paint layer's tiled, LZF-compressed, byte-planar pixels (other colour models, masks and non-paint layers are named in `KraDocument::notes`), and a `.kra` with no `mergedimage.png` opens as those layers composited source-over (every blend as Normal); but **File ▸ Open still makes one flat layer**: the `app-shell` route that opens `.xcf` layered (`doc.rs` / `import_xcf.rs`) is not wired to the Krita reader, so Photopea's layered KRA open is still a gap (`formats::kra::layers::tests::*`). Each reader has truncation and bit-flip sweeps that error without panicking. Tests: `raster` `formats::{float, icns, iff, kra, w11h_tests}::*`, app route `doc::formats_w11h_tests::exr_hdr_icns_iff_and_kra_open_as_documents` (`Editor::open_path`) |
| Open JPEG 2000, VTF, FITS, DICOM, DXF, CLIP, PXD, CDR, INDD; refuse AFPHOTO / SAI | 🔶 | W16-L (`raster::codec::formats::more_formats_w16`, `ImportFormat::{Jp2, Clip, Pxd, Vtf, Fits, Dicom, Dxf, Cdr, AfPhoto, Sai, Indd}` appended; recognised by content where the format has a signature, else by extension; offered by File ▸ Open's Images filter except the two refused). **JPEG 2000** (`.jp2` / `.j2k` / `.jpf`): `hayro-jpeg2000` 0.3.1 (pure Rust, `forbid(unsafe_code)`, SIMD off), 8 bits per channel, grey / RGB / CMYK-converted, alpha kept; it runs in the editor process, not the W15-A decode worker (whose protocol is keyed to AVIF / HEIC in files this change does not own); truncation and bit-flip sweeps pass without a panic. **VTF**: the largest mip of frame 0, versions 7.0-7.5, every Valve format except P8 (refused by name). **FITS**: the first 2D image HDU (primary or `XTENSION= 'IMAGE'`), BITPIX 8/16/32/64/-32/-64 with BSCALE / BZERO / BLANK, `NAXIS3 = 3` as RGB, stretched linearly min..max to 16 bits (not an asinh / percentile stretch), rows flipped to FITS's bottom-up order. **DICOM**: explicit / implicit VR little endian and RLE Lossless, first frame; MONOCHROME1/2 8-16 bit through rescale and the file's window (else min..max) to 16 bits, 8-bit RGB / YBR_FULL; JPEG-family, deflate and big-endian transfer syntaxes and PALETTE COLOR are refused by name. **DXF** (ASCII): LINE, LWPOLYLINE / POLYLINE (bulges as arcs), CIRCLE, ARC, ELLIPSE, SPLINE (de Boor), POINT, TEXT / MTEXT, colours from ACI / true colour / the layer table, drawn by `resvg` onto a white page fitted to 1024 px: a **flat picture, not vector layers** (Photopea opens DXF entities as vector layers; `dxf::to_svg` is the SVG, but no `app-shell` route turns it into layers yet); INSERT, HATCH and DIMENSION are not drawn; binary DXF is refused by name. **Previews only**: Clip Studio `.clip` (the `CanvasPreview.ImageData` PNG from its SQLite chunk, read by a bounded SQLite reader checked against a real SQLite-written database), a zipped Pixelmator Pro `.pxd` package (`QuickLook/Thumbnail.tiff`), CorelDRAW `.cdr` (X4+ ZIP thumbnail or a RIFF file's `DISP` bitmap) and InDesign `.indd` (the XMP JPEG thumbnail); their layers / drawings are **not** read (Photopea opens CLIP and PXD layered). **Refused by name**: Affinity `.afphoto` / `.afdesign` and PaintTool `.sai` / `.sai2`, with the reason and "export as PSD". None of the preview formats was checked against a file written by its own application. Tests: `raster` `formats::more_formats_w16::{jp2, vtf, fits, dicom, dxf, clip, pxd, foreign, sqlite}::tests::*`, app route `resource_import::w16_tests::file_open_of_fits_and_dxf_opens_documents` (`Action::Open`, the import job) |
| Open GIMP XCF | 🔶 | W10-F (`raster::codec::formats::xcf`): every XCF version GIMP writes, 32- and 64-bit offsets, uncompressed / RLE / zlib tiles; 8-bit RGB and grey (perceptual, and linear converted to sRGB); layer offsets, opacity (integer and float), visibility, applied layer masks, groups (item paths) and Normal / Multiply / Screen / Overlay / Soft & Hard light / Difference / Addition / Subtract / Darken / Lighten / Divide / Dodge / Burn / Grain extract & merge, legacy and 2.10+ codes. Every open route (File ▸ Open's job, `Editor::open_path`) builds a **layered document** (`app-shell` `import_xcf.rs`, `document_from_xcf`): one raster layer per GIMP layer at its own offset (off-canvas pixels kept), groups with pass-through kept, name, opacity, visibility, the modes above plus Dissolve mapped to the document's blend modes. What does not map is shown on File ▸ Open as an **"XCF import report"**: Grain extract / merge and unknown GIMP modes open as Normal (by layer name), Soft light's formula differs, an applied mask is baked into alpha, a greyscale image opens as RGB, legacy GIMP 2.8 modes (GIMP blends them in gamma space) blend in linear light here, a floating selection is left out (`file_open_of_an_xcf_builds_its_layers_and_reports_what_did_not_map`, fixture pinned to raster's writer by `the_committed_layered_xcf_fixture_is_the_test_writers_output`). Gaps: indexed and deeper-than-8-bit images are refused by name; masks are not kept as masks; channels, paths, text and parasites are not read |
| Export PPM / PGM / PBM, DDS, AVIF | ✅ | W10-F: `ExportFormat::{Ppm, Pgm, Pbm, Dds, DdsBc3, Avif(q)}` in Export As's format list (DDS twice: uncompressed BGRA and BC3 / DXT5 with a principal-axis block fit) and in File ▸ Export… by extension (`.dds` writes uncompressed). PPM writes binary `P6`, PGM `P5` (Rec. 601 luma), PBM `P4` (luma at half); all three flatten alpha first. AVIF: 8-bit 4:4:4 with an alpha item, quality 1-100 on the Export As slider, through `image`'s `avif` feature (`ravif` 0.13 over `rav1e` 0.8, pure Rust, no assembly; `cargo audit` clean). Export As estimates an AVIF's size from a real encode but shows "could not be previewed": `ExportFormat::reads_back` is still `false` for AVIF, so Export As never decodes the encoded bytes for a preview, although `raster::decode_bytes` in the application now reads an AVIF through the W15-A worker process (next row) (`the_new_formats_are_offered_and_avif_has_a_quality`, `file_export_writes_ppm_pgm_pbm_dds_and_avif`) |
| Export EXR, JPEG XL | 🔶 | W11-H: `ExportFormat::Exr` (in `ExportFormat::ALL`, so an Export As row and a File ▸ Export As menu row) writes 32-bit float RGBA OpenEXR, linear light, premultiplied, from the display-encoded 8- or 16-bit composite (`formats::float::encode_exr`; an 8-bit image reads back exactly and a 16-bit one within one 16-bit code, `an_exr_export_opens_again_within_one_16_bit_code`, `a_16_bit_source_goes_out_to_exr_at_16_bit_precision`), and from a 32 Bits/Channel document's linear float composite, unclipped (`formats::float::encode_exr_linear` through `depth32::write_float_tiff`, for File ▸ Export… and an Export As EXR row at 100%). `ExportFormat::Jxl` writes **lossless** 8-bit RGBA JPEG XL through `zune-jpegxl` 0.5 (pure Rust, MIT OR Apache-2.0 OR Zlib), read back by `jxl-oxide` exactly (`jpeg_xl_export_is_lossless_at_8_bits_and_reads_back`, one image wider than a 256 px group); it is in `ExportFormat::MIN_TWO_PIXELS` (offered by Export As through `writable()`, not a menu row) because the encoder refuses a one-pixel edge, and 16-bit input is refused because the encoder's 16-bit output read back with its alpha gone (`jpeg_xl_refuses_16_bit_samples`). No lossy JPEG XL: no pure-Rust lossy encoder exists. Export As shows both with a size from a real encode and a preview decoded from it (`ui` `exr_and_jpeg_xl_are_offered_sized_and_previewed`); the Export As job writes both (`app-shell` `doc::formats_w11h_tests::export_as_writes_exr_and_jpeg_xl`). File ▸ Export… writes both by file name (`doc::export_format_for` maps `.exr` and `.jxl`; the picker offers "OpenEXR" and "JPEG XL"; `file_export_writes_exr_and_jpeg_xl_by_name`). Gap: an Export As EXR row at a scale other than 100% writes the clipped 8/16-bit composite |
| Open DNG (camera RAW) | 🔶 | W13-C, `raster::codec::formats::raw` (own code, no new dependency): IFD0 + chain + `SubIFDs`, the full-resolution CFA or `LinearRaw` IFD; strips or tiles, uncompressed (8/16-bit, or 1-16-bit packed) or lossless JPEG (`formats::ljpeg`: SOF3, predictors 1-7, point transform, restart intervals); `LinearizationTable`, `BlackLevel` (repeat pattern, `DeltaH`/`DeltaV`), `WhiteLevel`, `ActiveArea`; white balance from `AsShotNeutral` (else the matrix's D65 response) with highlight clipping; Hamilton-Adams edge-directed green plus colour-difference red/blue for 2x2 Bayer (other patterns up to 6x6 averaged over 5x5); `AnalogBalance x CameraCalibration x ColorMatrix` (the D65 one, else `ColorMatrix2`, else 1) to sRGB, `BaselineExposure`, the sRGB curve; `DefaultCrop` and IFD0 `Orientation`; the codec returns 16-bit sRGB, and both open roads make a document that records 16 Bits/Channel (RGBA8 tiles, as for every 16-bit source, see the Open row above): File ▸ Open's background import job (`Action::Open` -> `request_open` -> the worker's `DecodedImage::decode_bytes` -> `poll_imports` -> `open_image_decoded`) and the synchronous `OpenDocument::open_image` that Open Recent, startup files and drops use; one test drives both (`dialogs::tests::a_dng_opens_as_a_sixteen_bit_document_and_a_vendor_raw_is_refused_by_name`). Tests build DNGs from a known scene through a known camera matrix and check the colours in every storage and Bayer phase, the edge fringe, orientation, crop, and truncated and bit-flipped copies (errors, never panics) (`formats::raw::tests`). Not done: no contrast S-curve (the look is scene-linear sRGB, not Adobe's default), no colour-temperature interpolation between the two matrices, `ForwardMatrix`, camera profiles, `OpcodeList1-3` (lens corrections, gain maps) and `DefaultScale`; lossy-JPEG, deflate/float and JPEG XL DNGs are refused by name; no RAW develop dialog. The lossless-JPEG tests cover predictor 1 only (what DNG writers use). |
| Open AVIF / HEIC | ✅ | W15-A: both open, decoded **in a separate worker process** so a decoder panic cannot close the editor. The release profile is `panic = "abort"` and both decoders panic on some damaged files: `rusty_av1d` 1.2.0 (BSD-2-Clause, a rav1d fork with a Rust API, default features off: no nasm) reaches `unwrap()` on a missing reference frame header (`src/decode.rs:4993`), and `heic-rs` 0.1.1 (MIT OR Apache-2.0, `forbid(unsafe_code)`) indexes past a slice at `src/hevc/decode/recon.rs:70` (W13-C measured 3 of 4000). So `raster::codec::formats::heif` never decodes in the calling process: the codec facade hands an AVIF / HEIC (a HEIC is found by its `ftyp` brand and travels as `ImportFormat::Avif`, the enum having no HEIC variant) to the decoder installed with `heif::install_isolated_decoder`, and with none installed refuses it by name (`heif::no_worker_refusal`). `studio-desktop`'s `main` first runs `app_shell::dialogs::decode_worker::run_if_worker` (the hidden `--decode-worker avif|heic` entry, before any console, logging, crash hook or window), then `decode_worker::install`: every open route (File > Open's job, Open Recent, drops, the command line) re-executes the editor's own binary (`std::env::current_exe`), writes the limits and the file to its stdin, reads back a header (width, height, depth, colour space, ICC length) that is checked against `ImportLimits` before a pixel is read or allocated, then exactly the pixels it implies; a worker still running 120 s after its spawn is killed; any unclean exit or malformed answer is "the AVIF decoder crashed on this file; it may be damaged" (or "the HEIC decoder …" for a HEIC: the message names the worker kind). Read: AVIF single and `grid` images through `heic-rs`'s ISOBMFF parser and colour conversion, alpha (`auxl`), 4:0:0 / 4:2:0 / 4:2:2 / 4:4:4, 8-bit to an 8-bit document and 10/12-bit to 16 Bits/Channel, YCbCr per the `nclx` or, without one, the AV1 sequence header; HEIC whatever `heic-rs` decodes (HEVC intra stills, grids, alpha, 8 or 16 bits). Both apply `irot` / `imir` / `clap` in association order (a HEIF file's orientation; the Exif Orientation tag is informative there and is not applied on top), keep an embedded ICC profile as the document's space, and open `nclx` primaries 12 as Display P3. `.avif`, `.heic` and `.heif` are in File > Open's filters. Tests: `raster` `formats::heif::tests::*` (in-process, well-formed files only: an 8-bit AVIF from `image`'s encoder and a 10-bit one from `ravif` to size, colours and alpha within 6/255, the 10-bit one as 16-bit samples; a libaom 4:2:0 file; `irot`, `imir` and their order; ICC and P3; a 1x2 grid; a declared 70 000 px side refused before decoding; HEIC grey, alpha, 10-bit and `irot` on synthetic HEVC; with no worker installed the facade refuses), `app-shell` `dialogs::decode_worker::tests::*` (the protocol over in-memory streams, the oversized-header refusal, truncated and trailing answers), `studio-desktop` `tests/decode_worker.rs` (the real binary as the worker: both formats to size and colours; 1000 bit-flipped copies all come back, 610 decoded, 348 refused, 42 panicked in the worker and came back as "crashed", none aborted the test process; a `test-panic` worker is a crash; a `test-hang` worker is killed at a 2 s deadline; `Editor::open_any` and File > Open's job open an AVIF (16 Bits/Channel) and a HEIC, and a file that makes the decoder panic fails to open while the editor carries on). The `test-*` worker kinds exist only in debug builds. Gaps: HDR transfer functions (PQ, HLG) and BT.2020 primaries are not converted (the samples open as sRGB); premultiplied alpha (`prem`) is not divided out; `iovl` overlays are refused by name; image sequences open their primary still only; Export As still does not preview AVIF (`ExportFormat::reads_back` is `false` for AVIF, so no preview decode is attempted; the decode itself would go through the worker); the only HEVC encoder in reach is `heic-rs`'s own synthetic builder, which writes flat grey, so HEIC colour conversion is verified on grey only. W16-K: a flat open decodes once (it decoded twice, the pixels and the source depth each asking the codec, so a HEIC / AVIF open started two workers); `menu_bridge::w16k_tests::a_flat_heic_open_starts_one_decode_worker` counts one worker call per open |
| SVG export with vector layers | 🔶 | W10-F (`app-shell` `doc_svg_export.rs`, `OpenDocument::vector_svg`): a solid-fill shape layer with a centred stroke is a `<path>` (its own path data, `matrix(...)` transform, fill / fill rule, stroke colour, width, cap, join, miter limit, dashes; the colours are written as the encoded values the compositor decodes, so a mid-tone renders at the composite's brightness — `mid_tone_shape_fill_and_stroke_colours_export_at_the_composite_brightness`; opacity x fill opacity is the element's `opacity`, so a translucent fill fades fill and stroke together as the compositor does — `fill_opacity_fades_a_stroked_shape_as_a_whole`); single-style horizontal text is `<text>` with one `<tspan>` per line (family, size, weight, slant, fill, alignment, leading — line positions approximate the shaper: first baseline at 0.8 em, lines 1.2 em x the leading multiple); a group below 100% opacity is one `<g opacity>` around its children, so the opacity applies once to the group as a whole (isolated and Normal pass-through groups alike); a base layer and the layers clipped to it (found as the compositor finds a clipping run) are one compositor render embedded as a PNG `<image>`; every other layer is its own compositor render embedded as a PNG `<image>` (its groups' opacity left to their `<g>`); a document with a non-Normal blend, an adjustment layer, or a group with effects, a mask, a transform of its own or an artboard goes out as one embedded image. Rendering the SVG back with resvg matches the composite outside text (`shapes_become_paths_text_becomes_text_and_rasters_become_images`) and, pixel for pixel within 3/255, for a clipped layer (`a_layer_clipped_to_the_one_below_is_written_with_its_base`), a 50% group of overlapping shapes, isolated and pass-through (`a_group_opacity_applies_once_to_the_whole_group`), and a clipping run inside a 50% group (`a_clipping_run_inside_a_translucent_group_is_faded_once`). Routes: File ▸ Export… to `.svg` (an SVG row in the export picker; `act_export` accepts it; `jobs::run_file_export` calls `doc::write_vector_svg` on the snapshot — `file_export_to_svg_writes_shape_paths_and_text`) and Export As's SVG rows at 100% (`jobs::run_export` rewrites them with the same writer — `export_as_svg_at_100_percent_writes_shape_paths_and_text`). Gap: an Export As SVG row at another scale stays the embedded-image SVG at that size |
| WebP lossy with a quality slider | ✅ | W11-H: `ExportFormat::WebPLossy(q)` (quality 1..=100, appended last to the enum) writes one VP8 key frame through `tiny-webp` 0.1.0 (pure Rust, `#![forbid(unsafe_code)]`, no dependencies with `default-features = false`, MIT OR Apache-2.0, rust-version 1.85; checked with `cargo +1.89 check -p raster`), alpha exact in an uncompressed `ALPH` chunk (so a transparent image stays large), 8-bit only, no ICC, VP8's 16 383 px edge limit refused as an error. Export As offers "WebP (lossy)" beside "WebP (lossless)" with its Quality field enabled (1-100), a size from a real encode and a preview decoded from it (`ui` `lossy_webp_is_offered_with_a_quality_sized_and_previewed`); the file reads back through the ordinary WebP decoder with exact alpha and quality trading size for PSNR (`raster` `w11h_tests::lossy_webp_export_reads_back_close_with_exact_alpha`, `lossy_webp_refuses_what_it_cannot_write_without_panicking`). W11-H evaluation (2026-09-24; 512×384 smooth and hard-edged images plus 1×1, 3×5 and 17×9, opaque and with alpha, quality 50/75/90, decoded by `image-webp` 0.2.4, RGB PSNR): `tiny-webp` 37.7/38.8/43.7 dB on the smooth image, every stream accepted; `gamut-webp` 0.3.1 (MIT OR Apache-2.0, validated against libwebp by its authors) 40.0/41.9/44.4 dB with smaller files, but it declares rust-version 1.92, above the workspace MSRV 1.89 that CI checks; `webpkit` 0.1.0 34.9/36.9/38.0 dB and rust-version 1.96; `vaam-image-webp` 0.1.0 7-13 dB on the small images; `fast-webp` 0.1.1 is out: it binds `libwebp-sys` (C), which this build keeps out of the tree. On the hard-edged image every encoder stays near 30 dB (4:2:0 chroma on saturated edges). Earlier history: W4-H: kept lossless on purpose. The WebP encoder already in the tree (`image-webp` 0.2, behind `image` 0.25) writes lossless VP8L only. Pure-Rust lossy encoders exist on crates.io and were evaluated on 2026-09-23 (encode 256×128 and 257×131 RGBA test images at quality 50 and 90, decode with `image-webp` 0.2.4, measure RGB PSNR; `cargo audit` on a lockfile holding the three permissively licensed crates found no advisories): `zenwebp` 0.4 is AGPL-3.0-only (or a commercial licence), so it is out on licence; `webp-rust` 0.3.1 (MIT) measured 15–17 dB PSNR at every quality, and its quality-90 stream for the 257×131 image was rejected by `image-webp` with `BitStreamError`; `vaam-image-webp` 0.1.0 (MIT/Apache-2.0) reached 30–40 dB on the 256×128 images but 22–23 dB on the 257×131 ones, and was first published on 2026-09-22; `tiny-webp` 0.1.0 (MIT/Apache-2.0, no dependencies with `default-features = false`) measured 34–44 dB on all four images, but it is a single 0.1.0 release first published on 2026-09-04 with 23 downloads. `tiny-webp` is the candidate to adopt once it has a track record; adopting it means a workspace manifest change and a quality slider in Export As. Until then the Export As row is labelled “WebP (lossless)” and has no quality control. W10-F re-checked: the `webp` crate binds libwebp (C), which this build keeps out of the tree, so WebP stays lossless while AVIF (pure Rust, with a quality slider) is the lossy modern format. W11-H adopted `tiny-webp` (above) |
| Export As PDF (vector, a page per artboard), EMF, DXF | 🔶 | W16-K (`raster::codec::export_vector`, `ExportFormat::{Pdf, Emf, Dxf}` appended, `ExportFormat::VECTOR`, rows in File ▸ Export As and the Export As format list). A 100% row is written from the layers (`menu_bridge::w16k::vector_doc`, called by the Export As worker): a solid-fill shape with a centred, undashed stroke and a single-style text layer (its glyph outlines, through its warp) are vector paths, an artboard's plate its background rectangle, and any other layer that layer rendered alone and cropped to its ink; a page whose stack uses a blend mode, a mask, a clipping run or an adjustment layer is one image of its composite. **PDF**: one page per artboard in reading order (else the canvas), 1 pt per pixel, alpha as `ExtGState` / soft masks; `a_pdf_export_is_one_vector_page_per_artboard` reads two pages back through `hayro` and finds no image in a vector-only export. **EMF**: the first page as GDI path records at 1/16 px, raster layers as `StretchDIBits` flattened onto white (GDI keeps no alpha; paint alpha is dropped). **DXF**: the first page's paths as R12 `POLYLINE`s (curves in 16 segments, nearest AutoCAD colour index); raster layers are not written (DXF carries no inline pixels). A scaled row writes the codec's one-image PDF / EMF (an empty DXF). Not done: text as selectable PDF text (it is outlines), gradient fills and layer effects as vectors, EMF / DXF of pages past the first |
| File ▸ Export ▸ Slices | ✅ | W4-H: one file per committed Slice-tool region, `<document>_01.<ext>`…, in the last confirmed Export As format and settings (PNG before any). W11-I: the slice set (rectangles, names, URL, alt) is saved in the `.rstudio` document (`editor_core::Document::slices`, append-only serde, omitted when empty) — every slice edit writes it through (`slices_export::persist_slices`, which writes only the active document's record, so a commit in one document never rewrites another's saved set) and marks the document dirty; `slices_export::restore_saved_slices` loads the saved set into the slice store on every open route (`Editor::open_path`, the File ▸ Open background job's `install_opened`, autosave recovery, Duplicate), so a reopened document shows its slices on the canvas and to Slice Select at once (`w11i_slices_tests`) |
| Drag-and-drop open, recent files | ✅ | W11-D: one routing table (`Editor::open_resource_file`, and `Editor::open_any` for the synchronous routes) serves the File ▸ Open picker, drag-and-drop (`Shell::on_dropped_files`), File ▸ Open Recent and command-line files (`Editor::open_paths`): `.abr` → Brushes panel, `.asl` → style presets, `.pat` `.grd` `.csh` `.aco` `.ase` `.icc` `.icm` → their libraries (row below), `.ttf` `.otf` `.ttc` `.otc` → session fonts, `.cube` → a Color Lookup adjustment layer on the active document (one undo step; refused with the reason with no document open); a library file dropped onto an open document goes to its importer, an image dropped there is still placed as a layer, anything else opens a tab. Tests: `editor::open_any::tests::{dropping_each_library_file_on_a_document_reaches_its_importer, dropping_a_library_file_on_an_empty_window_opens_no_document, recent_files_open_of_an_abr_adds_its_brushes}` (preset counts before/after through `Shell::on_dropped_files` and `Editor::open_paths`) |
| File ▸ Revert | ✅ | W11-D (`Editor::revert_active`, `MenuAction::Revert` under Save as PSD): re-reads the document's `.rstudio` package, else the image it was opened from, and applies one `Transaction` labelled "Revert" — every current layer deleted (locks lifted first), the saved layers restored with their pixels (tiles the file does not hold cleared, locks put back), canvas size, colour mode / bit depth, selection, guides and document records set; missing asset records are registered. Undo takes it back, so it asks nothing first (as Photoshop); the document is clean afterwards. Greyed with the reason for a document never saved or with no unsaved changes; refused for a 16- or 32-bit document whose file has another bit depth. Tests: `editor::open_any::tests::{revert_of_an_image_restores_its_pixels_and_is_undoable, revert_of_a_project_restores_the_saved_package, revert_of_a_new_document_is_refused}`. Gaps: the camera, colour samplers and the document's resolution / ICC assignment are not reverted; a revert across a bit-depth change of a deep document is refused rather than done |
| Open Photoshop resource files (.pat .grd .csh .aco .ase .icc .atn; W16-L: .acv .3dl .look) | 🔶 | W9-N (`asset_store::resources`, routed by `Editor::open_resource`): `.pat` (Photoshop `8BPT` grey/RGB 8-bit, and GIMP `GPAT`) → pattern presets (Layer Style pattern list, active pattern); `.grd` version 5 → first gradient becomes the gradient tools' ramp, and every gradient is a chip in the Gradient Editor's preset strip after the built-ins (noise gradients and version 3 refused); `.csh` → the Custom Shape tool's Shape list after the built-in library, drawn like a built-in shape; imported gradients and shapes are persisted in the presets file and re-listed on the next start; `.aco` v1/v2 and `.ase` → Swatches panel (colour books refused); `.icc`/`.icm` RGB profile → assigned to the active RGB document and written into exports that carry ICC (the assign is not an undo step). Since W10-G, imported gradients and custom shapes are listed in Edit ▸ Preset Manager (`edit_gaps::library_of` reads the preset store) and can be renamed, reordered or deleted there. W13-E: `.atn` is read (see the Actions row). W16-L (`app-shell/src/resource_import_w16.rs`, routed by `Editor::open_resource`): `.acv` → a **Curves adjustment layer** with its composite / red / green / blue curves (this build has no saved-Curves-preset list, so Photopea's "into the Curves presets" lands as a layer, like a `.cube`); `.3dl` (mesh line or `Mesh a b`, integer entries, blue fastest) and SpeedGrade `.look` (`<LUT><size>`/`<data>` hex `f32`, order assumed red-fastest like `.cube`, not confirmed against a SpeedGrade file) → a **Color Lookup** layer (`file_open_of_an_acv_adds_a_curves_layer_with_its_curves`, `file_open_of_a_3dl_or_look_adds_a_color_lookup_layer`). Gaps: the Preset Manager does not refresh the Gradient Editor's preset strip or the Custom Shape tool's Shape list, which are handed the stored presets once, on the first frame (`resource_import::drain_panel_imports`), so a rename or delete shows there from the next start. |

### Application
| Capability | Status | Notes |
| --- | --- | --- |
| Menu bar wired to real commands | ✅ | nine menus; every enabled item routes to real code (`every_enabled_menu_item_really_does_something`, `no_enabled_menu_item_resolves_to_a_no_op`); View ▸ Proof Colors / Gamut Warning and Image ▸ Mode ▸ Lab / CMYK / Indexed are enabled since W7-D |
| Keyboard shortcuts | ✅ | full customisable keymap with conflict detection; Photoshop's Ctrl+L / M / U / B / I, Backspace, Alt+Backspace and Ctrl+Backspace are bound (W5); W10-J: Ctrl+H (Extras), Alt+Ctrl+T (duplicate and Free Transform), Shift+[ / Shift+] (hardness by 25%), the number keys (the painting tool's opacity, two quick digits exact) and Alt+Shift+Ctrl+C (Content-Aware Scale) resolve through the keymap with no conflict (`the_w10j_chords_resolve_to_their_menu_actions`); W11-G: Ctrl+Shift+F (Fade), Ctrl+Alt+R (Refine Edge), Ctrl+P (Print), Alt+[ / Alt+] / Alt+, / Alt+. (layer below / above / bottom / top), Shift+Alt+<letter> (Photoshop's 23 blend-mode letters, N / M / S / O ...) and Shift+Plus / Shift+Minus (next / previous blend mode) on the active layer, or, with a painting tool active (Brush, Pencil, Clone Stamp, Pattern Stamp, Gradient, Paint Bucket), on that tool's options-bar Mode as Photoshop does, W13-M: tool letters follow Photopea's key handler — the bare letter picks its group and, pressed again, keeps the tool; Shift+<tool letter> steps to the next tool of the group, wrapping (one rule, `ui::keys::tool_for_letter`, used by `Editor::select_tool_letter` on the shipped route and by the `ui` crate's own route); W13X-6: from another tool a letter enters its group at the tool last used from it this session (`ui::palette::ToolLetterMemory`, recorded by `Editor::set_tool` whichever route chose the tool), else the first — `tool_letter_memory::a_tool_letter_enters_its_group_at_the_member_last_used_this_session` (tests/integration) and `palette::tests::a_tool_letter_enters_its_group_at_the_member_last_used`; gap: the memory is per session, not saved; Ctrl+F is Photopea's Find (its search over menu commands and tools), here Help > Search Commands, and Last Filter is painted Alt+Ctrl+F as in Photopea (`shell::w13m_tests::shift_and_a_tool_letter_steps_through_the_group_on_the_key_route`, `a_repeated_bare_tool_letter_keeps_the_tool_on_the_key_route`, `ctrl_f_opens_find_the_command_search_on_the_key_route`, `alt_ctrl_f_is_last_filter_on_the_key_route`); the Layers panel's search field has no chord, Shift+/ (Help > Keyboard Shortcut Sheet: a searchable read-only list built from the live keymap, user overrides included) and Ctrl+Shift+P (Help > Search Commands: the rows the menu bar enables this frame, resolved against the chrome's own per-frame menu context so Paste after a Copy, Revert for a saved file and the workspace-gated rows agree with the menu bar, filtered by name; Enter runs it through the menu-click route so a dialog row opens its dialog) — `the_w11g_chords_resolve_from_the_keys_winit_reports`, `the_layer_and_blend_chords_act_on_the_document`, `the_blend_chords_set_the_painting_tools_mode_not_the_layers`, `the_shortcut_sheet_lists_the_live_keymap`, `the_command_search_finds_gaussian_and_running_it_opens_its_dialog`, `the_command_search_enables_what_the_menu_bar_enables`. |
| Panels | ✅ | 26 (`ui::dock::PanelId::ALL`): Layers, History, Adjustments, Properties, Color, Swatches, Brushes, Character, Paragraph, Navigator, Info, Channels, Paths, Actions, Histogram — in two dock columns (W2); W10-I appends Animation; W10-B appends Layer Comps, Tool Presets, Glyphs, Notes, Character Styles and Paragraph Styles (their own row below); W11-I appends **CSS** (Window ▸ CSS, `ui::panels::css`): the active layer's `position`/`left`/`top`/`width`/`height` (the Properties frame), `opacity`, a fill layer's `background-color` or `linear-gradient`/`radial-gradient`, a shape's fill, `border` and `border-radius` (rounded rectangles), `box-shadow` / `text-shadow` from a drop shadow, and a text layer's `color` and font properties, with a Copy button (`tests/w11i_css_panel.rs`); W13-N appends Styles, Document Info and Guide Guy (their own row below) |
| Tool options bar | 🔶 | generated from each tool's options schema. W16-C: every float option carries a display unit (`tools::registry::float_display`): Tolerance shows and accepts 0-255, Opacity / Flow / Hardness / Exposure / Strength / Spacing / Smoothing / Roundness and the other 0-1 fractions show 0-100%, sizes px, the brush tip angle degrees, while the tool keeps its stored value (`no_float_option_is_shown_as_a_raw_fraction`; typing 32 into the bucket's Tolerance stores 32/255, `typing_32_into_the_bucket_tolerance_stores_32_over_255`; `opacity_shows_100_percent_on_the_brush_and_the_bucket`). A Commit check sits beside the tool name while a Free Transform (Warp / Perspective included), crop box or pen path is pending and confirms it as Enter does (`the_options_bar_commit_check_commits_a_live_free_transform`). Gaps: no Cancel (cross) button yet: the bar has no intent that reaches the shell's Escape route (`Shell::abandon_gesture`), and adding one needs `ui::Intent`, `menu_bridge`, `ChromeOutput` and the shell; the Commit check does not show for a Type session or a Perspective Crop, whose geometry the chrome does not publish into the workspace |
| Photopea visual design | 🔶 | one token system, nine themes since W15-D (the Themes row below), WCAG AA asserted by test; Photopea's chrome order and two dock columns landed in W2; the fit and finish is still converging on Photopea’s density and neutral greys |
| Preferences | ✅ | persisted, including the keymap editor; keymap edits apply live; Units drive rulers and readouts; the scratch directory is an editable text field (no folder picker) |
| Accessibility (screen readers / AccessKit) | 🔶 | egui's `accesskit` feature is on: the adapter is initialised at window build, its action requests route through a typed user event, and egui publishes a labelled node per widget. **No keyboard focus navigation:** Tab is withheld from egui and toggles the panels (`app-shell/src/shell.rs` `withhold_from_egui`; `tab_is_never_handed_to_egui_unless_egui_is_recording_it`). The on-device screen-reader walk needs a host with assistive tooling (C14). |

---

## Tier B — Pro

| Capability | Status | Notes |
| --- | --- | --- |
| Text layers with real shaping and fonts | ✅ | bidi, ligatures, kerning, contextual forms via cosmic-text; a Type click inside a text layer places the caret; switching tools or Escape confirms the text (W5) |
| Warp Text, Type on a Path, Convert to Shape, font list, loading font files | 🔶 | W9-K. `TextLayer::warp` / `TextLayer::path` (append-only, `#[serde(default)]`, skipped when unset). **Layer ▸ Text ▸ Warp Text…** opens a dialog (`ui::dialogs::WarpTextDialog`: Style, Bend, Horizontal and Vertical Distortion, -100..100 %) that confirms the whole warp as one `SetLayerKind` undo step (`warp_text_dialog_sets_style_bend_and_distortions_as_one_step`, drawn through the dialog host with Enter), and **Layer ▸ Text ▸ Warp Style** ▸ None / Arc / Arc Lower / Arc Upper / Arch / Bulge / Flag / Wave / Fish / Rise / Fisheye / Inflate / Squeeze / Twist sets the style in one click, also one undo step; `compositor::text::hash_layer` keys the run and tile caches on the warp and the path, so a warp (or its undo) on text the canvas already drew reaches the canvas (the menu test composites before and after); the rasteriser (`text_engine::warp`) fetches each glyph's outline, maps it through the envelope (Arc is a true circular arc) and fills it with its own anti-aliased scanline filler, so the text stays editable (`arc_warp_bends_the_baseline_onto_a_circle`, `every_warp_style_moves_ink_and_none_changes_nothing`, `warp_text_arc_from_the_layer_menu_bends_the_composited_text`). **Type on a Path**: a Type click within 6 px of the Paths panel's current path (the selected path, else the Work Path) or of a visible shape layer's outline as drawn (through its layer transform; `ToolContext::type_path_outlines`, document space) stores that outline, flattened, as the new text layer's baseline, starting where the click met it; glyph outlines follow the path along its left normal, wrapping on a closed path (`a_type_click_on_a_shape_outline_starts_type_on_a_path`, `type_on_a_circle_path_places_the_glyphs_on_the_circle`, `type_on_a_path_follows_a_moved_shape_and_the_work_path`). **Layer ▸ Text ▸ Convert to Shape** replaces the text layer (same place, name, transform, opacity, blending, effects) with a shape layer filled with the text's colour whose path is the glyph outlines through the warp or path (`text_engine::outline_svg`; curves kept when unwarped), one undo step (`convert_to_shape_makes_a_shape_layer_whose_coverage_matches_the_text`, `the_outline_svg_covers_the_rendered_ink`). The Type tools' **Font** combo lists the three generics then every family the compositor has (`tools::registry::type_font_choices`, append-only so a held index never changes family; `the_type_font_combo_lists_the_installed_families_and_a_pick_reaches_the_layer`). **File ▸ Open** of a `.ttf` / `.otf` / `.ttc` / `.otc` (W16-L: or `.woff` / `.woff2`, unwrapped by `text_engine::webfont` through `wuff`; `file_open_of_a_woff_font_loads_its_family_for_text`) (a Fonts filter) loads its faces for the session into the compositor, the session-font set and the Font list (`file_open_of_a_font_file_makes_its_family_available`); `FontLibrary::with_system_fonts` also scans a user fonts folder (`RASTER_STUDIO_USER_FONTS`, else `%APPDATA%\RasterStudio\fonts` / `$XDG_DATA_HOME/raster-studio/fonts`). Gaps: the Warp Text dialog has no live canvas preview (the canvas updates on confirm); the caret, hit test and selection stay in unwarped layout space (a click on warped or path text enters it only where the flat layout would be); the Work Path reaches the Type tool through the path the menu context parks each frame; glyphs bend with the path rather than rotating rigidly; no synthetic bold on warped/path glyphs; a loaded font file is not copied into the user fonts folder |
| Vector paths, pen tool, shape layers | 🔶 | the Pen drags out smooth anchors and Alt breaks a handle; Add / Delete / Convert Anchor, Path / Direct Selection; shapes with fill / stroke, width and corner radius; Custom Shape library. W9-F: the shape tools' Mode has **Path** (no layer, no pixels: the outline is published as the Work Path, the Paths panel's row, until the next drag or Escape — `shape::tests::path_mode_leaves_no_new_layer_but_a_work_path`); `ShapeLayer::fill_paint` (append-only, `#[serde(default)]`) makes the fill None / Solid / Gradient (W9-B's `GradientFill`, fitted to the shape's filled bounds) / Pattern (the tile travels with the fill), picked by the options bar's Fill Type (current gradient / current pattern) or the Properties shape page (Colour / Gradient) and composited per pixel (`compositor::shape::tests::a_gradient_filled_rectangle_composites_a_ramp`); `ShapeStroke::align` Inside / Centre / Outside (`outside_alignment_grows_the_stroked_bounds_by_the_width`), cap, join and dash / gap (in stroke widths) in the options bar (shape tools and pens); the Properties shape page has Align, Caps and Corners and one Dash slider that sets an even dash (gap equal to dash, in stroke widths) (`the_shape_page_draws_and_drives_the_stroke_and_fill_type_controls`), also honoured by Rasterize mode; Path Select's options bar has Combine (Unite / Subtract Front / Intersect / Exclude) and Align Components (six edges), applied to the clicked path's components as one undo step (`path_select_combine_and_align_edit_the_clicked_path`); **Layer ▸ Combine Shapes** ▸ Unite / Subtract Front / Intersect / Exclude folds the selected shape layers bottom to top through `vector::fold` into the bottom layer, one undo step (`combine_shapes_unite_merges_two_overlapping_rectangles_into_their_union`). W16-F (Photopea learn/vg-manipulation): **Path Select** selects one path COMPONENT (a click, Shift adds or removes one), a drag or an arrow moves only the selected components, and its options bar's Arrange (Bring to Front / Bring Forward / Send Backward / Send to Back) and Delete reorder or remove them, one step each (`tools::path_select::ComponentOp`, `path_select::w16f_tests`, `ui::view::toolbar` `the_path_select_bar_arranges_and_deletes_through_the_confirm`, `app-shell` `w16f_route_tests::path_select_moves_nudges_reorders_and_deletes_one_component`); **Direct Selection** keeps a knot selection (Shift-click adds or removes a knot, across components), a drag moves every selected knot, double-clicking a handle collapses it and double-clicking a knot converts it (a smooth knot's handles collapse, a corner's come back) (`shift_click_selects_two_knots_and_a_drag_moves_both`, `double_clicking_a_smooth_knot_collapses_its_handles_and_again_restores_them`, `double_clicking_a_handle_collapses_only_that_handle`). Gaps (W16-F): the Delete key does not delete selected components (Edit ▸ Clear's route does not reach the path tools; the options bar's Delete does); the canvas does not mark which components or knots are selected (no overlay reads the path tools' selection); a component's boolean operation is not stored per component (Combine merges the whole path, as before); Shift-click on the outline away from a knot still adds a knot there. Gaps: the Work Path lasts only while the shape tool is current (switching tools drops it, as a pen Work Path does) and a multi-component custom shape publishes its components as one anchor run; Path Select merges/aligns the components of the one path clicked, not a marquee of components across layers; boolean results are polygons (curves flattened); the Properties page cannot pick a pattern or edit the gradient's stops/angle; the pen keeps a colour fill (Fill Type is a shape-tool option); SVG export writes solid shape layers as `<path>` (W10-F; gradient / pattern fills and inside / outside strokes go out as embedded images). W13-I: the **Line** tool's arrowheads (Arrow Start / Arrow End, Arrow Width % and Arrow Length % of the weight, Concavity %; `tools::shape::LineArrows`, proportions as a `vector::ArrowStyle`) are unioned into the line's outline (`shape::line_with_arrows`), so the shape layer, Rasterize and Path mode all carry them (`shape::w13i_tests::*`) W16-G: **live shapes** — a drawn rectangle / rounded rectangle / ellipse / star (Star tool) / arrowless line keeps its parameters (`layer_model::LiveShape` in `ShapeLayer::live`, append-only), live only while they regenerate the path exactly (`tools::shape::live_shape_of`), so a path edited by hand is a plain path; Properties' **Live Shape** section edits W / H / X / Y, a rectangle's four corner radii with **Same Radii**, a star's points and inner radius and a line's weight, each one `SetLayerKind` step (`ui` `live_shape_props::tests::the_properties_panel_edits_a_live_rectangles_radius_in_a_real_frame`, `app_shell` `live_shape::tests::a_drawn_rectangles_corner_radius_changes_in_properties_as_one_step`); a live rectangle (with radii, `keyOriginRRectRadii`), ellipse or line is written to `.psd` as its `vogk` and opens live (`psd::live_origin`, `live_shape::tests::a_live_rectangle_keeps_its_radii_through_a_psd`) under a pure translation only. The Polygon slot is Photopea's **Parametric Shape** tool, its `pshape` list Polygon / Star / Arrow / Grid / Spiral with Photopea's parameters, a Polygon or Star drawn from its centre to a vertex (Shift snaps 15°) with **Corner Radius** (`vector::shapes::{parametric_arrow, grid, rounded_polygon}`; `tools::shape::live::tests::a_parametric_polygon_is_centred_on_the_press_with_rounded_corners`; its shapes are plain paths, as Photopea's `customShape` origination keeps them; `live_shape::tests::each_parametric_shape_draws_through_the_canvas_pointer`) — Square, Circle and Dot are not added: Photopea's bundle uses those strings for filter and symmetry shapes, not in `pshape`. The **Vector Gradient** tool (end of the gradient slot) drags the active shape's gradient start handle (moves it) or end handle (angle and scale), a press elsewhere starting the line, one undo step, the line drawn as a measured line (`live_shape::tests::the_vector_gradient_tool_moves_a_shapes_gradient_by_its_handle`). Stops are edited in the gradient editor (Photopea's tool has no on-canvas stops either). W16-G gaps: no gradient fill layer geometry and no solid-to-gradient on press (the tool sees only shape layers), no radial ratio/focus handles, handles in the shape's untransformed space, no star `vogk`, a line with arrowheads is a plain path (Photopea keeps it live), the options bar draws every Parametric key (not only the picked shape's), Sides defaults to 6 (Photopea 5), the Spiral is drawn in the drag box (Photopea's is centred and rotated) |
| Filters: blur family | ✅ | separable Gaussian; every filter in the Filter menu applies against the live document |
| Filters: sharpen, noise, distort, stylize, pixelate, render | ✅ | the whole library is reachable from the Filter menu |
| Filters: Photopea one-click and parity rows | ✅ | Blur ▸ Average, Blur, Blur More, Smart Blur; Sharpen ▸ Sharpen, Sharpen More, Sharpen Edges; Distort ▸ Displace (over a smart object: the generated dialog, map = the layer itself or a seeded cloud field; over a pixel layer: the W10-D external-map dialog, see the Filter Gallery / Displace / Vanishing Point row); Pixelate ▸ Facet, Fragment, Mezzotint; Stylize ▸ Extrude, Tiles, Trace Contour. Each opens the generated FilterDialog with live preview (`the_photopea_parity_filters_preview_through_their_dialogs`) |
| Filter ▸ Camera Raw / Lens Correction / Render ▸ Lighting Effects / Other ▸ HSB/HSL | 🔶 | W10-C. All four are generated `FilterDialog`s (live preview on the bounded proxy; OK is one undo step on a pixel layer; over a smart object each joins the smart-filter stack under its `FilterId` key and undoes in one — `menu_bridge::tests::w10c_filters_open_from_the_menu_and_land_as_one_step_or_a_smart_filter`). Camera Raw and Lens Correction are rows of the Filter menu itself (`FilterId::is_top_level`), Lighting Effects is under Render and HSB/HSL under Other (`menu::tests::w10c_filter_rows_sit_where_photopea_puts_them`). **Camera Raw** (`filters::camera_raw::CameraRaw`, straight linear light): white balance temperature/tint, exposure in stops (+1 doubles linear values), contrast about mid grey, highlights/shadows/whites/blacks and a four-region parametric tone curve (highlights, lights, darks, shadows) measured on perceptual lightness and applied as a luminance ratio so hue holds, texture and clarity (local contrast against a 2 px and a size-relative Gaussian), dehaze (dark-channel prior), vibrance and saturation; every control at 0 is the identity. **Lens Correction** (`filters::lens_correction::LensCorrection`, one inverse mapping sampled bilinearly through an Edges mode): distortion (+ applies barrel, pulling corners in; − pincushion), red/cyan and blue/yellow fringe (per-channel radial magnification against green), vignette amount and midpoint, vertical and horizontal perspective (keystone homography), angle, scale. **Lighting Effects** (`filters::lighting::LightingEffects`): one light of kind Spot / Point / Infinite with colour, intensity, focus, position, radius, angle, plus ambience and a texture channel (R/G/B/luminance, white-is-high, height) whose slopes tilt the surface normal; the light is placed by clicking or dragging on the dialog's preview (`dialogs::lighting::tests::dragging_on_the_lighting_preview_moves_the_light`). **HSB/HSL** (`filters::hsb::hsb_hsl`): input and output model RGB / HSB / HSL, converted on sRGB-encoded values as Photoshop's does; RGB→HSB→RGB and RGB→HSL→RGB round-trip within 1/255. Tests: `camera_raw::tests::*`, `lens_correction::tests::*`, `lighting::tests::*`, `hsb::tests::*` (filters), `filter_dialog::tests::w10c_*` (ui). Gaps: Camera Raw has the Basic panel and a parametric tone curve only — no point curve, HSL/Color Mixer, Detail, Optics, Geometry, Effects or Calibration tabs, and no before/after split; Lens Correction has the Custom tab only (no lens-profile database, no Auto Correction, no grid overlay or straighten tool on the image); Lighting Effects has one light per dialog (the engine takes up to 16), no gloss/metallic/exposure material properties, and its handle lives on the dialog's preview, not on the main canvas |
| Filter ▸ Liquify… / Edit ▸ Puppet Warp | 🔶 | W7-H. **Liquify** is a modal dialog with its own preview canvas (the layer copied at most 512 texels on the long side): tools Forward Warp, Reconstruct, Twirl Clockwise, Pucker, Bloat, Push Left, Freeze Mask and Thaw Mask; brush Size (document pixels), Pressure and Density; Reset. The engine is `filters::liquify::LiquifyField`, a backward displacement field (`f32` per cell, at most 512 cells on the long side) upsampled and applied with bilinear resampling; strokes compose, Reconstruct scales the field toward zero, the freeze mask holds content still. OK applies the field to the active pixel layer at full resolution (folded by the selection) as one undo step; Cancel writes nothing. **Puppet Warp** is a modal dialog over the layer's ink: `filters::puppet::PuppetMesh` triangulates every grid cell holding ink (alpha > 0, dilated one cell; density Fewer / Normal / More); a click adds a pin at the nearest vertex, dragging moves it, a secondary click removes it; Rigid mode is as-rigid-as-possible (local/global iteration, uniform weights), Linear is an inverse-distance blend; Enter commits one undo step. Tests: `liquify::tests::*`, `puppet::tests::*` (filters), `dialogs::liquify::tests::*`, `dialogs::puppet_warp::tests::*` (ui, including pointer drags on the drawn canvas), `menu_bridge::tests::w7h_liquify_opens_from_the_menu_and_lands_as_one_undo_step`, `menu_bridge::tests::w7h_puppet_warp_opens_from_the_menu_and_lands_as_one_undo_step`. Gaps: Liquify has no Twirl Counterclockwise, Smooth or Turbulence tool, no tablet pressure, no mesh save/load and no backdrop or show-mesh view options; Puppet Warp runs in a dialog rather than on the canvas, pins snap to mesh vertices, there is no pin depth, pin rotation or Expansion, and the mesh follows the ink on a grid rather than a traced outline; the tool, mode and density names are English literals from the engine, not catalogue keys |
| Filter ▸ Blur Gallery (Field / Iris / Tilt-Shift / Path / Spin) | 🔶 | W9-O. Filter ▸ Blur Gallery is a submenu of five rows (`MenuAction::BlurGallery(BlurGalleryKind)`), gated like Liquify (editable pixels). Each opens `ui::dialogs::BlurGalleryDialog` over the active layer (copied at most 512 texels on the long side; the preview is the same blur `scaled` to the copy) with draggable handles drawn on the preview: **Field** pins (a click on empty canvas adds one, a secondary click removes it; the Blur slider edits the selected pin); **Iris** centre, both radii (the first also turns the ellipse) and the focus handle; **Tilt-Shift** centre, focus line, transition line and a rotation handle, plus Blur, Distortion and Symmetric; **Path** points (a click appends one; Speed slider); **Spin** centre and radii, plus Angle and Feather. The engine is `filters::blur_gallery`: Field / Iris / Tilt-Shift are a spatially varying Gaussian (`variable_blur`: a per-pixel sigma interpolated between a stack of uniform blurs at 0, 1, 2, 4 … 256 px — true Gaussian to 8 px, three box passes above), Field amounts inverse-distance-squared interpolated between pins, Iris sharp inside the focus fraction and ramping to full blur at the rim, Tilt-Shift sharp within `focus` px of its line and ramping over `transition`; Tilt-Shift Distortion is a directional streak over the blurred side(s); Path is a line-integral motion blur along the inverse-distance-weighted path direction; Spin is a rotational blur about the centre inside its ellipse. OK applies at full resolution (folded by the selection) as one undo step; Cancel writes nothing; a zero blur is refused with a reason. Tests: `blur_gallery::tests::*` (filters: zero blur is the identity, Iris keeps the centre bit-exact and drops corner variance, Tilt-Shift's band stays bit-exact, Spin keeps the radial profile while averaging out an angular pattern, Path streaks along its path only), `dialogs::blur_gallery::tests::*` (ui, including pointer drags on the drawn canvas), `menu_bridge::tests::w9o_blur_gallery_opens_from_the_menu_and_lands_as_one_undo_step`. Gaps: handles live in a modal preview, not on the main canvas; one blur per dialog (Photopea stacks several blurs and several pins/irises in one session); no Bokeh / Light-range or Noise panels; Path Blur has straight segments only (no curve handles), one speed for the whole path and no per-end taper or strobe; Spin has one centre; slider labels are English literals (single words), not catalogue keys |
| Image ▸ Mode ▸ Lab / CMYK / Indexed | 🔶 | W7-D. All five W7-D modes (RGB, Grayscale, Lab, CMYK, Indexed) convert — and since W10-H Bitmap and Duotone make seven (row below) — each as one undo step (`editor_core::color_mode::convert_color_mode`: the mode flag plus one `PaintTiles` per layer). Tiles stay RGBA (Photopea's approach). **Lab** changes the flag only (8-bit sRGB round-trips). **CMYK** clamps every colour the documented naive ink model cannot print to its round trip (`color::cmyk`: sRGB approximations of SWOP cyan/magenta/yellow as subtractive filters, maximum GCR lowered by bisection where the inks cannot print the remainder, least-squares separation; the primaries' values are pinned in its docs and by `the_primaries_round_trip_to_the_documented_values`) — **not** an ICC press profile. **Indexed** opens Image ▸ Mode ▸ Indexed Color… (`ui::dialogs::IndexedColorDialog`: palette Exact / Web (216) / Uniform (never more entries than the count: a grey ramp below 8, otherwise an evenly spaced RGB box that fits, e.g. 16 -> 2x4x2, 256 -> 6x7x6 = 252) / Adaptive median cut, 2-256 colours, dither None / Floyd-Steinberg diffusion, run per 256-px tile) and quantises every layer onto one document palette (`color::quantize`). The raster exporter can write CMYK JPEG (baseline, Adobe APP14, inverted samples), CMYK TIFF (uncompressed, separated) and palette PNG-8 (`raster::export::ink`, chosen by `ExportPreset::ink`), and both app routes select it from the document's mode: File ▸ Export… (`jobs::run_file_export` and `OpenDocument::export_to` both through the one `doc::write_in_document_ink`, since W8-B) and Export As (`ExportPreset::for_color_mode`, which also drops a 16-bit row to 8 bits when it writes ink); GIF export of an indexed document writes its colours exactly. Since W8-B the palette PNG always writes (`ink::encode_indexed_png`): lossless at up to 256 RGBA colours, otherwise re-quantised with 1-bit alpha at `INDEXED_ALPHA_THRESHOLD` (one transparent entry, the opaque colours exact when they fit, else adaptive median cut) — so a soft stroke painted after the conversion no longer makes PNG export fail. Export As shows a note (`ExportAsDialog::ink_note`) when the selected format writes the document as RGB instead — always for Lab — and since W8-B File ▸ Export…'s status line says "a Lab document is written as RGB" (`doc::export_color_mode_note`). Info adds a `Lab` or `CMYK` row for a document in that mode (`ui::panels::navigator::mode_readout`) and the Color panel has a CMYK notation (C/M/Y/K percentages through the same ink model); since W8-B it follows the document's mode (`ColorState::follow_document_mode`: a Lab, CMYK or Grayscale document switches it to Lab, CMYK or the new Gray K% notation on a mode change; an RGB/Indexed one gives the user's notation back; a pick the user makes stands while the mode does). Since W8-B Image ▸ Adjustments ▸ Levels and Curves on a Lab document list Photoshop's Lab channels — Lightness, a, b, no composite row, opening on Lightness — with the L/a/b histograms (`AdjustmentDialog::set_lab_channels`, set by `dialog_host` from the document's mode; Levels gains the channel combo there), and the preview and the applied step run the mapping on those channels (`adjustments::LabTone`: linear sRGB to CIELAB, `L/100` and `(a+128)/255`, `(b+128)/255`, back to RGB; a stored composite, such as a plain Levels, acts on Lightness only, so it never casts a neutral; `menu_bridge::color_mode::run_lab_tone` in the `ApplyAdjustment` arm). View ▸ Proof Colors / Gamut Warning: `app_shell::presenter::ProofView` shows the `color::cmyk` round trip or paints the theme's Warning token where it moves a pixel by more than `GAMUT_THRESHOLD`, fed from the View flags every frame by `CanvasPresenter::read_view_settings` (the shell's one per-frame call). Tests: `color::cmyk::tests::*`, `color::quantize::tests::*`, `editor_core::color_mode::tests::*`, `raster::export::ink::tests::*`, `menu_bridge::color_mode::tests::*` (menu route, one undo step, the dialog, GIF round trip, File ▸ Export… of a CMYK document writing a four-component Adobe JPEG, Export As writing CMYK JPEG/TIFF and PNG-8, the Lab note), `chrome::tests::proof_colors_and_gamut_warning_tick_from_the_menu_and_change_the_canvas_texture`, `ui::view::docks::tests::the_color_panel_reads_cmyk_and_info_reads_the_documents_mode`, `ui::view::docks::tests::the_color_panel_reads_in_the_documents_mode_and_the_user_can_switch`, `menu_bridge::color_mode::tests::file_export_of_an_indexed_document_after_a_soft_stroke_writes_a_palette_png`, `menu_bridge::color_mode::tests::file_export_of_a_lab_document_says_in_the_status_that_it_writes_rgb`, `menu_bridge::color_mode::tests::the_shared_ink_branch_writes_cmyk_and_indexed_for_both_export_routes`, `menu_bridge::color_mode::tests::levels_on_a_lab_document_runs_on_lightness_through_the_dialog_and_menu`, `menu_bridge::color_mode::tests::curves_on_a_lab_document_moves_the_a_channel_and_rgb_documents_keep_green`, `menu_bridge::color_mode::tests::a_composite_levels_on_a_lab_document_keeps_the_grey_neutral`, `ui::dialogs::adjustment_dialog::tests::on_a_lab_document_*` (incl. `on_a_lab_document_the_default_channel_move_keeps_a_mid_grey_neutral`), `adjustments::lab_tone::tests::*`, `raster::export::tests::encoded_by_is_exactly_where_the_ink_encoder_writes`, `ui::dialogs::indexed_color::tests::*`, `ui::dialogs::export_as::tests::the_dialog_says_how_a_non_rgb_document_is_written_and_draws_it`. Gaps: no encoder writes Lab (a Lab document exports RGB, and both Export As and File ▸ Export… say so); since W10-H a Levels/Curves *adjustment layer* in a Lab document evaluates on L, a and b too (`compositor::PreparedAdjustment::new_in_mode` hands it to `adjustments::LabTone`, and the tile cache keys on it, so a mode change re-renders; `compositor::adjust::tests::a_curves_adjustment_layer_in_a_lab_document_runs_on_the_a_channel`); since W10-H Indexed Color flattens a layered document like Photoshop (the composite becomes one quantised "Background" layer in the same undo step, and the status line says the layers were flattened; `menu_bridge::color_mode::tests::indexed_color_flattens_a_layered_document_in_one_step_and_says_so`); the proof is an 8-bit display transform on the naive ink model, not an ICC soft proof |
| Filter ▸ Distort ▸ Kaleidoscope / Dents, Pixelate ▸ Shape Mosaic, Render ▸ Flame, Other ▸ Repeat / Color to Alpha / Dither / Particles, 3D ▸ Normal Map / Texture Dilation, Fourier ▸ Fourier / Inverse Fourier; gallery Distort and Stylize sets | 🔶 | W13-J. Twelve `FilterId`s (appended) in the submenus Photopea uses — two new groups, `FilterGroup::ThreeD` ("3D") and `FilterGroup::Fourier` — each a generated `FilterDialog` (live preview on the bounded proxy; OK is one undo step on a pixel layer, a smart filter under its `FilterId` key on a smart object). **Controls are Photopea's** — names, ranges and defaults read from its filter descriptors (`Kale`, `Dnts`, `ShMs`, `Rept`, `Ctoa`, `Dthr`, `Part`, `lightFilterGradient`, `Dila`) and dialog definitions: Kaleidoscope Mirrors 2-20 (6) and Angle 0-360; Dents Scale 0-200 (25), Refraction 0-200 (50), Turbulence 0-100 (10), with its descriptor-only Detail (10) and Random Seed (8438429) fixed as its dialog does not show them; Shape Mosaic Cell Size 2-200 px (12), Shape Square / Circle / Star, Spread XY / X / Y, Monochromatic, Invert; Repeat Scale 1-300 %, Row Shift -50-50 %, Space X / Y -99-200 %, Auto Color, Angle; Color to Alpha colour (black), Transparency Threshold (0 %), Opacity Threshold (100 %); Dither Palette Black & White / RGB 2x2x2 / 4x4x4 / 8x8x4, Method None / Floyd-Steinberg (default) / Bayer 4x4; Particles Count 0-100 % (10), Size 1-50 px (8), Depth (100 %), Brightness 10-1000 % (800), Color (white), Time 0-1, Turbulence, Blink (on), Fall, with the descriptor's seed; Normal Map = Photopea's Generate Normals: Blur 0-100 px, Scale 0-200 %, Invert, High / Medium / Low detail 0-100 % (100 each); Texture Dilation Crop 0-20 px (0), Radius 0-400 px (10). **Algorithms ported from Photopea's filter code**: Kaleidoscope's fold about the centre; Shape Mosaic (one red, green and blue shape a cell, sized by the channel mean in linear light, added over black, or CMY multiplied over white when inverted); Repeat (content trimmed by alpha and by its top-left colour, tiled on a lattice from the canvas origin, Auto Color / opaque-and-unspaced fills the gaps with that colour); Color to Alpha (Photopea's max-channel distance and un-mix, clamped); Dither (nearest palette colour in linear light, Floyd-Steinberg or a full-range 4x4 Bayer offset; Photopea's level spacing, so 8 levels top out at 252); Particles (Photopea's renderer and its multiply-with-carry generator bit for bit, blurred sprites per depth tenth, screened); Normal Map (three luma bands — squared, 8 px and 16 px box-Gaussian — weighted, 0.3/0.7 slopes, the tangents' cross product, flat = (127, 127, 255)); Texture Dilation (nearest covered pixel over 128/255 by an exact distance transform, within Crop + Radius, Crop shaving edges first). Dents keeps Photopea's structure (constant-length displacement, direction = noise x turbulence) on this crate's Perlin noise. Fourier / Inverse Fourier: the crate's own FFT (radix-2 plus Bluestein, `f64`, no dependency); log magnitude left, phase right, Hermitian self-mirror columns packed, rows shifted so the low frequencies sit mid-height. The Filter Gallery gains `GallerySet::Distort` (Diffuse Glow, Glass, Ocean Ripple) and `GallerySet::Stylize` (Glowing Edges), with Photopea's sliders, ranges and defaults, Glass with its seven textures in its order and Invert Texture (`filters::gallery_extra`). Tests: `filters` `fourier::tests::*` (the FFT against the textbook DFT at 11 lengths, `fourier_then_inverse_restores_the_image` within 1/255, `eight_bit_storage_loses_the_round_trip` = 7/255 through an 8-bit store), `distort_extra::tests::*` (Kaleidoscope's mirror quadrants, Dents' constant distance), `shape_mosaic::tests::*` (square side = sqrt of linear light, per-channel shapes), `render_extra::tests::*` (Photopea's generator against values from its source run in Node; one seeded particle lands where Photopea's placement formula puts it), `other_extra::tests::*` (Repeat's tiling, spacing, row shift, trim; Color to Alpha's formula; Bayer = exactly 48 of 256 white at encoded 50 % grey), `three_d::tests::*`, `gallery_extra::tests::*`, `gallery_sets::tests::*`; `ui` `menu::tests::w13j_filter_rows_sit_where_photopea_puts_them`, `dialogs::filter_dialog::tests::w13j_*` (every row's dialog runs its kernel; the dialogs carry Photopea's keys, ranges and defaults; inert-at-default controls come alive); `app-shell` `menu_bridge::tests::smart_filters::w13j_*` (every row opens its dialog through the dialog host, one undo step and a smart filter; Fourier then Inverse from the menu restores a 16-bit layer within 1/255; on an 8-bit document the chrome's status bar paints the 16 Bits/Channel note; the gallery's Distort / Stylize stack lands as one undo step) and the menu-bar walk `every_enabled_menu_item_really_does_something`. W13X-5, **Flame follows the path**: `filters::flame` renders along the Paths panel's current path (the selected path, else the Work Path, flattened in document pixels by `app_shell::flame_route` and handed over by `FilterParams::with_path`) with Photopea's eighteen `Flam` controls, ranges and defaults (Type of six, Length 20-1000 (140), Randomize Length, Width 5-600 (100), Angle, Interval 10-200 (100), Adapt Interval for Loops, Color (255, 110, 28), Quality of five (2: Low), Turbulent (50), Jag (0), Opacity (25), Lines 2-30 (10), Bottom (30), Style Normal / Violent / Flat, Shape Parallel / To the center / Spread / Oval / Pointing, Randomize Shape, Random Seed 0-100 (18)); with no path the menu row opens no dialog and the run answers Photopea's "Make a path first"; a smart filter stores the path under `path.` keys and keeps rendering along it after the Paths panel changes. Tests: `filters` `flame::tests::*` (every type paints near the path and nothing beyond its reach; no path is the refusal), `ui` `dialogs::filter_dialog::tests::w13x5_*`, `app_shell` `flame_route::tests::w13x5_*` (the menu and dialog route over a Paths-panel path, the refusal; the stored smart filter, re-run by the application's runner after the Paths panel holds another path, still burns along the path it was made with and not along the new one). Gaps: Flame's renderer is this build's, not a port of Photopea's (the controls mean what their names say; the flames do not look like Photopea's); a Flame dialog opened with no document behind it previews over a stand-in loop. Dents' noise, Glass's textures and the four gallery effects' pictures are this build's, not Photopea's (the gallery kernels are not ported); Repeat samples its copies bilinearly and Dither keeps the layer's alpha (Photopea's Dither output is opaque); Particles' sprite cache starts afresh each run (Photopea keeps it between runs). An 8-bit document's Fourier round trip is off by several levels (the spectrum is stored in 8 bits; the status bar says so and names 16 Bits/Channel; within 1/255 in a 16-bit document, tested through the menu; a 32-bit document takes the `f32` route, which is not tested for it); how lossy Photopea's own 8-bit round trip is was not measured. The menu calls the 3D normal filter "Normal Map"; Photopea calls it "Generate Normals" |
| Filter Gallery sets / Displace map / Vanishing Point | 🔶 | W10-D. **Filter Gallery**: besides the catalogue tab (every filter at its defaults), four tabs (six since W13-J: Distort and Stylize, row above) hold Photoshop's gallery sets as `filters::gallery_sets::GalleryEffect` — Artistic (Colored Pencil, Cutout, Dry Brush, Film Grain, Fresco, Neon Glow, Paint Daubs, Palette Knife, Plastic Wrap, Poster Edges, Rough Pastels, Smudge Stick, Sponge, Underpainting, Watercolor), Brush Strokes (Accented Edges, Angled Strokes, Crosshatch, Dark Strokes, Ink Outlines, Spatter, Sprayed Strokes, Sumi-e), Sketch (Bas Relief, Chalk & Charcoal, Charcoal, Chrome, Conte Crayon, Graphic Pen, Halftone Pattern, Note Paper, Photocopy, Plaster, Reticulation, Stamp, Torn Edges, Water Paper) and Texture (Craquelure, Grain, Mosaic Tiles, Patchwork, Stained Glass, Texturizer); each is a deterministic operation documented on its function (hashed/Perlin noise with a fixed per-effect seed) with Photoshop's sliders. A click puts an effect into the selected entry of the effect list (`GalleryStack`); New / Delete / Up / Down and a per-entry eye edit the list; the preview is the list over a 256-px crop at 100%. OK with a non-empty list parks a `FilterGallerySpec` for the `FilterGallery` arm, which applies the whole list to the full-resolution layer (folded by the selection) as ONE undo step; a catalogue pick still runs that filter's own invocation. **Displace** over a pixel layer opens `ui::dialogs::DisplaceMapDialog`: the map is any open document (flattened composite; other documents listed first) or an image file (Load…, decoded by `raster::decode_path`), read as stored 8-bit values with 128 = no shift (`filters::displace::encoded_map`); Horizontal / Vertical scale in pixels, Stretch to Fit / Tile, Wrap Around / Repeat Edge Pixels; OK is one undo step. **Filter ▸ Vanishing Point…** (`ui::dialogs::VanishingPointDialog`, engine `filters::vanishing_point`): a four-corner plane with draggable corner handles and an 8x8 grid drawn through the plane's homography (an invalid, bow-tie or collapsed plane is outlined in the danger colour and edits nothing); Clone mode is a perspective clone stamp (Alt+click sets the source, clicks/drags paint aligned dabs measured on the plane); Paste mode lays the in-process clipboard (Edit ▸ Copy) into the plane centred on the click; Undo takes back the last edit; OK applies the session as ONE undo step. Tests: `gallery_sets::tests::*`, `displace::tests::*`, `vanishing_point::tests::*` (filters: every effect changes a test gradient and is deterministic, a zero map is the identity, a known map shifts pixels, a paste lands with the plane's homography at its corners), `dialogs::filter_gallery::tests::*`, `dialogs::displace_map::tests::*`, `dialogs::vanishing_point::tests::*` (ui, incl. a click on a drawn effect tile and a drag of a drawn plane corner), `menu_bridge::tests::w10d_the_gallery_effect_list_lands_as_one_undo_step`, `menu_bridge::tests::w10d_displace_reads_an_open_document_or_a_file_as_its_map`, `menu_bridge::tests::w10d_vanishing_point_pastes_through_the_planes_homography_as_one_undo_step`. Gaps: the gallery effects are this build's own deterministic renderings, not pixel matches of Photoshop's; Texturizer has four procedural textures and cannot load a texture file; Sketch colours are fixed black/white (sepia for Conte Crayon), not the foreground/background colours; the gallery list and preview crop are not remembered between sessions; Vanishing Point has one plane (no perpendicular child planes), edits in a modal preview rather than on the main canvas, has no brush/healing modes, no transform of a pasted image after it lands, and no Last Filter replay; the Displace dialog's map list is built when it opens; the new dialogs' labels are single English words or engine names, not catalogue keys |
| Filter ▸ Convert for Smart Filters / smart filters | ✅ | W7-E. Convert for Smart Filters converts the active layer to a smart object (greyed with `SMART_FILTERS_ALREADY` over one); a filter from the Filter menu's filter rows confirmed over a smart object is appended to `SmartObjectLayer::filters` (key, parameters, eye, opacity, blend mode) as one `SetLayerKind` undo step and the source tiles are never rewritten. The compositor renders the source over its whole extent and runs the stack bottom-up through the Filter dialogs' own table (`compositor::smart`, cached by source-tile + stack hash). The Layers panel draws a "Smart Filters" block under the layer — eye, delete, double-click re-opens the dialog at the stored parameters over the object's own pixels and the confirm replaces that entry — also when another layer was active (the double-click selects the object first; `arm_smart_filter_edit` / `filter_dialog_source` work from the requested layer, and `run_filter_invocation` targets only the ACTIVE smart object — an armed re-edit replaces its entry only when the arm names that very object, so a stale arm never lands a filter on another layer). The stack saves in `.rstudio`; a PSD export writes the stack as Photoshop's `filterFX` on the placed layer for the nineteen filters Photoshop has (W16-J; see Smart objects), and the filtered appearance as the layer's pixels for any other filter. Not yet: the Filter Gallery, Last Filter, Liquify and Puppet Warp stay greyed over a smart object (they need editable pixels; none of them joins the stack); mip levels > 0 filter at full-resolution parameters. W10-I: a filter's name drags — dropped on another filter's row it takes that place in the stack (`compositor::smart::move_filter`, one `SetLayerKind`), and the compositor renders the new order; each filter row has a blending-options button that opens a row under it with the filter's own blend mode (a combo of every `BlendMode`, one stack command) and opacity slider (an `Intent::EditLayerKind`, folded per drag by `Editor::apply_kind_edit`, so one drag is one undo step). W10-I, the **filter mask**: `SmartObjectLayer::filter_mask` (a `LayerMask`, serde-optional) is the filters' shared mask, its coverage stored under its own `MaskId` in the object's layer space; the compositor mixes the stack's result back toward the unfiltered source by its coverage (`compositor::smart::apply_stack_masked`, honouring enable / invert / density; the mask's tiles and flags are in the filtered-extent cache key and the tile compositor's key). It is painted through `editor_core::PixelTarget::FilterMask` (appended last): `EditTargetKind::FilterMask`, `Editor::edit_target_filter_mask`, the shared tool resolver pins it and `ToolContext::paint_filter_mask` makes the painting tools address it, mapped like the layer's content. Layer ▸ Smart Filter ▸ Add Filter Mask (white over every source tile, aims painting at it) / Edit Filter Mask / Disable-Enable Filter Mask / Delete Filter Mask, each one undo step and greyed with the reason (`NO_SMART_FILTERS`, `FILTER_MASK_EXISTS`, `NO_FILTER_MASK`); the Layers panel's Smart Filters row carries an Add Filter Mask button or the mask's thumbnail well (the application's coverage thumbnail, `Workspace::filter_mask_thumbs`, framed while it is the edit target) with an eye and a delete button, raising the same rows. Duplicate Layer and New Smart Object via Copy re-key the copy's filter mask; Image Size resamples it. Tests: W10-I `menu_bridge::layer_extras::tests::{the_filter_mask_row_adds_a_paintable_mask_that_limits_the_smart_filters (chrome frames: the row's add button, a brush stroke through `ToolPointer`, the composite, the uploaded thumbnail, the eye and the trash, undo), a_copied_smart_object_gets_its_own_filter_mask, image_size_resamples_the_filter_mask_too, the_smart_filter_rows_grey_out_with_their_reasons, a_smart_filter_opacity_drag_is_one_undo_step (eight chrome frames of a slider drag, one undo step)}`, `smart::composite_tests::the_filter_mask_limits_the_smart_filters_to_where_it_is_white`. Filter-mask gaps: no feather or density control (the mask's own fields are honoured but not offered), no Ctrl+click load-as-selection on its well, a PSD export does not write it (the filtered look is written as pixels, as before), and a layer mask's own unlinked pose is ignored when painting the filter mask (it is painted in the object's layer space). Tests: `smart::composite_tests::*` (incl. W10-I `a_reordered_stack_renders_in_its_new_order`), `menu_bridge::tests::smart_filters::*` (incl. `double_clicking_a_non_active_smart_objects_filter_reopens_it_at_its_params`, a real Layers-panel double-click through `Chrome::ui`), `docks::tests::a_smart_objects_filters_are_rows_under_it_with_eye_delete_and_re_edit`, W10-I `docks::tests::dragging_a_smart_filter_onto_another_reorders_the_stack` and `docks::tests::a_smart_filters_blending_options_set_its_mode_and_opacity` (headless Layers-panel frames: a real drag, a combo pick, a slider press) |
| Smart objects | ✅ | placed raster sources with editable transforms, embedded + linked origins, replace-contents refresh, full-extent storage; verified through real routes (cards 044–050, interchange tests). W9-M: PSD import turns a `SoLd`/`PlLd` layer whose file is in the document's `lnk2`/`lnk3`/`lnkD` block (`psd::placed`, bounded: entry count, name length, one decode budget, per-entry sub-cursors) into a live smart object — the embedded file becomes its asset, its decoded pixels its source tiles, the four `Trnf` corners its transform; a missing, external or undecodable file opens as the layer's pixels, named in the report. PSD export writes an embedded, unfiltered smart object as `SoLd` + `PlLd` with its asset's own bytes in `lnk2` (`import::psd_live::tests::shape_smart_object_and_sixteen_bit_layers_survive_a_psd_round_trip`, `a_smart_object_whose_file_is_missing_opens_as_pixels_and_is_named`, `psd::placed::tests::*`). W10-I, Layer ▸ Smart Object (`menu_bridge::layer_extras`, `Editor::export_smart_object_contents` / `relink_smart_object`): **New Smart Object via Copy** makes a copy above the original over a fresh `AssetId` carrying a copy of the source record, so replacing the copy leaves the original's source and pixels alone; **Export Contents…** writes the embedded bytes byte for byte (a linked object's file is copied) to the picked path, suggesting the extension the bytes' signature names; **Convert to Layers** replaces the object in place with a group taking its name, visibility, opacity, blend, style, clipping and mask, holding a layered PSD source's own layers (tree, pixels and masks, posed by the object's transform) or else one pixel layer of the source at the object's transform (smart filters do not carry over, and the status says so); **Relink to File…** repoints a linked object (it stays linked, pixels re-read) and is greyed over an embedded one with the reason (`ui::menu::RELINK_EMBEDDED`, read from `MenuContext::smart_object_linked`); each is one undo step. Tests: `menu_bridge::layer_extras::tests::{new_smart_object_via_copy_does_not_share_the_source, export_contents_writes_the_embedded_source_to_the_picked_file, relink_to_file_repoints_a_linked_object_and_refuses_an_embedded_one, convert_to_layers_unpacks_a_placed_image_into_a_group_in_place, convert_to_layers_unpacks_an_embedded_psds_layers}`, each clicked through the menu bar's `Chrome::menu_click`. The PSD test moves the object by (5, 3) first and checks every unpacked layer carries that pose and the composite is the PSD's picture moved by (5, 3). W16-J: a linked smart object exports as `SoLd` + a `liFE` entry in `lnkE` naming its file (native `originalPath` and a `file://` `fullPath`; no cached copy, date and size written as unknown) and imports linked again, read from that path; smart filters travel as Photoshop's `filterFX` descriptor in `SoLd` (`psd::placed::smart_filters`: Average, Blur, Blur More, Box Blur, Gaussian Blur, Motion Blur, Surface Blur, Sharpen, Sharpen More, Sharpen Edges, Unsharp Mask, Add Noise, Despeckle, Dust & Scratches, Median, Mosaic, High Pass, Maximum, Minimum, with each filter's switch, opacity and blend mode) and open live (`import::psd_live::w16_tests::{a_smart_object_with_a_gaussian_blur_round_trips_with_the_filter_live, a_linked_smart_object_round_trips_as_linked}`). Gaps: any other filter, a Wrap/Mirror edge setting or a smart-filter mask still exports as rendered pixels (named with the reason); thresholds, Motion Blur's angle and Add Noise's amount are stored on Photoshop's grid (whole levels, whole degrees, per cent); on import a linked file that cannot be read or a `filterFX` entry this build has no filter for opens as the layer's pixels (named); the `filterFX` layout follows Photopea's writer and has not been checked against a Photoshop-written file; Convert to Layers of a PSD source into a 16-bit document unpacks the composite as one layer; non-affine (`nonAffineTransform`) and warp placement is read as the affine of three corners |
| PSD import | 🔶 | RGB and greyscale files at 8, 16 or 32 bits per channel. W16-B: every Photoshop colour mode is read (`psd::ColorMode` gains Bitmap, Indexed, CMYK, Multichannel, Duotone and Lab, appended; only unassigned codes are refused by name) and decoded by `psd::colour_modes::to_working_rgb` with the app's own colour model (`app_shell::import::psd_colour_modes`): CMYK (inverted per Adobe) through `color::cmyk`, Lab (8/16-bit, offset-encoded) through `color::model`, Indexed through the 768-byte palette with resources 1046/1047 (a palette of any other length is a file error), 1-bit Bitmap unpacked (raw or RLE rows; a Bitmap file's layer section is skipped, as Photoshop keeps no layers in one), Duotone as its grey base printed through the ink record's colours and curves (`DuotoneRecord`, parsed on the `.ado` layout — not yet checked against a Photoshop-written file; an unreadable record opens as Grayscale with the reason, a colour-book ink with a stand-in colour, named), per layer and for the flat composite; the document opens in the matching mode (`DocumentMeta::color_mode`: CMYK, Lab, Indexed, Grayscale, Bitmap, Duotone). Multichannel has no document mode: its first three inks show as CMY in an RGB document (one ink as Grayscale) and the channels past them are left out and named. A 16-bit CMYK / Lab file is edited at 8-bit precision (named in the report). Tests: `psd` `colour_modes::tests::*`, `app-shell` `import::psd_colour_modes::tests::*` (synthetic files in each mode through `document_from_psd`). W11-A: adjustment layers (`nvrt` `levl` `curv` `brit` `hue2` `blnc` `blwh` `phfl` `mixr` `post` `thrs` `grdm` `selc` `expA` `vibA` `clrL`) open as live adjustment layers (`psd::adjustments::decode`; see the Adjustment layers row). W9-B: `SoCo` / `GdFl` / `PtFl` fill layers open as live fill layers. W9-M: vector shape layers (`vmsk`/`vsms` + `SoCo`/`vscg`/`GdFl`/`PtFl` + `vstk`) open as live shape layers, placed smart objects (`SoLd`/`PlLd` + `lnk2`/`lnk3`/`lnkD`) as live smart objects, a 16-bit file as a 16-bit document; groups, masks (with density/feather), blend modes, all four channel encodings, type layers as editable text styled from the text engine data (W9-C: per-run font via the FontSet, size, fill, tracking, faux bold/italic, underline/strikethrough and super/subscript — a later run that differs becomes a span; leading, caps, horizontal/vertical scale and baseline shift once per layer, from the first run; the first paragraph run's justification, indents and spacing; auto leading as a multiple of the paragraph's `AutoLeading` factor (1.2 when unstated); point/box frame; the `TySh` transform placed by Photoshop's anchor — point text's first baseline at its left end, centre or right end as aligned (measured by shaping the layer with the shared font library), box text by its `BoxBounds` corner; a PostScript name such as `ArialMT`, `TimesNewRomanPSMT` or `Montserrat-Bold` matches the installed family; a missing font is kept by name and its stand-in reported; a later run's different leading/caps/scale/baseline shift, manual kerning on any run and a later paragraph run that differs are named in the report, not applied; unreadable engine data falls back to default styling with the reason in the report; warp, ligature/anti-alias switches and other engine-data keys this build does not read are neither applied nor reported), the layer effects (W11-B: all ten kinds — drop shadow, inner shadow, outer glow, inner glow, bevel and emboss, satin, colour, gradient and pattern overlays, solid stroke — with shadow/glow/bevel contours: drop shadow, stroke, colour overlay and outer glow through `import_effects`' own decoders, the pattern overlay through `psd::pattern`, and the other five through `psd::effects::map_rest`, the mapping the `.asl` import also uses (repeated effects map only when their `...Multi` lists sit inside the `lfx2` descriptor; the separate `lmfx` block Photoshop CC writes for them is not read); W13-B: a gradient- or pattern-filled stroke (the pattern resolved against the file's patterns), a gradient-filled glow and a gradient overlay's `Ofst` (a percentage of the layer's box, or the canvas) map too, through `psd::effects::import_effects_in`; a pattern stroke naming a pattern the file lacks, a pattern-filled glow and a noise gradient are named in the report), each layer's colour label (`lclr` 0..=7, W13-B), ICC retention, full layer extents; W11-C: guides (1032) as the document's guides, saved paths (2000–2997) and the work path (1025) as path layers (no-fill, no-stroke shape layers, the Paths panel's rows; the work path arrives as a saved path named "Work Path"), the merged image's named alpha channels (1006/1045) as saved selections, and the user and layer slices (1050: version 6, and versions 7-8 as a descriptor whose `layerGenerated` origin reads as a layer slice, tested on a descriptor this build writes (`psd` `resource::tests::a_descriptor_1050_resource_yields_its_user_and_layer_slices`), not on a Photoshop file; auto-generated fill slices skipped) as `Document::slices`, which `slices_export::restore_saved_slices` loads into the Slice tools' store (`psd::resource`, bounded parsers; `import::psd_resources`, `import::psd_resources::tests::psd_slices_reach_the_slice_store_and_edits_are_written_back`); a per-layer fidelity report (`PsdNotes`) names what was read but did not map (payloads this build does not read, such as the text keys above, are not listed) — see `docs/PSD-THUMBNAIL-SUPPORT.md`. Gaps (each named in the report, `app-shell` `import.rs`): a 32-bit file opens as an 8-bit document (its pixels converted down, although the editor has 32 Bits/Channel since W10-H); a greyscale file opens as RGB; a linked (not embedded) or undecodable smart object opens as pixels; a pattern-filled glow, a noise gradient, Photo Filter version 3, a Color Lookup with no embedded `.cube` and Curves stored as 256-entry maps do not map (the separate `lmfx` repeated-effects block is neither read nor reported: `import.rs` never looks at the key); nothing here has been checked against a file Photoshop or Photopea wrote beyond the card-073 fixtures |
| PSD export | 🔶 | W16-B: Save as PSD writes a CMYK, Lab, Indexed or Grayscale document in its own mode (`psd::colour_modes::from_working_rgb`; CMYK inverted through `color::cmyk::rgb8_to_cmyk`, Lab offset-encoded, Grayscale as Rec. 601 luma, Indexed flat with the document's own colours as the palette — median cut past 256 — and a transparent index), so a CMYK or Lab file opened and saved comes back within one code for separations this build's ink model makes (an ink split it would not choose, e.g. rich black, is re-separated); a Bitmap document is written as Grayscale and a Duotone one as RGB with its inks applied (no 1-bit or duotone writer; both named in the save notes). Layered export verified by independent re-read (structure, masks, effects as editable lfx2 descriptors — W11-B: all ten kinds, inner shadow `IrSh`, inner glow `IrGl`, bevel and emboss `ebbl`, satin `ChFX` and gradient overlay `GrFl` included, with contours, each written as the inverse of its import mapping and re-read field for field (`psd` `effects::w11b_rest_effect_tests::*`, including `a_layer_with_all_ten_effects_reimports_all_ten`); W13-B: a gradient- or pattern-filled stroke, a gradient-filled glow, a gradient overlay's offset (`Ofst` as percentages of the written layer box, or the canvas for a ramp not aligned with the layer) and the extra instances of a repeated effect (`dropShadowMulti`, `innerShadowMulti`, `frameFXMulti`, `solidFillMulti`, `gradientFillMulti` inside `lfx2`) are written and re-read with equal parameters (`two_drop_shadows_export_as_a_multi_list_and_reimport_as_two`, `every_repeatable_kind_round_trips_its_extra_instances_through_a_psd`, `a_gradient_overlay_offset_round_trips_as_a_percentage_of_its_box`, `a_gradient_filled_stroke_round_trips_through_a_psd`, `a_pattern_filled_stroke_round_trips_through_a_psd_and_its_pattern_block`, `gradient_filled_outer_and_inner_glows_round_trip_through_a_psd`, and through the app's save + open `import::tests::repeated_gradient_pattern_and_offset_effects_survive_a_psd_save_and_reopen`); a pattern-filled glow (no Photoshop form), a pattern with no pixels and an offset on a layer with an empty box are named as not written, appearance-preserving text/shape/smart-object fallback pixels, merged preview matching the composite at ≤1/255). W9-M: a shape layer is written as a vector shape layer (`vmsk` path in canvas pixels through the layer transform, `SoCo` — or `GdFl`/`PtFl` for a gradient/pattern fill — `vstk` stroke with width/colour/opacity/cap/join/alignment/dash, `vogk` live rectangle for an axis-aligned rectangle, pixel-data-irrelevant flag) over its rendered pixels; an embedded smart object as `SoLd`+`PlLd`+`lnk2` (W16-J: a linked one with `lnkE`, smart filters as `filterFX`); a pattern overlay with pixels as a `patternFill` effect plus the `Patt` block; a 16-bit document as a 16-bit file (raster layers at their stored samples); document-level blocks are padded to four bytes so psd-tools finds the ones after an `Lr16` section. psd-tools 1.19.0 reads the W9-M fixture as a `shape` layer (1 path, 4 knots, `Rectangle` origination, stroke 2.0 round), a `smartobject` of kind `data` (`logo.png`, PNG bytes, transform box = the placed corners) and a 16-bit pixel layer whose samples equal the source codes. Gaps: a translucent solid fill (a `.psd` fill has no alpha), a stroke under a non-uniform transform, a path with an arc, a pattern-filled shape under any transform or a gradient-filled one under more than a translation keep the rendered-pixels fallback (named); rendered previews and the merged image of a 16-bit file are 8-bit values widened; NOT yet verified in Photoshop/Photopea (card 081's manual step is pending) and the editable text export (TySh, every style run written in the engine data since W9-C — the weight in a `Family-SemiBold`-style font name, proportional leading as auto leading with the paragraph factor, the transform at Photoshop's anchor; a weight between the named ones is written as the nearest and named in the report — and re-read by this build's own importer) has not been independently verified in Photoshop. W11-C: guides are written as 1032, path layers as saved-path resources from 2000 (not as layer records), saved selections as named alpha channels in the merged image (1006 + 1045; up to the 56-channel ceiling, 52 on an RGBA file: the ones past it are named in the save notes and the save goes ahead, `saved_selections_past_the_channel_ceiling_are_named_not_a_failed_save`), and a pixel mask's density/feather in its parameter block (`import::psd_resources::tests::*`, `psd` `resource::tests::*`); the document's slices as a version-6 1050 resource of user slices (name, URL, alt text); a no-fill, no-stroke shape layer that is hidden or carries a mask, effects, clipping, a non-Normal blend or reduced opacity/fill stays a layer record (`a_styled_empty_shape_layer_stays_a_layer_record`); guide locks, the Paths panel's unsaved Work Path and a slice's target/message/cell text are not written. W11-A: adjustment layers are written under their own keys (`psd::adjustments::encode`; Auto, Desaturate, Equalize, Shadows/Highlights, Replace Color, HDR Toning and Match Color have no `.psd` key and a setting the layout cannot spell is refused, each written — since W16-J — as a pixel layer holding its effect on the layers under it, never an empty layer, and named in a note: `import::psd_live::w16_tests::no_adjustment_exports_as_an_empty_layer` reopens each of the seven and compares the composite; Photoshop has no exact adjustment layer for any of them, so none is substituted). Layer colour labels are written as `lclr` (W13-B). Also not written live: a smart object with a filter outside the nineteen `filterFX` mappings, a Wrap/Mirror edge or a smart-filter mask (rendered pixels, named); a 32 Bits/Channel document is written as an 8-bit file (`psd_from_document` writes 16 bits only for a 16-bit document, and `raster::rgba8_view` clips and rounds the `f32` tiles), with no note — see `docs/PSD-THUMBNAIL-SUPPORT.md` |
| Channels panel | 🔶 | isolate, then paint, erase, fill, filter or bake an adjustment into one RGB component (`mask_paint_to_channel`); saved-selection rows load; thumbnails. W10-B: selecting a **layer-mask row** makes its layer active, aims painting at the mask and shows the mask alone in grayscale (the card-059 mask view); selecting a colour row puts the composite back (`selecting_a_mask_channel_shows_it_alone_and_targets_it`). A **saved-selection (alpha) row's eye** runs `MenuAction::EditAlphaChannel`: a hidden scratch layer whose mask is the saved coverage becomes active with the mask as the edit target, and the canvas shows it alone in grayscale (the composite and exports are unchanged, the layer is hidden); the eye again (`CloseAlphaChannel`) stores the painted coverage back into the saved selection (`Command::SetSavedSelection`, appended) and removes the scratch layer in one transaction: open, each paint and close are each one undo step, and undoing the close restores the saved selection's previous coverage (`menu_bridge::w10b_panel_tests::viewing_an_alpha_channel_shows_its_grayscale_on_the_canvas`, `menu_bridge::alpha_channel`). Close Alpha Channel is greyed with `ui::menu::NO_ALPHA_CHANNEL_OPEN` while no channel is open (`MenuContext::alpha_editing`; `close_alpha_channel_is_disabled_until_a_channel_is_open`). Gaps: no per-channel histogram; the scratch layer is listed in the Layers panel while the channel is open |
| Layer Comps, Tool Presets, Glyphs, Notes, Character / Paragraph Styles panels | 🔶 | W10-B, six `PanelId`s appended to `ALL` (Window menu). The document keeps comps, notes, styles and the style links in `layer_model::DocumentExtras` (append-only serde, omitted while empty), edited through `Command::SetDocumentExtras` built by `editor_core::extras`, so every panel gesture is one undo step. **Layer Comps**: New records every layer's visibility, transform, opacity, fill, blend mode and layer style; a row click applies (only differing properties, locks respected); Update, Delete, Previous / Next (wrapping). **Tool Presets**: New saves the active tool with its touched options (and a ramp tool's gradient); a row click resets the tool, restores the options and selects it (the options-bar intents); the list is kept in the preferences file (`Preferences::tool_presets`, lenient, synced once a frame by `Preferences::sync_panel_presets` like the brush presets: `prefs::tests::a_tool_preset_survives_a_prefs_save_and_load_and_applies`). **Glyphs**: the active text layer's font's characters among ~220 candidates (punctuation, Latin-1, quotes, dashes, currency, arrows, a few maths signs), kept only when the font's own face has the glyph, drawn from a per-family atlas rasterised by `text_engine`; a click raises `Intent::InsertGlyph`, which the shell performs (`menu_bridge::glyph_insert`): with a typing session open the character is typed at its caret into the draft and confirmed with the run; otherwise it is appended to the active text layer's text, one undo step (`a_glyph_clicked_in_the_panel_is_typed_into_the_live_session`, `shell::tests::a_glyph_pick_lands_at_the_live_sessions_caret_through_the_shell`). **Notes**: the **Note tool** (`ToolId::Note`, `tools::note::NoteTool`, last in the Eyedropper slot, I) pins a note where a click lands on the canvas (`ToolRequest::PlaceNote`, one undo step: `note_click_pins_a_note_on_the_document_in_one_undo_step`); New pins one at the view's centre; each row edits the text, centres the view on the pin, or deletes; the canvas overlay draws a numbered pin at every note while View > Extras is on (`canvas_extras::tests::notes_are_pinned_on_the_canvas_at_their_document_positions`); notes are saved with the document and never reach the composite or an export. The six panels' labels, buttons and hints are catalogue keys (`ui::strings::tr`); three names the panels create stay English literals (a new comp's default name `Layer Comp {n}`, a new note's starting text `Note`, the `Tool` fallback in a new tool preset's name). **Character / Paragraph Styles**: New records the active text layer's run (family, size, base style) or paragraph block; a row click puts the style on the active text layer and links it; Redefine takes the style from the active layer and restyles every layer wearing it in one step; Clear unlinks; Delete removes. Tests: `ui/tests/w10b_document_panels.rs` (headless panel frames: `a_layer_comp_restores_visibility_through_the_panel`, `a_tool_preset_restores_the_tool_and_its_options`, `clicking_a_glyph_inserts_it_into_the_active_text_layer`, `a_note_is_pinned_listed_and_deleted_through_the_panel`, `a_paragraph_style_change_updates_both_layers_that_use_it`, …), `menu_bridge::w10b_panel_tests::{a_note_survives_save_and_reopen_and_is_absent_from_the_export, a_paragraph_style_redefined_in_the_panel_restyles_two_layers_in_one_undo}`, `editor_core::extras::tests::*`. Gaps: a note's text is edited in the Notes panel (no popup on the canvas) and a pin cannot be dragged; no View > Show > Notes of its own; comps do not record layer-mask position or smart-filter state |
| Paths panel | ✅ | the Pen's uncommitted path is the work path (`the_pens_uncommitted_path_is_the_paths_panels_work_path`); save as a named path; Fill, Stroke, Load as Selection, Make Work Path from Selection, New, Delete (W4) |
| Colour management | ✅ | sRGB and Display P3 are real; an embedded ICC profile is carried, composites through its profile and re-tags on export — `a_tagged_image_composites_through_its_profile_and_retags_on_export` |
| Image ▸ Mode ▸ Bitmap / Duotone, Image ▸ Apply Image / Calculations | 🔶 | W10-H. **Bitmap…** (from Grayscale only; greyed with the reason otherwise) asks 50% Threshold / Pattern Dither (8x8 Bayer) / Diffusion Dither (Floyd-Steinberg, serpentine) / Halftone Screen (cell in pixels, angle, round / square / diamond / line dot) (`ui::dialogs::BitmapDialog`, `color::bitmap`); the composite over white becomes ONE opaque layer of pure 0/255 pixels, every old layer deleted, one undo step (1-bit semantics in RGBA tiles, at conversion only: nothing constrains later painting, so a brush on a Bitmap document lays grey and anti-aliased pixels). **Duotone…** (from Grayscale only) takes 1-4 inks, each with a colour and a 5-point curve (ink % at 0/25/50/75/100% tint) over a live black-to-white preview (`ui::dialogs::DuotoneDialog`, `color::duotone`: subtractive ink model, not an ICC press simulation); every grey is printed through the inks and **baked into the tiles** (the layers stay) — the inks cannot be re-edited afterwards and nothing constrains later painting. **Apply Image…** blends a source (any open document of the same size, its merged image or one layer, RGB or one channel, invert) into the active layer with any blend mode but Dissolve, an opacity, Preserve Transparency and an optional mask channel, limited by the selection; one undo step, 16-bit layers read and written at 16 bits. **Calculations…** blends one channel of Source 1 onto one of Source 2 (mode, opacity, mask) into a new alpha channel (a saved selection "Alpha N"), the selection, or a new grayscale document (`menu_bridge::apply_image`). Tests: `color::bitmap::tests::*`, `color::duotone::tests::*`, `editor_core::color_mode::tests::bitmap_and_duotone_are_known_pixel_modes_reached_from_grayscale`, `ui::dialogs::{bitmap,duotone,apply_image,calculations}::tests::*`, `menu_bridge::apply_image::tests::*`, `menu_bridge::color_mode::tests::bitmap_*` / `duotone_*`. Gaps: no Custom Pattern bitmap method and no output resolution (the halftone cell is in pixels); Bitmap and Duotone export as RGB; Duotone curves have five points, not Photoshop's thirteen; Apply Image has no Add/Subtract scale and offset |
| 32 bits per channel | 🔶 | W10-H. Image ▸ Mode ▸ 32 Bits/Channel (`ui::menu::ChannelDepth::ThirtyTwo`; RGB and Grayscale documents only, and a 32-bit document changes colour mode only after going to 16 or 8) converts every layer tile to `raster::PixelFormat::RgbaF32` in one undoable step (`app_shell::depth32::depth_conversion`, `raster::depth32`): 8/16 → 32 lossless, 32 → 16 clipped into 0..1 and rounded, 32 → 8 through 16 with the dither. Tiles are straight-alpha `f32` in the document's encoding (not linear light as in Photoshop) and keep values above 1.0. At `f32`: the compositor reads them (`compositor::w10h_float_tests`), every filter and adjustment run through `edit_active_pixels` reads, filters and writes `f32` without clipping (`depth32::edit_active_pixels_f32`), `.rstudio` saves and reopens them (`project_format::tiles::MAX_TILE_BYTES` is an f32 tile), and File ▸ Export… (the worker and `export_to`) writes a `.tif` as a 32-bit float TIFF of the linear composite (`depth32::write_float_tiff`, `raster::depth32::encode_tiff_rgbaf32`, the `image` crate's TIFF encoder — no new dependency). Image ▸ Image Rotation 180°/Flip Canvas and Layer ▸ Transform flips/180° (`menu_bridge::remap_all_layers`/`remap_active_layer`), Edit ▸ Clear, Edit ▸ Fill and Image ▸ Image Size (`depth32::resample_layer_f32_edits`) also read, move and write `f32` samples. Everything else reads an `f32` tile clipped through the shared 8/16-bit readers (`raster::rgba8_view`, `rgba16_samples`, `tile_alpha16`) and its 8/16-bit output is landed back as `f32` by the apply boundary (`depth32::fit_to_f32_document`, `raster::depth32::widen_over_rgbaf32`): a sample keeps its old `f32` value where the output equals it rounded and the old value is inside 0..1 (so within half an output code); an old value outside 0..1 is kept on a match only while a stroke of an in-place tool is applied (`depth32::in_place_tool`: Brush, Pencil, Color Replacement, the three erasers, Gradient, Paint Bucket, Pattern Fill, Dodge, Burn, Sponge, Red Eye) and only when the whole pixel matches — the boundary cannot tell a pixel the stroke left alone from one it painted to the clipped colour, so painting the clipped colour over HDR (white at 100% over a 3.0 highlight) leaves the HDR value in place and a float-TIFF export keeps it; under any other edit that lands 8/16-bit tiles (Free Transform, Patch, Content-Aware Move, Clone Stamp, Healing, Smudge, a menu command not listed above) it lands clipped, so an HDR value the edit covers is lost rather than left stale where moved content belongs. The stroke tools compute at 16 bits there (`ColorPatch::load_native` reads an f32 tile as 16-bit). Dragging a layer to another document refuses a deeper layer by its real depths (`Editor::duplicate_layer_into_document`); an 8/16-bit layer dropped into a 32-bit document lands as `f32`. Tests: `raster::depth32::tests::*`, `app_shell::depth32::tests::*` (menu conversion + undo byte-exact, 32 → 16 clipping, Filter ▸ Blur keeping HDR, a brush drag through the pointer route keeping untouched f32 pixels, File ▸ Export… writing a float TIFF with HDR, flips/180° moving every f32 value, the boundary clipping moved HDR outside a stroke, Clear and Image Size staying f32, Edit ▸ Fill keeping the unfilled HDR pixels, a white Brush stroke over 3.0 keeping the 3.0 (the limit above), the cross-document depth guard), `ui::menu::tests::thirty_two_bits_is_an_image_mode_row_for_rgb_and_grayscale`, `project_format::tiles::tests::a_32_bit_tile_saves_and_reopens_byte_for_byte`. W11-H: File ▸ Open of an EXR or HDR makes a 32-bit document, and File ▸ Export… to `.exr` (and an Export As EXR row at 100%) writes the float composite, values above 1.0 kept. Gaps: no HDR Toning on the way down (values above 1.0 clip), no 32-bit New Document, no File ▸ Open of a float TIFF into a 32-bit document, PSD export writes 8 bits, every Export As row but EXR at 100% writes the clipped 8/16-bit composite, an Export As TIFF row included (only File ▸ Export… to a `.tif` name writes the float TIFF: `jobs::run_file_export` calls `depth32::write_float_tiff` for any format, the Export As batch `jobs::run_export` only for an EXR row at scale 1), and so does every format other than TIFF and EXR, and the File Info window's depth row says 8-bit for a 32-bit document |
| 16-bit per channel | 🔶 | a user can work at 16 bits: New Document accepts 16 bits (RGBA16 base tiles), Image > Mode > 8/16 Bits/Channel converts every raster tile in one undoable step (8 -> 16 lossless, 16 -> 8 rounded), a tool stroke on a 16-bit layer lands as RGBA16 tiles keeping the untouched pixels' 16-bit codes, the compositor composites RGBA16 tiles in `f32`, a 16-bit document exports 16 bits to PNG/TIFF (`mode_sixteen_then_a_brush_stroke_then_a_png_export_round_trips_at_sixteen_bits`), and it saves and reopens as `.rstudio` with the same RGBA16 tiles (W5, `a_16_bit_document_saves_and_reopens_with_the_same_pixels`); since W7-C filters, adjustments, Fill, Clear, Free Transform (resampling modes and a floated selection), Edit > Transform's flips and turns, every Image > Image Rotation (90, 180, Arbitrary, flips), Image Size, Crop to Selection and Trim read and write a 16-bit layer at 16 bits (`brightness_on_a_sixteen_bit_document_is_not_rounded_through_eight_bits`, `image_size_half_then_double_on_a_sixteen_bit_ramp_keeps_every_code`, `free_transform_distort_on_a_sixteen_bit_layer_resamples_at_sixteen_bits`, `an_opened_sixteen_bit_png_free_transformed_by_a_sub_pixel_stays_sixteen_bit`, `image_rotation_ninety_on_a_sixteen_bit_document_moves_every_code`), and Canvas Size keeps every code; since W10-H the stroke tools (Brush, Pencil, Eraser and the other `StrokeTool` brushes) read the layer through `ColorPatch::load_native` and blend and write 16-bit dabs (`menu_bridge::color_mode::tests::one_percent_flow_strokes_on_a_sixteen_bit_layer_write_sixteen_bit_codes`), though Clone Stamp and the Healing Brush read their source rounded to 8 bits; the bucket, gradient and other non-stroke tools, Stroke, Apply Mask, Defringe, Layer via Copy/Cut, Grayscale and Content-Aware Fill still compute at 8-bit precision (P2.5b, see the gaps list) |
| Actions / recorded command replay | 🔶 | the Actions panel records, stops and replays (`recording_three_edits_replays_onto_a_second_document`); named actions show their steps and save to / load from `actions.json`, size-capped and written atomically (W5). W10-E: File ▸ Automate ▸ Batch… plays one over every image in a folder (see Batch export). **W13-E sets and `.atn`**: `asset_store::resources::atn` reads version-16 action sets (set, then actions, then steps, each step an event id (four-character code or string id) plus an action descriptor), bounded (counts, strings, descriptor depth), and writes them back (`write_then_parse_is_the_identity_and_rewrites_the_same_bytes` on a hand-built file); `atn::interpret` maps make layer, set selection (all / none / rectangle), inverse, Fill, Image Size, Canvas Size, Invert, Desaturate, Equalize, Brightness/Contrast, Gaussian Blur, Unsharp Mask, Median, rotate and flip of the canvas or layer, and Save onto the menu / dialog routes (`app_shell::atn_play`); any other step says why. File ▸ Open of a `.atn` adds a set (`Editor::import_action_set`); `NamedAction` gained `set`, `imported` (parametric steps) and `off` (unchecked steps). Play reports unchecked, skipped (no equivalent) and failed steps by name (`PlayReport`); the flat panel lists an unmapped step as "skipped on play: why" and an unchecked one as "(unchecked)". The Actions dock draws the published `ActionSetsView` under its flat list (`ui::panels::actions::draw_sets`, called from `actions_body`): set rows (click selects; the twirl folds), their actions, and each step with a check box and its skip reason; the footer has New set, Rename set (an in-place field), Record into set, Export set as .atn, Load .atn, Play from step (the selected step row) and Delete set. Each click queues a `ui::panels::actions::ActionSetRequest` handled in `sync_actions_panel`; set names are kept in the presets file. Tests: `atn::tests::*` (asset-store), `actions_library::atn_route_tests::{an_opened_atn_plays_its_mapped_steps_on_the_document_and_reports_the_unmapped_one, sets_are_made_renamed_and_deleted_and_an_action_plays_from_a_step, an_exported_set_reads_back_with_its_steps}` (File ▸ Open, then Play taken by the real chrome frame; the set tests open Window ▸ Actions through the menu bridge and click the drawn controls with pointer events, typing the new name into the rename field). **W16-H**: `atn::interpret` (`asset_store::resources::atn_more`) also maps Levels, Curves, Hue/Saturation, Color Balance, Black & White, Vibrance, Exposure, Threshold, Posterize, Gradient Map, Photo Filter, Channel Mixer (with their descriptor parameters), Crop, Trim, Convert Mode / bit depth, Free Transform (offset, scale, angle, skew, reference point) of the layer or selection and the Move tool's offset, Duplicate, Delete, Merge Down / Visible, Flatten, New Group, Group Layers, layer name / opacity / blend, Show / Hide, Arrange, Select layer (name, index, forward / backward / front / back), Color Range (sampled Lab range), Feather, Expand, Contract, Border, Smooth, Stroke, Copy, Copy Merged, Paste, Cut, Layer via Copy / Cut, Add Noise, Motion Blur, High Pass, Smart Sharpen, and Save As / Export (the export dialog); each plays through its menu or dialog route (`app_shell::atn_play_more`), and `StepOp::to_step` writes each back as its Photoshop descriptor (`atn::w16_tests::every_w16_operation_exports_and_parses_back_as_itself`). Export (`app_shell::atn_record::steps_for`) writes every recorded command that carries its parameters: new layer / group, delete, layer name / opacity / blend, visibility, select all / none / rectangle, free transform (decomposed), Image Size, Canvas Size / Crop (a crop rectangle), mode and depth, and by undo label Invert, Desaturate, Equalize, flips, 180° and layer turns, merges, Flatten, Group, Duplicate, Paste, Layer via Copy / Cut. Tests: `actions_library::atn_w16_route_tests::{a_photoshop_levels_image_size_blur_duplicate_action_plays_and_changes_the_document, a_recording_of_the_common_edits_exports_every_step_and_replays_to_the_same_document, a_free_transform_step_scales_the_layer_about_its_centre, a_recorded_transform_decomposes_into_the_same_matrix}`. Gaps: a recorded pixel step (a brush stroke, a filter or adjustment applied through its dialog) keeps its pixels, not its settings, so it replays as pixels and is left out of an export (counted), as are a mask / lasso selection, a layer restack and a 90° canvas turn (the command keeps no direction); on import, Hue/Saturation of one colour range, Levels / Curves from a preset file, Color Range presets and an angled crop are skipped with the reason, as is every event not listed; Batch plays an action's recorded edits only |
| File ▸ Script (Photoshop-DOM JavaScript) | 🔶 | W13-K: File ▸ Script… (`MenuAction::Script`) opens `ui::dialogs::ScriptDialog` (code box, Run, output log), drawn by `menu_bridge::draw`; Run parks the source and clicks the Script row through the chrome, so the run is `menu_bridge::perform` → `app_shell::script::run`. The engine is `boa_engine` 0.21 (Unlicense OR MIT, pure Rust, default features off) on its own thread; its one native function is a channel to `script::host`, which answers each DOM call on the interaction thread through the existing routes (`Editor::dispatch` for New / Duplicate Layer, Open and Export; `Editor::apply_command`; `menu_bridge::perform` for Flatten, Merge Visible / Down, Inverse, Clear; `menu_bridge::fill_selection_with` for `selection.fill`). DOM subset (`script/prelude.js`): `app.documents` (+`add`), `activeDocument`, `foregroundColor` / `backgroundColor`, `open` (File ▸ Open's picker, W13X-6: prefilled with the script's file name), `SolidColor` / `RGBColor.hexValue`; `Document` `name` `width` `height` `resolution` (always 72, the document stores none) `layers` `artLayers` `layerSets` (+`add`, `getByName`) `activeLayer` `selection` `resizeImage` `resizeCanvas` `crop` `flatten` `mergeVisibleLayers` `saveAs` (File ▸ Export…'s picker, W13X-6: opened in the document's export folder with the script's file name); `ArtLayer` / `LayerSet` `name` `opacity` `fillOpacity` `visible` `blendMode` `kind` (set `LayerKind.TEXT`) `bounds` `parent` `translate` `resize` `rotate` `duplicate` `remove` `merge`, `textItem` `contents` `size` `font` `color` `position`; `Selection` `select` (polygon, `SelectionType`) `selectAll` `deselect` `invert` `fill` `clear` `bounds`; `alert`, `console.log`, `$.writeln`. A run is ONE history step per document (`script::host::Host::finish` undoes the run's steps and re-applies them as one `Transaction` labelled `Script`; a saved package's journal is diverted to a side file during the run and gets that one transaction). Limits: a step budget over `Script::evaluate_async_with_budget`, a wall-clock limit, `boa`'s loop-iteration and recursion limits. A `.jsx` / `.js` through File ▸ Open, a drop or Open Recent opens in the window and runs only on Run. Tests: `script::tests::*` (the window route and one Undo, the budget stop, a syntax error, an uncaught error, the DOM reads/writes, the `.jsx` route, the journal), `dialogs::script::tests::*`. Gaps: no `executeAction` / action descriptors, no adjustments or filters from a script, no `doc.close()`, no reads or writes of a path the script chooses (by design: a name it passes only prefills the picker's file-name box, last path component only — `script::tests::a_scripts_file_names_are_suggested_to_the_open_and_export_pickers`), `select` refuses a `feather`, `resizeCanvas` fails on a document holding a text layer (the canvas-size engine refuses text layers), a single long builtin call is not interrupted. W16-K: the window lists three demos at its top (Hello; Grid, Photopea's 5 x 6 grid of fading copies; Rotate, every layer named "rotate"), a click loads one; **Save** keeps the code box under a name as `<config>/scripts/<name>.jsx`, listed at the window's bottom (a click loads it, its bin deletes it) - Photopea keeps its saved scripts in the browser's Local Storage (`menu_bridge::w16k_tests::the_script_window_runs_a_demo_and_saves_and_deletes_a_script`) |
| Batch export | ✅ | Export As runs several presets in one run (`raster::export::export_batch_to_dir`); Export Layers and Export Slices write one file per layer / slice. W10-E: File ▸ Automate ▸ Batch… (`ui::dialogs::BatchDialog`, `app_shell::automate`) opens every image directly in a source folder (File ▸ Open's extensions plus `.psd`, name order), replays the chosen Action on it (the Actions panel's replay: steps retargeted by stack position, tile bytes re-inserted), flattens it and writes it to a destination folder in a chosen format / JPEG quality / scale; Convert Formats… does the same with no Action. Both run on the job worker (`jobs::Spawner`) with "n of N files" on the status line (polled from `menu_bridge::context`); a file that fails is skipped and listed in `batch-errors.txt`. Tests: `w10e_tests::batch_over_three_files_writes_three_outputs` (menu click, dialog, Enter, a recorded Invert adjustment layer inverts all three), `w10e_tests::convert_formats_runs_on_the_worker_and_logs_what_failed` (a real worker thread, progress, half-size JPEGs, the error log), `ui::dialogs::batch::tests::*`. Gaps: sub-folders are not entered; no "override Open / Save As commands" options; outputs are 8-bit |
| Image ▸ Variables (data sets) | 🔶 | W10-E: Define… binds text-replacement variables to text layers and visibility variables to any layer; Data Sets… imports a CSV (RFC 4180 quoting; header row = variable names, each further row one set; an unknown name or a ragged row is refused), previews a set on the document as one undo step (`SetLayerKind` text replacement, `SetLayerProperties` visibility) and exports one flattened file per set, `<document>_<set>.<ext>`, from a staged copy of the document (`app_shell::variables`, `ui::dialogs::variables`). Tests: `w10e_tests::a_variables_csv_with_two_rows_exports_two_files_with_the_substituted_text` (each file equals the composite of the document with that set's text and visibility), `ui::dialogs::variables::tests::*`. Gaps: no pixel-replacement variables; replaced text drops the layer's styled ranges and kerning (the base style runs over the new text); definitions and data sets are kept per open document for the session — a `.rstudio` save does not store them |
| File ▸ Export ▸ Color Lookup Tables | 🔶 | W10-E: the visible adjustment layers (bottom to top, each mixed in by opacity x fill, through `compositor::PreparedAdjustment` — the compositor's own evaluation) sampled on a 17 / 33 / 65-point identity lattice and written as an Adobe/Resolve `.cube` (`app_shell::file_extras::{stack_lut, cube_text}`). Tests: `w10e_tests::an_exported_identity_stack_lut_is_identity` (an identity Levels exports the identity, a hidden Invert is left out, a shown one exports 1 - x; read back through `adjustments::Lut3d::parse_cube`). Gaps: `.cube` only (no `.3dl`, `.csp`, ICC device link); layer masks, clipping, non-Normal blend modes and spatial adjustments cannot be expressed in a LUT and are not |
| File ▸ Export ▸ PDF | ✅ | W10-E: a raster PDF (one image XObject, no vector content) of the flattened composite on an image-size page at a chosen ppi, A4 or US Letter (fitted, centred, optionally landscape), File Info as the `/Info` dictionary (`raster::pdf::encode_pdf_on_page`, `ui::dialogs::ExportPdfDialog`). Tests: `w10e_tests::pdf_export_parses` (xref rows land on their objects, trailer, A4 media box, title, bytes equal to the encoder's page), `raster::pdf::tests::a_pdf_on_an_a4_page_with_info_parses_back_to_its_page_image_and_info` (the image stream inflates to the samples) |
| Image ▸ Vectorize Bitmap | 🔶 | W10-E: the active pixel layer traced into one shape layer per colour, one undo step, source optionally hidden (`app_shell::vectorize`, `vector::trace`: median-cut posterize to N colours, a potrace-style colour stack so the shapes tile the image with no hairline gaps, crack-following contours with holes, Schneider least-squares cubic fitting with corner detection — documented in the module header). Tests: `w10e_tests::vectorizing_a_two_colour_image_yields_two_shape_layers_whose_union_covers_the_image`, `vector::trace::tests::*`. Gaps: no speckle filter / minimum area; colours are the posterized means, not a user palette |
| Edit ▸ Assign / Convert to Profile, Image ▸ Reduce Colors / Wavelet Decompose, View ▸ Clear Slices / Slices from Guides | ✅ | W13-F (`app_shell::menu_bridge::menu_w13f`). **Assign Profile ▸** sRGB IEC61966-2.1 / Adobe RGB (1998) / Display P3 / ProPhoto RGB / Profile from File… (an RGB `.icc`/`.icm`, checked by `asset_store::resources::icc::parse`) re-tags the document with `editor_core::Command::SetMetaColorSpace` (appended, serde-additive; its inverse carries the old tag, its dirty reach is the whole canvas), one undo step, no pixel number changed, so the canvas shows the numbers differently: W13X-2 colour-manages the canvas (`app_shell::presenter::DisplayTransform`, applied in `CanvasPresenter::composite_masked` to every full and per-tile upload, converts the document's profile to the sRGB texture, relative colorimetric, clipped; `CanvasPresenter::follow_display_space` re-sends the whole canvas when the tag moves), as Photopea displays a tagged file. **Convert to Profile…** opens `ui::dialogs::w13f::ConvertProfileDialog` in the dialog host (`ActiveDialog::W13f`): the destination (the document's own built-in profile greyed with its reason), the rendering intent and black point compensation; the confirmed spec is parked for the `ConvertToProfile` arm, which rewrites every pixel layer (8 or 16 bits) through linear light and appends `SetMetaColorSpace` to the same `Transaction`, so one undo restores the numbers and the tag together, and the canvas keeps showing the same colours: Adobe RGB to sRGB within 2 codes on every channel; sRGB to Adobe RGB within 2 codes on at least 97% of channels and further (up to what one 8-bit Adobe RGB code spans on screen, 7 codes seen) on saturated pixels near sRGB's gamut edge; ProPhoto RGB and Display P3 are not measured. A tag this engine cannot transform (an ICC profile that is not a matrix-shaper: LUT, Lab-PCS, gray; `ColorSpace::is_transform_supported` false) gets no display conversion: the canvas and Pattern Preview show its numbers. Perceptual and Saturation run as Relative Colorimetric (matrix-shaper profiles carry no tables for them, the ICC fallback; the dialog says so); Absolute Colorimetric scales the D50 PCS value by the source/destination `wtpt` media whites (`color::icc::absolute_colorimetric`; the built-in D65 spaces count as D65, `MatrixShaper::media_white` reads `wtpt`); black point compensation maps the source's black onto the destination's in linear light and is off under Absolute; colours outside the target clip. Adobe RGB (wtpt D65) and ProPhoto (wtpt D50) are spec-conformant v2.1 matrix-shaper profiles written by `color::icc::matrix_shaper_profile`. W13-F also fixed `MatrixShaper::parse` (`acsp` at byte 36; `para` parameters as s15Fixed16 with the ICC.1:2010 table 68 counts and kind 4 formula). **Reduce Colors…** (`ReduceColorsDialog`: palette Exact / Web / Uniform / Adaptive, any count 2-256, no dither or diffusion) maps the active pixel layer onto the palette (`color::quantize`), limited by the selection, one undo step, Fade-able. **Wavelet Decompose…** (`WaveletDialog`: 2-7 scales) hides the active layer and inserts a Normal residual and N Linear Light detail layers above it, one undo step; each detail code is solved against the compositor's own linear-light arithmetic, so the stack recomposites to the source. **Slices from Guides** (View row, and a button on the Slice tool's options bar, `ui::view::toolbar`, raising the same action) cuts the canvas along the guides into one slice per cell; **Clear Slices** empties the set; both one undo step. Every new menu label, refusal, dialog string and status line resolves through `ui::strings::tr` (`ui.w13f.*` keys). Tests (app-shell, through the menu bar, `Chrome::menu_click`, the dialog host's own `ui` and whole `Chrome::ui` frames): `menu_w13f::tests::every_row_sits_in_its_menu_and_says_why_when_it_cannot_run`, `assign_profile_retags_in_one_undo_step_and_leaves_every_number_alone` (Undo restores the tag), `convert_to_profile_asks_then_rewrites_numbers_and_tag_in_one_undo_step` (nearest codes; sRGB green lands on (144, 255, 60); one Undo restores numbers and tag), `the_rendering_intent_and_black_point_change_the_numbers_as_the_icc_defines`, `a_profile_from_a_file_is_assigned_and_converted_to`, `reduce_colors_asks_then_puts_the_layer_on_that_palette_in_one_undo_step` (5 colours, a count no fixed row offered), `wavelet_decompose_asks_then_splits_the_layer_into_a_stack_that_recomposites_to_it` (2, 5, 7, 3 and 6 scales, within 1/255), `clear_slices_and_slices_from_guides_replace_the_set_one_undo_step_each`, `the_slice_tools_options_bar_cuts_slices_from_the_guides`, `the_canvas_shows_the_documents_profile_assign_changes_it_and_convert_keeps_it` (Assign moves the presented bytes to the nearest code of the exact Adobe RGB to sRGB transform with no number changed; Convert to sRGB keeps them within 2 codes; Convert back to Adobe RGB holds each pixel to the step its new 8-bit numbers allow and at least 97% of channels within 2 codes), `pattern_preview_on_an_adobe_rgb_document_draws_the_canvas_bytes` (every tile edge, the whole copy and the GPU texture readback equal the presented bytes), `a_profile_the_engine_cannot_transform_shows_the_numbers_on_canvas_and_in_pattern_preview`; `color::icc::tests::w13f_*`; `ui::dialogs::w13f::tests::*`. Limits: Reduce Colors and Wavelet Decompose need an 8-bit RGB pixel layer and Wavelet an sRGB-tagged document; the recomposition is exact only on opaque pixels of a Normal, 100%, unmasked source; vector, text and fill layer colours are not converted; the display conversion targets sRGB, not the monitor's own profile; the Navigator / History whole-canvas preview (`chrome::composite_preview`) is not colour-managed yet, so it disagrees with the canvas on a non-sRGB document |
| View ▸ Pattern Preview | ✅ | W13-F: a View flag (`ui::ViewFlag::PatternPreview`, appended), ticked in the View menu; `menu_w13f::paint_pattern_preview`, called by `Chrome::ui` before the extras on their layer, draws the document's composite (through the canvas's own `presenter::DisplayTransform`, so each copy holds the canvas texture's bytes; sampled to at most 2048 px on its long edge, rebuilt when the content revision or the profile moves; the canvas's view-only passes, Proof Colors, channel eyes and mask view, are not applied to the copies) in every grid cell around the canvas through the document camera, up to 12 copies out, clipped to the canvas area. Test: `menu_w13f::tests::pattern_preview_tiles_the_canvas_around_itself_and_unticks` (whole chrome frames: the copies sit exactly one document away on each side, none covers the canvas, the picture equals the composite, and unticking removes them), `pattern_preview_on_an_adobe_rgb_document_draws_the_canvas_bytes` (an Adobe RGB-tagged document: every tile-edge row and column, and the whole copy, equal the presenter's upload bytes; with a GPU adapter, the synced texture's readback too, and an Assign back to sRGB re-sends the whole texture with no dirty tile) |
| File Info (XMP) and EXIF on export | 🔶 | W10-E: File ▸ File Info… edits title, author, description, keywords and copyright (`ui::dialogs::FileInfoDialog`); Export As writes them as an XMP packet into PNG (`iTXt`), JPEG (`APP1`) and TIFF (tag 700) and carries the source file's EXIF (a JPEG's `APP1` or a PNG's `eXIf`) into JPEG exports, behind a Metadata checkbox (`raster::metadata`, `ExportPreset::embedded`); a document opened from a file with XMP starts from it. Tests: `w10e_tests::the_xmp_title_round_trips_through_png_and_exif_is_kept_on_jpeg_export`, `raster::metadata::tests::*`. Gaps: only these five Dublin Core fields; the fields are kept per open document for the session (a `.rstudio` save does not store them); EXIF is carried unchanged (orientation / size tags are not rewritten) and only into JPEG |
| Brush / gradient / layer-style editors | 🔶 | the Brush editor, the Gradient editor (stops paint) and the Layer Style dialog (ten effects plus a Blending Options page) are hosted dialogs; all ten effects render in the compositor (`compositor::effects::render`). W7-B: a pattern fill carries its own pixels (`layer_model::PatternTile`, content-hashed into the tile cache key), so Pattern Overlay and pattern-filled glows/strokes draw (blend mode, opacity, scale, angle, offset, link with layer), and the document saves and reopens with the pattern; the dialog's Pattern Overlay page picks one of the Define Pattern presets and shows a swatch (`compositor/src/pattern_tests.rs`, `app-shell/src/doc_pattern_tests.rs`). W8-D: PSD import reads the `Patt`/`Pat2`/`Pat3` pattern blocks (`psd::pattern`, bounded; 8-bit RGB/greyscale, raw/RLE) and maps pattern overlays onto this effect (W9-B: a Pattern fill layer now opens as a live pattern fill layer instead — see Layers ▸ Fill layers), and the fidelity report stops listing them (`import::w8d_pattern_tests`). W9-M: PSD export writes a pattern overlay whose pattern has pixels as a `patternFill` effect (blend, opacity, scale, angle, phase, link) with the pattern in the `Patt` block, and it re-imports as the same overlay (`import::psd_live::tests::a_pattern_overlay_travels_with_its_pattern`, `effects::w9m_pattern_export_tests::*`). Gaps: a pattern-filled glow or stroke is still not written (named); a pattern in another image mode (indexed, CMYK, Lab ...), at 16/32-bit depth or ZIP-compressed is refused (noted in the import report), and an overlay or fill naming it, or naming a pattern the file lacks, stays unmapped; the dialog has no control that sets a glow or stroke fill to a pattern (such a fill renders when a document or style carries one). The Layer Style dialog's own preview is a labelled approximate schematic; the canvas shows the real composite. W9-H: Layer ▸ Layer Style ▸ Apply Style Preset… opens the dialog on its Styles page, a tile grid of every style preset (defined or imported); a click puts that style in the dialog and Apply Style commits it as one undo step (with no preset defined the item reports that instead). File ▸ Open of an `.asl` adds the library's styles to the style presets and opens no document (`Editor::import_style_library`; framing in `asset_store::asl`). Effects decode through `psd::import_effects`, whose `psd::effects::map_rest` (W11-B: moved there from `menu_bridge::asl_import`, so `.psd` and `.asl` share it) maps inner shadow, inner glow, bevel and emboss, satin, gradient overlay, contours, Photoshop CC repeated effects (`dropShadowMulti` and the like); pattern overlays resolve against the library's own pattern section (`psd::pattern::pattern_overlay`). Tests: `asl_import::tests::file_open_of_an_asl_adds_its_styles_and_apply_style_preset_offers_them_in_a_styles_grid` (menu route, drawn tile clicked, Enter), `a_library_in_photoshops_layout_maps_every_kind_it_carries`, and the ignored `photoshop_authored_libraries_import`, which needs `RASTER_STUDIO_ASL_DIR` (run locally over five Photoshop-authored libraries from Krita's test data: all parse, and 3 of 8 styles name something not imported: one an inner glow, one a stroke and a 0% fill opacity, one a 0% fill opacity). Gaps: a gradient- or pattern-filled glow or stroke, a noise gradient and a gradient overlay's `Ofst` offset do not map from `.asl`; a style's Blending Options (`blendOptions`: fill opacity, opacity, blend mode, Blend If) are not applied, because a style preset carries the effect block only, and each one set away from Photoshop's default is named as not imported in the status line (`a_styles_blending_options_are_named_as_not_imported`); Blend If does not apply to pass-through groups or to the exterior effects |
| Autosave | ✅ | |
| Localization | 🔶 | W16-N: Window ▸ Language (Photopea's More ▸ Language) and the Preferences dialog offer English plus twelve full translations — German, Spanish, French, Italian, Polish, Portuguese (Brazil), Turkish, Russian, Ukrainian, Simplified Chinese, Japanese, Korean — stored in preferences and applied live (`MenuAction::SetLanguage` → `Pick::Preferences` → `Editor::set_preferences` → `ui::strings::set_locale`). Each language is a table `crates/ui/src/i18n/<code>.tsv` keyed by the English source; `strings::tests::every_language_table_translates_every_catalogue_string` fails if any table misses (or keeps a stale row for) any `tr()` key's English, any menu title / submenu / row (`MenuAction::label_in` draws them; `label` stays English), panel tab title (`PanelId::title`), blend-mode name, Adjustments button, Info row, Character / Paragraph choice, docks field label, shipped brush preset name, `Command::label` history name (`editor_core::command::HISTORY_LABELS`) or `tr_en("…")` literal, or changes a `{placeholder}` or menu ellipsis. CJK draws with a bundled Noto Sans CJK SC subset (OFL-1.1) in egui's fallback chain (`strings::install_fonts`); `app-shell/tests/w16n_language.rs` clicks Window ▸ Language in a headless frame and reads the bar back in German, Japanese, Korean and Chinese with no missing glyph. Window ▸ Glass Menus (stored) fills menus with `design::egui_theme::glass_menu_fill` (translucent, no blur). NOT localized: strings outside the catalogue — dialog titles and bodies not yet routed through `tr()` (the `no_localized_literals` gate still scans only `src/view` and `src/dialogs`, with its two whole-file and three named exemptions), `format!` status-line messages in `app-shell`, the menus' disabled-row reasons, `src/canvas`, the Layers filter tooltip, a new group's `Group`, `Transaction`-named history steps that are not menu rows, the `Recent n` slots. NOT offered: the rest of Photopea's ~40 languages (no translations; right-to-left scripts would need a mirrored layout). Japanese Han characters take the SC face's forms. |
| Styles, Document Info, Guide Guy panels | ✅ | W13-N, three `PanelId`s appended to `ALL` (Window menu). **Styles** (`ui::panels::styles`): every style preset — New Style Preset, the panel's + (`MenuAction::DefineStylePreset`) or an `.asl` library opened with File ▸ Open — as a swatch (a schematic of its drop shadow, colour or gradient overlay and stroke); the application publishes the parsed presets each frame (`menu_bridge::w13n_ops::frame`, called from `menu_bridge::draw`, as `StylesView`), and a click raises `MenuAction::ApplyStyleAt(i)`, the style onto the active layer as one `SetLayerProperties` step (a stored preset whose effects do not parse — a preset library stores them verbatim — is left out of the panel, and the panel and the click count through the same list, so later swatches keep their own style). **Document Info** (`ui::panels::doc_info::DocInfo`): size, print size at the rulers' resolution, mode and bits per channel, profile name, layer count, memory (distinct pixel tiles x uncompressed tile size at the document's depth — not the process's resident size). **Guide Guy** (`ui::panels::guide_guy`): margins, columns / rows with gutters (New Guide Layout's own arithmetic, `GuideLayoutSpec::guides`), vertical / horizontal centre guides, replace or add; the proposed guides are previewed over a canvas thumbnail in the panel and Apply emits one `Command::SetGuides`. Tests: `ui::panels::w13n_panel_tests::*` (Window-menu row, headless frames, real clicks), `menu_bridge::w13n_ops::tests::a_click_on_a_styles_swatch_applies_the_style_as_one_step` (the chrome's own frame and click, one undo step), `…::a_broken_style_preset_does_not_shift_the_styles_panel`. Gaps: no style reorder in the panel (W16-E added rename, delete, Tiles/List and `.asl` export: see Panel menus); the Guide Guy preview is in the panel, not on the canvas |
| Select ▸ Magic Cut | ✅ | W13-N (`ui::dialogs::magic_cut`, hosted by `menu_bridge::w13n_ops`): the row opens a window over the active pixel layer; foreground / background brush strokes painted on its pane become GrabCut's hard labels (`selection::grabcut` on a grid of at most 256 px, the foreground strokes' box grown by 15% of the canvas's longer side as probable foreground), the foreground connected to a foreground stroke is kept and brought back to full resolution bilinearly, then Refine Edge's pipeline (`selection::refine_mask`: Smooth, Shift Edge, Feather) runs; Preview shows the cut dimmed outside, OK (or Enter) parks the mask and clicks the row again, which lands it as the Selection (`SetSelection`), a Layer Mask (the Reveal Selection route) or a New Layer (the Layer via Copy route, made active), one undo step each, the user's selection left as it was. The pane draws a copy of the layer at most 512 px on its longer side (`magic_cut::PREVIEW_MAX_SIDE`), so a layer past the GPU's largest texture opens. The window is hosted by `w13n_ops`, not the chrome's dialog host: the scrim takes the pointer, and while it is up egui's keyboard focus is parked on a sink id, so `Shell::keyboard_owner` reports egui focus and `route_key` hands no chord (Ctrl+Z, a document switch) to the keymap; the cut is refused unless the document and layer it was painted over are still active. That window is drawn (and the keyboard held) from the menu bar's draw, which does not run in Full Screen Mode, so there the row (reachable through Help ▸ Search Commands) refuses to open it with an error saying to leave Full Screen Mode; Merge Channels, Resize Images and Generate Mockups refuse the same way. Tests: `menu_bridge::w13n_ops::tests::magic_cut_strokes_painted_on_its_pane_select_the_disc` (real drags on the pane, IoU > 0.9), `…::magic_cut_masks_the_layer_or_copies_the_cut_to_a_new_layer`, `…::a_w13n_window_keeps_keymap_chords_off_the_document_behind_it`, `…::a_magic_cut_lands_only_on_the_document_it_was_painted_over`, `ui::dialogs::magic_cut::tests::*` (incl. a 9000 px layer drawn with an 8192 texture limit). Gaps: the cut is computed on the working grid, so edges are as fine as that grid plus the refinement, not re-cut at full resolution as Select Subject's band refinement does |
| Image ▸ Merge Channels | 🔶 | W13-N (`w13n_ops::merge_channels`, `ui::dialogs::merge_channels`): the row opens a window listing the open Grayscale-mode documents of the first one's size, with a Red, Green and Blue picker (first three in tab order to start); OK (or Enter) builds a new RGB document with each channel from its pick; greyed with fewer than three open documents, refused with the count otherwise. Test: `…::merge_channels_builds_an_rgb_document_from_three_grayscale_ones` (swapped picks through the chrome's frame and Enter). W13X-4: the Channels panel's menu (header overflow button) has a Merge Channels… row that raises the same `MenuAction::MergeChannels` (`ui::panels::channels::w13x4_channels_tests::merge_channels_in_the_channels_menu_raises_the_merge_row`, `app_shell::spot_channel::tests::merge_channels_from_the_channels_menu_reaches_the_merge_handler`). Gaps: RGB mode only (no Multichannel / Lab choice) |
| Channels ▸ New Spot Channel | 🔶 | W13X-4. The Channels panel's menu row opens New Spot Channel (`ui::dialogs::spot_channel`: name, ink R/G/B, solidity 0-100%); OK is one `Command::SetSpotChannels` step appending an `editor_core::spot::SpotChannel` whose coverage is the current selection (empty with none). The channel is a `Document` field (saved in `.rstudio`, omitted while empty), listed under the alpha channels with its ink swatch and solidity, and composited over the layer stack as ink (`compositor::spot`: Multiply at 0% solidity to Normal at 100%, in the tile cache key). Save As PSD writes it after the saved selections as a named merged-image channel with a DisplayInfo (1077) record of kind 2 (spot), ink and solidity; opening such a `.psd` moves those channels back to spot channels (`app_shell::spot_channel`). Tests: `editor_core::spot::tests::*`, `compositor::spot::tests::*`, `ui::panels::channels::w13x4_channels_tests::*`, `app_shell::spot_channel::tests::*` (the chrome's own Channels menu, Enter, the composite, `.rstudio` and `.psd` round trips). W16-E: a spot row is selectable (the footer's Load / Delete act on it), Ctrl+click loads its coverage as the selection, a double-click or its options button opens Spot Channel Options (name, ink, solidity; one step) and its bin deletes it (one step). Gaps: no reordering or painting of a spot channel once made; no eye (a spot channel cannot be hidden: `SpotChannel` has no visibility and the composite always inks it); the `.psd`'s merged preview is this build's composite, which already shows the ink; raster exports show it too |
| Panel menus: Swatches, Brushes, Styles, Channels, History; mask popups; Layer Comps flags; Notes author; Navigator angle | 🔶 | W16-E (`ui::panels::panel_menus_w16`, `app-shell/src/panel_menus_w16.rs`). The header overflow of **Swatches / Brushes / Styles** shows Photopea's preset menu: **Open .ACO / .ABR / .ASL** (the File ▸ Open picker and route), **Export as .ACO / .ABR / .ASL** (the item last clicked, else the whole list; `asset_store::resources::aco::write`, `asset_store::abr::write_abr` — v6.2 sampled tips, a round tip drawn at its diameter, hardness, roundness and angle — and `asset_store::asl::write_asl` over `psd::effects::export_effects`), **Name Change**, **Delete**, **Tiles/List**, **Define New**. **Channels**: footer Load (a mask through Select Mask Pixels, an alpha or spot channel as one `SetSelection`, the composite's luminosity or a component's values from the composite), New (an empty "Alpha N") and Delete (alpha, spot or layer mask; greyed on a colour channel) are live, the panel menu gains New / Delete, and Ctrl+click on any channel row loads it. **History** menu: Clear History, New Snapshot. The raster-mask popup gains Delete and Apply; the vector-mask thumbnail gets a right-click menu (Disable / Enable, Delete). **Layer Comps**: Visibility / Position / Appearance toggles per comp (`CompFlags`, appended, all on by default) and Apply restores only what they flag; a **Last Document State** row (`DocumentExtras::last_document_state`, appended) kept when a comp is applied from it and put back by its row. **Notes**: an Author field per note (one step). **Navigator**: an Angle field (degrees, wrapped to -180..180) that turns the document camera. Tests: `asset_store::abr::w16e_write_tests::*`, `asset_store::resources::aco::w16e_write_tests::*`, `layer_model::doc_extras::w16e_tests::*`, `ui::panels::panel_menus_w16::w16e_panel_tests::*`, `menu_bridge::panel_menus_w16::tests::*` (the chrome's own frames and clicks; export read back by the File ▸ Open readers). Gaps: no **New Folder** (the three preset lists are flat, no folder model); an exported `.abr` carries no brush names or dynamics (no `desc` section; the reader names brushes after the file); style export writes the effect block only (blending options and pattern pixels are not written, the status line names what was left out); the export picker is the generic save dialog with the library's extension enforced; Photopea's "click a comp name selects without applying" is not copied (a row click still applies); Clear History also drops the History panel's snapshots |
| File ▸ Automate ▸ PDF Presentation / Resize Images / Crop and Straighten Photos / Generate Mockups | 🔶 | W13-N (`w13n_ops`, rows in File ▸ Automate in Photopea's order). **PDF Presentation**: every open document, in tab order, as one page of `Presentation.pdf` (a free name) in the chosen folder, each page its document's size at 72 ppi; the pages are `raster::pdf::encode_pdf_on_page`'s own image streams assembled into one PDF with a checked cross-reference table. **Resize Images**: a folder window (`ui::dialogs::resize_images::FolderJobDialog`, source / destination / box / enlarge) — every image of the source folder fitted into the box (Lanczos 3 in linear light) and written under its name in its own format (PNG when this build does not write that format). **Crop and Straighten Photos**: the active document's composite; the background is the border pixels' median, each connected region that differs is a photo, straightened by the smallest rectangle around its edge pixels and cropped a pixel inside it, one new document each. **Generate Mockups**: the same folder window over the active smart object; each image of the folder is put in it with Replace Contents, the document composited and written as `<image>.png`, and the replacement undone. Tests: `…::pdf_presentation_writes_one_page_per_open_document`, `…::resize_images_fits_every_image_of_the_folder_into_the_box`, `…::find_photos_straightens_and_crops_each_photo`, `…::crop_and_straighten_opens_one_document_per_photo`, `…::generate_mockups_exports_the_smart_object_showing_each_image`. Gaps: PDF Presentation takes the open documents (no file list, page size or transition options); Resize Images and Generate Mockups run on the interaction thread and do not recurse into sub-folders; Generate Mockups leaves the last replacement on the redo stack |
| Layer ▸ Text ▸ Convert to Point Text / Convert to Paragraph Text | ✅ | W13-N (`text_engine::frame_convert`, `w13n_ops::convert_text`): Point: a hard line break at each place a line wrapped (measured with the compositor's own shaping: `compositor::text_selection_rects` + `text_hit_index`), the wrapped space becoming the break, style spans and kerning moved past an inserted one; Paragraph: a box the laid-out lines' size (+1 px), so nothing re-wraps; one `SetLayerKind` step each; refused when the layer already is that kind. Tests: `…::converting_text_between_point_and_paragraph_keeps_its_lines`, `text_engine::frame_convert::tests::*`. Gaps: text past a fixed box's bottom is kept as more point lines (Photoshop deletes it); both rows are enabled on any text layer and the one that does not apply refuses |
| Layer ▸ Text ▸ Warp Text ▸ Custom (editable mesh) | 🔶 | W13X-5. `WarpStyle::Custom` (appended) and `TextWarp::mesh` (`Option<[[f32; 2]; 16]>`, appended, skipped when `None`, finite-checked by `TextLayer::validate`): a 4x4 Bezier control mesh as fractions of the text's line-box bounds, rendered as a bicubic Bezier patch over the glyph outlines (`text_engine::warp`: `FLAT_MESH` is the identity, `mesh_handles`, `with_mesh_handle`); `compositor::text::hash_layer` keys the caches on the mesh. The Warp Text dialog offers Custom (bend and distortions greyed, with a hint); the layer is then **in warp mode** while it is the active, unlocked layer and the Move tool is in hand: `app_shell::warp_custom` draws the mesh (24 grid lines and 16 handle squares) through `ToolPointer::text_overlay_geometry`, and a Move press within 6 screen px of a handle drags it (live draft outside history), the release landing ONE `SetLayerKind` undo step. Tests: `text_engine` `warp::custom_tests::*` (flat mesh = identity; a dragged handle moves the glyph ink under it, the far glyph stays), `layer_model` `text::w9k_warp_path_tests::a_custom_mesh_is_append_only_serde`, `ui` `dialogs::warp_text::tests::w13x5_custom_is_offered_and_keeps_the_layers_mesh`, `app_shell` `warp_custom::tests::w13x5_the_mesh_is_drawn_and_a_dragged_handle_bends_the_text_in_one_step` (overlay drawn, real pointer drags through `ToolPointer::handle`, composite follows, one undo step each). Gaps: Layer ▸ Text ▸ Warp Style (the one-click submenu, `ui::menu`) does not list Custom; Photoshop's Shell Lower / Shell Upper styles and Vertical warp orientation have no place in the layer model, so a `.psd` using them imports as Arc Lower / Arc Upper, horizontal, named in the import report. W15-C: a PSD export writes the layer's warp in the `TySh` warp descriptor (`psd::text::write_warp` via `build_styled_warped`: `warpStyle` for every style, `warpValue`, `warpPerspective`, `warpPerspectiveOther`, `warpRotate`; Custom adds `bounds`, `uOrder`/`vOrder` and `customEnvelopeWarp` / `meshPoints`, an `ObAr` of `Hrzn`/`Vrtc` pixel lists) and import reads it back (`psd::text::warp_spec`, `app_shell::import::psd_apply_text_warp`); tests `psd` `text::warp_tests::*` (all 17 `warpStyle` values and a Custom mesh round-trip with equal parameters) and `app_shell` `import::w15c_text_warp_tests::*` (every style and a dragged Custom mesh through `psd_from_document` / `document_from_psd` re-import with the same outline within 1 px); no Edit ▸ Transform ▸ Warp route to the mesh; the handles are the 16 control points only (no split lines, no grid presets) |
| Themes (Photopea's theme set) | 🔶 | W13X-4 + W15-D: `design::Theme` has nine themes: the app's own Light and Dark, and all seven of Photopea's (`iV.ml` in Photopea's `pp.js`) — Light Grey, Blue, Dark Blue, Purple and Black appended in W13X-4, Dark Grey and White appended in W15-D (`design::tokens::photopea_themes`: Photopea's `--base` panel, `--bg-canvas` pasteboard, `--bg-bbtn` / `--bg-bbtnOver` button fills, `--text-color` and `--accent`, with the rest of the role ramp derived). The picker lists them in Photopea's own order after Light and Dark. White is #F7F7F7 panel / #E0E0E0 pasteboard / #E0E0E0, #D6D6D6 buttons / #333333 text / #3482F6 accent — all six Photopea numbers (text on the accent is the dark #0B1526, since white stands 3.70:1 on #3482F6; Photopea has no number for that role); Dark Grey is #474747 / #252525 / #5D5D5D, #6A6A6A / #D5D5D5, with the accent the one departure (below). Light and Dark stay (a saved `light` / `dark` preference keeps drawing what it drew); they are modelled on Photopea's White and Dark Grey but are not those palettes: Light is panel #F0F0F0 / pasteboard #D8D8D8 / buttons #FFFFFF, #E8E8E8 / text #16161A / accent #0B62CE against White's #F7F7F7 / #E0E0E0 / #E0E0E0, #D6D6D6 / #333333 / #3482F6; Dark keeps the #474747 panel but is pasteboard #2B2B2B / buttons #555555, #606060 / text #F2F2F2 / accent #5B9BF0 against Dark Grey's #252525 / #5D5D5D, #6A6A6A / #D5D5D5 / #3482F6 (they predate W13X-4 and the shipped UI, tests and shots are built on them). Picked in Edit ▸ Preferences… ▸ Theme (`ui::dialogs::ThemeChoice`, one row per theme) or Window ▸ Appearance, saved in `preferences.json` and installed by the shell's per-frame sync (`app_shell::prefs::theme_to_install`). Every theme passes every `design` gate (`tests/token_gates.rs` iterates `Theme::ALL`); to do so 6 of the 42 Photopea numbers mapped in the seven themes move: Light Grey's text #393837 → #222221 (it clears the #A2A2A2 well at only 4.58:1, leaving no dimmer shade for secondary/tertiary text, which must differ from primary and also clear 4.5:1) and accent #3482F6 → #0B5CC4 (2.81:1 on the panel, gate 3:1); Dark Grey's, Blue's and Purple's accent #3482F6 → #5B9BF0 (2.51:1 / 2.59:1 / 2.69:1 on the panel); Dark Blue's panel #222531 → #303445 (1.15:1 over the #171921 pasteboard, gate 1.4:1). Every other mapped number is Photopea's, pinned by `design::tokens::photopea_themes::tests::the_photopea_numbers_are_carried_over_except_the_listed_departures`. `--shot` with `RASTER_SHOT_THEME=<key>` captured all seven W13X-4 themes (panel / pasteboard / header pixels equal to each theme's tokens), and W15-D's `dark-grey` and `white` shots read panel #474747 / #F7F7F7, pasteboard #252525 / #E0E0E0 and button #5D5D5D / #E0E0E0 off the captured pixels. Tests: `design::theme::tests::*`, `app_shell::prefs::tests::picking_a_theme_in_preferences_installs_its_visuals` (the chrome's Preferences dialog, Enter, the installed style and the painted panel colour, per theme), `…::every_photopea_theme_persists_and_round_trips_through_the_dialog`, `…::dark_grey_and_white_are_saved_by_name_and_light_dark_still_load` (`"darkgrey"` / `"white"` in the file; an old `"light"` / `"dark"` file still loads Light / Dark), `design::theme::tests::every_photopea_theme_is_offered_under_its_photopea_name`, `ui::dialogs::preferences::tests::the_theme_control_lists_every_photopea_theme_and_a_click_picks_it` (the drawn Theme combo, opened by a click, paints all seven Photopea names; clicks on the drawn White and Dark Grey rows pick them). Gaps: the six gate-driven departures above (Dark Grey's accent among them) |

---

## Tier C — Deferred (documented, not implied)

| Capability | Why deferred |
| --- | --- |
| Full CMYK / prepress / spot colours | Needs a real ICC engine and a print workflow; large and orthogonal to core editing. (Image ▸ Mode ▸ CMYK exists since W7-D on a documented naive ink model — see its Tier B row — but there is no press profile and no ink channels; since W13X-4 a document can carry spot channels, composited as ink and written to `.psd` — see Channels ▸ New Spot Channel.) |
| EPS text in its own fonts, shading and patterns; Sketch / XD / Figma symbols and effects, Paint.NET layers | W15-E: EPS artwork opens through a bounded PostScript interpreter (see the Tier A "Open PDF / AI ..." row); what it does not do is set text in the file's embedded fonts (a fallback system font draws it: rasterising Type 1 / Type 42 / CFF outlines is not built), draw smooth shading, patterns, `charpath` or masked images (each is named in the status line); W13X-8: Sketch, XD and Figma open as layers, but symbol / component instances are not expanded and gradients, effects, non-union boolean operations, blend modes and masks are reported rather than kept (same row); W13X-7: Paint.NET layers open (Tier A row), except from the gzip-wrapped older layout and object graphs its reader does not recognise, which open the thumbnail and say why. |
| Proprietary camera RAW files (CR2, CR3, NEF, ARW, RAF, ORF, RW2) | W13-C: recognised by content (their signatures, or a CFA / vendor-compressed IFD with no `DNGVersion`; a TIFF-shaped file named `.nef` etc. whose raw IFD the sniff does not reach goes to the same refusal only on the routes that open by path and pass its extension as a hint: Open Recent, files named on the command line, and a file dropped with no document open. File > Open's background worker decodes the bytes with no extension hint, so there such a file opens as the TIFF picture it contains) and refused naming the format, advising a DNG conversion. Every Rust reader for them is copyleft (`rawloader`, `rawler`, `quickraw`: LGPL-2.1; `zenraw`: AGPL-3.0) and each vendor compression is its own reverse-engineered codec. DNG opens (see the Tier A "Open DNG" row). |
| Licensing and auto-update crates | Dropped from the workspace (P3.2); neither crate exists: both were complete and tested with zero dependents — entitlement checks and update feeds are release-engineering for a shipped product, not this build. |
| Video timeline | ⚠️ W13-L. The model is `editor_core::timeline::DocumentTimeline` on `Document::timeline` (Timeline mode, length 100 ms-10 min, 1-60 fps, playhead, one `LayerTrack` per animated layer: `in_ms`/`out_ms` and time-ordered opacity, position, scale and rotation keys (W13X-9: `ScaleKey`, `RotationKey`, `KeyProperty::Scale` / `Rotation` appended), each key with an `Interpolation` (Linear / EaseIn `f*f` / EaseOut `1-(1-f)^2` / Hold, serde default Linear, omitted when linear) for the stretch to the next key; saved in the `.rstudio` package, omitted while default) edited only through `Command::SetTimeline` (appended last), so each edit is one undo step; `timeline::set_timeline` puts the values at the playhead on the layers in the same transaction. The playhead is not an edit (Photopea): `timeline::seek` moves it and writes the values at `t` on the layers with no command, no history step and no dirty flag, and `SetTimeline` keeps the document's playhead on apply and undo. Keys are absolute (opacity; a position key is the transform's translation on a track that keys no scale or rotation (wave 13's meaning, kept for every saved wave-13 track: the linear part is left as the layer has it), and where the layer centre is, `M(c) - c`, on a track marked `LayerTrack::centred` (appended, serde default false); `timeline::set_timeline` sets the marker when a track first keys scale or rotation and converts each translation `p` to `L c + p - c` with the layer's linear part `L` then, so the frame does not move; scale factors (sign kept, magnitude floored at `MIN_SCALE` = 0.001 by `timeline::clamp_scale`, so a mirrored layer keys a negative factor) and degrees replace the transform's linear part, turned about the centre `c` of the canvas-sized pixel box, `timeline::transform_at`), interpolated per key and held outside them; a tracked layer shows while `in_ms <= t < out_ms`. `timeline::document_at(doc, t)` is the document at `t`; `app_shell::timeline::render_at` composites it through `compositor::composite_region`. UI (`ui::panels::animation::timeline`, behind the panel's Frames / Timeline switch): a ruler click / drag raises `ui::Intent::SeekTimeline` on every frame the time under the pointer changes, routed `menu_bridge::Pick::SeekTimeline` → `ChromeOutput::seek_timeline` → `Shell::apply_chrome` → `Editor::seek_timeline` (seek + `invalidate_all`), so the canvas follows the scrub live, one bar per top-level layer with draggable in / out handles, opacity / position / scale / rotation key lanes (a Hold key drawn square) that select on click and move on drag (one command on the release), Opacity / Position / Scale / Rotation key buttons, Linear / Ease In / Ease Out / Hold buttons for the selected key (`timeline::set_interpolation`, one undo step) and the delete button, fps and length fields, Play seeking the canvas to each frame it advances (the well also shows the thumbnails at their interpolated opacity, as quads at the playhead's position, scale and rotation) and Stop leaving it on the frame it stopped at. Export: `ExportFormat::Mp4(quality)` (appended last; `VIDEO` list, write-only) through `raster::codec::formats::mp4` — W15-B: **H.264 by default** (`Mp4Codec::H264`, `formats::mp4::h264`): Cisco's OpenH264 compiled from source by `openh264` 0.9.8 / `openh264-sys2` (BSD-2-Clause, the `cc` crate; no Cisco binary), High profile (profile_idc 100, CAVLC: the bindings expose no CABAC switch), level from the size and frame rate, a key frame placed by time (the first frame, and every frame that starts 2 s or more after the last key frame, forced with `force_intra_frame`; only a single frame shown longer than 2 s leaves a longer gap), buffer-based rate control holding the QP in a band from the quality field (100 = QP 12 .. 1 = QP 45) with its bit-rate budget clamped to the level's maximum bit rate (Table A-1 `MaxBR`; OpenH264 refuses to open past it, which had broken 1080p60 and every 4K video), an `avc1` sample entry with its `avcC` (4-byte NAL lengths, the SPS / PPS out of band, the High-profile 4:2:0 8-bit tail), an odd edge padded to even by repeating the last column / row, at most 3840x2160 (OpenH264's limit, refused past it naming AV1); the AV1 option is `ExportFormat::Mp4Av1(quality)` (appended last), chosen in the MP4 row's Codec field of Export As, for a still or an animation (`raster::animation::can_animate` / `encode_animation` route both codecs): AV1 from `rav1e` 0.8.1 (BSD-2-Clause, pure Rust, default features off: no nasm, no rayon; already in the tree behind `ravif`). Both: 8-bit 4:2:0 BT.709 limited range, alpha flattened onto white, at least 16x16, in an ISO BMFF file written here (`ftyp`, `moov` with one `avc1` / `av01` track and its `avcC` / `av1C` and `colr`, `stts` per-frame durations in ms, `stss` key frames, one `mdat` chunk). W15-B verified by `raster::codec::formats::mp4::tests::an_h264_mp4_parses_back_and_its_first_frame_decodes_close_to_the_source` (box walk ftyp / moov / trak / avc1 / avcC, 5 frames and their durations, and OpenH264's own decoder turning frames 1 and 5 back into the source's 4:2:0 planes at over 30 dB PSNR; 46.6 dB measured for frame 1 at quality 80, 64x48), `an_odd_sized_h264_mp4_is_padded_to_even`, `h264_key_frames_recur_and_stss_names_exactly_the_idr_samples` and `h264_key_frames_follow_time_with_mixed_durations` (`stss` = the IDR samples; 40 ms + 10 x 500 ms gets key frames 1, 6, 10), `h264_encodes_1080p60_and_4k_at_video_frame_rates` (1920x1080 at 16 ms, 3840x2160 at 10 / 16 / 33 / 40 ms, 2160x3840, at quality 50-100), `the_h264_quality_slider_trades_size_for_fidelity`, `raster::animation::tests::both_mp4_codecs_write_every_frame_with_its_delay`, `app_shell::timeline::tests::export_as_mp4_writes_the_timelines_frames_at_its_frame_rate` (the timeline through `Editor::request_export`: `avc1` by default, `av01` for `Mp4Av1`, 4 frames of 250 ms each), and `ui::dialogs::export_as::tests::an_mp4_row_chooses_its_codec_by_pointer_and_h264_is_the_default` (headless egui frames: the Codec combo is clicked open and AV1 clicked, the job carries `Mp4Av1` at the same quality, and it encodes `av01` while the default encodes `avc1`). File ▸ Export As lists MP4 (`ExportFormat::VIDEO` chained into the submenu) and the dialog's Format list offers it, with a quality field; with Animated on, `Editor::request_export` writes a Timeline-mode document's frames (one per 1/fps second, `timeline::export_frames`, bounded by `MAX_ANIMATION_FRAMES` / `MAX_ANIMATION_BYTES`) or a Frames-mode document's `_a_` frames with their delays; the dialog's Animated caption names the timeline's frames, fps and length in Timeline mode (`ExportAsDialog::set_timeline_frames`) and the `_a_` layers otherwise. A written file decodes in ffmpeg 8.1 (`ffprobe`: av1, yuv420p, bt709, 5 frames of 97x61). Tests: `editor_core::timeline::tests::*` (interpolation at t, W13X-9 eased and hold values at t, scale / rotation about the centre, a flipped layer keying its negative scale and staying mirrored, a wave-13 position key on a half-scale layer evaluating to its translation and migrating unmoved on the first rotation key, pre-W13X-9 timelines reading back linear, seek with no history and no dirty flag, undo after a scrub keeping the playhead, add / move / delete keys, JSON and MessagePack round trip), `raster::codec::formats::mp4::tests::*` (the box structure parses back with frame count, size, durations, `av1C`, key frame 1; refusals; truncated / corrupted files are errors, not panics; W13X-9: a `stsz` naming more than `MAX_PROBE_SAMPLES` = 1 000 000 samples or more than the file holds is refused before any table is sized by it), `app_shell::timeline::tests::*` (W13X-9: scale and rotation keys change the composited frame, Hold keeps it until the next key; the timeline survives `.rstudio` save and open; `render_at` alpha at 0 / 500 / 990 ms; Export As MP4 through `Editor::request_export` in Timeline mode = 4 frames of 250 ms at 32x24, in Frames mode = the `_a_` delays; the File ▸ Export As ▸ MP4 row found in the drawn menu and clicked through `Chrome::menu_click` opens the dialog showing MP4, Quality, Animated and the timeline caption; opening the exported MP4 is refused), `shell::w13l_tests::*` (real `Chrome::ui` frames + `Shell::apply_chrome`: a ruler drag raises the canvas composite's alpha frame by frame before the release, playback does the same at 0.75 / 1.5 / 2.25 s, Stop holds, history depth and the dirty flag unchanged, Undo takes back the last key and keeps the playhead), `ui::panels::animation::timeline::tests::*` (the whole `Workspace::ui`: W13X-9 Rotation / Scale key buttons turning and shrinking the layer about its centre, Ease In / Hold from the panel, the preview quad turned and scaled, the Hold key drawn square; mode switch, bar and key geometry, a ruler drag seeking the layer opacity frame by frame, key add / drag / delete, out-handle drag, playback seeks and tint), `ui::dialogs::export_as::tests::in_timeline_mode_the_animated_caption_names_the_timeline`. W16-M video layers: `raster::codec::formats::mp4::video::decode_in_this_process` reads the first video track's samples (`avc1` + `avcC` through OpenH264's decoder, the same compiled-from-source `openh264` 0.9.8; `av01` 8-bit 4:2:0 through `rusty_av1d` 1.2.0; any other sample entry refused by name, e.g. HEVC), BT.709 to opaque RGBA8, bounded before a picture is decoded (`MAX_VIDEO_FRAMES` = 1000, `max_video_bytes` = 1 GiB or the limits' allocation ceiling, the coded size against `ImportLimits`) and per picture (same size as the first); the application runs it only in the decode worker (`app_shell::dialogs::decode_worker`, kind `video`, `decode_video_in_worker`, its answer header checked against the same bounds before a frame is read; `install` routes `app_shell::timeline::video_layers` to it). The record is `editor_core::timeline::VideoClip` in `DocumentTimeline::videos` (appended, serde default; layer, name, source path, size, `start_ms`, frame durations, and each frame's `TileMap` in the document's tile store, `#[serde(skip)]` so a reopened document re-reads the frames from `source` on its first seek, `Editor::reload_video_frames`); `timeline::document_at` / `seek` put the frame at `t - start_ms` on the layer's pixels, `set_timeline` paints it as a `PaintTiles` in the same undo step, and the bar's `in_ms` / `out_ms` trim it. A video layer is a raster layer plus that record (no new `LayerKind`: appending one would break every exhaustive match in the workspace). `Editor::open_video_path` (new document, Timeline mode, length and fps from the video, clean with no undo step) and `Editor::add_media_path` (one "Add Media" step at the playhead). Tests: `raster::codec::formats::mp4::video::tests::*` (an exported H.264 MP4 and an AV1 one decode back to their frame count, durations and colours; limits, truncation, garbage and an `hvc1` entry are errors), `editor_core::timeline::video::tests::*` (frame at t, seek / `document_at`, trim and undo, the saved record without frames), `app_shell::timeline::video_layers::tests::*` (through `Editor`: an exported H.264 MP4 opens as one video layer with 3 frames and `render_at` matches each frame at t; Add Media at the playhead, trimming, Export As MP4 carrying the video's frame, undo; a truncated MP4 opens nothing; a saved and reopened document reloads its frames), `studio-desktop` `tests/w16m_video_worker.rs` (the real binary as the worker: frames decode, 45 truncated / bit-flipped / garbage files come back as results, the editor route reports a malformed file as an error with the editor and its document intact), `ui::panels::animation::tests::timeline_mode_has_add_media_and_a_row_per_video_layer`. Gaps: W16R: File ▸ Open, Open Recent, drag-and-drop and the command line open a video as a document holding its video layer (`Editor::open_resource_file` asks `Editor::open_video_file` first, in `editor_open_any.rs`), and File ▸ Place Embedded / Linked and the timeline's Add Media add one at the playhead (`Editor::place_path` asks `Editor::place_video_file` first); the Open and Place pickers offer `.mp4` / `.m4v` / `.mov` (`dialogs::VIDEO_EXTENSIONS`). Route test: `video_layers::tests::file_open_and_place_route_an_mp4_to_a_video_layer` (File ▸ Open through `Action::Open`, then Place Embedded), red with either call removed. frames are decoded once, whole (not on demand per playhead position); no New Video Group; OpenH264 decodes what OpenH264 supports (a stream it cannot is an error naming it); no audio (an audio track is ignored: no permissively licensed pure-Rust AAC decoder, symphonia is MPL-2.0; MP4 export writes no audio track); W15-B: H.264 stops at 3840x2160, the H.264 stream is CAVLC High profile (no CABAC, no B-frames: OpenH264 writes neither here), and the OpenH264 C++ build has been built and tested on Windows / MSVC only (the Linux and macOS CI runners have not been observed building it yet); rows are top-level layers only; keying scale / rotation drops a layer's shear; the scale / rotation pivot is the canvas-sized pixel box's centre, not the painted content's. A seek writes the tracked layers' values directly (no history), so undoing an older layer edit after a scrub can leave a tracked value off the timeline until the next seek. |
| Collaboration, cloud, mobile | Explicit non-goals: this is a local-first desktop app whose own code makes no network calls, so nothing that needs a server (cloud storage, sharing or publishing online) is offered. |
| Perfect PSD round-tripping | We target correct reopen in Photoshop and Photopea, not byte fidelity. |

---

## Known gaps in what is marked done

Kept here rather than buried, because a ✅ with a footnote is still a claim:

- **16-bit editing is reachable; not every pixel path is deep yet (P2.5b, partly done).**
  *Done (W3-H):* the compositor reads an RGBA16 tile at its own depth and
  composites it in `f32` (`fill_layer`) -
  `a_sixteen_bit_ramp_composites_at_sixteen_bit_precision`,
  `a_sixteen_bit_ramp_composited_then_exported_equals_its_source`.
  *Done (W4-F):* users reach it. New Document accepts 16 bits and builds the
  base layer from RGBA16 tiles (`a_sixteen_bit_new_document_is_created_at_sixteen_bits`);
  Image > Mode > 8/16 Bits/Channel is enabled (the current depth is the
  checked, greyed row) and runs one undoable Transaction
  (`Command::SetMetaBitDepth` plus a per-layer tile rewrite; undo is
  byte-exact - `converting_to_eight_bits_then_undoing_restores_the_sixteen_bit_tiles_byte_exact`);
  the tools read a 16-bit tile rounded to 8 bits and their output is widened
  back at the apply boundary, keeping the exact 16-bit code of every pixel
  the tool did not change; export follows the document's depth, not only the
  source's (`mode_sixteen_then_a_brush_stroke_then_a_png_export_round_trips_at_sixteen_bits`).
  The app-shell's whole-layer reader (`pixels::read_layer`, behind Edit >
  Transform, filters, adjustments baked to pixels, fill, stroke, clear,
  layer via copy/cut, apply mask, defringe) and the other tile readers
  (Image Size, Canvas Size's background fill, Reveal All's bounds, Image >
  Mode > Grayscale, channel-limited painting) read an RGBA16 tile rounded to
  8 bits, and their RGBA8 output is widened back to RGBA16 at the apply
  boundary (`mode_sixteen_then_flip_rotate_filter_and_adjustment_match_the_eight_bit_document`,
  `an_opened_sixteen_bit_png_flipped_twice_is_the_identity`,
  `image_size_on_a_sixteen_bit_document_matches_the_eight_bit_document`,
  `canvas_size_fill_on_a_sixteen_bit_document_keeps_the_old_pixels`,
  `grayscale_on_a_sixteen_bit_document_converts_its_tiles`). Image > Mode >
  8 Bits/Channel dithers (Photoshop's default "Use Dither";
  `mode_eight_from_the_menu_dithers_between_codes_and_keeps_exact_codes`).
  *Done (W7-C):* the menu's whole-layer edits run at the layer's own depth
  in a 16-bit document. `OpenDocument::layer_rgba16` reads every tile at 16
  bits (an RGBA8 tile widened losslessly) and `layer_rgba16_command` writes
  RGBA16 tiles; `filters::FilterBuffer::from_rgba16` / `to_rgba16` carry the
  layer into and out of the f32 engines. Every filter and adjustment applied
  from the menu or its dialog (`edit_active_pixels`), Fill, Clear, Edit >
  Transform's flips and 90/180 turns and Image > Image Rotation 180 and flips
  (`remap_*`), and Image > Image Size (`resample_layer16_edits`) take that
  road; an 8-bit document keeps the RGBA8 road unchanged. Round 2: Free
  Transform's resample (Distort, Perspective, Warp and a floated selection)
  loads its plane with `tools::patch::ColorPatch::load_native`, which reads
  a tile through `TileAccess::native_bytes` (the tool context's
  `DocumentTiles` answers with the stored bytes, bypassing `NarrowedReads`),
  decodes RGBA16 at full precision and commits RGBA16 tiles, writing every
  pixel it did not change back with its exact stored code. Round 3: the
  plane takes the document's depth from the store
  (`TileAccess::sixteen_bit_document`, which the tool context's
  `DocumentTiles` answers from `meta.bit_depth` via `at_document_depth`)
  rather than from tile lengths, so a 16-bit document whose tiles are still
  RGBA8 (every opened 16-bit PNG or TIFF) widens them losslessly and
  resamples at 16 bits too
  (`an_opened_sixteen_bit_png_free_transformed_by_a_sub_pixel_stays_sixteen_bit`,
  `a_native_plane_in_a_sixteen_bit_document_widens_rgba8_tiles`); the whole-layer
  Scale/Rotate/Skew stays a layer transform (no resample). Image > Image
  Rotation 90 CW/CCW and Arbitrary (`rotated90_layer16`, `rotated_layer16`)
  and Crop to Selection / Trim (`reframed_layer16`) read the layer's
  composite at 16 bits and write RGBA16 tiles. Tests:
  `brightness_on_a_sixteen_bit_document_is_not_rounded_through_eight_bits`
  (the result equals the f32 engine's 16-bit answer, over 90% of samples off
  the 8-bit grid), `gaussian_zero_levels_and_curves_identity_keep_every_sixteen_bit_code`,
  `rotate_ninety_twice_on_a_sixteen_bit_document_moves_every_code_exactly`,
  `image_size_half_then_double_on_a_sixteen_bit_ramp_keeps_every_code`
  (interior within one code), `a_half_opacity_fill_on_a_sixteen_bit_document_is_computed_at_sixteen_bits`,
  `canvas_size_on_a_sixteen_bit_document_keeps_every_code_exactly`,
  `rgba16_round_trips_within_one_code`,
  `free_transform_distort_on_a_sixteen_bit_layer_resamples_at_sixteen_bits`,
  `a_floating_free_transform_on_a_sixteen_bit_layer_keeps_every_code`,
  `image_rotation_ninety_on_a_sixteen_bit_document_moves_every_code`,
  `image_rotation_arbitrary_on_a_sixteen_bit_document_interpolates_at_sixteen_bits`,
  `crop_to_selection_on_a_sixteen_bit_document_keeps_every_code`,
  `clear_under_a_partial_selection_keeps_a_sixteen_bit_remainder`,
  `near_identity_levels_and_curves_through_the_menu_stay_at_sixteen_bits`,
  `a_native_plane_over_a_sixteen_bit_tile_keeps_every_code`,
  `a_native_plane_writes_untouched_sixteen_bit_pixels_back_exactly`.
  *Still open:* the painting tools other than the stroke tools (bucket,
  gradient and the rest still read through `NarrowedReads` into the RGBA8
  `ColorPatch::load`; since W10-H Brush, Pencil, Eraser and the other
  `StrokeTool` brushes blend and write 16-bit dabs, but Clone Stamp and the
  Healing Brush read their source through the RGBA8 `ColorPatch::load`, so
  cloned content arrives as widened 8-bit codes),
  Stroke, Apply Mask, Defringe, Layer via Copy/Cut, Grayscale and Edit >
  Fill > Content-Aware (it synthesises the fill from `pixels::read_layer`;
  Content-Aware Scale runs at 16 bits) still compute at 8-bit precision: a
  pixel they change comes back as a widened 8-bit code. The Filter dialog's preview is
  rendered from an 8-bit read (the applied result is 16-bit). There is no user switch for the 16 -> 8 dither (it is always
  on from the menu). `open_path` still decodes a 16-bit source to RGBA8 tiles
  (the document's depth says 16, the first edit of a tile widens it). W9-M:
  a 16-bit document exports as a 16-bit `.psd` whose raster layers carry
  their stored 16-bit samples (`psd_live::set_deep_pixels`), and a 16-bit
  `.psd` opens as a 16-bit document keeping every sample (a tile whose
  samples are all exact 8-bit codes is stored RGBA8, the opened-PNG
  convention, so
  `a_sixteen_bit_document_saves_as_psd_with_the_eight_bit_twins_layer_pixels`
  still holds); rendered previews (shape, smart object, text), masks and the
  merged image in that file are 8-bit values widened exactly. The tile
  readers that need only coverage (the compositor's `alpha_bounds` for the
  Move tool and the Properties panel, the Move tool's auto-select pick) read
  RGBA16 alpha through `raster::tile_alpha16`.
- **Stylus pressure is verified with synthetic events only.** winit `Touch`
  events feed `Shell::set_pen_pressure` through `pen_input.rs` and drive the
  mouse's pointer route; the shell tests use synthetic events, and a
  physical pen on each platform is still needed to confirm the OS's event
  order. Size, Flow and Opacity from Pressure toggles exist; a hovering pen
  moves the pointer and the brush ring; tilt, rotation and the eraser end are
  not read.
- **No keyboard focus navigation.** Tab is withheld from egui and toggles
  the panels; controls are reached with the pointer.
- **The OS print dialog outside Windows.** W13-M: on Windows File ▸ Print
  opens the system Print dialog (comdlg32 `PrintDlgW`, owned by the app
  window so it is modal to it) and prints the flattened image through GDI
  (`app_shell::editor::print`: one page, centred at 72 ppi, scaled down to the
  printable area), falling back to the PDF when no printer can be opened;
  File ▸ Print as PDF… writes the print-ready PDF on every platform. The
  placement maths, the DIB bytes, the platform dispatch and the dialog's
  owner are unit-tested, and the GDI half prints a real page through
  "Microsoft Print to PDF" to a file (`editor::print::tests::*`; skipped
  where that printer is absent); the modal dialog itself is not driven by
  any test. On macOS and Linux Print still writes the tested single-page PDF
  for the system viewer to print: their print dialogs belong to toolkits this
  build does not link.
- **No View toggle is refused any more:** View ▸ Proof Colors and Gamut
  Warning are enabled and change the canvas since W7-D, and so are Image ▸
  Mode ▸ Lab / CMYK / Indexed (`ui::view_flag_unavailable` stays as the one
  table a future refusal would go in). Every
  enabled item routes to real code; `menu_bridge::unavailable_reason` keeps
  only the File-Info note (W10-E: the XMP fields are kept for the session, not in the document). An adjustment dialog refuses to confirm
  an all-identity setting. The route coverage is pinned by `menu_bridge`'s
  `no_enabled_menu_item_resolves_to_a_no_op` digest.
- **Per-channel masking stops at colour components.** The Channels panel
  isolates, paints into, erases within, fills, filters and bakes adjustments
  into a single RGB component — every one rides `mask_paint_to_channel` at
  the command boundary (`the_eraser_through_the_red_channel_clears_only_red`,
  `gaussian_blur_through_the_red_channel_blurs_only_red`), so the masked
  command reaches history and the journal. Since W10-B a layer-mask row and
  a saved-selection (alpha) row can be viewed alone in grayscale and
  painted (see the Channels panel row); the panel has no per-channel
  histogram.
- **Absent and not yet placed in Tier C** (checked at `25b66e0`: no menu
  row, tool or route exists for any of them):
  Filter ▸ Adaptive Wide Angle; Take a Picture (camera
  capture); generative and neural-model features (no model ships: Select ▸
  Subject, Magic Cut and the Object Selection tool are classical saliency /
  GrabCut); lossy JPEG XL export (no pure-Rust lossy encoder, see Export
  EXR, JPEG XL); AVIF / HEIC HDR (PQ, HLG) and BT.2020 colour (W15-A opens
  both in a decode worker process; those samples open as sRGB, see its
  row); opening the vendor camera RAWs
  (refused by name, no permissive reader; DNG opens).
  Placed in Tier C, each with its reason: EPS text in its embedded fonts,
  shading and patterns (W15-E draws the rest of an EPS), audio (Video timeline; W15-B:
  H.264 output now exists; W16-M: video layers decode H.264 / AV1 MP4 in the
  decode worker and open through File ▸ Open and Place), a press profile for CMYK, and collaboration, cloud storage,
  sharing online and mobile (no server: the app's own code makes no network
  calls).
- **Release job never run.** CI's `release` job (a `v*` tag or a manual
  `release_dry_run`) has not run; no tag exists.

## Release gate

1. Every Tier A row is ✅ or has its gap named above.
2. Every Tier B row is ✅, named as partial, or moved to Tier C with a reason.
3. `cargo check --workspace --all-targets`, `cargo clippy` with `-D warnings`,
   `cargo fmt --check` and `cargo test --workspace` are green.
4. The app launches, opens a real image, edits it, saves, reopens and exports
   correctly — verified by running it, not by prose.
