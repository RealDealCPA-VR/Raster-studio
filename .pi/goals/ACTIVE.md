# Active Goal

Status: ACTIVE
Goal: "go through the implementation plan and do every task in the list systematically until fully completed" (the plan: `raster-studio/docs/THUMBNAIL-WORKFLOW-IMPLEMENTATION-PLAN.md`, 92 main cards 001–092 + 6 optional cards 093–098; execution guidance: `raster-studio/docs/GLM-THUMBNAIL-START-HERE.md`)
Started: 2026-09-07
Last updated: 2026-09-09 (M2 exit gate PASS; T015–T024 complete; next T025)

## Restart recovery checkpoint — 2026-09-09

- Historical note retained: HEAD is still `9ba62c1`; all goal work is uncommitted.
  T001–T014 (M0+M1) complete; M0 and M1 exit gates PASS. **M2 (Phase 2, 015–024)
  complete this session — exit gate PASS** (68 binaries, 0 failed, 3480 passed,
  14 ignored). Cards 025–092 remain; 093–098 optional deferred.

## Scope and acceptance criteria
- [x] A1: M0 (001–006): baseline report exists with observed-vs-predicted gap evidence; progress ledger + deterministic fixtures + shell-route regression harness + reuse inventory + frozen acceptance scene exist; baseline reproducers for E01, E06, E08, E09 demonstrated failing. — DONE 2026-09-09: all six cards verified; M0 exit gate PASS (fmt/check/clippy/test --locked --workspace; 68 binaries ok, 0 failed, 15 ignored); reproducer outputs recorded in THUMBNAIL-BASELINE.md Task 004 section; scene+matrix frozen in Task 006 section. One gate repair: fixture `▸`→`>` (ui glyph gate).
- [x] A2: M1 (007–014): unified selection/target snapshot, bounds queries, coordinate conversion, typed tool options, pending-edit lifecycle, live canvas sessions, preview render — all with passing shell-route tests; workspace gate green. — DONE 2026-09-09: T007–T014 all verified (rows in the progress ledger); **M1 exit phase gate PASS** 2026-09-09: fmt clean; check PASS; clippy -D warnings 0 errors; `cargo test --locked --workspace` PASS (68 binaries ok, 0 failed, 15 ignored).
- [x] A2: M1 (007–014): unified selection/target snapshot, bounds queries, coordinate conversion, typed tool options, pending-edit lifecycle, live canvas sessions, preview render — all with passing shell-route tests; workspace gate green. — DONE 2026-09-09: T007–T014 all verified (rows in the progress ledger); **M1 exit phase gate PASS** 2026-09-09: fmt clean; check PASS; clippy -D warnings 0 errors; `cargo test --locked --workspace` PASS (68 binaries ok, 0 failed, 15 ignored).
- [ ] A3: M2 (015–033): rich text persists, renders fully styled, survives save/reopen, is editable on canvas (caret/range/IME/confirm/cancel) — per-card checks pass; workspace gate green. — IN PROGRESS: Phase 2 (015–024) COMPLETE — all ten cards verified; **M2 exit phase gate PASS** 2026-09-09 (68 binaries, 0 failed, 3480 passed, 14 ignored). Phase 3 (025–033, canvas editing) remains within A3.
- [ ] A4: M3 (034–054): object-bounds transforms without source resampling, multi-layer move, rendered-content hit testing, wired Move options, live previews, full-source placement, OS image clipboard, Paste Into with real mask — per-card checks pass; workspace gate green. — IN PROGRESS: **A4's M4-054 phase COMPLETE** (PHASE 5 GATE PASS 2026-09-09). A5 (055–071) begun: T055 verified (mask thumbnail wells; shared resolver; pinned target triple; E08 flipped green); T056 verified (selection edits undoable + composable through Command::SetSelection, coalesced per gesture; the Select menu rides the same route; save/recovery preserves committed selections); T057 verified (the four mask-creation ops route to the app with real layer-space coverage; E09 flipped green); T058 verified (mask-space painting through the pose; pose-aware coverage read/write with the clear rule; invert/fill target the mask; per-document colour wells); T059 verified (mask view modes Composite/Grayscale/Overlay on the presenter's last mile; real mask thumbnails; well popup with view + ops; accent target badge; inverted + density-0 semantics); T060 verified (RefineMask dialog: selection::refine_mask pipeline smooth→shift→feather→contrast, RAW-store preview over three backdrops, bounded at 256px, one-transaction confirm, host-path discriminator test). T061 verified (boundary-refine brush: StrokeOp::RefineBoundary mask-only op, band-bounded smooth+midpoint regrade, hard-coverage fixed points, strength option; reviewer PASS; full workspace gate PASS); T062 verified (edge color cleanup as a separate edit: filters::defringe engine + Remove Color Fringe dialog/menu/host wiring, one reversible transaction, coverage untouched; reviewer PASS). T063+ remain.
- [ ] A5: M4 (055–071): mask thumbnail targeting, undoable selections, four distinct mask-creation ops, mask painting/visualization, edge refinement, layer-stack management, effects, reusable styles, clipped adjustments, asset replacement, sample composition, native acceptance walk (M4 delivery).
- [ ] A6: M5 (072–081): PSD fidelity contract doc, independent fixtures, editable-text + effects + masks/adjustments import, fidelity report, appearance-preserving export, interchange acceptance matrix with independent-reader evidence.
- [ ] A7: M6 (082–092): workspace polish, navigation/shortcuts, export dimension/aspect/color verification, small-preview workflow, measured performance, off-thread long ops, round-trip/recovery tests, full gates, human acceptance walk, documentation reconciliation.
- [ ] A8: Optional 093–098: remain `optional deferred` unless the user makes automation required (plan's own condition); documented in progress ledger, not silently dropped.
- [ ] A9: Every card lands with its plan-stated Done/check satisfied, recorded in `raster-studio/docs/THUMBNAIL-WORKFLOW-PROGRESS.md` with exact commands and evidence; phase gates (`cargo fmt/check/clippy/test --locked --workspace`) green at each phase boundary.

## Constraints and decisions
- Cargo commands run from `raster-studio/` (inner workspace), with `--locked` where the plan demands it. Integration package is `integration-tests`; new card tests go in `tests/integration/tests/` (thumbnail_workflow.rs for green work, thumbnail_reproducers.rs for retained failing reproducers).
- Preserve editability: native text/shape transforms alter model transforms; placed sources keep original pixels; rasterization is explicit only.
- One compositor, one text model (rich vocabulary lives in `layer-model`; text-engine consumes/re-exports; no JSON in `TextLayer.text`), one interaction-target contract.
- History holds committed edits only; one labeled entry per committed gesture; cancel leaves none.
- Document version currently 3, manifest 2; bump document version explicitly when rich-text semantics land; keep old-format fixtures; do not bump package manifest for layer-data-only changes.
- Stay local: no cloud, no silent downloads. Subject extraction (093–096) is optional and separately gated.
- Do not weaken tests to accept placeholders; a blocked check is recorded, not passed.
- Host: Windows 11, GPU available (`--shot` pipeline works); rustc/cargo 1.98.1. Real-photo quality gates and OS-level clipboard/IME checks are recorded honestly as host-bound where they cannot run.
- Pre-existing unrelated state: `docs/CORRECTIONS-TODO.md` C14 (hardware NVDA verification) intentionally open — device-bound, not this plan's scope.
- Reproducers stay `#[ignore]`d until their owning cards (E01→016+021, E06→046, E08→055, E09→057) land fix+test together, then flip in that card. E09 runtime truth recorded: a bare `LayerMask::new` has no coverage tiles and composites fully hidden — all four mask ops are identical AND fully hide the layer today.
- Edit-target tool consumption lands with cards 010/055 (007 owns the state only); `alpha_bounds` is raster-only by design; `ALPHA_INK_CACHE` clears wholesale when full (bounded, coarse).

## Baseline
- Workspace state: branch `main`, commit `9ba62c1`. All goal work uncommitted (plan docs, fixtures, harness, T007/T008 code + tests, ledger updates). Preserve everything.
- Checks before changes: `cargo fmt --all --check` PASS; `cargo check --locked --workspace --all-targets` PASS; full gate PASS 2026-09-09 (68 binaries, 15 ignored). Toolchain: cargo/rustc 1.98.1, Windows 11.

## Todo
### M0 — reproducible baseline (COMPLETE; gate PASS)
- [x] T001: Baseline report `docs/THUMBNAIL-BASELINE.md` — DONE 2026-09-07/09: E01–E14 CONFIRMED.
- [x] T002: Progress ledger `docs/THUMBNAIL-WORKFLOW-PROGRESS.md` — DONE 2026-09-07.
- [x] T003: Deterministic fixtures — DONE 2026-09-07/09: fixture/thumbnail.rs + committed assets + 5 green tests + 1 ignored materializer (re-verified this session).
- [x] T004: Shell-route harness + E01/E06/E08/E09 reproducers — DONE 2026-09-09: app.rs shell routes; thumbnail_reproducers.rs (4 `#[ignore]`d, each fails for the intended behavior; outputs in baseline); `ROUTED` eprintln hygiene removed; integration package green; app-shell green; fmt/clippy clean.
- [x] T005: Reuse/dependency inventory — DONE 2026-09-09: 12-subsystem function-level inventory in baseline "Task 005"; lockfile facts (cosmic-text 0.17.2 declared 0.17 by text-engine only; arboard 3.6.1 transitive via egui-winit, image-data off; dejavu 2.37; glam 0.29.3); no new core deps planned.
- [x] T006: Freeze acceptance scene + matrix — DONE 2026-09-09: baseline "Task 006" (7-layer scene, 3 sizes, zooms automated, scaling 150/200% manual-hardware, 19 steps mapped, fix order frozen E01→E03/E05→E06→E08/E09→E04).
### M1 — interaction foundation (007–014)
- [x] T007: Editor selection/target snapshot — DONE 2026-09-09: `app-shell/src/edit_target.rs` (EditTargetKind/EditTarget/EditTargets + read-time validation, Content fallback); Editor field+`edit_target()`/`set_edit_target_kind()`; `ui::Intent::SetEditTarget` raised by Properties Layer/Mask control; `Pick::EditTarget`→`ChromeOutput::edit_target`→shell apply. 6 new tests. app-shell 403+10 green; ui/integration green; fmt/clippy clean. Tool consumption in 010/055.
- [x] T008: Reusable bounds queries — DONE 2026-09-09: `compositor/src/bounds.rs` (content/document/styled bounds via Ctx internals made pub(crate); `alpha_bounds` tile-hash-cached precise ink; empty→None). 8 tests. compositor 178+1 green; fmt/clippy clean.
- [x] T009: Coordinate conversion helpers (`app-shell/src/interaction_geometry.rs`) | Verify: round-trips at 3 zooms × 3 scale factors; linked + unlinked mask spaces — DONE 2026-09-09: `interaction_geometry.rs` (screen↔document via ui::CanvasCamera; document↔layer-local via ancestor-composed `document_transform_of`; singular refused via determinant → GeometryError; level conjugation identical to Ctx::level_transform; MaskSpace WithContent/Document per `mask.linked` mirroring render_source). Convention documented in the module: transforms are parent-relative, composed root-first exactly once. 6 tests: 3 zooms, 3 display scales (ppp 1/1.5/2), scaled+translated round-trip at levels 0–2, singular→Err, nested group∘child composed once, linked/unlinked mask sampling. Verified twice (same session re-run + fresh run): 6/6 green; app-shell full suite 409 green; fmt clean; clippy 0 errors.
- [x] T010: Typed tool options forwarding (chrome.rs, tool_input.rs, tools) | Verify: Auto-Select boolean reaches running Move tool; unknown keys fail in dev checks — DONE 2026-09-09: tools side first — `ToolSetting {Float,Int,Bool,Choice,Color}` (tools-owned enum, re-exported; UI types stay out of tools) + `Tool::set_setting(key, setting) -> Result<(), ToolError>` defaulting Choice→set_choice (existing choice tools unchanged) and everything else→Err(UnknownOption); new `ToolError::{UnknownOption, OptionKindMismatch}`; MoveTool implements set_setting for its spec'd keys (auto_select/show_transform Bool — show_transform's consumption is cards 012/038) refusing unknown/mismatched loudly. chrome: `tool_options(tool)` forwards every declared option with the workspace's value (schema defaults included — coherent state for rebuilt tools), replacing the choice-only filter. Boundary: `shell.rs` converts ui::OptionValue→tools::ToolSetting (UI types never reach a tool); `ToolPointer::handle`'s choices param is now typed settings applied at pointer-down (gesture settings pinned like the brush), failures surfaced in `PointerOutcome::failed`, never swallowed. Tests: 2 tools-level (adoption + loud refusals), 1 chrome boundary test (defaults forward; set values forward), 1 real-route behavioral test (Auto-Select forwarded → Move claims the ink under the pointer, not the active layer; canvas layer unmoved). app-shell 411 green; tools 94 green; integration-tests green; fmt clean; clippy 0 errors.
- [x] T011: Pending-edit lifecycle (`edit_session.rs`) | Verify: long drag = one undo entry; cancel = none; tab switch cannot retarget — DONE 2026-09-09: `crates/app-shell/src/edit_session.rs` (new) — `PendingEdit {document, targets (captured once at begin, validated against the tree), label, generation}` + `EditSessions` (one live gesture; second begin refused); Editor field + `pending_edit()`/`begin_edit()`/`commit_edit()`/`cancel_edit()`. Commit wraps the gesture's commands in ONE labeled `Command::Transaction` (long drag = one entry; undo label = gesture label; undo takes it all back at once). Cancel applies nothing (idempotent). Commit refused — document+history untouched — when the active document is no longer the gesture's (tab switch), when no gesture is live, or when a command is refused. Focus loss/tool change/save specs documented in the module doc (cancel emits nothing per tools contract; pending edits never journaled). 7 tests: one-labeled-entry (+undo whole gesture), cancel-leaves-nothing (+idempotent), tab-switch-cannot-retarget, failed-commit-unchanged, targets-captured-once, second-begin-refused, begin-refuses-ghost-target. app-shell 418 green; fmt clean; clippy 0 errors; integration green. Pointer-route preview wiring lands with T012/T013 as planned.
- [x] T012: Publish live tool geometry to CanvasSessions | Verify: shell-driven gesture shows handles in a captured real frame; cancel clears — DONE 2026-09-09: tools — `SessionGeometry::Transform {state, mode, active}` + `Tool::live_geometry()` (default None; the same call publishes AND un-publishes); TransformTool answers with its live state/mode/grabbed handle. tool_input — `ToolPointer::live_geometry()` (+ `session_doc` field: the document claimed with the gesture, retained for the geometry's lifetime so a transform's handles survive its own pointer-up; cleared when the geometry ends). chrome — `publish_tool_geometry(geometry, active_document)` (the production publisher; wrong/absent document clears) + `paint_live_tool_geometry` in `Chrome::ui` — the overlays' actual route, since `CanvasHost::central_panel` is never drawn: same `ui::canvas::paint::transform` painter the canvas view uses, against the same camera the surface rendered with, clipped to the docks' leftover. shell — publishes after every pointer sample. Tests: pointer-route lifecycle (gesture → geometry follows the dragged corner with the grabbed handle named; Escape → cleared) + the visibility check through a real painted frame: published session → closed-line quad at the dragged corner's screen position in `painted_shapes`; ended session → no quad. Publication driven by a real gesture, not by filling the UI field. app-shell 420 green; tools 94; integration green; fmt clean; clippy 0 errors.
- [x] T013: Temporary content previews via the one compositor | Verify: pixels follow handles; cancel restores exact baseline; commit == preview; per-doc preview identity — DONE 2026-09-09: compositor — `CompositeOptions.preview_generation` (in every tile's cache key via the hashed options: the generation IS the invalidation, old bounds included) + `LayerOverride` + `composite_region_with` (reads the committed document, writes nothing); `Ctx::level_transform` consults the override with the same level conjugation. doc.rs — `PreviewOverride` held OUTSIDE the Document (never journaled/saved), `set_preview`/`clear_preview` (generation bumped per set), `composite` routes the override through `composite_region_with` while committed frames keep the cached path. tools — TransformTool captures its layer at pointer-down (`pub layer`), publishes it in `SessionGeometry::Transform`; `quad_affine` made pub (the affine the preview renders with, straight from the handle state). shell — `ToolPointer::settle_preview` (the one settle route: live transform session → set_preview from the handle state; ended session → clear) called after every pointer sample and by `abandon_gesture`. Tests: doc-level (preview moves pixels on screen; no history entry; no added dirty flag; clear restores exact baseline bytes — cache identity proven; per-doc lens isolation) + the end-to-end route (preview moves pixels BEFORE release; cancel → exact baseline + no entry; commit → settled preview becomes committed pixels with worst channel diff ≤ 3, one undo entry, no double transform). app-shell 423 green; tools 94; compositor 178; integration green; fmt clean; clippy 0 errors.
- [x] T014: Interaction foundation integration test + workspace gate | Verify: typed options, selection, coords, overlays, preview/commit/cancel, focus loss, undo pass through real input paths — DONE 2026-09-09: `thumbnail_workflow.rs::the_interaction_foundation_holds_through_the_real_shell_routes` assembles the foundation through the shipping routes: typed options (Auto-Select forwarded → Move claims the ink under the pointer; a no-motion click creates no history), bounds (content = stored extent; alpha = precise ink box), edit target (content→mask once a mask exists), pending edit (two commands = one labeled entry; undo takes the whole gesture back), coordinate round-trip through the fixture camera. Overlay/preview/commit-cancel/focus-loss coverage lives in the per-card tests (T012's painted-frame + publish/clear lifecycle; T013's preview-before-release/cancel/commit route; T011's tab-switch + failed-commit guarantees). **M1 exit phase gate PASS**: fmt clean; check PASS; clippy -D warnings 0 errors; `cargo test --locked --workspace` PASS (68 binaries ok, 0 failed, 15 ignored).
### M2 — styled text persists (015–024)
- [x] T015: Persisted text schema in `layer-model/src/text.rs` | Verify: schema covers bold/condensed headline + green secondary + legacy three-field compat; no dep cycle — DONE 2026-09-09: `crates/layer-model/src/text.rs` (new, `pub mod text`): `TextLayer {text, font_family, size_px (single authority — BaseStyle structurally carries neither), style: BaseStyle {weight/slant/fill(linear straight RGBA)/underline/strikethrough/script/tracking/ligatures/kerning/synthetic flags — mirroring text_engine::CharStyle so conversion is a field copy}, spans: Vec<StyleSpan{start,end,style:StyleOverride}>, paragraph: Paragraph {alignment/leading/first_line_indent/space_before/after}, frame: Frame {Point|Box}, kerning: Vec<Kern>}`; every rich field `#[serde(default)]` so legacy three-field payloads load with black/regular/auto-leading point-text defaults (CharStyle::default-matching); `TextLayer::legacy` ctor + `is_legacy`. 5 tests: acceptance-scene schema (white bold condensed headline + green secondary with span/frame/kerning) serde round-trip; legacy three-field JSON loads unchanged; defaults preserve black/regular; family/size single authority (span-level overrides only); linear-RGBA colours. layer-model 122 green; clippy -D warnings 0; no dep cycle (text-engine already depends on layer-model). The TextLayer swap + lossless conversion = card 016.
- [x] T016: Lossless TextLayer↔TextRun conversion — DONE 2026-09-09: layer.rs re-exports the rich schema; text-engine `From` impls lossless both ways; literal callers updated; E01 reproducer flipped green. Evidence: THUMBNAIL-WORKFLOW-PROGRESS.md row 016.
- [x] T017: Text payload/range validation — DONE 2026-09-09: `TextLayer::validate()` + TextError + `normalized_spans()`; editor-core InvalidPayload gate. Evidence: THUMBNAIL-WORKFLOW-PROGRESS.md row 017.
- [x] T018: Document version bump + migration for rich text | Verify: old fixtures load unchanged; new styled doc reopens identical; future-version rejection explicit — DONE 2026-09-09: `DOCUMENT_FORMAT_VERSION` 3→4 (doc comment names card 018's semantics); migrate.rs gains the version-3→4 step (the schema's own serde defaults ARE the migration — no data changes hands) and the chain-covers test enforces order; `a_v1_fixture_opens_through_the_migration_path` stamps to the CURRENT version (now 4) via the constant; docs/file-format.md documents the version-4 default migration. New test `a_styled_text_document_round_trips_through_the_native_package`: white bold condensed headline with a green span + box frame + kerning saves and reopens with an identical model AND composite at the current version. Future-version rejection explicit (the +1 test now refuses 5). Workspace 68 ok / 0 FAILED; fmt+clippy clean.
- [x] T019: Render complete text styles — DONE 2026-09-09 (with T020; card 016's lossless conversion already carried style through shape/render_linear, which groups glyphs by colour, so the defect was cache identity alone): compositor/src/text.rs `hash_layer(&TextLayer) -> u64` (DefaultHasher; floats via to_bits, enums by discriminant — text/family/size/base style incl. weight/slant/fill/underline/strikethrough/script/tracking/ligatures/kern-toggle/synthetic flags, spans with StyleOverride fields, paragraph alignment/leading/indents/spacing, Frame::Point/Box, manual kerning list) feeds the new `RunKey.content`; the full persisted run reaches the shaper/rasterizer. 5 styled tests: weight/tracking/color change the run (bold vs regular ink differs; green's G≥R; tracking shifts; warm cache deterministic), paragraph alignment/leading/kerning/box reflow reach layout, a span colours only its own bytes, glyph edges carry partial alpha (0<a<1 pixels exist), editing any styled field invalidates.
- [x] T020: Text cache identity/invalidation — DONE 2026-09-09 (with T019): `RunKey {text, family, size_bits, generation, content}` — content = hash_layer, so color/weight/box-width/kerning-only edits re-key; undo (layer back to old content) restores the first render pixel-identically (`!differs` — pointer identity dropped as eviction-dependent); fill-only/span-only/frame-only/kerning-only changes each alter warm-cache output. Two engine-test pitfalls fixed: manual kern index 0 is a documented no-op (kern_shift requires index > line start — use index 1); testkit's load_font ONCE mutates the global library mid-suite (clears runs, changes face resolution), so every styled test pins `text_fixture_family()` before its first render. compositor 183 green ×3 runs; workspace gate PASS (68 binaries, 3460 passed, 0 failed); fmt clean; clippy -D warnings 0 errors.
- [x] T021: Character/Paragraph controls write persistent data — DONE 2026-09-09: ui — `panels/text.rs` gains `fill_to_swatch`/`swatch_to_fill` (model linear straight RGBA <-> egui picker sRGB8; egui 0.29 stores PREMULTIPLIED Color32 so readback goes through `to_srgba_unmultiplied`; alpha bypasses the transfer as a plain coverage fraction both ways — 8-bit quantisation makes the swatch a fixed point so an unchanged picker emits nothing); `view/docks.rs` character_body gains the Fill row (`inspector_field` + `ui.color_edit_button_srgba` — the direct fill control the surface lacked; `Character::set_color` had zero callers). Module doc records the range-vs-whole-layer rule (whole-layer until canvas text sessions land in 025+; nothing on the surface promises range application, so no control is disabled) and the gesture contract (harvest stamps, apply_kind_edit folds). SetLayerKind validation (card 017) gates the apply. New integration test `the_character_panel_edits_reach_the_pixels_undo_and_reopen`: panel setters -> commit -> menu_bridge::pick (intent answered, not unrouted) -> apply_kind_edit with a stamped gesture -> pixels differ -> 3 fields + a second same-gesture frame = ONE undo entry -> undo restores exact baseline bytes -> redo -> save/reopen -> model fields (size 48, Weight 700, linear fill) and panel readback identical. ui 960 green (+5: fill round-trip, swatch round-trip, alpha-fraction); integration-tests green; fmt clean; clippy 0 errors; workspace 68 binaries / 3464 passed / 0 failed.
- [x] T022: Font selection + substitution reporting | Verify: family change updates headline; missing family reported; licensed fixture used in tests — DONE 2026-09-09: text-engine FontStretch (usWidthClass 1–9) + CharStyle/StyleOverride.stretch + FaceRecord.stretch + resolve(family,weight,stretch,slant); substitution = OWN documented policy (`FontLibrary::substitute_for` — missing → pinned default sans; `attrs_for` applies the SAME rule so report+renderer agree by construction; cosmic-text's own fallback sorts emoji-first, not mirrored); requested name retained everywhere (verbatim in model, never rewritten in edit/save path). layer-model Stretch + serde defaults. compositor: `hash_layer` now THE canonical fingerprint — **tile-cache repair**: `hash_layer_props`' Text arm hashed only text/family/size (weight/fill/stretch-only edits served stale tiles forever; masked by earlier tests co-varying size_px) → now hashes `crate::text::hash_layer(t)`. Facades `font_family_faces`/`font_substitute_for`. ui: searchable family picker (free text + live candidate popup), Face combo listing installed widths (DejaVu condensed = same family, usWidthClass 4 = SemiCondensed), substitution notice; 3 catalogue keys. integration: end-to-end (condensed adoption changes pixels via panel route, one undo entry; missing family reported + name survives save/reopen; egui UI font width unchanged across load_font). Reviewer PASS → fixes applied: file-format.md duplicated tail repaired; candidate-pick clears the in-progress buffer (Enter no longer commits the search string over the pick); `has_family` case contract PINNED by test (fontdb 0.23 `Family::Name` = exact equality — report and shaper agree; test fails loudly if a fontdb upgrade folds case). Font-file load route: deferred (only-if-needed; seam = compositor::load_font already exercised; no native dialog dep — recorded, not dropped). Reviewer follow-up recorded: load_font does not invalidate per-document TILE caches (no production caller today; hard requirement when the deferred route lands).
- [x] T023: Point vs paragraph geometry | Verify: frame resize reflows without font-size change; point text unwraps except at breaks; survives reopen — DONE 2026-09-09: compositor facades `text_overset_lines` (early-returns without shaping for point/auto-height per reviewer; counts lines past the fixed box height, same 1e-4 epsilon as `ShapedText::overflows`) + `text_content_height` (seed so fixing a height never instantly oversets — proven by test). panels/text.rs: `Paragraph::{set_boxed,set_box_width,set_box_height,box_size}` + DEFAULT_BOX_WIDTH_PX=200 / MIN 8 / MAX 4096 (panel range deliberately stricter than model validate — documented); point↔box keeps text and explicit breaks, drops dimensions on return; refusals never clamp. docks paragraph_body: Frame checkbox, Width slider, Fixed-height checkbox (seeded from laid-out content height), Box-height slider, overset hint row; all changes flow through the pre-existing single commit (one intent per frame). 5 catalogue keys (localization gate caught "Box height" → key added). TypeTool already defaults to point text (drag=box = card 032; no tools change). Tests: compositor overset test; panel geometry test (size never moves, refusals, round-trips); integration end-to-end (Box(90,None) reflows pixels with size_px 24 unchanged → fixed 40px height reports overset → save/reopen retains Box(90,Some(40))+24). Reviewer PASS → fixes applied (early-return shaping guard; panel-validate range doc). Reviewer notes recorded: seed relies on panel-path origin [0,0] + first-line top 0 (engine-side overset_line_count suggested); DragValue 2-decimal rounding can round a seeded height down; Frame/Width/Height literals need keys at wave end.
- [x] T024: Styled headline end-to-end verification | Verify: two headlines authored via shell/panels survive save/reopen/export/undo; workspace gate — DONE 2026-09-09: integration test `the_styled_headline_workflow_verifies_end_to_end`: dark backdrop via command route; both headlines created via Command::create_layer (the Type tool's route) + every style via the panel route (weight 400→700, fill black→white, subhead black→green, tracking 0→90 — four single-field edits, each proven to rewrite the composite, with absolute colour pins: a fully-white pixel after the white fill, a green-dominant pixel after the subhead colour); undo walks all four back to exact bytes (undo bool asserted), redo reproduces the exact later bytes; model preservation against shell-independent full-TextLayer literals; save/reopen → model equality + byte-identical composite; export = composite → raster::encode PNG → decode_path → pixel equality. Manual item recorded separately (font-dependent visual judgment — synthetic emboldening; never claimed by the test): see progress ledger row 024. Reviewer PASS after the manual item was recorded; reviewer notes: export satisfied via the app's encode path not the export dialog (headless card); determinism is comparative (fresh decode + second compositor instance) — absolute pins now added; fixture loads regular face only, bold arrives via synthesis. **M2 exit phase gate PASS 2026-09-09**: fmt clean; check PASS; clippy -D warnings 0 errors; `cargo test --locked --workspace` PASS — 68 binaries ok, 0 failed, 3480 passed, 14 ignored.
### M3 — canvas text editing (025–033)
- [x] T025: Rich text-session state (replace append-only) | Verify: mid-string caret + range representable; cancel restores exact original, no history debris — DONE 2026-09-09 (doer complete + reviewer round 1 REJECT → all four criticals fixed; **VERIFIED 2026-09-09 — reviewer round 3 PASS**): TextSession v2 {original, draft, caret, anchor, composition, created_layer} + selection/place_caret(char-boundary clamp)/select/replace_selection(span validity floor: before=untouched, after=shift, inside=start collapses to the edit point + everything clamped to new_len — reviewer's repro test added)/delete_selection/set_commit_clear_composition (preedit replaced wholesale, never accumulated — fixed per reviewer). Typing renders history-free via ToolRequest::TextDraft → OpenDocument::apply_text_draft (direct kind substitution, mark_all dirty, refuses missing/wrong-kind/invalid payloads + blanket locks); Confirm = ToolRequest::TextConfirm {layer, original, draft} — the shell restores the original (history-free) THEN applies SetLayerKind so the inverse is the ORIGINAL and one undo takes the whole session back (reviewer critical 1; tested: undo_after_a_confirmed_run_takes_the_whole_session_back); Cancel: created layer → DeleteLayer (no stray layer), entered layer → TextDraft(original) (zero entries) — tested at both levels. shell.rs: Enter→Confirm, Escape→Cancel during sessions (dialog gate preserved); abandon_gesture + set_tool route live sessions through Cancel (reviewer critical 2). Tool::cancel keeps its emit-nothing contract. Tests: tools 100 (6 new/updated), app-shell 426+10 (+4: typing-history-free+confirm-one-entry, cancel-no-stray-layer, undo-whole-session, doc draft reconciliation incl. lock refusal), integration 11+1 green; clippy -D warnings 0; fmt clean. Reviewer warnings accepted as recorded limitations: text routes are document-agnostic (tab-switch mid-session refused with status error; card 026 owns session targeting); per-keystroke mark_all = full visible recomposite (bounded by panel/typing; optimize with card 030's overlay work); clamp_boundary floors only (card 027 adds the ceil variant for trailing edges); no Shell-level focus-loss integration test (routing in shell.rs abandon_gesture, semantics tested at tool_input level).
- [ ] T026: Enter existing text layer | Verify: clicking transformed headline edits same LayerId; hit policy defined
- [ ] T027: Caret movement + range selection | Verify: grapheme/bidi/emoji fixtures safe; caret agrees with rendering post-transform/zoom
- [ ] T028: Insert/delete + text clipboard routing | Verify: word replace preserves surrounding styles; Ctrl+A/C/X/V belong to text while editing
- [ ] T029: IME composition/focus safety | Verify: synthetic preedit/commit inserts once; manual IME check recorded or marked pending
- [ ] T030: Accurate caret/selection overlays | Verify: alignment at 33/100/200% zoom; old origin approximation gone; blink not dirtying doc
- [ ] T031: Text confirm/cancel/history | Verify: Enter=newline, Ctrl+Enter=confirm, Escape=cancel; one cohesive transaction; save cannot split a draft
- [ ] T032: Paragraph box create/resize gestures | Verify: click=point, drag=paragraph, frame resize one undoable edit
- [ ] T033: Repeated headline editing verification | Verify: full edit sequence + frame capture with caret; phase gate
### M4 — object-aware Move/Transform (034–045)
- [ ] T034–T045 as in plan (see progress ledger rows; each with its own verify)
### M5–M10 (046–092)
- [ ] T046–T092 as in plan (see progress ledger rows 046–092; each with its own verify)
### Optional (explicitly non-blocking)
- [ ] T093–T098: optional deferred unless the user requires automation

## Progress and evidence
- 2026-09-07: Goal initialized; old PRODUCTION-TODO ledger archived; baseline T001/T002 done; T003 done 2026-09-07 (fixture module + committed assets; re-verified 2026-09-09).
- 2026-09-09 (this session, T004): fixture suite rerun PASS (5+1 ignored); pickup recorded; harness extended in `tests/integration/src/app.rs` (shell_editor/shell_pointer/shell_stroke via ToolPointer::handle, menu_intent via Workspace::menu_context, apply_intent, tile-map readers, set_selection); `tests/integration/tests/thumbnail_reproducers.rs` with 4 `#[ignore]`d reproducers — each run shows the intended failure (verbatim outputs in THUMBNAIL-BASELINE.md Task 004 section): E01 weight-only commit → None; E06 1 column stored vs 22; E08 mask untouched after mask-focused stroke (stroke provably reached Brush + committed); E09 Reveal All composites fully hidden (bare no-tile mask; pre-op composite alpha 255). E09 runtime-refined (absent mask tile = zero coverage; stronger than source-traced). Hygiene: stray `ROUTED` eprintln removed from tool_input.rs. Deps: +ui (regular), +text-engine (dev) for integration-tests. Checks: integration package green (11/10/6/5+1; reproducers 4 ignored normal mode); app-shell 397+10 green; fmt clean; clippy clean.
- 2026-09-09: T005 DONE — baseline "Task 005" section: 12-subsystem inventory with exact functions/owners; dependency facts verified in Cargo.lock; no new core deps; no duplicate shaper/compositor.
- 2026-09-09: T006 DONE — baseline "Task 006" section: frozen scene (7-row layer table incl. oversized placed background + Alternatives + shadow effect + clipped adjustment + masked portrait + Subhead + background), sizes/zooms/scaling matrix (150/200% scaling = manual-hardware, marked), 19 acceptance steps mapped, fix order frozen.
- 2026-09-09: **M0 exit phase gate PASS**: fmt clean; check PASS; clippy -D warnings PASS; `cargo test --locked --workspace` PASS (68 binaries ok, 0 failed, 15 ignored). Gate repair: `fixture/thumbnail.rs` string-literal `▸` (U+25B8) → `>` (ui::no_typed_ui_glyphs rejects unrenderable glyphs in library source strings); the failure predated the session (T003's uncommitted work; recovery had rerun only the integration package). Gate rerun green.
- 2026-09-09: T007 DONE — edit_target.rs (kind/snapshot/per-doc store/read-time validation); Editor integration; `Intent::SetEditTarget` from the Properties control; Pick::EditTarget routing; ChromeOutput field; shell application. 6 tests. app-shell 403+10; ui 9 binaries; integration-tests green; fmt/clippy clean.
- 2026-09-09: T008 DONE — compositor/src/bounds.rs: content/document/styled bounds (Ctx internals pub(crate)), alpha_bounds with bounded tile-hash cache; 8 tests incl. negative-transform containment, compositor-pinned nested-group answer, shadow-reach growth, alpha precision + cache freshness. compositor 178+1; fmt/clippy -D warnings clean workspace-wide.
- 2026-09-09: T013 DONE — compositor preview pipeline: CompositeOptions.preview_generation (cache identity), LayerOverride + composite_region_with, Ctx::level_transform override consultation; doc.rs PreviewOverride outside the Document (no history/dirty/journal); TransformTool layer capture + quad_affine pub; ToolPointer::settle_preview after every sample + abandon. 5 new tests (3 doc + 1 end-to-end route + reuse). app-shell 423.
- 2026-09-09: T014 DONE — thumbnail_workflow.rs::the_interaction_foundation_holds_through_the_real_shell_routes (typed options, bounds, edit target, pending edit, camera round-trip). **M1 exit phase gate PASS** (fmt/check/clippy 0/test --locked --workspace: 68 ok, 15 ignored).
- 2026-09-09: T009 DONE — app-shell/src/interaction_geometry.rs: screen↔document over ui::CanvasCamera; document↔layer-local via ancestor-composed `document_transform_of` (convention documented: transforms parent-relative, composed root-first once; level conjugation identical to Ctx); singular refused; MaskSpace split per mask.linked. 6 tests, verified twice; app-shell 409; fmt clean; clippy 0 errors.
- 2026-09-09: T022 DONE — font selection + substitution reporting. Engine: FontStretch (usWidthClass), substitution policy shared by `substitute_for` and `attrs_for` (report=render by construction); condensed face reachable (DejaVu condensed = same family, usWidthClass 4). **Tile-cache repair**: composite.rs `hash_layer_props` Text arm now hashes the canonical `hash_layer` (was text/family/size only — weight/fill/stretch-only edits served stale tiles; T021 masked it by co-varying size). UI: searchable family picker + Face combo + substitution notice. Integration: end-to-end font test. Reviewer PASS; its findings fixed (file-format.md duplicate tail; candidate-pick buffer clear; case-contract pin test — fontdb 0.23 exact-match). Focused suites: text-engine 13+15+23+12+14+14+7+2, layer-model 127, compositor 185, app-shell 423, ui 964+9 binaries, integration thumbnail_workflow 9+1 — all green; clippy -D warnings 0; fmt clean. Latent follow-up recorded: load_font does not invalidate per-doc tile caches (deferred font-file route must add a font generation to CompositeOptions).

## Pickup verification
- 2026-09-07 (goal init): fresh context; verified old goal closed, archived ledger, inspected git status/log, ran fmt.
- 2026-09-09 (resumed session, T004 pickup): git status matched handoff; fixture suite rerun PASS (5+1); four gap routes re-verified at source; T004 executed end-to-end.
- 2026-09-09 (mid-session): every card's checks run immediately after its edits (per-card focused suites + fmt + clippy -D warnings); M0 gate rerun fully green after the glyph repair.
- 2026-09-09 (T022 pickup, fresh context): git status matches handoff Files changed; T021 focused checks rerun PASS (integration thumbnail_workflow 8+1 ignored; ui 960+61 across all binaries); plan cards 022-024 reread; T022 started.
- 2026-09-09: T023 DONE — point vs paragraph geometry (see T023 checkbox for full evidence). compositor 186; ui 965+9 binaries; integration thumbnail_workflow 10+1; text-engine suites green; clippy -D warnings 0; fmt clean. Reviewer PASS with fixes applied; suggestions recorded in the checkbox entry.
- 2026-09-09: T025 SURVEY (read-only, no edits — head start for the next context): tools/src/text.rs `TextSession {layer, origin, text: String}` is append-only (insert appends at end, backspace pops the last char — module doc states the limits honestly); `payload()` rebuilds the whole TextLayer each keystroke; TypeTool defaults sans-serif/24px. text-engine/src/edit.rs ALREADY provides the editing geometry on ShapedText: `caret_stops(line)`, `line_at_y`, `line_of_index`, `hit_test(x,y)`, `caret_rect(index)`, `selection_rects(start,end)` — byte-index based, ligature/bidi-aware. So T025 = session state only: add `caret: usize` (+ selection anchor or range) to TextSession, route insert/delete through caret positions with grapheme-safe boundaries (reuse engine helpers), keep the module's stated contract (cancel restores the exact original layer — capture the layer's TextLayer at session begin; today cancel pops the created layer, which works for click-created layers but T026's enter-existing-layer needs the snapshot semantics), and keep `payload()` assembling from the session. Tests: tools-level (caret insert/delete mid-string, cancel = exact original, no history debris) + shell route (T026+). See plan card 025 for the check list.
- 2026-09-09: T024 DONE + **M2 exit phase gate PASS** — fmt clean; check PASS; clippy -D warnings 0; `cargo test --locked --workspace` 68 binaries ok / 0 failed / 3480 passed / 14 ignored. T024 evidence: see checkbox. Phasing note: A3's "M2 (015–033)" merges the plan's Phase 2 (015–024, styled text persists/renders) with Phase 3 (025–033, canvas editing); the M2 exit gate is the Phase-2 gate (after 024) and has now passed; canvas editing (025–033) gates at Phase 3's end. 024 reviewer PASS (after manual-item record): undo chain order verified correct, non-vacuous, expectations field-complete for the changed fields; defaults-only fields covered by 018/022/023 tests.

## T022 design decisions (2026-09-09)
- Substitution is OUR documented policy, not cosmic-text's fallback: `FontLibrary::substitute_for(requested)` — None when family present/empty/library empty; otherwise the library's pinned default sans family. `attrs_for` applies the same rule (missing → Family::SansSerif) so renderer and report agree by construction; the requested name stays in the document. Rationale: cosmic-text 0.17's own missing-family pick sorts FontMatchKey ascending and prefers emoji faces (derived Ord on not_emoji) — not mirrorable honestly.
- Face selection is real: `FontStretch` (9-variant usWidthClass mirror of fontdb Stretch=ttf_parser::Width) added to CharStyle/BaseStyle + spans, serde-defaulted, hashed in compositor RunKey.content, selectable in the Character panel; dejavu fixture ships `sans_condensed` so condensed tests are deterministic.
- Font-file load route: deferred ("only if needed"); load_font embedded route already exists, no native file-dialog dep in tree (plan: no new core deps). Recorded, not silently dropped.
- UI font-safety check: load_font must leave egui's fonts untouched (asserted in the integration test).

## Handoff
- Current checkpoint: Phase 3 (M3) passed; cards now run card-by-card toward the next phase gate.
- Last completed: T056 (undoable/composable selection edits: the selection-apply loop emits Command::SetSelection with exact cloned inverses, one gesture coalesced into one step, the Select menu (All/Deselect/Inverse/Modify/save/load/reselect) rides the same route, steps reporting counts selection gestures, save/reopen preserves committed selections, abandoned gestures leave no trace; reviewer PASS round 3 after two rounds of claimed-comment-fix verification failures — all four items verified applied by grep + read).
- In progress: none — T061 (targeted boundary-refine brush) is the next dependency-ready card.
- Next exact action: T061 per plan card (docs/THUMBNAIL-WORKFLOW-IMPLEMENTATION-PLAN.md — read it first), then card-by-card in doer/reviewer style: implement → focused suites + clippy/fmt → independent reviewer subagent → fix all findings → mark verified in BOTH ledgers. Carried items: T033 frame capture, T032 pointer-frame-handle unit, quick-mask transform framing, per-participant Move previews, dock Confirm/Cancel buttons, manual device/IME checks, zero-edit cancel micro-test, locked-active-transaction drill test, un-routed paint tools through sample_to_layer_of, StrokeBuffer clip-space conflation, SmartObject done-check variant, T041 polish (error payload, pivot semantics, Skew-mode aspect pin, constructed-corner validation, numeric-field wiring), T042 remainder (align/distribute actions, smart-guide drawing, hidden-layer candidates, user SnapSettings, effects reach, shift-snap precedence, self-snap dead zone, per-gesture candidate rebuild), T043 remainder (live-preview pose for unlinked masks, PSD mask-offset mapping for non-identity transforms, multi-chain link IDs, unbounded-mask resample cost).
- Files changed (uncommitted, preserve): T004–T025 set — crates/tools/src/{text.rs,tool.rs}, crates/app-shell/src/{doc.rs,tool_input.rs,shell.rs,menu-adjacent}, crates/ui, crates/compositor, crates/text-engine, crates/layer-model, tests/integration — see the progress ledger rows for the per-card detail.
- Commands/checks (round-3 verified): tools 99+20+30, app-shell 428+10, integration 11+10+6+1(3i)+11(1i), ui 965+9 — all green; clippy -D warnings 0; fmt clean.
- Decisions/assumptions: drafts render via direct history-free kind substitution + restore-then-commit reconciliation (observably satisfies the card; NOT the T013 LayerOverride — recorded deviation); second click / menu action = CONFIRM a live session; toolbox switch = cancel it (product call to revisit); created-layer cancel = create+delete pair. Reviewer follow-ups for card 026: session×document scoping, perform_text_confirm status clobber, dead steps arithmetic in commit drain.
- Blockers/risks: none open; cards 026–092 remain (multi-session work; ledgers at every boundary; workspace gate at phase boundaries only).
- Context note: refreshed at the T026 verification boundary (reviewer round 2 PASS). Next context: full pickup protocol — read this ledger + progress ledger, verify git status, rerun `cargo test --locked -p tools -p app-shell --offline`, then start T027. Note: ACTIVE.md todo checkboxes T026+ are tracked in the progress ledger's status table (row 026 = verified); keep both in step at each boundary.
- T026 FIRST UNIT LANDED (2026-09-09): compositor facade `text_hit_index(run, x, y) -> Option<usize>` (text.rs) — shapes with the one engine library, returns None outside the laid-out block padded by half a line height, else `ShapedText::hit_test`'s caret byte index; exported from lib.rs. Test `a_click_places_the_caret_inside_the_block_and_misses_outside_it` (inside = engine-agreeing caret index; left edge = byte 0; far past/below = None). PITFALL FIXED IN THE TEST: the engine mutex must be scoped — holding `engine()` across a facade call deadlocks. compositor 187+1 green; clippy 0; fmt clean.
- 2026-09-09: T050 DONE + reviewer round 2 PASS — full evidence in progress ledger row 050. Round 1 findings all fixed: (1) sibling layers sharing a linked asset now refresh together (grouped per (doc, asset), decode once, ONE transaction, stamp after apply); (2) the recorded source size rides the undoable transaction via the new editor-core Command::SetAssetSourceSize (additive wire format, AssetNotFound refusal, inverse restores the previous Option) — refresh→undo→48px-file now renormalizes against the restored 16px record (discriminated by test); minors fixed: missing-source report pinned ("skipped" + status "missing"), ghost-tile clearing pinned (600×500→32×32 shrink), history-depth assert replaced the dead computation, refresh label names the layer, no-op refresh (identical content after reopen) records NO undo entry. Deferrals recorded in the ledger row (linked-mask canvas-fixed alignment = T043 remainder; degenerate transform refuses loudly; mtime stamps re-decode once after reopen).

- 2026-09-09: T051 DONE + reviewer round 1 PASS (2 warnings fixed in-place: clipboard.rs doc link now names set_image_clipboard and distinguishes it from the internal Edit▸Copy store; the screenshot check became a repeatable #[ignore]d helper AND was demonstrated: a FOREIGN 7×5 image seeded onto the OS clipboard by a .NET process was read through the adapter, and the arboard set→get round trip is byte-exact — both run on this Windows host, sequentially; concurrent runs of the two helpers race on the one OS clipboard, recorded). Editor injection seam: image_clipboard field + set_image_clipboard + image_clipboard_mut, distinct from the internal clipboard store. See progress ledger row 051 for the full evidence.

- 2026-09-09: T052 DONE + reviewer round 2 PASS (round 1's critical per-frame OS-clipboard probe fixed by a 1 s-TTL cache with focus invalidation; Paste Into gated on the internal store via additive ClipboardState.external_pixels; keyboard Ctrl+C/X/V through Action::Copy/Cut/Paste dispatched via the SAME menu route; process-wide clipboard mutex — parallel-test heap corruption root-caused and removed; round-2's dead egui-focus guard removed after the reviewer proved route_key already ignores chords under egui text focus). Foreign-payload evidence: a .NET-seeded 11×7 image (OleFlushClipboard so it survives the owner process — delayed rendering dies with it) pasted through the REAL menu route as a centered SmartObject. Hermeticity: FakeClipboard injected in all three test fixtures. See progress ledger row 052 for full evidence and recorded deferrals (Linux clipboard-manager byte-exactness, TTL staleness, FakeClipboard::seed validation bypass).

- 2026-09-09: T056 DONE + reviewer round 3 PASS (round 2 FAILed because two comment fixes were claimed but not applied — LESSON: verify every claimed comment edit by grep before sending to review; the reviewer's pass criterion was the dispatch-comment fix, verified). Selection edits ride Command::SetSelection with exact inverses, coalesced per gesture; the Select menu + save/load/reselect ride the same route; the marquee/undo/redo/compose/save/abandon tests pin the card's checks; the "Select" history label replaces the gizmo-era mislabel; steps reporting counts selection gestures. See progress ledger row 056.

- 2026-09-09: T055 DONE + reviewer round 2 PASS (round 1 REJECT: clicking a mask thumbnail on a NON-ACTIVE row aimed the target at the previously active layer — fixed by making both wells select the row first, discriminated by a new ui test; the border now isolates the target row's well; the pin upgraded to the FULL target triple via PinnedEditTarget + the shared resolve_edit_target used by both routes; off_pointer folded into the resolver unconditionally in round 2). E08 REPRODUCER FLIPPED GREEN (the mask-focused stroke edits coverage; recorded in the reproducers table). Deferrals recorded: property_focus echo divergence (inherited card 007 design), remove-mask-mid-gesture error naming, thumb_scale layout polish. See progress ledger row 055.

- 2026-09-09: T057 DONE + reviewer round 2 PASS (round 1 REJECT: 2 criticals — Hide Selection hid outside the selection's bounding tiles (absent-tile violation) and the document→layer-local conversion was missing; round 2 PASS-with-warnings, all findings fixed: floor on negative pre-image corners, bounded walk (256 tiles/axis → Err instead of gigabytes), stale-doc fix, transformed-layer end-to-end test, comment fixes). Landed: mask_creation_coverage helper (layer-local coverage; pre-imaged selection; content ∪ canvas-pre-image walk; per-mode pixels; all-zero tiles omitted; Result-returning), create_mask atomic transaction (SetLayerProperties + PaintTiles(Mask)) with the already-masked + missing-selection gates pinned, ui menu routes Mask(op) action intents to the app. E09 REPRODUCER FLIPPED GREEN (drives menu_bridge::perform; the gate check asserts the op resolves ENABLED). Tests: four-distinct-real-coverages, multi-tile convention (far tile STORED revealed), fractional 128-verbatim, moved-layer doc-space reveal, refusal double-gate. GOTCHA for future cards: a test whose probe indexes composite pixels must stay INSIDE the canvas — the +200px-move probe read out-of-bounds garbage; and Selection::coverage_at returns a 0..1 f32 (dividing by 255 again double-scales). See progress ledger row 057.

- 2026-09-09: T058 DONE + reviewer round 2 PASS (round 1 FAIL, 2 reproduced criticals: the coverage writer's clear path never ran — Invert silently no-op'd on all-255 tiles — and the mask fill lerped toward WHITE so a black fill changed nothing; warnings: double opacity on solid fills, pose-less fill/invert, document-space stroke clipping, cross-doc colour-backup clobber — ALL fixed and pinned). Landed: sample_to_mask_of + sample_to_paint_target_of (both ctx sites); ToolContext.paint_space_canvas (canvas in the paint target's space; stroke dabs clip against it); fill_mask_coverage (blend toward the LUMINANCE value by the paint alpha = CoveragePatch::blend shape; non-Normal refused; opacity baked on the solid route like the content fill); pose-aware read_mask_coverage + write_mask_coverage (pixel-center convention floor(pose·(m+0.5)−0.5) both ways; pre-image walk capped 256 tiles/axis; clear rule for all-zero results); MaskOp::Invert → invert_mask in STORE space (absent → stored all-255; all-zero results cleared; out-of-grid tiles flip in place; pose-exact); per-DOCUMENT colour wells (doc_colors map + set_edit_target_kind stash/swap/restore + no-op guard) fixing the tab-switch desync. GOTCHAS: 50% white on a revealed mask legitimately changes nothing (blend toward 1.0) — pin blends with black/50%-black instead; mask_tiles returns None when the store empties (assert None-or-empty, not Some(0)); probing pose-mapped paint must read STORE bytes directly, canvas-space reads go through the pose now. Deferrals recorded (reviewer-accepted): bucket/gradient/shape pose routing, clear/stroke selection mask targeting, backup pruning on close. See progress ledger row 058.

- 2026-09-09: T059 DONE + reviewer round 3 PASS (round 1 FAIL — 3 criticals: unregistered popup strings (blank rows; KNOWN_KEYS now pins them), the Overlay tint discarded by the alpha-keyed shader (FIX: carried alpha, raise-only), mask.inverted ignored by views+thumbnail (FIX: flip in apply_mask_view + mask_thumbnail + apply_mask); round 2 caught read_mask_coverage sampling through the FORWARD pose after the canvas_to_mask signature took the inverse (FIX: pose_inverse hoisted everywhere + a discriminating store-side probe test); round 3 closed the badge test's dead branch (probe the second row's badge id directly) and RECORDED apply_mask's mixed-space bake on transformed layers with the cov(M⁻¹·s) derivation). Landed: MaskViewMode panel state + set_mask_view/apply_mask_view in composite_masked (every upload path); mask_thumbnail + mask_thumbs; well popup (view modes direct-write, ops via the menu route after right-click-selects-row, fresh-flag close, cull-order lifecycle, has_mask guard, doc-switch clear, clamped anchor); accent target badge; strings + KNOWN_KEYS. GOTCHAS: egui's alpha-keyed shader (mix by alpha) discards RGB where alpha=0 — any canvas-space tint must carry alpha; tr() returns “” for unregistered keys and no_localized_literals does NOT check registration — add every new key to KNOWN_KEYS; canvas_to_mask takes the POSE INVERSE (watch the direction at every call site). Deferrals recorded in ledger row 059 (live-transform pose, density/feather at store strength, session-only persistence, undo re-anchor, carried-alpha approximation, apply_mask mixed-space bake → T043 remainder/063). See progress ledger row 059.

- 2026-09-09: T060 DONE + reviewer round 3 PASS (round 1 FAIL — the preview double-applied the baseline: layer_pixels carries the enabled mask in alpha, multiplied again by the refined coverage; round 2 fixed the critical via pixels::read_layer RAW content + bounded preview + doc/placement fixes but FAILED on process integrity — four claimed comment fixes not applied; round 3 fixed every doc line and added the host-path discriminator test the reviewer required). Landed: selection::RefineParams/refine_mask (smooth→shift→feather→contrast; contrast = the midpoint-pinned coverage curve) + the RefineMask dialog (RAW-store content, pose-aware baseline, canvas-aligned sampling because the refined rect grows, bounded preview ≤256px with the morphology downscaled — confirm always full-res, three backdrops, identity refused at the dialog AND DialogAction::is_valid) + the full wiring (Layer ▸ Refine Mask… gated on mask, host builder refusing maskless, DialogAction → refine_mask_with = ONE write_mask_coverage transaction). GOTCHAS: layer_pixels is the MASKED composite — never feed it where unmasked content is needed (read_layer is the raw store); the refined mask's rect GROWS (feather) — always canvas-align via coverage_at; egui pixel-center conventions apply to downscale mapping; cross-crate test seams cannot be #[cfg(test)] (the dependency's test cfg doesn't propagate). Deferrals recorded in the module doc + ledger row 060 (mixed-space on transformed layers → T043 remainder, opacity/blend ignored in the preview, downscaled-radii/step-granularity approximations, checker approximation, no shortcuts). See progress ledger row 060.

- 2026-09-09: T055 SURVEY (read-only — head start): card 055 = mask-thumbnail click selects the MASK as the edit target; content click selects content; clear border/target label; pointer AND off-pointer ops through the SAME target resolver, target pinned per gesture; quick mask stays a separate temporary mode restoring the prior target on exit. Existing seams: T007's EditTarget/EditTargetKind + Editor::edit_target()/set_edit_target_kind() + ChromeOutput::edit_target + ui Intent::SetEditTarget (Properties Layer/Mask control already writes it); chrome.rs edit_target field (line ~195); ui/src/panels/properties.rs:28 has the mask thumbnail. Design sketch: the Layers panel mask thumbnail click (ui/src/view/layers or panels/layers — find the thumbnail widget) raises Intent::SetEditTarget{Mask(layer)}; content thumbnail click raises {Content(layer)}; chrome draws a target border/label on the selected thumbnail (paint in the layers view per Pick::EditTarget state); ToolPointer's gesture-begin pins EditTargetKind for the gesture (tools read the target from ToolContext — check tool_input.rs context creation: what does the brush read today? card 040 wired mask painting to the EditTarget? verify: the E01-era defect was brush-on-mask painting content; card 040 may have landed the consumption — READ THE LEDGER ROW 040 first); quick-mask mode = temporary override, restore previous on exit. Tests: ui-level thumbnail click → intent; shell route: intent applied → editor.edit_target updated; brush-on-selected-mask changes coverage tiles only (mask_tile_map changes, layer_tile_map unchanged); switching back paints content; undo/remove-mask/tab-switch cases still valid. See plan card 055 + ledger row 040.

- 2026-09-09: T054 DONE + PHASE 5 GATE PASS — reviewer PASS round 1 (its paste-into-persistence warning fixed in-place: tile map + mask tile map pinned after reopen; history-depth pin on the refusal; dead fixture call removed). The integration workflow test rides the real Place/paste/linked routes and the real persistence path (embedded assets in the package; sources renamed away; linked file deleted). Gate evidence: fmt PASS, check PASS, clippy -D warnings PASS, cargo test --locked --workspace 68 binaries 0 failed. See progress ledger row 054.

- 2026-09-09: T053 DONE + reviewer round 1 PASS (its 2 warnings fixed in-place: off-canvas selection refuses instead of an invisible paste behind a success message; the fractional test's ink reference corrected to original(9,9) — the pixel the origin-pinned clip actually holds; plus the exact-created-id selection and a selection-bounds-bounded coverage loop). The full image is retained under a linked selection-derived mask in one transaction; disabling the mask reveals it; fractional coverage blends (compositing runs in the working/linear space — the exact byte is not pinned, recorded). See progress ledger row 053 for full evidence.

- 2026-09-09: T053 SURVEY (read-only — head start): card 053 = Paste Into with a REAL mask replacing destructive alpha multiplication. Current state: menu_bridge::paste(into=true) multiplies the pasted raster's alpha by the selection coverage (pixels::mask_by_selection) — destructive, unrecoverable. Card demands: (1) paste the FULL image (internal store route today; card 052's external route may also need into-support — decide: external + into = place the external payload then attach the selection mask, or keep external out of scope and document), attach a selection-derived raster mask (LayerMask with coverage tiles — respect the compositor's missing-tile coverage convention: absent tile = fully hidden, cf. T025-era E09 note); (2) a FOCUSED selection-to-coverage helper (selection shape -> coverage tiles) reusable by card 057's four mask-creation ops — propose menu_bridge::pixels or a new small module; look at editor-core::Selection variants (Rect/Ellipse/etc?) and how BrushTool targets masks (PixelTarget::Mask) for the storage shape; (3) mask LINKED state + transform alignment: the pasted layer's transform must map the mask correctly (unlinked mask = document-space coverage; card 043's compensation semantics); (4) disabling the mask reveals the full original (LayerMask has a disabled flag? check layer-model::LayerMask fields); (5) undo removes layer+mask in ONE step (the Transaction already creates the layer + paints tiles; add the mask attachment command inside it). Verify list: Paste Into shows only the selected area; disabling the mask reveals the full image; undo removes both in one step; fractional coverage maps through the layer transform. Tests: menu_bridge-level (full pixels stored under mask, disabled mask reveals, one undo entry) + fractional-coverage fixture. Check editor_core Selection::coverage/rasterize helpers that may already exist from card 009 (bounds) before writing new ones.

- 2026-09-09: T051 SURVEY (read-only — head start): card 051 = injectable image clipboard service (proposed crates/app-shell/src/clipboard.rs) with real OS impl + deterministic fake, straight-RGBA normalization with bounded dimensions, busy/unsupported as recoverable errors, shutdown release. Facts verified: arboard 3.6.1 is ALREADY a direct app-shell dependency (text clipboard in shell.rs route_text_clipboard_key) and its DEFAULT features include `image-data` (registry Cargo.toml: default = ["image-data"]) — so ImageData{rgba: Vec<u8>, width, height} (straight RGBA per docs) is available with NO manifest change (add a comment naming card 051); egui-winit's arboard is the one with image-data off (irrelevant). Design sketch: trait ImageClipboard { fn get_image(&mut self) -> Result<Option<ClipboardImage>, ClipboardError>; fn set_image(&mut self, img: &ClipboardImage) -> Result<(), ClipboardError> } — Option=None for a text-only clipboard (a NORMAL result, not an error: the paste router falls back to internal content); ClipboardError::{Busy, Unsupported, Unavailable} recoverable; real impl wraps arboard::Clipboard created per operation (the existing text route's pattern — Windows ignores DestructorError; resource release = Drop, no global to shut down: record the shutdown item honestly); fake = in-memory slot for tests. Normalization: bound dimensions (refuse > some sane max, e.g. 16384 — document the bound), straight RGBA in/out (arboard IS straight RGBA on Windows/Linux; macOS BGRA? — verify against arboard 3.6.1 source before claiming). Shell wiring: Editor/Shell field `clipboard: Box<dyn ImageClipboard>` defaulting to the real impl; route_image_paste seam for card 052. Tests: fake round-trips RGB (no alpha) and transparent RGBA (alpha preserved); normalization bounds; busy/unsupported mapped to errors not panics. Manual item (Windows screenshot read) recorded per card. See plan card 051 + arboard docs link in the plan.

- T026 SURVEY (read-only, 2026-09-09 — head start): ToolContext already carries `layer_stack: Vec<LayerId>` top-most-first (tools/src/tool.rs:557) and `canvas`; the engine's `ShapedText::hit_test(x, y)` (text-engine/src/edit.rs) + `interaction_geometry::{screen_to_document, document_transform_of}` (app-shell) give the click→layer-local→caret chain; the T007 `Pick::EditTarget` route shows how the chrome reports structured picks to the shell. Design sketch: extend ToolContext with the layer payloads the Type tool needs (or resolve in the shell's pointer route before Tool::on_pointer_up: find the top-most unlocked text layer whose transformed ShapedText bounds contain the click, hit-test to a byte index, begin a session with created_layer=false + original=captured payload + caret=hit index); double-click on a Layers-panel text row → new Intent; empty canvas → the existing create path; session must carry DocumentId (round-3 follow-up). Hit policy to define+test: top-most text layer wins; non-text layers do not capture Type clicks (they fall through to create); locked layers are skipped for entering but their pixels still hit-block? — decide and test per the plan's "Respect locks and nested groups".
