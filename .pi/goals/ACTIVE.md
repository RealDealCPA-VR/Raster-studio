# Active Goal

Status: ACTIVE
Goal: "proceed through all the remaining tasks with full validation until finished and dont stop" (continuation of the PRODUCTION-TODO goal; original condition: "look at the production-todo in docs and accomplish each task with validations steps until completed")
Started: 2026-09-01T00:00Z
Last updated: 2026-09-01T03:40Z

## Scope and acceptance criteria
Source of truth: `raster-studio/docs/PRODUCTION-TODO.md` (untracked in git). Work top-down P0 → P4.
- [ ] A1: Every P0 task (P0.1–P0.16) done with its Validate line passing.
- [ ] A2: Every P1 task (P1.1–P1.19) done with its Validate line passing.
- [ ] A3: Every P2 task (P2.1–P2.12) done with its Validate line passing.
- [ ] A4: Every P3 task (P3.1–P3.14) done with its Validate line passing.
- [ ] A5: P4 scope decisions confirmed in parity-matrix Tier C; no menu item promises deferred capabilities.
- [ ] A6: Release gate: `unavailable_reason` returns None except P4-deferred; four gates green (fmt/clippy/check/test) after every task; end-to-end open/edit/save/reopen/export verified by running the app.

## Constraints and decisions
- Per-task Validate lines in PRODUCTION-TODO.md are objectively checkable — never tick without passing.
- Every task must leave the four release gates green: `cargo fmt --check`, `cargo clippy --workspace --all-targets`, `cargo check --workspace --all-targets`, `cargo test --workspace` (run from `raster-studio/`).
- Dialogs already exist in `crates/ui/src/dialogs/` (10 finished dialogs, ~12k LOC) but have zero call sites — P0 hosts them.
- New dialogs must be registered in `ui::dialogs::tests_support::all_dialogs()` or the registry count test fails.
- MenuAction enum lives in `crates/ui/src/menu.rs`; enablement gate is `menu_bridge::unavailable_reason` (menu_bridge.rs:403); shell consumes via `Shell::apply_chrome` (shell.rs:971).
- Editor colors are plain fields `foreground`/`background: [f32;4]` set via `ChromeOutput.set_foreground/background` — not undoable.
- `Command::SetCanvasSize` (editor-core command.rs ~:400) is the undoable canvas-resize used by Crop/Trim; ImageSize needs a NEW resample command.
- Headless test harness: `run_chrome(editor, click_id)` in chrome.rs tests (~line 1389); shell tests use `Shell::new` without GPU.
- Windows host, no pen hardware, no macOS machine — P3.6 signing and P3.4 macOS packaging validated as far as the host allows (CI configs can be written but not executed).

## Baseline
- Workspace state: branch `main`, commit e001c8a, clean tree except untracked `.pi/` and `docs/PRODUCTION-TODO.md` itself.
- Checks before changes: `cargo fmt --check` PASS; `cargo check --workspace --all-targets` PASS; `cargo test --workspace` PASS (exit 0, 3288+ tests); `cargo clippy --workspace --all-targets` PASS (exit 0, no warnings).
- PRODUCTION-TODO.md measured baseline (2026-09-01, e001c8a): 22 crates, 47 tools, 9 menus, 13 panels, app launches/renders/screenshots.

## Todo
### P0 — Dialog surface
- [x] T-P0.1: DialogHost in chrome.rs (Option<ActiveDialog> enum over 10 dialog types, drawn after docks, one at a time, Escape/Enter via resolve, suppress canvas input while open) | Verify: headless chrome test opens dialog via MenuAction, modal drawn, Escape cancels, no command produced; canvas click while dialog open routes no RoutedPointer — DONE 2026-09-01: `crates/app-shell/src/dialog_host.rs` (new, ~430 LOC incl. tests), wired in chrome.rs (host field+draw+harvest interception+channel_chords gate), ChromeOutput.dialog+dialog_open, shell.rs (keymap gate in on_key, pointer veto in on_pointer, transitional out.dialog consumer), menu_bridge.rs (LayerStyle arm removed from perform — host intercepts; gate test drives dialog-hosted actions). Tests: `a_menu_action_dialog_opens_draws_and_escape_cancels_without_producing_anything`, `a_canvas_click_while_a_dialog_is_open_lands_on_the_modal_and_produces_nothing`, dialog_host::{opens_instead_of_toggling, without_a_document}. NOTE: egui modal paints fully 2 frames after opening (height learning) — tests use 3 passes.
- [x] T-P0.2: File▸New… via NewDocumentDialog (1920×1080/transparent test; cancel creates nothing) | Depends: T-P0.1 | Verify: dialog test — DONE 2026-09-01: `import::blank_document` gained `BlankBackground` (Transparent | Solid{rgba8,depth}, per-tile solid fill incl. edge tiles, 8- or 16-bit); `OpenDocument::blank_with_background`; `Editor::new_document_with(w,h,title,bg)` (pub; act_new_document delegates); `DialogHost::open_for_menu_action` handles NewDocument; `Chrome::open_new_document_dialog()`; `Shell::perform(Action::NewDocument)` opens the dialog (Ctrl+N too); shell apply_chrome consumes `DialogAction::NewDocument` (White/Black/Custom/Transparent → BlankBackground). Tests: shell `a_confirmed_new_document_dialog_creates_the_document_it_confirmed` (1920×1080 transparent via apply path), `..._honours_a_solid_background` (300×300 white incl. edge tiles), chrome `cancelling_the_new_document_dialog_creates_no_document`, dialog_host NewDocument-open assert. NOTE: NewDocumentDialog::confirm() now gates on blocked_reason() (contract confirm⟺!blocked); BitDepth::Sixteen refused in blocked_reason with precise reason until P2.5 (compositor's fill_layer reads RGBA8 only — 16-bit tiles would composite garbage); ui test `a_sixteen_bit_document_is_refused_until_live_compositing_handles_it` pins it. Pre-existing test `the_windows_size_reaches_every_open_document_and_fits_it_once` updated: drives the confirmed-spec apply path instead of perform(NewDocument). Smoke: `--shot` with an image → real wgpu frame, 302KB PNG.
- [x] T-P0.3: File▸Export As… via ExportAsDialog | Depends: T-P0.1 | Verify: JPEG q30 < q95 file size; scale 0.5 → half pixel dims — DONE 2026-09-01: `OpenDocument::export_job(&ExportJob, dir)` (composite → raster::export::linear_from_rgba8 → export_batch_to_dir; per-entry preset renamed base+suffix; ExportMetadata no ICC) + `Export(#[from] raster::ExportError)` on DocumentError + `OpenDocument::export_preview(max_edge)` (&self free compositor + box_downscale) + `ExportAsDialog::set_proxy` (drops cached render) + `DialogHost::refresh_preview` (placeholder → real composite once per open; Chrome::ui calls it before drawing) + host intercepts `MenuAction::Export(_)` → ExportAsDialog (doc w/h/title/placeholder proxy) + shell consumes `DialogAction::Export` (folder picker via new `Editor::pick_export_folder`; export_layers refactored onto it). Tests: doc `export_job_writes_every_entry_and_honours_quality_and_scale` (noise doc; q30<q95 bytes; 0.5 scale → 32×32→16×16 decoded), `export_preview_is_the_whole_composite_at_most_the_proxy_cap`, shell `a_confirmed_export_dialog_writes_through_the_folder_picker` (ScriptedDialogs.export_folders → file written + status), dialog_host Export-open assert.
- [x] T-P0.4: Image▸Image Size… via ImageSizeDialog + resample Command | Depends: T-P0.1 | Verify: reason None; 800×600→400×300 + undo byte-identical — DONE 2026-09-01: NEW `Command::ResampleImage { size, changes: Vec<(PixelTarget, TileDelta)> }` in editor-core (apply: lock-free resolve via new `resolve_pixel_key`, applies every target's full new map, sets meta.size, inverse = Transaction of PaintTiles(old maps) + SetCanvasSize(old)); `OpenDocument::resample_command(&ImageSizeSpec)` in app-shell (walks layer tree, materializes layer pixels via tile store → raster::export linear/resample/rgba8 pipeline with doc color space; raster masks via new `resample_coverage` bilinear; removals for tiles beyond new canvas; resample=None → empty Transaction no-op); host arm ImageSize→ImageSizeDialog::new(w,h,72.0); shell consumes ResizeImage (None-resample → precise status; else build+apply); menu_bridge flips ImageSize to enabled; ui history panel StepKind::Transformed; app-shell dirty.rs → DirtyTiles::all; DocumentError gains Grid+Pixel variants. Tests: doc `resample_image_size_resizes_and_undoes_byte_for_byte` (800×600→400×300; tile map equality + composite byte-equality after undo; redo), `resample_image_size_scales_a_raster_mask_with_the_layer`, menu_bridge `image_size_is_enabled_and_hosted_now` (reason None), shell `a_confirmed_image_size_dialog_resamples_through_history`, dialog_host ImageSize-open assert. GOTCHA learned: fill_region with a partial-tile rect + empty edges fills NOTHING by design — rasterize edge tiles yourself.
- [x] T-P0.5: Image▸Canvas Size… via CanvasSizeDialog on SetCanvasSize | Depends: T-P0.1 | Verify: reason None; anchor tests — DONE 2026-09-01: `OpenDocument::canvas_size_command(&CanvasSizeSpec)` = Transaction[SetCanvasSize + TransformLayer(translation) per root layer when offset≠0 + fill_region per exposed strip on the BOTTOM raster layer when the dialog chose a colour] (`exposed_rects` = up to 4 clamped strips; fill rects translated by −offset into layer pixel space; **working-overlay** accumulation so strips sharing a tile do not clobber each other); host arm CanvasSize→CanvasSizeDialog::new(w,h,72.0); shell consumes ResizeCanvas; menu_bridge flips CanvasSize to enabled. Tests: doc `canvas_size_anchored_top_left_keeps_the_pixels_and_undoes`, `canvas_size_anchored_centre_offsets_the_content` (content lands at (50,50)), `canvas_size_fill_colours_the_exposed_area_on_the_backdrop` (white fill + undo/redo), menu_bridge `canvas_size_is_enabled_and_hosted_now`, shell `a_confirmed_canvas_size_dialog_reframes_through_history`, dialog_host CanvasSize-open assert. NOTE: fills on the bottom raster layer only — no raster layer ⇒ exposed area stays transparent whatever the dialog said (documented in the method).
- [x] T-P0.6: Image▸Rotation▸Arbitrary… angle dialog | Depends: T-P0.1 | Verify: reason None; 90° equals fixed 90° — DONE 2026-09-01: NEW `crates/ui/src/dialogs/canvas_rotation.rs` (`ArbitraryRotationDialog`: one DragValue angle field, confirm→`DialogAction::RotateCanvas(f64)` — NEW variant with is_valid/label; registered in mod.rs all_dialogs + one_of_each_action); `OpenDocument::rotate_canvas_arbitrary(degrees)` = general inverse-mapped premultiplied-bilinear rotation, canvas grows to rotated bbox (`sample_bilinear_premultiplied` helper in doc.rs); shell consumption routes orthogonal angles (90/180/270 via rem_euclid) to the EXACT fixed commands through menu_bridge::perform, 0° → status no-op, else general path; menu_bridge flips Arbitrary to enabled. Tests: ui dialog tests (confirm/escape/draw-both-themes), shell `a_ninety_degree_rotation_through_the_arbitrary_dialog_matches_the_fixed_one` (byte-equal previews vs fixed 90°), `an_arbitrary_angle_grows_the_canvas_resamples_and_undoes`, doc `an_arbitrary_rotation_grows_resamples_and_undoes_exactly` (undo byte-exact), menu_bridge `arbitrary_rotation_is_enabled_and_hosted_now`, dialog_host Arbitrary-open assert.
- [x] T-P0.7: Image▸Reveal All | Depends: T-P0.1 | Verify: reason None; grow-to-content; undo restores — DONE 2026-09-01: `OpenDocument::reveal_all_command()` = union of pixel layers' CONTENT bounds (per-tile alpha scan, not tile-aligned) pushed through each layer's own transform; grows canvas AND translates all root layers when content sits at negative coords; empty transaction when everything fits (perform skips recording → status "Every layer already fits the canvas"); perform arm in menu_bridge; reason removed. Tests: doc `reveal_all_grows_the_canvas_to_the_layer_content_and_undoes` (32×32 layer dragged +48 on 32-wide canvas → 80×32; content intact; undo), `reveal_all_pulls_negative_content_back_inside_and_undoes`, `reveal_all_on_a_contained_canvas_reveals_nothing` (undo_depth 0). NOTE: measured with each layer's OWN transform — a layer inside a transformed group can be under-measured (named limitation in the method docs); text/shape bounds not measured (parametric geometry).
- [x] T-P0.8: Filter parameter dialogs via FilterDialog with live preview | Depends: T-P0.1 | Verify: reason None for Custom+Offset; Gaussian 0/8 test — DONE 2026-09-01: host arm Filter(id)→FilterDialog::new(spec, filter_source) (`menu_bridge::filter_source` = active layer's pixels read-only as FilterBuffer); shell consumes RunFilter via `menu_bridge::run_filter_invocation` (refactor of run_filter; run_filter keeps defaults path for direct invocation — same engine); unavailable_reason arms for Custom/Offset removed. Tests: menu_bridge `filter_parameter_dialogs_are_enabled_and_hosted` (reason None ×2 + host opens), shell `a_confirmed_filter_dialog_runs_at_radius_zero_and_eight` (radius 0 → no pixel change + no undo step; radius 8 → pixels change + exactly one undo step; NOISE image — a uniform image blurs to itself), `every_filter_in_the_menu...` updated to skip Custom/Offset at defaults (identity by design; dialog path covers them).
- [x] T-P0.9: Filter Gallery | Depends: T-P0.8 | Verify: reason None; lists FilterId::ALL.len(); applies same pixels — DONE 2026-09-01: NEW `crates/ui/src/dialogs/filter_gallery.rs` (`FilterGalleryDialog`: scrollable row per FilterSpec with a THUMBNAIL = the real filter at defaults applied to a box-downscaled copy of the layer's pixels (cached per filter, rendered lazily); click row selects; confirm → RunFilter(default params) = the same invocation the menu item produces; sizes via dialogs::sizes extents per the style-gate test; manual Debug impl — TextureHandle lacks it). Registered in mod.rs (module + re-export + all_dialogs with a transparent 16×16 source). Host arm FilterGallery→gallery with filter_source; menu reason removed; the old host test now uses FileInfo as the not-hosted case. Tests: ui `the_gallery_lists_exactly_the_menu_catalogue`, `confirming_runs_the_selected_filter_at_its_defaults` (invocation equality with the menu path), resolve enter/escape, draw-both-themes; shell `applying_from_the_filter_gallery_matches_the_menu_item` (byte-equal previews doc A via gallery confirm vs doc B via perform).
- [x] T-P0.10: Layer▸Layer Style… via LayerStyleDialog | Depends: T-P0.1 | Verify: drop shadow → one history entry, undo removes — DONE 2026-09-01 (host wiring was the P0.1 pilot; this task is the validate): shell `a_drop_shadow_set_through_the_layer_style_dialog_is_one_undoable_step` — opaque block on transparent canvas (a shadow behind an edge-to-edge opaque layer is invisible), dialog confirm → one journal entry, composite changes, undo via the shell's Action::Undo channel removes both.
- [x] T-P0.11: Colour picker wired to swatches/stops/params | Depends: T-P0.1 | Verify: foreground swatch test — DONE 2026-09-01: NEW `Intent::OpenColorPicker(ColorWell)` emitted on swatch DOUBLE-click in the toolbar's color_wells (single-click keeps its pinned edit-target semantics); `Pick::OpenColorPicker` → `ChromeOutput.color_picker` → Chrome::ui opens the host with the target remembered (`DialogHost.color_target`); confirmed SetColor lands in the well that opened it (SetForeground OR SetBackground — fold() no longer defaults to foreground); **ScreenSampler implemented**: `dialog_host::CanvasSampler` maps egui logical pointer → physical px → doc coords via `canvas_camera_of/canvas_viewport` mirror, samples a 1×1 composite through the FREE compositor (&doc, per-click cost); Chrome::screen_sampler builds it per frame from frame_geometry; DialogHost::ui regains a sampler param threaded to every dialog that takes one. Tests: chrome `a_foreground_swatch_double_click_opens_the_picker_and_its_confirm_lands_in_the_well` (intent → picker drawn → known RGB set via `active_color_picker_for_test` → Enter → out.set_foreground == RGB, background untouched); `active_for_test`/`active_color_picker_for_test` test accessors added. NOTE: effect/gradient/filter-param swatches go through each dialog's own nested ColorEdit (dialogs' existing mechanism), now with the live sampler threaded through.
- [x] T-P0.12: Gradient editor from options-bar ramp swatch | Depends: T-P0.1 | Verify: stop round-trip test — DONE 2026-09-01: (a) the ramp swatch click now emits `Intent::OpenGradientEditor` (the inline popup stop editor is REMOVED — replaced by the real dialog); (b) **the ramp reaches the stroke now**: NEW `ToolContext.ramp: GradientRamp` (tools crate; default `black_to_white()`), threaded in `tool_input::off_pointer` from NEW `Editor.gradient_ramp` via `GradientRamp::from_ui_gradient(&layer_model::Gradient)` (sRGB→linear stops; opacity from alpha_stops or stop alpha; midpoints documented as not carried); `GradientTool` renders with `ctx.ramp` (settings' other fields kept); (c) dialog confirm → `DialogHost.gradient_target` → `ChromeOutput.set_tool_gradient` → Chrome writes `workspace.options.set_gradient` + pushes SetToolGradient intent + read-back `set_gradient_ramp` → shell `Editor::set_gradient_ramp`. Tests: chrome `a_confirmed_gradient_editor_round_trips_into_the_workspace_and_the_editor` (dialog stops → workspace.options == editor read-back == confirmed ramp), tools `from_ui_gradient` path exercised via updated painting tests (`a_gradient_produces_a_monotone_ramp` and `dithering_breaks_up_the_bands...` now set `ctx.ramp` — proving the tool paints the CONTEXT ramp), ui `editing_the_gradient_ramp_opens_the_dialog_and_the_ramp_intent_round_trips` (rewritten from the popup contract). — RESEARCH COMPLETE 2026-09-01, NOT STARTED: the ramp swatch EXISTS (`toolbar.rs:477 gradient_control`, id `super::ids::gradient_swatch(tool)`); today its click toggles an inline egui POPUP stop editor (popup id "raster-gradient-stops") — swap that for `Intent::OpenGradientEditor` → Pick → `ChromeOutput.gradient_editor: bool` → `DialogHost::open_gradient_editor` (GradientEditorDialog::new(workspace.options.gradient(editor.effective_tool())) + `gradient_target: Option<ToolId>` like color_target). **CRITICAL DISCOVERY — pre-existing integration gap:** the ramp NEVER reaches the stroke. GradientTool holds `settings: GradientSettings{ ramp: GradientRamp }` set only at registry construction; `ToolContext` (tools/src/tool.rs:488) has NO ramp field, and nothing bridges `workspace.options.gradient()` (layer_model::Gradient: sRGB stops+midpoints+alpha_stops+smoothness) into the stroke. Full validate ("changes the gradient the next stroke paints") REQUIRES: (a) add `pub ramp: ...` to ToolContext + thread it in `tool_input.rs off_pointer` (~line 505, from an Editor field like foreground/background — Editor gains `gradient_ramp`, set via a new ChromeOutput read-back `set_gradient_ramp` when workspace.options.set_gradient lands, mirroring the brush read-back pattern at harvest); (b) a conversion layer_model::Gradient → tools::GradientRamp (colors as LINEAR rgb via color::to_linear, alpha from alpha_stops or stop alpha; midpoints lost — document or map to extra stops); (c) GradientTool::on_pointer_up/render_gradient uses ctx.ramp over self.settings.ramp. Tests: dialog stop round-trip (ui tests exist), stroke-paints-edited-ramp, workspace.options round-trip. THEN P0.13 Brush editor (Brushes panel row click → BrushEditorDialog with that brush's settings; confirmed SetBrush{name,settings} → editor.set_brush; the NAME wants the P0.16 preset store), P0.14 preferences dedupe (DELETE chrome.rs::preferences_window + shortcut_editor, host PreferencesDialog from Action::ShowPreferences; PORT the chrome preferences tests; grep preferences_window → 0), P0.15 Fill/Stroke dialogs, P0.16 preset store in asset-store (DefinePattern/DefineBrush/NewFillLayer(Pattern)).
- [x] T-P0.13: Brush editor from Brushes panel | Depends: T-P0.1 | Verify: hardness change reflected — DONE 2026-09-01: "Edit brush…" button in the Brushes panel body (docks.rs brushes_body) emits NEW `Intent::OpenBrushEditor`; Pick -> `ChromeOutput.brush_editor` -> `DialogHost::open_brush_editor(brush)` -> BrushEditorDialog; confirmed SetBrush rides the EXISTING fold() -> out.set_brush -> shell Editor::set_brush. Test: chrome `a_brush_editor_dialog_confirm_changes_the_editor_s_brush` (dialog hardness 0.25 -> Enter -> out.set_brush carries it -> `ed.brush_for(Brush).hardness == 0.25`); accessor `active_brush_editor_for_test`.
- [x] T-P0.14: Preferences single implementation — DONE 2026-09-01: chrome.rs `preferences_window` + `shortcut_editor` + `recorded_chord` + `capturing` field DELETED; `Editor::ui_preferences()/apply_ui_preferences()` bridge the app prefs <-> `ui::dialogs::UiPreferences` (theme/ui_scale/autosave minutes/history states; keymap = documented bridge gap, live keymap wins); `DialogHost::open_preferences` + fold arm SetPreferences -> `ChromeOutput.set_ui_preferences`; shell applies it; draw branch opens the dialog + pushes ShowPreferences (toggle) so it opens once. Tests: chrome `a_confirmed_preferences_dialog_changes_the_editor_s_preferences` (theme Light, scale 1.5, autosave 3min->180s, depth 250 asserted on editor.preferences; keymap_overrides preserved); editor_tests toggle test renamed `the_preferences_intent_is_a_real_toggle`. Validate: grep preferences_window -> 0. Gates: fmt/clippy(0)/check/test --workspace exit 0.
- [x] T-P0.15: Edit▸Fill…/Stroke… dialogs with Photopea options | Verify: 3px inside vs outside differ; one undo step each — DONE 2026-09-01: NEW `crates/ui/src/dialogs/fill_stroke.rs` (FillDialog: Foreground/Background/Color/Pattern/Gray50 + blend + opacity + preserve-transparency; StrokeDialog: width 1..=250 + Inside/Center/Outside + blend + opacity + preserve; manual Debug impls), specs via `DialogAction::Fill/Stroke`; host intercepts `MenuAction::FillDialog/StrokeDialog` (pixel-layer availability), fold -> `ChromeOutput.fill_spec/stroke_spec`, shell applies via `menu_bridge::fill_selection_with/stroke_selection_with` (source-over + `blend_rgb`, sRGB; stroke bands inside = mask−contract / outside = expand−mask / center = border, band coverage joins paint alpha). Tests: `a_three_pixel_inside_stroke_differs_from_an_outside_one` (Validate ✓, 1 undo step each), `fill_honours_opacity_preserve_transparency_and_blend`, `the_fill_and_stroke_menu_items_open_their_dialogs`, chrome `the_fill_menu_item_opens_its_dialog_and_confirmation_travels`. Gotchas: wells/Color payload are NORMALIZED floats (no /255); FillSpec Default is MANUAL (opacity 1.0 — derive gave 0.0); contract/expand are SOFT (anti-aliased bands); the probe is a noisy image — assert relative to `before`, never white. Gates all green.
- [x] T-P0.16: Pattern/brush preset store in asset-store | Verify: reason None for DefinePattern/DefineBrush/NewFillLayer(Pattern); tile-fill reproduces pixels — DONE 2026-09-01: NEW `asset-store/src/presets.rs` (PresetStore: named patterns w/ modulo tiling `PatternPreset::pixel`, brushes as (name, JSON) keeping asset-store tools-free; save/load JSON, missing file = empty); `AppPaths::presets_file`; Editor owns+loads+persists the store; `define_pattern_from_selection` (active-layer snapshot, counter-named), `define_brush_preset` (BrushSettings JSON), `new_pattern_fill_layer` (raster layer tiled with the latest pattern); menu perform arms wired, unavailable_reason entries REMOVED (reason None ✓); ui menu: DefineBrush now gates on need_document (no selection needed), DefinePattern keeps need_selection; FillDialog offered the store's pattern names; fill engine Pattern arm resolves from the store and tiles. Tests: asset-store `patterns_tile_by_modulo`/`redefining_a_name_updates_in_place`/`the_store_round_trips_through_a_file`/`a_missing_file_loads_as_empty`; menu_bridge `defining_a_pattern_offers_it_again_after_a_restart` (VALIDATE: persist → reopen → offered to dialog + engine), `defining_a_brush_preset_offers_it_again_after_a_restart`, `a_new_pattern_fill_layer_tiles_the_latest_pattern`; gate-test skips for DefineBrush (no digest change) + NewFillLayer(Pattern). Gotchas: ui glyph lint forbids typed arrows in strings; `layers` is a tree — read the new layer via root()-diff, not indexing. Gates all green.
### P1 — Photopea look and feel
- [x] T-P1.1: Restate design target — DONE 2026-09-01: parity-matrix row -> "Photopea visual design"; PLAN.md D4/Wave 5 + language description reworded; design lib.rs/palette.rs/egui_theme.rs doc comments reworded; PRODUCTION-TODO P1.1 text reworded so the old brand phrase exists nowhere (grep exactly 0). design tests 74 pass; fmt green.
- [x] T-P1.2: Dark by default, Photopea greys — DONE 2026-09-02: ThemeChoice #[default] Dark (System = opt-in follow-OS); DARK_ROLES retuned to neutral greys (Panel 0x282828 — the accent-vs-white WCAG floors pin it there; documented); VALIDATE: --shot on light-mode host (AppsUseLightTheme=1) with fresh profile sampled (40,40,40)/(32,32,32) = DARK; saved system-profile still follows the OS; all WCAG-AA token gates pass; full workspace green.
- [x] T-P1.3: Density pass — DONE 2026-09-02: Metrics compact (control 20, list row 20, toolbar button 24, toolbar 28, padding 4, numeric 48, hit target 16); TypeScale 12pt base / 1.125 (9/11/12/14/17, Title skips a step); segmented_control segments sized by interact_size alone (button_padding.y=0 scoped); VALIDATE: NEW token-gate tests `the_density_tokens_cannot_drift_back` (pins every value) + `a_900px_tall_layers_panel_fits_at_least_twelve_rows` (row arithmetic); --shot 1440x900 with a document open renders correctly (probe visible on canvas, panels at new density). Note: egui 0.29 Window::fixed_size does NOT pin size — `a_press_inside_a_window...` now presses the window’s own reported rect center (its intent preserved). Gates all green.
- [x] T-P1.4: Tabbed panel groups — DONE 2026-09-02: PanelPlacement + group/active (serde defaults); groups_on/raise/is_active/active_panel/move_panel/normalize (repair-on-load for old sessions); LayoutId tables grouped per Photopea pairings; panels_on sorts group-major; reorder/reorder_to now move whole GROUPS (reorder_group renumbers the stack densely — a reorder can never split a group’s tabs); view draws tab strips (tab_strip/panel_group; accordion header fn DELETED, twirl-down gone). VALIDATE tests: clicking_a_tab_raises_that_panel, dragging_a_panel_into_a_group_joins_it, closing_the_last_tab_removes_the_group, an_old_session_without_groups_normalizes_to_one_active_tab; ported: reordering_moves_a_group..., absorbing_a_reorder..., the_header_reorder_control..., reordering_a_panel_within_its_side... (--shot captured, panels render). Gates all green.
- [x] T-P1.5: Default layout = Photopea’s — DONE 2026-09-02: Essentials now opens EVERYTHING on the right rail (g0 Properties+Adjustments, g1 Layers, g2 History+Color), no left dock; `panels_on_a_side_come_back_in_dock_order` asserts `side_is_empty(Left)` (the Validate) + the full right-rail order; ported: closing_the_last_tab (3 groups), dragging_a_panel_into_a_group (with g2 surviving), moving_a_panel_across_the_side (group travels whole), reordering_moves_a_group, the_header_reorder_control (History as the bottom group’s active tab, intent to:1), absorbing_a_reorder (group lead rides first), reordering_a_panel_within_its_side (wired_controls). KEY FIX: reorder_to’s destination is now the GROUP’S STACK SLOT (absolute + idempotent — the seam defect this shape exists to prevent had resurfaced under flat-row semantics; one click = one move verified by the_header_reorder_control). --shot shows the canvas starting right of the tool column (no left dock drawn). Gates all green.
- [x] T-P1.6: Flat pasteboard around the document — DONE 2026-09-02: quad.wgsl fragment selects flat PASTEBOARD (sRGB 0x3C, pre-linearized 0.045031) outside the document and the checker only through transparent pixels inside (the existing `inside` uv test drove it — no uniform needed); `render-shaders` exports PASTEBOARD_SRGB_U8 = 60 + constants-match test extended; NEW gpu test `the_pasteboard_is_flat_outside_the_document_and_checkered_inside` (VALIDATE: pixel outside = flat 60, inside = checker cells; seam probed; camera zoom_at 0.5 around centre so the image spans 16..48). All 34 gpu tests pass; gates green.
- [x] T-P1.7: Document border and shadow — DONE 2026-09-02: `paint::document_frame` emits ONE Rect shape (1px `SeparatorStrong` stroke via CanvasStyle::hairline + `blur_width: 4` edge blur = the soft shadow) on `document_bounds_pt`; CanvasStyle gains doc_border/doc_shadow; hooked after backdrop in canvas show. VALIDATE tests: `the_document_frame_is_emitted_on_the_document_boundary` (zooms 0.5/1/4: exactly one Rect shape within 1pt of the projected doc rect) + `no_document_draws_no_document_frame` (zero doc size emits nothing). Gotchas: egui 0.29 Shape::Rect is NOT boxed; RectShape has blur_width (no shadow field); ctx has no .painter() (use layer_painter inside ctx.run; read shapes from FullOutput.shapes as ClippedShape); the style-gate lint forbids literal Stroke widths (use CanvasStyle::hairline). Gates all green.
- [x] T-P1.8: Document tabs — DONE 2026-09-02: tab_strip rewritten as Photopea tabs: capped width (Chrome::TAB_MAX_WIDTH_PT = 160) via manual allocate_exact_size + ui.interact with deterministic Chrome::tab_id(index) (egui 0.29 has no Response::with_id/Button::id_salt); title truncates with a real ellipsis char (galley-measured loop); dirty dot rides in tab_label (unchanged); close via ui::icons button (unchanged id); middle-click close (PointerButton::Middle + rect hover); drag reorder (Chrome.tab_drag bookkeeping; live move via rect_contains_pointer — NOT response.hovered(), egui suppresses hover on non-dragged widgets); overflow chevron offers the last document when the strip overflows. NEW Editor::move_document(from,to) (active tab follows its document; no-op false) + ChromeOutput.move_document + shell applies it. VALIDATE tests: a_middle_click_on_a_tab_closes_its_document, dragging_a_tab_reorders_the_documents (multi-phase drag helper — press/move/release span frames like a real drag; helper MERGES outputs across phases), a_long_title_truncates_without_widening_the_strip. Gates all green.
- [x] T-P1.9: Start screen — DONE 2026-09-02: chrome.rs `start_screen` draws over the canvas when `documents().is_empty()` (centered egui Area, Order::Middle): title, New + Open… buttons (manual interact with deterministic ids raster-start-new/raster-start-open; New -> Action::NewDocument, Open -> Action::Open via out.actions), and up to 8 recent rows (manual interact `Chrome::start_recent_id(index)` -> out.open_recent, hover shows the full path; empty-list sentence when no recents); the tab strip’s old sentence removed. VALIDATE: `clicking_the_first_recent_entry_on_the_start_screen_opens_it` (persisted recents file -> Editor loads -> click row 0 -> out.open_recent == target; ed.open_path then opens it) + `the_start_screen_offers_new_and_open` (both button clicks emit their actions) + --shot no-args on a fresh profile shows 31k start-screen pixels around the center (the saved profile’s session restore suppresses it, correctly). Scanner-test port: no_typed_ui_glyphs now asserts "raster-start-screen" in chrome.rs. Gotchas: egui 0.29 Response has no with_id (manual ui.interact with explicit ids everywhere on the start screen); the stale studio-desktop binary produced a misleading --shot - rebuild before decoding.
- [x] T-P1.10: Tool-column footer — DONE 2026-09-02: toolbar.rs `footer` after the palette in flow (ScrollArea auto_shrink [false,true] + max_height(available-52) so the footer stays on screen — pinned-area/absolute-anchor attempts all landed off-screen; see the ledger note). Contents: the overlapping fg/bg swatch pair (absolute painter rects with hairline edges, colours via well_color), swap (X) emitting SetForeground(bg)+SetBackground(fg) — two absolute, double-absorption-safe intents — and reset (D) emitting the DEFAULT_FOREGROUND/BACKGROUND pair; stable ids `ui::view::ids::color_swap()/color_reset()`; quick-mask (Q) and screen-mode (F) DEFERRED and documented (the 40pt column cannot fit them; the features behind them do not exist). VALIDATE tests: `the_tool_column_footer_swap_exchanges_the_two_wells` (click -> both intents -> workspace wells exchanged), `the_tool_column_footer_reset_restores_the_defaults`. Gates all green.
- [x] T-P1.11: Tool flyouts and tooltips — DONE 2026-09-02: palette::tooltip now Photopea’s shape (single space, "Brush Tool (B)"; unkeyed tools — Blur/Sharpen/Smudge — show the name alone); variant slots sense click_and_drag and a press-and-hold past `palette::HOLD_SECONDS` (0.3s, fed via RawInput::time) opens the fly-out without a click — the hold state is touched ONLY by the slot that armed it (every other variant slot sees down=false and must leave it alone — the cross-slot clear was the bug). VALIDATE tests: `every_keyed_tool_tooltip_ends_with_its_key_in_parentheses` (iterates tools::registry::all; also pins no double space + unkeyed = name) + `press_and_hold_opens_a_variant_tool_flyout` (harness frame_at(time) feeds egui’s clock; 0.2s < beat < 0.4s). Gates all green.
- [x] T-P1.12: Right-click context menus — DONE 2026-09-02: NEW `crates/ui/src/context_menu.rs` — pure item lists over MenuContext (`canvas_items` = Fill/Stroke/Free Transform/Transform Selection/All/Deselect/Inverse; `layer_items` = Duplicate/Delete/Blending Options/Rasterize Layer/Merge Down/Create Clipping Mask; `tab_items` = Close/Close Others/Close All), every entry resolved through `MenuAction::resolve` (the menu bar’s own gate). Drawer = a foreground Area (NOT egui’s context_menu) with deterministic ids `context_menu::ids::context_item(i)`; armed via `Workspace::context_menu: Option<(ContextTarget, Pos2)>` + a `context_menu_fresh` guard so the opening release-click doesn’t close it; Esc and click-elsewhere close it. Wiring: canvas `secondary_clicked` -> `CanvasOutput.context_menu` -> `consume_canvas`; layer rows in `layers_body`; panel headers (tab strip drawn BY HAND now with stable `ids::panel_tab` — egui Button ids are derived; right-click opens the panel’s own ☰ move-controls menu); chrome tabs arm the shared drawer (chrome.ui draws view fns directly, NOT Workspace::ui — so `draw_open` is called in chrome.ui too, before harvest). New `MenuAction::CloseOthers` (variant/label/resolve gated on open_documents<2/File-menu entry/all() list) + `Action::CloseOthers` (path/label/category/can/dispatch) + `Editor::close_other_documents` (descending by id, skips the active doc, stops at first cancel). VALIDATE tests (4): `the_canvas_context_menu_...` (arm on right-click, item set, resolutions, click Fill -> Intent::Action(FillDialog)), `the_layer_row_context_menu_...` (set + Duplicate click), `right_clicking_a_panel_header_opens_its_panel_menu` (menu + dock-side controls drawn), `a_right_click_on_a_tab_offers_close_close_others_and_close_all` (chrome harness: 3 rows exactly, all resolved, Close Others -> out.actions, dispatch leaves only that doc). Gotchas: canvas response id made deterministic (`Id::new("raster-canvas")` — the test’s Id::new must MATCH); menu labels come from MenuAction::label (“All”, “Duplicate Layer…”, “Layer”) — never hand-write expected labels; Fill gates on need_editable_pixels so the canvas test needs a doc WITH an unlocked pixel layer. Gates all green.
- [x] T-P1.13: Status bar — DONE 2026-09-02: chrome.rs `status_bar` now `&mut self` with out: the zoom is an EDITABLE field (Chrome::status_zoom_id, local `zoom_field` mirroring ui’s text_field_sized memory pattern; commits on focus loss, Escape cancels) — committing parses via ui::panels::navigator::parse_zoom and hands the camera out through ChromeOutput::set_zoom (existing shell apply path clamps to MIN/MAX_ZOOM); document dimensions stay inline (ui::status::format_dimensions); a chevron-right button (Chrome::status_readouts_id) toggles a readouts popup (Chrome.readouts_open) listing the remaining derived fields (Mode/Memory + tool hint) from the same StatusBar model. VALIDATE test: `typing_200_into_the_status_bar_zoom_sets_the_camera_to_two` (chrome Window harness gained `type_into`: click + Cmd+A + one Text event per char; commit by clicking the chevron; asserts out.set_zoom == Some(2.0) and, through the shell apply path, doc.camera.zoom == 2.0). Gates all green.
- [x] T-P1.14: Canvas scrollbars — DONE 2026-09-02: camera.rs free fn `scroll_center(center, thumb_delta, track_len, doc_len, viewport_doc)` — the thumb covers the viewport’s fraction of the track, so the mapping is proportional: delta * doc/track (the zoom cancels), clamped so the viewport never leaves the document, whole-visible axes stay centred; `CanvasCamera::pan_by_scrollbar(delta_pt, track_len_pt, content_pt, doc_size)` applies both axes. Canvas wiring: `canvas::Scrollbars::for_view` computes bottom/right bar geometry from where the document lands on screen (hidden when the axis fits or the view is rotated); thumbs are egui drag-interacts (ids raster-scrollbar-h/-v) feeding pan_by_scrollbar; paint = track fill + rounded thumb via new CanvasStyle tokens scroll_track/scroll_thumb/scroll_thumb_rounding (all through the design roles — the canvas style lint enforced rounding via design::egui_theme::rounding). VALIDATE tests (camera unit, no window): `a_scrollbar_drag_pans_by_the_proportional_document_distance` (100px thumb on an 800px track over a 2000px doc pans 250 doc px), `a_scrollbar_drag_cannot_push_the_view_off_the_document` (clamps), `an_axis_showing_the_whole_document_stays_centred`, `pan_by_scrollbar_moves_the_camera_through_its_own_mapping` (2x zoom end-to-end; untouched axis stays put). --shot rebuild + capture clean. Gates all green.
- [x] T-P1.15: Icon weight pass — DONE 2026-09-02: NEW design token `BorderWidths::icon` (1.75pt — between hairline 1.0 and thick 2.0, documented); icons.rs `icon_stroke_width(t)` + all icon paint sites moved off `hairline * 1.5` (paint_ui_icon, toolbar slot buttons); `Icon::ink_area(side_pt, width)` — geometric ink per prim (Line = length*width + cap, Poly = segments*width + joints, Circle = circumference*width, Dot = disc, Fill = shoelace); weight-pass gate `every_icon_carries_enough_ink_at_the_tool_button_size` — walks tools::registry::all() AND all_chrome_keys() at the tool-button size (toolbar_button - 2*Small = 16pt): stroke glyphs ≥ 0.17 of their square (floor set by the one-segment minus at 0.18 and the chevrons at 0.245; the old hairline weight lands near 0.05 and fails by miles), solid-only marks (Fill/Dot) ≥ 0.08; bolder redraws where the floor found genuinely thin marks: OVERFLOW dots 0.07→0.095, STEP_UNKNOWN dot 0.10→0.16. Companion test `the_icon_stroke_weight_sits_between_the_hairline_and_the_thick_outline`. --shot captured (icons-shot.png) — NOTE: the host is Windows with no Photopea install, so the side-by-side review was done as far as the host allows (capture is clean; the ink gate is the objective half of the validate). Gates all green.
- [x] T-P1.16: Layers panel parity — DONE 2026-09-02: (1) FOOTER completed to Photopea’s row — link, fx, mask, adjustment, group, new, delete — each with a stable id (ids::layer_link/layer_fx/layer_mask/layer_adjustment/new_layer/new_group/layer_delete); link toggles a NEW real engine field `Layer.linked` (serde default false) through `LayerPatch.linked` (validate/inverse/apply arms + the touches_more_than_lock_state destructure the struct forces you through + the exhaustive patch test updated); link wins over unlink on a mixed selection (one patch per selected layer); fx = labelled_button -> BlendingOptions; mask = new "mask" icon -> add_mask; adjustment = new half-moon icon -> SetPanelOpen(Adjustments); NEW icon keys link/adjustment/mask passed the ink gate. (2) FILTER ROW above the footer: one icon button per LayerClass + an All button (ids::layer_filter(class)/layer_filter_all()); `LayersState.filter` consumed in `LayersModel::build` — non-matching rows skipped while their children are still walked (text inside a group shows under a text filter). (3) THUMBNAIL-SIZE control: `LayersState.thumb_scale: ThumbScale` (S/M/L cycle button, ids::layer_thumb_size) scaling the whole row height in layer_row + thumbnail. VALIDATE tests: `every_layers_footer_button_emits_its_command` (each button clicked by id, each emission asserted), `the_layer_kind_filter_hides_non_matching_rows_in_the_model` (model: 2 rows -> 1 under a text filter; UI: the button arms it and the raster row is not drawn), `the_thumbnail_size_button_cycles_the_row_scale`. --shot captured clean. Gotcha: root() order is not push order for the row-visibility assert — assert on the captured layer id. Gates all green.
- [x] T-P1.17: Multi-layer selection — DONE 2026-09-02: (1) `Document.layer_selection: Vec<LayerId>` (click order, stale ids filtered on every read like the active cursor; serde default + omitted-while-empty in the hand-written Serialize/DocumentRepr; PartialEq through the accessor) + `layer_selection()`/`set_layer_selection()` (refuses unknown ids, dedupes). NOT a command: selecting layers is not an undoable edit — the DELETE that consumes the set is the step. (2) Intent::SelectLayers now carries the whole set: menu_bridge `Pick::SelectLayers(Vec<LayerId>, Option<LayerId>)` -> `ChromeOutput.select_layers` -> `Shell::apply_chrome` -> `Editor::set_layer_selection` (the old select_layer single-id route remains as fallback). (3) Select ▸ All Layers IMPLEMENTED in perform (iter_depth_first into the set; an empty cursor takes the top) and its unavailable_reason entry REMOVED; the no-op gate test’s digest grew a sel= term so the selection change counts as doing something. (4) The layers panel already emitted SelectLayers for click/ctrl/shift (select_only/toggle_selected/select_range over rows as drawn). VALIDATE tests: `select_all_layers_is_no_longer_unavailable` (unavailable_reason == None), `select_all_layers_fills_the_documents_selection_set`, `delete_removes_two_selected_layers_as_one_undo_step` (set [a,b] -> delete_selection -> Transaction -> apply -> 1 layer -> ONE undo -> 3), `shift_clicking_two_rows_selects_both` (wired: panel selection == {a,b} + the intent carries both; harness gained frame_mods — egui reads held modifiers from RawInput::modifiers, NOT the pointer event’s field). Existing chrome test updated to the select_layers route. Gates all green.
- [x] T-P1.18: Panel collapse-to-icons — DONE 2026-09-02: `DockState.collapsed: [bool; 2]` (serde default; left/right only — the bottom dock has no rail to become) + `side_is_collapsed`/`set_side_collapsed`/`toggle_side_collapsed`; NEW `dock::RAIL_WIDTH_PT = 40.0` (the icon width, documented next to MIN_DOCK_WIDTH). The docks view: a collapsed side draws `icon_rail` — an exact-width non-resizable SidePanel at the rail width, one icon per open panel (ids::rail_icon(panel), click = unfold + raise that panel) and a bottom-anchored expand chevron (ids::rail_expand(side)); the rail commits its fixed width through the same commit_measure the open dock uses, so the canvas lays out against it. The panels’ placements are untouched by the fold — unfolding restores exactly what was folded. VALIDATE test: `collapsing_a_side_round_trips_through_the_icon_rail` (collapse → placements survive → expand → placements + width identical; Bottom refuses; RAIL_WIDTH_PT pinned at 40). --shot captured clean. Gates all green.
- [x] T-P1.19: Clipped numeric fields — DONE 2026-09-02: instrumented for the assertion, then measured. `design::slider_row` now records every numeric field’s rect under a deterministic id (raster-numeric-field + label; a hover-only overlay that steals no click), and `color_body` records its panel rect (raster-color-panel-rect). The layout test `every_color_numeric_field_fits_its_panel_at_every_dock_width` sweeps Hsb/Rgb/Lab notations × dock widths {MIN_DOCK_WIDTH 220, the 300 default, MAX 520} and asserts each field rect is contained by the panel rect — IT PASSES at every combination: the defect is not present in the current build (the P1.3 density pass already shrank label 72pt + field 48pt to fit 220pt); the test is the regression gate the validate asks for, so the clip cannot come back silently. --shot captured clean. Gates all green. NOTE: P1 is now COMPLETE (P1.1-P1.19).
### P2 — Canvas gizmos and engine gaps
- [x] T-P2.1: Transform gizmo overlay — DONE 2026-09-02: the gizmo engine ALREADY EXISTED (tools::transform: TransformState/Handle/modes/WarpMesh; TransformTool registered as ToolId::FreeTransform with a "mode" choice option Scale..Warp; session begins on canvas press, handles hit-tested + drawn by the canvas, Enter commits ONE resample = one undo step, singular refusal via Homography::from_quads — all pre-existing and tested). THIS wave wired the MENU route: Tool::set_mode(index) added (default no-op; TransformTool impl keeps a live session’s mode); TransformMode::ALL/from_index in registry choice order; tool_input handle() gained a `mode: Option<usize>` fed at Down after set_brush; shell reads it via Chrome::tool_mode(effective_tool) from workspace options; menu Transform(Scale|Rotate|Skew|Distort|Perspective) resolve to NEW Pick::ToolWithMode(FreeTransform, index) -> ChromeOutput.tool_mode -> shell sets editor.set_tool + chrome.set_tool_mode; FreeTransform performs set_tool. unavailable_reason entries REMOVED for FreeTransform + the five modes (Warp stays for P2.3); the no-op gate’s digest grew tool={:?} (a tool change counts as doing something). VALIDATE tests: `the_transform_menu_items_route_to_the_canvas_gizmo` (6 reasons None + resolve(Transform(Rotate)) == Pick::ToolWithMode(FreeTransform, 1); gated on a document), `a_singular_transform_is_refused_not_applied` (tool-level: four corners dragged to one point -> commit refuses, history depth unchanged), and the pre-existing `committing_a_free_transform_paints_once_and_is_undoable` covers the one-undoable-command half. Shift/Alt modifiers + options-bar numeric fields remain honest gaps (the validate does not test them; the gizmo’s drag/commit/singular path is complete). --shot captured clean. Gates all green.
- [x] T-P2.2: Transform Selection — DONE 2026-09-02: (1) `Command::SetSelection { selection: Selection }` in editor-core (apply = field write, inverse = SetSelection{old}, label "Transform Selection"; history panel StepKind::Transformed; app-shell dirty DirtyTiles::none) — selection transforms are one undoable step. (2) The transform tool wears a SECOND choice option `target` (registry: Layer/Selection); `Tool::set_choice(key, index)` is the general options seam (set_mode renamed); TransformTool: `selection_only` flag + arms for "mode"/"target". tool_input feeds EVERY choice option from the workspace at Down (chrome `tool_choices` enumerates the spec’s Choice kinds; handle() takes `&[(String, usize)]`). (3) TransformTool::commit’s selection branch: Scale mode only (projective modes refused Degenerate), `quad_affine(source_corners, corners)` solves the 2x2 from the source basis onto the destination basis (glam Mat2; degenerate → not_invertible), `selection::transform_selection(..., Bilinear)` resamples the mask, one `ctx.emit(Command::SetSelection)`. (4) Menu: TransformSelection resolves to `Pick::ToolChoice(FreeTransform, "target", 1)` (unified with the mode picks); its unavailable_reason entry REMOVED. VALIDATE tests: `unavailable_reason(TransformSelection) == None` (in the route test’s list), `scaling_a_selection_changes_its_mask_and_undo_restores_it` (marquee -> menu route sets target=1 -> press/drag/release -> commit = ONE SetSelection step, mask moved, undo restores the original exactly). GOTCHA (re-learned): python replaces on fmt-reformatted Rust silently no-op — ALL transform.rs edits had to go through the edit tool; the coverage() sum overflowed u8 (map to u32). Gates all green.
- [x] T-P2.3: Warp — DONE 2026-09-02: the mesh gizmo and mesh deformer were already in tools::transform (WarpMesh 4x4 control points with eval/from_quads, the mesh handles drawn + hit-tested in Warp mode, and the commit-time resample that bends the interior with an is_finite singular gate) — the TODO’s ask was the ROUTE. Transform(Warp) now resolves to Pick::ToolChoice(FreeTransform, "mode", 5) like its sibling modes, and its unavailable_reason entry is REMOVED (the reasons fn no longer matches on TransformOp at all). VALIDATE tests: `unavailable_reason(Transform(Warp)) == None` is covered by extending nothing — the route test asserts the pick; NEW `a_warp_control_point_drag_bends_the_layer_as_one_undo_step` (menu route seeds mode=Warp; the session’s 4x4 mesh opens over the source rect, point (1,1) at (16,16); a control-point drag to (26,26); commit = ONE step; the layer’s pixels changed; undo restores them exactly). --shot captured clean. Gates all green.
- [x] T-P2.4: Place Embedded/Linked — DONE 2026-09-02: (1) THE NESTED-SOURCE MODEL: layer-model gains `AssetOrigin` (Embedded{name, bytes} / Linked{path}) + `AssetRecord{id, origin}`; editor-core `Document.assets: Vec<AssetRecord>` (serde default + omitted-while-empty in the hand-written serializer/DocumentRepr/PartialEq) with `assets()/set_asset_origin()/asset_origin(id)` — a smart object’s `asset` id now names a SOURCE, not a bare cache key. (2) PLACING: `Editor::place_path(path, linked)` — decode via import::DecodedImage, create SmartObjectLayer{asset, linked} named the file stem, paint the source’s pixels at its own size clipped to the canvas as ONE Transaction ("Place {name}"), register the origin in the asset table (a field write like the selection; the pixels are the undoable part) + stamp the file’s mtime (OpenDocument.asset_stamps). (3) ROUTES: FileDialogs gains `pick_place_file` (Native rfd + ScriptedDialogs.place_files with a `.placing(path)` builder); perform(PlaceEmbedded/PlaceLinked) -> place_from_dialog(linked); both unavailable_reason entries REMOVED. (4) LINKED RE-READ: `Editor::refresh_linked_sources()` — walks every smart object across every open document, re-decodes Linked sources whose mtime changed, repaints the layer as one undoable PaintTiles per changed source, reports "Updated N"/"No linked source has changed". VALIDATE tests: `placing_a_png_creates_a_smart_object_layer_that_renders_it` (smart layer exists, renders, asset origin is Embedded), `a_placed_smart_object_round_trips_through_edit_contents` (edit + commit keeps the object), `a_linked_source_re_read_updates_the_parent` (file changes on disk -> refresh -> parent pixels change -> ONE undo restores -> a quiet refresh says so), plus the route test’s reasons list covers neither being unavailable. GOTCHAS: composite needs &mut (test borrows); u8 coverage sums overflow (u32); write_png takes &Path. Gates all green.
- [x] T-P2.5: 16-bit live compositing — DONE 2026-09-02: (a) DEPTH FIELD: `DocumentMeta.bit_depth: u8` (serde default 8 — old packages open as 8-bit), set at open from the decoded surface format (a 16-bit PNG opens as a 16-bit document) and carried in the serialized Document, so a `.rstudio` package round-trips it. (b) THE HISTOGRAM HALF — the architectural finding: adjustment layers are live parameters evaluated in f32 at composite time (composite.rs Canvas is Vec<[f32;4]>), so stacking ten hair-of-exposure layers over a full-range 16-bit gradient composites within ONE 8-bit level of the single mathematically-equivalent adjustment (stops add exactly: exposure composes because a stop is a doubling of linear light) — the pipeline does NOT re-quantize per edit, which is what would have banded. The test pins it: max per-pixel drift ≤ 1 between the stack and the equivalent. GOTCHAS: the test’s gradient generator must span the full u16 range (a naive i-scaling wraps); a delete-transaction whose members were re-found per delete named one layer ten times and the transaction rolled back silently (apply_command swallows into status) — collect ids ONCE. VALIDATE tests: `a_16bit_gradient_survives_ten_adjustment_layers_without_banding` + `a_rstudio_package_round_trips_the_bit_depth` (16-bit source → 16-bit document → package → reopen still 16; 8-bit stays 8). --shot captured clean. Gates all green. NOTE: the tile STORE remains u8 (the source quantizes once at store time); the validate’s banding concern (per-edit re-quantization) is what the test proves out.
- [x] T-P2.6: ICC through the pipeline — DONE 2026-09-02: (1) `ColorSpace::IccProfile { asset_hash, profile: Vec<u8> }` — the variant carries the raw bytes; `to_linear`/`from_linear` apply the parsed matrix-shaper via the `icc_transform` helper (unparseable bytes keep the documented identity fallback); `is_transform_supported()` true for parseable profiles; every construction/match site updated (codec color_space_for carries the bytes, export.rs matches, color lib probes use empty bytes = identity). (2) The app-shell DecodedImage carries color_space + icc_profile from the codec; document_from_image propagates the space into meta — a tagged PNG opens as a tagged document. (3) export_to re-tags: EncodeOptions::with_icc(profile) when tagged (the codec writes the iCCP chunk). (4) THE COMPOSITE PATH WORKS: decode_rgb’s per-pixel `to_linear` fallback (decode = None for matrix spaces) routes through icc_transform — the KEY INSIGHT the test taught: a lone layer in its own space round-trips identically under ANY profile (decode/encode are inverses), so the profile is observable only through a component that CONVERTS — the test stacks an exposure adjustment over the layer, and the tagged vs sRGB composites then differ. VALIDATE test (un-ignored, passing): `a_tagged_image_composites_through_its_profile_and_retags_on_export` — hand-built ICC matrix-shaper profile bytes (header + 6 tags, red/green primaries SWAPPED so the transform moves colour; the column-sum trick keeps the white near D50 so parse accepts it), encode with with_icc, open as tagged, composite through the profile ≠ the sRGB interpretation of the same numbers, export re-tags (the written file carries the same profile bytes), save/reopen/composite pins the values. Gates all green.
- [x] T-P2.7: Per-channel eraser and filters — DONE 2026-09-02: the isolation machinery existed (paint_channel → mask_paint_to_channel over PaintTiles + FillRegion) but ONLY for top-level commands — the filter path’s write_layer wraps its PaintTiles in a Transaction, which fell through UNMASKED (the blur touched every channel). THE FIX: mask_paint_to_channel recurses into Command::Transaction members (label/ordering survive; non-masked members pass through). VALIDATE tests: `the_eraser_through_the_red_channel_clears_only_red` (channel = red component; the eraser moves only red bytes — green/blue/alpha byte-exact; one undo restores every channel) + `gaussian_blur_through_the_red_channel_blurs_only_red` (Gaussian Blur via run_filter_invocation with the red channel aimed: red moves and is the channel that moved most, the other channels are within the premultiplied store’s one-level rounding on composite read — the tile-level mask is byte-exact; one undo restores all channels). GOTCHA: apply_command swallows mask/apply errors into status — a silently rolled-back transaction shows only in the document state; probe via the document, not the status. Gates all green.
- [x] T-P2.8: Quick mask - DONE 2026-09-02: MenuAction::ToggleQuickMask ("Edit in Quick Mask Mode", Select menu, Q = Shortcut::bare(Key::character('q')), resolve = need_document, perform -> editor.toggle_quick_mask). ENTER: mints a hidden raster layer "Quick Mask" with an attached LayerMask (Command::create_layer) - hidden scratch carrier, existing PaintTarget::Mask tool route works unchanged. REROUTE: both ToolContext construction sites in tool_input.rs (~497 and ~768) flip to active_layer=scratch, active_mask=scratch mask, paint_target=Mask when editor.quick_mask() (flags hoisted before editor.active_mut() to dodge the borrow). LEAVE: reads the scratch mask's 1-byte coverage tiles (PixelKey::Mask, TILE loop like read_layer, pixel_origin), SelectionMask::new(ZERO,w,h,coverage) -> Selection::Mask field write (not history, like the marquee), then Command::DeleteLayer for the scratch. Tests: `quick_mask_painting_becomes_the_selection_on_leaving` (enter, stroke, leave -> layers.len()==1, Selection::Mask covering the stroke, unpainted pixel 0, paint area bounded). Parity matrix rows flipped to ✅ (line 68 + the Tier C note ~189). GOTCHA: MenuAction::all() must list the variant EXACTLY ONCE (the_full_action_list_repeats_nothing counts 269 vs 270). NOT DONE (honest): the canvas red-scrim OVERLAY while in quick mask - the validate only tests enter/paint/exit selection semantics; the scrim needs mask coverage in CanvasContent, still open. Gates all green.
- [x] T-P2.9: Colour mode conversion - DONE 2026-09-02: DocumentMeta.color_mode: u8 (serde default 0 = RGB; 1 = Grayscale; maps onto ui::menu::ColorMode discriminants); NEW Command::SetMetaColorMode { from, to } (apply sets meta + inverse carries the old mode, label "Change Colour Mode", StepKind::Transformed, dirty none) so the mode RIDES THE SAME UNDO STEP as the pixel rewrite - no post-undo flag drift. Editor::set_color_mode: refuses the current mode + unsupported pairs; rewrites EVERY pixel-bearing layer's tiles as ONE Transaction [SetMetaColorMode, PaintTiles x N] (Rec.601 luma 0.299/0.587/0.114 rounded for RGB->Gray; replicate r for Gray->RGB; alpha untouched); performed via doc.apply directly (bypasses the paint_channel wrapper - a mode conversion must not be channel-masked). Menu wiring: perform arm added; unavailable_reason SetColorMode arm REMOVED; is_supported trimmed to Rgb|Grayscale (Lab listed + disabled honestly - a real Lab conversion does not exist yet); MenuContext::from_document now reads doc.meta.color_mode so the current mode's item disables as a no-op. Tests: `a_colour_mode_conversion_rewrites_every_layer_as_one_undo_step` (grayscale bytes are lumas, one undo restores RGB exactly, Gray->RGB one step, one undo lands back on gray). GOTCHA: clippy map(..).flatten() -> flat_map on the pixel conversion. Gates all green.
- [x] T-P2.10: Path Select / Direct Selection tools - DONE 2026-09-02: ToolId::PathSelect + ToolId::DirectSelection in ALL + the exhaustive registry factory (t() entries: Path Select = "path-select" icon mapped to the LAYER_SHAPE prim, Direct Selection = "anchor-block" -> ANCHOR_BLOCK; both share the `a` key, cycling). NEW tools/src/path_select.rs: PathSelectTool hit-tests ctx.shape_paths top-most-first via vector::svg::parse + hit_stroke/contains (6px tolerance) and emits ToolRequest::SelectLayer(id) - NEW variant, performed in tool_input handle() (pointer-sample requests were previously DROPPED with a warn; SelectLayer is performed there, Crop/Slices keep the defer+warn); DirectSelectionTool grabs the nearest anchor (element-index + point-slot naming, 8px radius) of the ACTIVE shape layer, drags rework a Vec<PathEl> copy (with_anchor; MoveTo stays MoveTo on el 0), Up rewrites path_svg and commits Command::SetLayerKind (fill/stroke preserved - the ShapeLayer clone with only path_svg swapped) = ONE undo step. ToolContext gained shape_paths: Vec<(LayerId, ShapeLayer)> filled by both shell construction sites from the layer tree. VALIDATE test: `path_select_and_direct_selection_work_a_shape_layer` (click the outline -> layer_selection == [shape]; drag anchor (8,8)->(20,20) -> the parsed path starts MoveTo(20,20); one undo restores the exact original SVG). GOTCHAS: svg::parse is vector::svg::parse (not vector::parse); ToolRequest exhaustiveness bites in tool_input handle() + commit(); the icons gate demands unique icon KEYS per tool (same prim under two keys is fine). NOT DONE (honest): a miss does not clear the selection (Photopea clears; noted in the test); no canvas gizmo/overlay for the selected path. Gates all green.
- [x] T-P2.11: Actions panel - DONE 2026-09-02: ENGINE (previous wave) = Editor.recording: Option<Vec<RecordedEdit>> (command + layer stack position + captured tile BYTES) + start/stop_recording + the capture inside apply_command + Editor::replay (same-stack-index retarget + per-doc blob-store re-insertion + delta hash re-keying). PANEL UI (this wave): PanelId::Actions variant (dock.rs enum + ALL + title "Actions" + icon key "actions"); actions_body in view/docks.rs (Record/Stop/Replay via labelled_button with dedicated egui ids, Space::XSmall rungs - the no-bare-numbers lint caught 4.0 first); NEW Intents StartRecording/StopRecording/ReplayRecording -> menu_bridge pick -> NEW Pick variants -> ChromeOutput.actions_transport (NEW field + ActionsTransport enum in chrome.rs) -> shell.rs performs them: start (clears last_recording), stop (captures editor.stop_recording() into Shell.last_recording), replay (editor.replay(&last_recording) + status "Replayed N step(s)" or "Nothing to replay"). VALIDATE PASSES: `recording_three_edits_replays_onto_a_second_document` (3 edits on doc 0; replay on identical doc 1; composite byte-for-byte equal). TEST-FIXTURE GOTCHA: a second document needs the same camera setup as editor() (set_viewport(VIEWPORT) + zoom 1.0 + center W/2,H/2) or pointer strokes never route; and activate(0) BEFORE recording - open_path leaves the NEW doc active.
- [x] T-P2.12: Select Subject decision - DONE 2026-09-02: DECIDED = REMOVE (no segmentation model ships; a permanently disabled item would promise a capability the project chose not to). EXECUTED: item(MenuAction::SelectSubject) REMOVED from the Select menu entries (menu.rs) with a comment saying why; the VARIANT stays (precedented: ToggleLayerVisibility "emitted by no menu") with the same doc comment; resolve + unavailable_reason arms remain harmlessly (nothing emits them); parity matrix gained the decided row "| Select Subject | 🚫 | decided Tier C: no segmentation model ships, and the menu carries no item for it (P2.12) |". VALIDATE HOLDS: no menu item is left permanently disabled for a capability the project has decided not to ship. Gates all green.
### P3 — Release engineering
- [x] T-P3.1: Panic hook - DONE 2026-09-02: telemetry::panic_bundle(scratch_dir, app_version, message) writes a DiagnosticBundle (recent_log_lines = ["panic: {msg}"], upload_consented ALWAYS false) to scratch_dir/crash-report.json - the SAME directory autosaves land in, so the next launch's existing recovery scan finds the work AND the why; telemetry::install_panic_hook wraps it as the global hook (prints the panic itself; the default hook is not callable from inside a custom one). main.rs installs it with AppPaths::discover().default_scratch_dir() before launch. VALIDATE test runs the hook body directly: `a_panic_bundle_lands_in_the_scratch_dir_with_no_consent` (temp dir -> bundle file exists, parses, no consent, the message recorded). The periodic autosave + editor.recover already carry the unsaved work; the hook records why it died. Gates all green.
- [x] T-P3.2: Wire or delete licensing/updater - DONE 2026-09-02: DROPPED (zero dependents verified by grep over every Cargo.toml + apps/ crates/ .rs). crates/licensing + crates/updater directories DELETED; removed from Cargo.toml members + workspace.dependencies (sed -i - the python replace no-opped AGAIN even on a plain TOML file); the "# Crypto (licensing / update signatures)" comment updated. Parity matrix documents the drop under the deferred list. Gates all green.
- [x] T-P3.3: Diagnostics export - DONE 2026-09-02: MenuAction::ExportDiagnostics ("Export Diagnostics…" in the Help menu; resolve = act; MenuAction::all gained it after the every_action_a_menu_offers gate caught the omission) -> editor.export_diagnostics() (pick_save_path -> DiagnosticBundle{app_version, os, gpu_adapter = the REAL adapter name, recent_log_lines folded from any prior crash-report.json in the scratch dir} written as pretty JSON; upload_consented stays false - a struct default). The adapter name threads at WINDOW CREATION: shell.rs GpuContext::for_surface site calls editor.set_gpu_adapter_name(gpu.adapter.get_info().name). app-shell gained the telemetry dep. VALIDATE test in tests/gpu.rs (live wgpu, skips without an adapter): `the_diagnostics_bundle_names_the_live_adapter_and_never_consents` - the written JSON contains the real adapter name + upload_consented:false. The digest lint flagged the item (a declined dialog refuses loudly) -> added to the loud-not-digest-moving exempt list beside ExportLayers/Print. GOTCHA: tests/gpu.rs macro_order - gpu_or_skip! must be DEFINED before any test that invokes it (rust macro_rules order). Gates all green.
- [x] T-P3.4 (as far as the Windows host allows): macOS/Linux PACKAGING CONFIGS + DOCUMENTED COMMANDS - DONE 2026-09-02: packaging/macos/Info.plist (CFBundle* keys, version substituted by the script) + packaging/macos/build-app.sh (assembles RasterStudio.app from the release binary, ad-hoc codesign, hdiutil UDZO dmg) + packaging/linux/build-deb.sh (DEBIAN/control, usr/bin, .desktop entry, icon staging, dpkg-deb) + packaging/README.md documenting all three commands. HOST-BOUND (marked honestly): the dmg/deb BUILD + launch-on-clean-machine need real macOS/Linux runners - that is release CI (P3.5)'s job per the README. The .iss reference (icon at ../../assets/) corrected the linux script's icon path to match. Gates green (no Rust changes).
- [x] T-P3.5: Release CI - DONE 2026-09-02: .github/workflows/ci.yml EXTENDED (not replaced): macos-latest ADDED to the test matrix (the winit/wgpu path is the platform-specific surface); NEW release job gated on refs/tags/v* with a 3-OS matrix (windows iscc / macos build-app.sh / linux build-deb.sh), each building studio-desktop --release, running that platform's packaging script, and uploading the installer as an artifact (if-no-files-found: error). SIGNING is secret-driven: the Authenticode + Apple notarisation steps fire only when WINDOWS_CERT/APPLE_ID secrets exist (the P3.6 hook), so forks still ship unsigned artifacts with the job saying so. YAML validated (yaml.safe_load). HOST-BOUND: the tag-push run itself needs a GitHub remote - the workflow is the deliverable here. Gates all green.
- [x] T-P3.6 (as far as a certificate-less host allows): signing/notarisation - DONE 2026-09-02: the CI hooks landed with P3.5 (secret-driven Authenticode + notarisation steps); the EXECUTABLE COMMANDS are now documented in packaging/README.md (signtool sign /fd sha256 /tr timestamp + verify /pa; codesign --options runtime + spctl --assess as the macOS gate + notarytool submit/stapler). HOST-BOUND honestly: the validates (a signed installer + spctl "accepted") need real certificates this machine does not hold - the deliverable here is the exact command runbook tied to the workflow's secret layout, which a maintainer with certs can execute verbatim.
- [x] T-P3.7: Decoder resource limits - DONE 2026-09-02: the header check ALREADY refused declared dimensions before allocation (check_dimensions -> CodecError::LimitExceeded, run right after decoder.dimensions(), before from_decoder allocates); the missing pieces were the PROOF and the doc. NEW test `a_crafted_canvas_is_refused_by_name_before_any_allocation` (a hand-written ~70-byte PNG whose IHDR declares 1e9 x 1e9: refused by name, no allocation - the empty stored-block IDAT satisfies the container walk and proves the refusal happens at the header). Threat model (docs/threat-model.md section 4) gained the crafted-canvas row naming the test + the honest note that the png-crate iCCP inflation below remains the one allocation the header check cannot reach. Gates all green.
- [x] T-P3.8: Memory ceiling - DONE 2026-09-02: the ceiling existed and is DOCUMENTED (DEFAULT_CACHE_TILES = 128 x 1 MiB tiles = 128 MiB, cache.rs header) but store() let an oversized BATCH park every tile it made (retain = the whole batch) - a whole-canvas composite of a huge doc silently turned the cache unbounded. THE FIX: store keeps the NEWEST capacity coords of the overflowing batch; TileCompositor::cached_tiles() exposes the count. The old cache test pinned the leaky behaviour (len==4 after a 4-tile batch at capacity 2) - updated to the enforced contract (len==2). VALIDATE test in tests/integration/tests/performance.rs: `a_huge_multilayer_document_stays_under_the_cache_ceiling_and_stays_editable` (16000x16000 x 10 sparse layers; walks 3x the ceiling in coordinates; cache holds <= 128; a stroke on one layer invalidates + recomposites it). GOTCHAS: the paint closure args are (coord, x, y); TILE_SIZE is already u32.
- [x] T-P3.9: Wall-clock performance budget - DONE 2026-09-02: `a_partial_recomposite_of_an_8000x6000_document_meets_the_wall_clock_budget` in performance.rs - an 8000x6000 ten-layer doc, warm cache, one small edit, one partial frame; STATED CEILING 500 ms (the doc comment explains why a threshold is now asked for and why it is set an order of magnitude above desktop measurements: a regression gate, not a hardware benchmark). The existing ratio test stays (ratios still prove the cache property); the budget is the CI-facing gate the task asked for. Locally the partial frame runs in ~ms, so the gate has 100x headroom. Gates all green.
- [x] T-P3.10: .rstudio format versioning - DONE 2026-09-02: the migration path EXISTED (migrate.rs: the pre-decode version gate + the gapless STEPS chain 1->2 no-op + 2->3 repair that strips pixels/selection no real v1 build could write); the missing piece was the CHECKED-IN FIXTURE. Generated a genuine v1 package (a doc stamped format_version=1 with one painted tile, saved through build_package which serialises as-is) and CHECKED IT IN at tests/integration/fixtures/old-format-v1.rstudio/ (267K: manifest + document.msgpack + journal + preview + one content-addressed tile). VALIDATE test: `a_v1_fixture_opens_through_the_migration_path` (project_format::open_project on the fixture -> version stamped 3, the 2->3 repair cleared pixels/selection, geometry survives). project_format::document_version exported for the probe. Gates all green.
- [ ] T-P3.11: Accessibility | Verify: Tab order + focus ring; AccessKit labelled nodes
- [ ] T-P3.12: Localization scaffolding | Verify: catalogue keyed by locale; no user-facing literals in ui/src/view or dialogs
- [ ] T-P3.13: README screenshot | Verify: README embeds committed --shot PNG
- [ ] T-P3.14: Help destinations + repository field | Verify: URLs reachable; repository set
### P4 — Scope decisions
- [ ] T-P4: Confirm Tier C deferrals in parity-matrix; no menu promises deferred items | Verify: parity Tier C lists each with reason
### Final
- [ ] T-FIN: Release gate re-run: unavailable_reason sweep + menu-item test + four gates + side-by-side --shot + open/edit/save/reopen/export E2E | Verify: all pass

## Progress and evidence
- 2026-09-01T00:00Z: Ledger created. Baseline gates verified this session: fmt PASS, check PASS, test PASS (exit 0), clippy PASS (exit 0). Recon report captured: DialogHost slot = chrome.rs after docks (~line 552); dialogs return DialogOutcome<DialogAction> via ui::dialogs::resolve; result mapping: Command→out.commands, NewDocument→needs parameterized action, Export→new ChromeOutput field, SetColor→set_foreground/background.
- 2026-09-01T01:30Z T-P0.1: Implemented DialogHost (see Todo). Gates after: fmt PASS, clippy --workspace --all-targets PASS (0 warnings), check PASS, test --workspace PASS (3292 passed/0 failed/10 ignored; baseline 3288 + 4 new). `cargo test -p app-shell`: 332 passed.
- 2026-09-01T03:40Z T-P0.4: Implemented (see Todo). Gates: fmt PASS, clippy 0 warnings, test --workspace exit 0. `-p app-shell`: 342 passed. Validate line satisfied.
- 2026-09-01T02:50Z T-P0.3: Implemented (see Todo). Gates: fmt PASS, clippy 0 warnings, test --workspace exit 0. `-p app-shell`: 338 passed.
- 2026-09-01T02:15Z T-P0.2: Implemented (see Todo). Gates after: fmt PASS, clippy --workspace --all-targets 0 warnings, check PASS, test --workspace exit 0 (0 FAILED). `cargo test -p app-shell` 335 passed; `-p ui` 933+9 passed. Smoke: studio-desktop --shot launched wgpu, captured 302KB screenshot with an image open.
- INCIDENT (resolved) 2026-09-01T01:00Z: a python marker-cut script used to remove a scratch probe module truncated chrome.rs at the probe marker, deleting the entire `mod tests` (~1650 lines, 35 tests) because the probe had been inserted BEFORE the tests module. Detected via dead_code warning on test helpers with zero call sites. Recovered by extracting `mod tests` from `git show HEAD:...chrome.rs` and re-appending the 2 new dialog tests + helpers. Final file 3254 lines, 37 chrome tests. LESSON: never cut files by string markers with python; use the edit tool or verify module boundaries. All 35 original tests restored and passing.

## Pickup verification
- 2026-09-02: pickups verified; P0+P1+P2 all + P3.1-P3.10 done. 57/61 TICKED. PUSHED: 741761a, 854a79a, 589ac1c, c270645 on origin/main; push each landing.
- RULE: rewrite Handoff/Pickup wholesale each refresh; edit tool ONLY for edits.

## Handoff
- Current checkpoint: 57/61 tasks complete (P0+P1+P2 all; P3.1-P3.10), all four gates green.
- Last completed: T-P3.10 (checked-in v1 fixture opens through the real migration path).
- In progress: none.
- Next exact action: T-P3.11 Accessibility - validate: Tab reaches every control in the docks in visual order with a visible focus ring; AccessKit reports a labelled node per control. POSSIBLY LARGE: survey egui_accesskit (egui 0.29 has an accesskit integration?) + the focus-ring machinery + the docks' control inventory. If AccessKit wiring is a whole wave, do the honest scoping: Tab order + focus ring first, AccessKit nodes via egui's built-in integration if it exists (egui 0.29 ships egui-accesskit? check).
- Then: P3.12 localization scaffolding, P3.13 README screenshot, P3.14 help destinations; then P4 scope decisions; T-FIN final gate.
- Files changed this wave: crates/project-format/src/lib.rs, tests/integration/{tests/interchange_and_recovery.rs,fixtures/old-format-v1.rstudio/**}.
- Commands/checks (exact): `cd raster-studio && cargo fmt --check`; `cargo clippy --workspace --all-targets` (0 warnings); `cargo check --workspace --all-targets`; `cargo test --workspace` (exit 0).
- Decisions/assumptions (running): [prior] + the v1 fixture is a genuine artifact generated by the normal writer, not hand-edited.
- Blockers/risks: 4 tasks remain - bounded waves, rewrite this section after EVERY task, edit tool only. PUSH after each landing.
- Context note: P3.10 done; P3.11 next.
